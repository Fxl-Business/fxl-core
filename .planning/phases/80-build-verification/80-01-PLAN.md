---
phase: 80-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/**/*.ts", "src/**/*.tsx"]
autonomous: true
requirements: [VERIF-01, VERIF-02]

must_haves:
  truths:
    - "npx tsc --noEmit exits with code 0 and zero errors"
    - "npm run build completes successfully and produces dist/ output"
  artifacts:
    - path: "dist/"
      provides: "Production build bundle"
  key_links:
    - from: "npx tsc --noEmit"
      to: "all .ts/.tsx files"
      via: "TypeScript compiler strict mode"
      pattern: "zero errors"
    - from: "npm run build"
      to: "dist/"
      via: "Vite production build"
      pattern: "build completed"
---

<objective>
Ensure the entire v4.1 milestone codebase passes mandatory quality gates: zero TypeScript errors and a clean production build.

Purpose: This is the final gate before shipping v4.1. All code from phases 75-79 must compile and build cleanly.
Output: Passing tsc --noEmit, successful production build in dist/
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all TypeScript errors (VERIF-01)</name>
  <files>src/**/*.ts, src/**/*.tsx</files>
  <read_first>tsconfig.json, package.json</read_first>
  <action>
Run `npx tsc --noEmit` and capture the full error output. For each error:

1. Read the file containing the error
2. Fix the type error using proper TypeScript typing — NEVER use `any` as a fix (per CLAUDE.md rules)
3. Common fixes to apply:
   - Missing type imports: add the import
   - Incompatible types: correct the type annotation or add proper type narrowing
   - Missing properties: add required properties to objects/interfaces
   - Unused variables: remove them or prefix with underscore if intentional
   - Null/undefined issues: add proper null checks or optional chaining

After fixing all errors, run `npx tsc --noEmit` again to confirm zero errors.

If no errors exist on first run, the task is already complete — document that in the summary.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - No `any` types introduced as fixes (grep -r "as any" on changed files returns no new occurrences)
    - No `@ts-ignore` or `@ts-expect-error` comments added
  </acceptance_criteria>
  <done>npx tsc --noEmit returns zero errors across the entire codebase</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build succeeds (VERIF-02)</name>
  <files>dist/</files>
  <read_first>vite.config.ts, package.json</read_first>
  <action>
Run `npm run build` (which executes `vite build` via package.json).

If the build fails:
1. Read the error output carefully — Vite build errors are distinct from tsc errors (e.g., import resolution, CSS issues, environment variable references, chunk size)
2. Fix the root cause in the source file
3. Re-run `npm run build`

After successful build, verify that `dist/` directory exists and contains:
- `dist/index.html`
- `dist/assets/` with .js and .css files

If build succeeds on first run, document the bundle stats in the summary.
  </action>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT_CODE=$?" && ls dist/index.html dist/assets/*.js dist/assets/*.css 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `dist/index.html` exists
    - `dist/assets/` contains at least one .js file and one .css file
    - No build warnings about missing imports or unresolved dependencies
  </acceptance_criteria>
  <done>npm run build completes without errors and generates a valid production bundle in dist/</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0, zero errors
2. `npm run build` — exits 0, dist/ contains valid bundle
3. No `any` types or `@ts-ignore` introduced as fixes
</verification>

<success_criteria>
- VERIF-01: TypeScript compilation passes with zero errors
- VERIF-02: Production build succeeds and generates dist/ with index.html and assets
</success_criteria>

<output>
After completion, create `.planning/phases/80-build-verification/80-01-SUMMARY.md`
</output>
