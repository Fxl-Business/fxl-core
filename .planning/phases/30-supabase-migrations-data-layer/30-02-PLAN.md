---
phase: 30-supabase-migrations-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kb-service.ts
  - src/lib/kb-service.test.ts
  - src/lib/tasks-service.ts
  - src/lib/tasks-service.test.ts
autonomous: true
requirements: [KB-01, TASK-01]

must_haves:
  truths:
    - "kb-service.ts exports typed CRUD functions (create, list, getById, update, delete, search) that TypeScript compiles without errors"
    - "tasks-service.ts exports typed CRUD functions (create, list, getById, update, updateStatus, delete) that TypeScript compiles without errors"
    - "searchKnowledgeEntries calls .textSearch with config: 'portuguese'"
    - "All service functions follow the throw-on-error pattern from comments.ts"
  artifacts:
    - path: "src/lib/kb-service.ts"
      provides: "Knowledge Base CRUD + FTS service"
      exports: ["createKnowledgeEntry", "listKnowledgeEntries", "getKnowledgeEntry", "updateKnowledgeEntry", "deleteKnowledgeEntry", "searchKnowledgeEntries"]
    - path: "src/lib/tasks-service.ts"
      provides: "Tasks CRUD service"
      exports: ["createTask", "listTasks", "getTask", "updateTask", "updateTaskStatus", "deleteTask"]
    - path: "src/lib/kb-service.test.ts"
      provides: "Unit tests for KB service with Supabase mock"
    - path: "src/lib/tasks-service.test.ts"
      provides: "Unit tests for tasks service with Supabase mock"
  key_links:
    - from: "src/lib/kb-service.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase }"
      pattern: "from '@/lib/supabase'"
    - from: "src/lib/tasks-service.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase }"
      pattern: "from '@/lib/supabase'"
    - from: "src/lib/kb-service.ts"
      to: "knowledge_entries table"
      via: "supabase.from('knowledge_entries')"
      pattern: "\\.from\\('knowledge_entries'\\)"
    - from: "src/lib/tasks-service.ts"
      to: "tasks table"
      via: "supabase.from('tasks')"
      pattern: "\\.from\\('tasks'\\)"
---

<objective>
Create typed service modules for knowledge_entries and tasks tables with full CRUD operations, full-text search for KB, and unit tests with mocked Supabase client.

Purpose: Service layer is the data access contract that Phases 31 (KB UI) and 32 (Tasks UI) will consume. Building it before UI ensures clean separation and testability.
Output: Two service modules + two test files, all compiling cleanly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-supabase-migrations-data-layer/30-RESEARCH.md

@src/lib/supabase.ts
@tools/wireframe-builder/lib/comments.ts
</context>

<interfaces>
<!-- From src/lib/supabase.ts — the client import all services use -->
<!-- import { supabase } from '@/lib/supabase' -->

<!-- From tools/wireframe-builder/lib/comments.ts — the service pattern to replicate -->
<!-- Pattern: named async function, destructure { data, error }, if (error) throw error, return data as Type -->

<!-- From 30-RESEARCH.md — types to create -->
<!-- KnowledgeEntryType = 'bug' | 'decision' | 'pattern' | 'lesson' -->
<!-- TaskStatus = 'todo' | 'in_progress' | 'done' | 'blocked' -->
<!-- TaskPriority = 'low' | 'medium' | 'high' -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KB service — types, tests, and implementation</name>
  <files>src/lib/kb-service.ts, src/lib/kb-service.test.ts</files>
  <behavior>
    - createKnowledgeEntry({entry_type, title, body, tags?, client_slug?, created_by?}) -> KnowledgeEntry (calls .insert then .select().single())
    - listKnowledgeEntries({entry_type?, client_slug?, tag?}) -> KnowledgeEntry[] (applies .eq filters when provided, .contains for tag)
    - getKnowledgeEntry(id) -> KnowledgeEntry (calls .eq('id', id).single())
    - updateKnowledgeEntry(id, {title?, body?, tags?, entry_type?, client_slug?}) -> KnowledgeEntry (calls .update then .select().single())
    - deleteKnowledgeEntry(id) -> void (calls .delete().eq('id', id))
    - searchKnowledgeEntries(query) -> KnowledgeEntry[] (calls .textSearch('search_vec', query, {config: 'portuguese'}))
    - All functions throw on Supabase error (never swallow errors)
    - Never include search_vec in insert/update calls (GENERATED column is read-only)
  </behavior>
  <action>
1. Create `src/lib/kb-service.test.ts` first using vi.mock pattern from the project:
   - Mock `@/lib/supabase` with vi.mock()
   - Create a mock Supabase client with fluent chain (from().insert().select().single() etc.)
   - Write tests for each of the 6 exported functions
   - For searchKnowledgeEntries, assert that .textSearch is called with 'search_vec', query, and {config: 'portuguese'}
   - For listKnowledgeEntries with tag filter, assert .contains('tags', [tag]) is called

2. Create `src/lib/kb-service.ts`:
   - Export type `KnowledgeEntryType = 'bug' | 'decision' | 'pattern' | 'lesson'`
   - Export interface `KnowledgeEntry` with all columns (id, entry_type, title, body, tags, client_slug, created_by, created_at, updated_at) — do NOT include search_vec
   - Export interfaces `CreateKnowledgeEntryParams` and `UpdateKnowledgeEntryParams`
   - Implement all 6 functions following comments.ts pattern: destructure {data, error}, if (error) throw error, return data as Type
   - listKnowledgeEntries: build query with conditional .eq() filters and .contains() for tag, order by created_at desc
   - searchKnowledgeEntries: use .textSearch('search_vec', query, {config: 'portuguese'}) — ALWAYS pass portuguese config

3. Run tests to confirm green.

Service file location: `src/lib/` (per RESEARCH.md recommendation — will move to `src/modules/knowledge-base/lib/` in Phase 31 when module structure exists).
  </action>
  <verify>
    <automated>npx vitest run src/lib/kb-service.test.ts</automated>
  </verify>
  <done>kb-service.ts exports 6 typed CRUD+search functions, kb-service.test.ts has passing tests for all 6 functions, npx tsc --noEmit passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tasks service — types, tests, and implementation</name>
  <files>src/lib/tasks-service.ts, src/lib/tasks-service.test.ts</files>
  <behavior>
    - createTask({title, description?, status?, priority?, client_slug?, due_date?, created_by?}) -> Task (calls .insert then .select().single())
    - listTasks({status?, client_slug?, priority?}) -> Task[] (applies .eq filters when provided, order by created_at desc)
    - getTask(id) -> Task (calls .eq('id', id).single())
    - updateTask(id, {title?, description?, status?, priority?, client_slug?, due_date?}) -> Task (calls .update then .select().single())
    - updateTaskStatus(id, status) -> Task (shorthand for updating only status field)
    - deleteTask(id) -> void (calls .delete().eq('id', id))
    - All functions throw on Supabase error
    - due_date typed as string | null (not Date — Supabase returns ISO date strings)
  </behavior>
  <action>
1. Create `src/lib/tasks-service.test.ts` first:
   - Mock `@/lib/supabase` with vi.mock()
   - Write tests for each of the 6 exported functions
   - For updateTaskStatus, assert only status field is passed to .update()
   - For listTasks with filters, assert correct .eq() calls

2. Create `src/lib/tasks-service.ts`:
   - Export type `TaskStatus = 'todo' | 'in_progress' | 'done' | 'blocked'`
   - Export type `TaskPriority = 'low' | 'medium' | 'high'`
   - Export interface `Task` with all columns (id, title, description, status, priority, client_slug, due_date as string | null, created_by, created_at, updated_at)
   - Export interfaces `CreateTaskParams` and `UpdateTaskParams`
   - Implement all 6 functions following comments.ts pattern
   - listTasks: build query with conditional .eq() filters, order by created_at desc

3. Run tests to confirm green.

4. Run `npx tsc --noEmit` to verify zero TypeScript errors across entire codebase.

Service file location: `src/lib/` (will move to `src/modules/tasks/lib/` in Phase 32).
  </action>
  <verify>
    <automated>npx vitest run src/lib/tasks-service.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>tasks-service.ts exports 6 typed CRUD functions, tasks-service.test.ts has passing tests for all 6 functions, npx tsc --noEmit reports zero errors</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/kb-service.test.ts src/lib/tasks-service.test.ts` — all tests pass
- `npx tsc --noEmit` — zero errors
- Both service files import from `@/lib/supabase` (not direct Supabase client in components)
- No `any` types used anywhere
</verification>

<success_criteria>
- kb-service.ts and tasks-service.ts compile without errors and export typed CRUD functions
- All unit tests pass with mocked Supabase client
- searchKnowledgeEntries uses portuguese config for textSearch
- Service pattern matches existing comments.ts convention (throw on error, explicit types)
</success_criteria>

<output>
After completion, create `.planning/phases/30-supabase-migrations-data-layer/30-02-SUMMARY.md`
</output>
