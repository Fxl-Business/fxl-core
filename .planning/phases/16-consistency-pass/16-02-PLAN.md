---
phase: 16-consistency-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/clients/FinanceiroContaAzul/Index.tsx
  - src/pages/clients/FinanceiroContaAzul/DocViewer.tsx
  - src/pages/clients/BlueprintTextView.tsx
  - src/pages/clients/BriefingForm.tsx
  - src/pages/Login.tsx
  - src/pages/Profile.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CONSIST-02
  - CONSIST-03

must_haves:
  truths:
    - "Client index page has breadcrumb nav and badge pill matching doc page pattern"
    - "Client doc viewer has breadcrumb nav and text-4xl title"
    - "Blueprint and Briefing pages use text-4xl title scale"
    - "Login page has slate-50 background with subtle brand presence"
    - "Profile page has slate-50 background"
    - "SignUp inline route uses same container styling as Login"
  artifacts:
    - path: "src/pages/clients/FinanceiroContaAzul/Index.tsx"
      provides: "Client index with new visual language"
      contains: "text-4xl font-extrabold tracking-tight text-slate-900"
    - path: "src/pages/clients/FinanceiroContaAzul/DocViewer.tsx"
      provides: "Client doc viewer with breadcrumb pattern"
      contains: "text-4xl font-extrabold tracking-tight text-slate-900"
    - path: "src/pages/clients/BlueprintTextView.tsx"
      provides: "Blueprint view with updated title scale"
      contains: "text-4xl font-extrabold tracking-tight"
    - path: "src/pages/clients/BriefingForm.tsx"
      provides: "Briefing form with updated title scale"
      contains: "text-4xl font-extrabold tracking-tight"
    - path: "src/pages/Login.tsx"
      provides: "Login with slate-50 background and brand elements"
      contains: "bg-slate-50"
    - path: "src/pages/Profile.tsx"
      provides: "Profile with slate-50 background"
      contains: "bg-slate-50"
    - path: "src/App.tsx"
      provides: "SignUp route with consistent container styling"
      contains: "bg-slate-50"
  key_links:
    - from: "src/pages/clients/FinanceiroContaAzul/Index.tsx"
      to: "doc page visual language"
      via: "breadcrumb + badge pill classes"
      pattern: "bg-indigo-50.*text-indigo-600.*ring-indigo-600"
    - from: "src/pages/Login.tsx"
      to: "app background palette"
      via: "bg-slate-50 class"
      pattern: "bg-slate-50"
---

<objective>
Restyle client pages (FinanceiroIndex, DocViewer, BlueprintTextView, BriefingForm) and auth pages (Login, Profile, SignUp route) to match the new visual language.

Purpose: CONSIST-02 ensures client pages use the same header pattern, typography scale, and color palette as doc pages. CONSIST-03 ensures auth pages feel cohesive with the app even though they render outside the Layout shell.

Output: 7 files updated with consistent typography, breadcrumb navigation on client pages, and slate-50 backgrounds on auth pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-consistency-pass/16-RESEARCH.md

@src/pages/clients/FinanceiroContaAzul/Index.tsx
@src/pages/clients/FinanceiroContaAzul/DocViewer.tsx
@src/pages/clients/BlueprintTextView.tsx
@src/pages/clients/BriefingForm.tsx
@src/pages/Login.tsx
@src/pages/Profile.tsx
@src/App.tsx

<interfaces>
<!-- Visual language reference (from Phases 12-15, documented in 16-RESEARCH.md) -->

Breadcrumb Pattern (from DocBreadcrumb):
```typescript
<nav className="mb-4 flex items-center gap-2 text-sm text-slate-500 dark:text-slate-400">
  <span>Section</span>
  <ChevronRight className="h-3.5 w-3.5 text-slate-400" />
  <span className="font-medium text-slate-900 dark:text-foreground">Page Name</span>
</nav>
```

Badge Pill Pattern (from DocPageHeader):
```typescript
<span className="mb-3 inline-flex items-center rounded-full bg-indigo-50 px-3 py-1 text-xs font-medium text-indigo-600 ring-1 ring-inset ring-indigo-600/20 dark:bg-indigo-950/50 dark:text-indigo-400 dark:ring-indigo-400/30">
  Badge Text
</span>
```

Title Pattern:
```typescript
<h1 className="mt-2 text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground">
  Page Title
</h1>
<p className="mt-3 text-lg text-slate-600 dark:text-slate-400">
  Description text
</p>
```

Table Styling:
- Header bg: bg-slate-50/80 dark:bg-slate-800/50
- Header text: text-slate-600 dark:text-slate-400
- Border: border-slate-200 dark:border-slate-700
- Links: text-indigo-600 hover:text-indigo-700 dark:text-indigo-400

Auth Page Container:
- bg-slate-50 dark:bg-background (instead of bg-background)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle client pages with breadcrumb nav and new typography</name>
  <files>src/pages/clients/FinanceiroContaAzul/Index.tsx, src/pages/clients/FinanceiroContaAzul/DocViewer.tsx, src/pages/clients/BlueprintTextView.tsx, src/pages/clients/BriefingForm.tsx</files>
  <action>
Update four client pages to match the doc page visual language. Import ChevronRight from lucide-react where needed.

**FinanceiroIndex.tsx** (`src/pages/clients/FinanceiroContaAzul/Index.tsx`):
Add `import { ChevronRight } from 'lucide-react'` to existing lucide imports (there are none currently -- add the import).

Replace the entire header block (the mb-1 div + h1 + p) with:
```typescript
<nav className="mb-4 flex items-center gap-2 text-sm text-slate-500 dark:text-slate-400">
  <span>Clientes</span>
  <ChevronRight className="h-3.5 w-3.5 text-slate-400" />
  <span className="font-medium text-slate-900 dark:text-foreground">Financeiro Conta Azul</span>
</nav>
<span className="mb-3 inline-flex items-center rounded-full bg-indigo-50 px-3 py-1 text-xs font-medium text-indigo-600 ring-1 ring-inset ring-indigo-600/20 dark:bg-indigo-950/50 dark:text-indigo-400 dark:ring-indigo-400/30">
  Clientes
</span>
<h1 className="mt-2 text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground">
  Financeiro Conta Azul
</h1>
<p className="mt-3 text-lg text-slate-600 dark:text-slate-400">
  Dashboard financeiro focado em analise de dados exportados do Conta Azul.
</p>
```

Update the "Documentos" section header h2: replace `text-sm font-semibold text-foreground` with `text-xs font-bold uppercase tracking-wider text-slate-900 dark:text-foreground`.

Update the table container: replace `border border-border bg-card` with `border border-slate-200 bg-white dark:border-slate-700 dark:bg-card`.

Update thead row: replace `border-b border-border bg-muted` with `border-b border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/50`.

Update thead th: replace `text-muted-foreground` with `text-slate-600 dark:text-slate-400`.

Update tbody tr: replace `border-t border-border` with `border-t border-slate-200 dark:border-slate-700`.

Update td font-medium: replace `text-foreground` with `text-slate-900 dark:text-foreground`.

Update Abrir links: replace `text-primary` with `text-indigo-600 hover:text-indigo-700 dark:text-indigo-400`.

Update the "Prompt Master" section header h2: replace `text-sm font-semibold text-foreground` with `text-xs font-bold uppercase tracking-wider text-slate-900 dark:text-foreground`.

Update the prompt description p: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`.

Keep STATUS_COLORS as-is (semantic colors for status states are appropriate).

**DocViewer.tsx** (`src/pages/clients/FinanceiroContaAzul/DocViewer.tsx`):
Add `import { ChevronRight } from 'lucide-react'`.

Replace the header block (mb-6 div) with:
```typescript
<div className="mb-6">
  <nav className="mb-4 flex items-center gap-2 text-sm text-slate-500 dark:text-slate-400">
    <span>Clientes</span>
    <ChevronRight className="h-3.5 w-3.5 text-slate-400" />
    <span>Financeiro Conta Azul</span>
    <ChevronRight className="h-3.5 w-3.5 text-slate-400" />
    <span className="font-medium text-slate-900 dark:text-foreground">{entry.title}</span>
  </nav>
  <h1 className="text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground">{entry.title}</h1>
  <span className="mt-2 inline-block rounded bg-slate-100 px-2 py-0.5 font-mono text-xs text-slate-500 dark:bg-slate-800 dark:text-slate-400">
    {entry.filename}
  </span>
</div>
```

Update the not-found text: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`.

**BlueprintTextView.tsx** (`src/pages/clients/BlueprintTextView.tsx`):
Update the page header h1 only: replace `text-2xl font-bold tracking-tight text-foreground` with `text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground`.

Update loading state text: replace `text-muted-foreground` on both Loader2 and p with `text-slate-400 dark:text-slate-500` and `text-slate-500 dark:text-slate-400` respectively.

Update error state p: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`.

Keep Card/Badge/Button components unchanged -- they use shadcn which inherits the palette through CSS vars.

**BriefingForm.tsx** (`src/pages/clients/BriefingForm.tsx`):
Update the page header h1 only: replace `text-2xl font-semibold tracking-tight text-foreground` with `text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground`.

Update description p: replace `text-sm text-muted-foreground` with `text-lg text-slate-600 dark:text-slate-400`.

Do NOT change any Card/Label/Input/Select/Button styling -- those are shadcn components that inherit the palette through CSS vars, and the form is already well-styled. Only the page title and description need alignment.

Update loading state similarly to BlueprintTextView: `text-slate-400`/`text-slate-500` pattern.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>All four client pages use the new typography scale (text-4xl extrabold for titles). FinanceiroIndex and DocViewer have breadcrumb navigation. Table uses explicit slate borders. All explicit color classes have dark: variants.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Login, Profile, and SignUp containers</name>
  <files>src/pages/Login.tsx, src/pages/Profile.tsx, src/App.tsx</files>
  <action>
Update auth page containers to feel cohesive with the app. These pages render outside the Layout shell, so they need their own background and brand presence.

**Login.tsx** (`src/pages/Login.tsx`):
Replace the wrapper div className:
- FROM: `min-h-screen bg-background flex items-center justify-center px-4`
- TO: `min-h-screen bg-slate-50 flex flex-col items-center justify-center px-4 dark:bg-background`

Add a brand header above the SignIn component (inside the wrapper div, before SignIn):
```typescript
<div className="mb-8 text-center">
  <h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-foreground">Nucleo FXL</h1>
  <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Acesse a plataforma operacional</p>
</div>
```

**Profile.tsx** (`src/pages/Profile.tsx`):
Replace the wrapper div className:
- FROM: `min-h-screen bg-background flex items-center justify-center px-4 py-12`
- TO: `min-h-screen bg-slate-50 flex items-center justify-center px-4 py-12 dark:bg-background`

No brand header needed -- UserProfile component is self-contained.

**App.tsx** (`src/App.tsx`):
Update the inline SignUp route wrapper div className:
- FROM: `min-h-screen bg-background flex items-center justify-center px-4`
- TO: `min-h-screen bg-slate-50 flex flex-col items-center justify-center px-4 dark:bg-background`

Add the same brand header before the SignUp component:
```typescript
<div className="mb-8 text-center">
  <h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-foreground">Nucleo FXL</h1>
  <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Crie sua conta</p>
</div>
```

IMPORTANT: Do NOT change any Clerk component props or appearance configuration. The Clerk shadcn theme already reads the app's CSS vars from globals.css. Only the wrapper containers and optional brand elements change.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>Login, Profile, and SignUp pages use bg-slate-50 backgrounds. Login and SignUp show brand header. All three have dark: variants. Clerk components remain untouched.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Visual inspection: Client index page shows breadcrumb, badge pill, large title
3. Visual inspection: Client doc viewer shows 3-level breadcrumb and large title
4. Visual inspection: Blueprint and Briefing pages show large title matching doc pages
5. Visual inspection: Login page shows slate-50 bg with brand header above Clerk form
6. Visual inspection: Profile page shows slate-50 bg
7. Visual inspection: Toggle dark mode on all updated pages -- no broken colors
</verification>

<success_criteria>
- Client pages use text-4xl extrabold title scale, breadcrumb nav, and explicit slate/indigo classes
- Login and SignUp pages show brand header with "Nucleo FXL"
- All auth pages use bg-slate-50 instead of bg-background
- TypeScript compiles without errors
- Dark mode works on all updated pages
</success_criteria>

<output>
After completion, create `.planning/phases/16-consistency-pass/16-02-SUMMARY.md`
</output>
