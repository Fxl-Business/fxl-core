---
phase: 16-consistency-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.tsx
  - src/components/docs/Callout.tsx
  - src/components/docs/PromptBlock.tsx
  - src/components/ui/PromptBlock.tsx
  - src/components/docs/InfoBlock.tsx
autonomous: true
requirements:
  - CONSIST-01
  - CONSIST-04

must_haves:
  truths:
    - "Home page title renders at text-4xl font-extrabold scale, matching doc pages"
    - "Home page section headers use uppercase xs bold tracking-wider pattern from sidebar"
    - "Home page cards use explicit slate borders with indigo hover states"
    - "Callout info type uses indigo palette (not blue)"
    - "PromptBlock pre content uses dark bg-slate-900 theme matching code blocks"
    - "InfoBlock info type uses indigo palette (not blue)"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Home page with new visual language"
      contains: "text-4xl font-extrabold tracking-tight text-slate-900"
    - path: "src/components/docs/Callout.tsx"
      provides: "Callout with indigo info palette"
      contains: "border-indigo-200 bg-indigo-50"
    - path: "src/components/docs/PromptBlock.tsx"
      provides: "PromptBlock with dark theme pre block"
      contains: "bg-slate-900"
    - path: "src/components/ui/PromptBlock.tsx"
      provides: "UI PromptBlock with dark theme pre block"
      contains: "bg-slate-900"
    - path: "src/components/docs/InfoBlock.tsx"
      provides: "InfoBlock with indigo info palette"
      contains: "bg-indigo-50 border-indigo-200"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "Tailwind classes"
      via: "explicit color classes"
      pattern: "text-slate-900.*dark:text-foreground"
    - from: "src/components/docs/Callout.tsx"
      to: "indigo palette"
      via: "styles object"
      pattern: "border-indigo-200 bg-indigo-50"
    - from: "src/components/docs/PromptBlock.tsx"
      to: "dark code block theme"
      via: "pre className"
      pattern: "bg-slate-900"
---

<objective>
Restyle Home page and shared doc components (Callout, PromptBlock, InfoBlock) to match the new slate + indigo visual language established in Phases 12-15.

Purpose: CONSIST-01 and CONSIST-04 ensure the landing page and reusable doc components use the same typography scale, color palette, and card styling as the doc pages -- eliminating visual inconsistency.

Output: 5 files updated with explicit slate/indigo Tailwind classes, dark mode variants, and matching typography scale.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-consistency-pass/16-RESEARCH.md

@src/pages/Home.tsx
@src/components/docs/Callout.tsx
@src/components/docs/PromptBlock.tsx
@src/components/ui/PromptBlock.tsx
@src/components/docs/InfoBlock.tsx

<interfaces>
<!-- Visual language reference (from Phases 12-15, documented in 16-RESEARCH.md) -->

Typography Scale:
- Page title:     text-4xl font-extrabold tracking-tight text-slate-900 sm:text-5xl dark:text-foreground
- Description:    text-lg text-slate-600 dark:text-slate-400
- Section header: text-xs font-bold uppercase tracking-wider text-slate-900 dark:text-foreground
- Body text:      text-base leading-relaxed text-slate-600
- Small label:    text-xs text-slate-500

Color Palette:
- Card border:    border-slate-200 dark:border-slate-700
- Card bg:        bg-white dark:bg-card
- Hover:          hover:border-indigo-200 hover:shadow-sm dark:hover:border-indigo-800
- Active/accent:  text-indigo-600, border-indigo-600, bg-indigo-50
- Badge pill:     bg-indigo-50 text-indigo-600 ring-1 ring-inset ring-indigo-600/20 dark:bg-indigo-950/50 dark:text-indigo-400 dark:ring-indigo-400/30
- Icon container: bg-indigo-50 text-indigo-600 dark:bg-indigo-950/50 dark:text-indigo-400
- Muted text:     text-slate-500 dark:text-slate-400

Code/Prompt Blocks:
- bg-slate-900 rounded-xl (dark theme)
- Header: bg-indigo-50 dark:bg-indigo-950/30, text-indigo-600 dark:text-indigo-400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Home page to new visual language</name>
  <files>src/pages/Home.tsx</files>
  <action>
Update all Tailwind classes in Home.tsx to match the slate + indigo visual language. Specific changes:

**Page header:**
- Title: replace `text-2xl font-bold text-foreground` with `text-4xl font-extrabold tracking-tight text-slate-900 dark:text-foreground`
- Description: replace `mt-1 text-sm text-muted-foreground` with `mt-3 text-lg text-slate-600 dark:text-slate-400`

**Section headers (h2 elements -- "O que vai fazer hoje?", "Documentacao", "Clientes"):**
- Replace `text-sm font-semibold uppercase tracking-wide text-muted-foreground` with `text-xs font-bold uppercase tracking-wider text-slate-900 dark:text-foreground`

**Quick action cards (the 3-card grid):**
- Outer div: replace `border border-border bg-card` with `border border-slate-200 bg-white dark:border-slate-700 dark:bg-card`
- Hover: replace `hover:border-primary hover:shadow-md` with `hover:border-indigo-200 hover:shadow-sm dark:hover:border-indigo-800`
- Icon container: replace `bg-muted text-muted-foreground` with `bg-indigo-50 text-indigo-600 dark:bg-indigo-950/50 dark:text-indigo-400`
- Card title: replace `text-foreground` with `text-slate-900 dark:text-foreground`
- Card description: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`
- Arrow icon: replace `text-muted-foreground` with `text-slate-400 dark:text-slate-500`

**Documentation section cards:**
- Border: replace `border border-border bg-card` with `border border-slate-200 bg-white dark:border-slate-700 dark:bg-card`
- Hover: replace `hover:border-primary` with `hover:border-indigo-200 hover:shadow-sm dark:hover:border-indigo-800`
- Badge text: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`
- Card title h3: replace `text-foreground` with `text-slate-900 dark:text-foreground`
- Card description: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`

**Client cards:**
- Border: replace `border border-border bg-card` with `border border-slate-200 bg-white dark:border-slate-700 dark:bg-card`
- Client badge: replace `bg-muted` + `text-muted-foreground` with `bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-400`
- Client name hover: replace `group-hover:text-primary` with `group-hover:text-indigo-600 dark:group-hover:text-indigo-400`
- Client name: replace `text-foreground` with `text-slate-900 dark:text-foreground`
- Client description: replace `text-muted-foreground` with `text-slate-500 dark:text-slate-400`

Keep green status badge as-is (semantic color for status). Do NOT change the component structure or data arrays -- only className strings.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>Home page uses text-4xl title, explicit slate/indigo classes, indigo icon containers, and dark: variants on all updated elements. Zero semantic-only tokens remain for visual hierarchy elements.</done>
</task>

<task type="auto">
  <name>Task 2: Update Callout, PromptBlock (both), and InfoBlock to new palette</name>
  <files>src/components/docs/Callout.tsx, src/components/docs/PromptBlock.tsx, src/components/ui/PromptBlock.tsx, src/components/docs/InfoBlock.tsx</files>
  <action>
Update four shared components to match the new visual language. All changes are CSS-class-only.

**Callout.tsx** (`src/components/docs/Callout.tsx`):
Replace the `styles` object:
```
info: 'border-indigo-200 bg-indigo-50 text-indigo-900 dark:border-indigo-800 dark:bg-indigo-950/50 dark:text-indigo-200'
warning: 'border-amber-200 bg-amber-50 text-amber-900 dark:border-amber-800 dark:bg-amber-950/50 dark:text-amber-200'
```
Keep the outer div structure and ReactMarkdown rendering unchanged.

**PromptBlock (docs/)** (`src/components/docs/PromptBlock.tsx`):
- Outer container: replace `border border-border` with `border border-slate-200 dark:border-slate-700` and add `rounded-xl` (replace `rounded-lg`)
- Header bar: replace `border-b border-border bg-primary/5` with `border-b border-slate-700/50 bg-indigo-50 dark:bg-indigo-950/30 dark:border-indigo-800/50`
- Label text: replace `text-primary` with `text-indigo-600 dark:text-indigo-400`
- Pre block: replace `bg-muted p-4 font-mono text-xs leading-relaxed text-muted-foreground` with `rounded-b-xl bg-slate-900 p-4 font-mono text-xs leading-relaxed text-slate-300 dark:text-slate-400`
- Keep the Button with copy/check functionality unchanged.

**PromptBlock (ui/)** (`src/components/ui/PromptBlock.tsx`):
Apply the same visual changes as the docs/ version:
- Outer container: in the `cn()` call, replace `rounded-lg border border-border` with `rounded-xl border border-slate-200 dark:border-slate-700`
- Header bar: replace `border-b border-border bg-primary/5` with `border-b border-slate-700/50 bg-indigo-50 dark:bg-indigo-950/30 dark:border-indigo-800/50`
- Label text: replace `text-primary` with `text-indigo-600 dark:text-indigo-400`
- Pre block: replace `bg-muted p-4 font-mono text-xs leading-relaxed text-muted-foreground` with `rounded-b-xl bg-slate-900 p-4 font-mono text-xs leading-relaxed text-slate-300 dark:text-slate-400`
- Keep the copy button and `cn()` className prop passthrough unchanged.

**InfoBlock.tsx** (`src/components/docs/InfoBlock.tsx`):
Update the `config` object's color classes:
```
info: {
  bg: 'bg-indigo-50 border-indigo-200 dark:bg-indigo-950/50 dark:border-indigo-800',
  text: 'text-indigo-800 dark:text-indigo-200',
  iconColor: 'text-indigo-500 dark:text-indigo-400',
}
warning: {
  bg: 'bg-amber-50 border-amber-200 dark:bg-amber-950/50 dark:border-amber-800',
  text: 'text-amber-800 dark:text-amber-200',
  iconColor: 'text-amber-500 dark:text-amber-400',
}
success: {
  bg: 'bg-green-50 border-green-200 dark:bg-green-950/50 dark:border-green-800',
  text: 'text-green-800 dark:text-green-200',
  iconColor: 'text-green-500 dark:text-green-400',
}
```
Keep the component structure, cn() usage, and icon imports unchanged.

IMPORTANT: Both PromptBlock components must end up visually identical (same color classes on header, pre, label). Only the Button vs plain button difference remains.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>Callout info uses indigo (not blue). Both PromptBlocks have dark bg-slate-900 pre blocks with indigo header bars. InfoBlock info uses indigo (not blue). All four components have dark: variants for every explicit color class.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Visual inspection: Home page shows large title, indigo icon containers, slate card borders
3. Visual inspection: Callout info blocks show indigo (not blue) on any doc page with callouts
4. Visual inspection: PromptBlock shows dark pre area on client index and doc pages
5. Visual inspection: Toggle dark mode -- all updated elements remain visually coherent
</verification>

<success_criteria>
- Home page is visually consistent with doc pages (same title scale, same card treatment)
- No blue-colored elements remain in Callout or InfoBlock (replaced with indigo)
- PromptBlock pre content uses dark theme matching code blocks
- All explicit color classes have corresponding dark: variants
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-consistency-pass/16-01-SUMMARY.md`
</output>
