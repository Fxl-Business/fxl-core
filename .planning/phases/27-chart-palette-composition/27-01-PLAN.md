---
phase: 27-chart-palette-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/components/BarLineChart.tsx
  - tools/wireframe-builder/components/StackedBarChartComponent.tsx
  - tools/wireframe-builder/components/HorizontalBarChartComponent.tsx
  - tools/wireframe-builder/components/AreaChartComponent.tsx
  - tools/wireframe-builder/components/StackedAreaChartComponent.tsx
  - tools/wireframe-builder/components/DonutChart.tsx
  - tools/wireframe-builder/components/WaterfallChart.tsx
  - tools/wireframe-builder/components/ParetoChart.tsx
  - tools/wireframe-builder/components/ComposedChartComponent.tsx
  - tools/wireframe-builder/components/GaugeChartComponent.tsx
  - tools/wireframe-builder/components/RadarChartComponent.tsx
  - tools/wireframe-builder/components/ScatterChartComponent.tsx
  - tools/wireframe-builder/components/FunnelChartComponent.tsx
  - tools/wireframe-builder/components/BubbleChartComponent.tsx
  - tools/wireframe-builder/components/TreemapComponent.tsx
autonomous: true
requirements: [CHRT-01, CHRT-02, CHRT-03, CHRT-04]

must_haves:
  truths:
    - "All 15 chart components render with rounded-xl border and shadow-sm containers"
    - "All chart titles use font-bold instead of font-semibold"
    - "StackedBarChart, StackedAreaChart, and ComposedChart show custom rounded-full dot legends instead of Recharts Legend"
    - "DonutChart legend dots are rounded-full (circles) not rounded-sm (squares)"
    - "BarLineChart and StackedBarChart bars show muted-to-full opacity transition on hover"
  artifacts:
    - path: "tools/wireframe-builder/components/BarLineChart.tsx"
      provides: "Bar chart with activeBar opacity + container restyle"
      contains: "rounded-xl"
    - path: "tools/wireframe-builder/components/StackedBarChartComponent.tsx"
      provides: "Stacked bar with custom legend + activeBar + container restyle"
      contains: "rounded-full"
    - path: "tools/wireframe-builder/components/DonutChart.tsx"
      provides: "Donut chart with rounded-full legend dots"
      contains: "rounded-full"
  key_links:
    - from: "all chart components"
      to: "wireframe-tokens.css"
      via: "var(--wf-chart-*) tokens in fill/stroke/style"
      pattern: "var\\(--wf-chart-"
---

<objective>
Restyle all 15 chart components to match the v1.4 premium financial dashboard aesthetic.

Purpose: Charts are the core data visualization layer. Their containers, titles, legends, and hover behavior must match the rounded-xl shadow-sm card treatment established in Phase 24 (KPI cards) and Phase 25 (tables).

Output: All chart components updated with rounded-xl containers, font-bold titles, custom dot legends (where applicable), and activeBar opacity hover on bar charts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-chart-palette-composition/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Container + title + legend restyle across all 15 chart components</name>
  <files>
    tools/wireframe-builder/components/BarLineChart.tsx
    tools/wireframe-builder/components/StackedBarChartComponent.tsx
    tools/wireframe-builder/components/HorizontalBarChartComponent.tsx
    tools/wireframe-builder/components/AreaChartComponent.tsx
    tools/wireframe-builder/components/StackedAreaChartComponent.tsx
    tools/wireframe-builder/components/DonutChart.tsx
    tools/wireframe-builder/components/WaterfallChart.tsx
    tools/wireframe-builder/components/ParetoChart.tsx
    tools/wireframe-builder/components/ComposedChartComponent.tsx
    tools/wireframe-builder/components/GaugeChartComponent.tsx
    tools/wireframe-builder/components/RadarChartComponent.tsx
    tools/wireframe-builder/components/ScatterChartComponent.tsx
    tools/wireframe-builder/components/FunnelChartComponent.tsx
    tools/wireframe-builder/components/BubbleChartComponent.tsx
    tools/wireframe-builder/components/TreemapComponent.tsx
  </files>
  <action>
    Apply three mechanical changes across ALL 15 chart component files:

    **A) Container upgrade (all 15 files):**
    Change `className="rounded-lg border border-wf-card-border bg-wf-card p-4"` to `className="rounded-xl border border-wf-card-border bg-wf-card p-4 shadow-sm"` on the outermost card div.
    Note: WaterfallChart.tsx has TWO container divs (detailed view ~line 204 and simple view ~line 255) — update BOTH.

    **B) Title upgrade (all 15 files):**
    Change `font-semibold` to `font-bold` on the title `<p>` element.
    Note: WaterfallChart.tsx has multiple `font-semibold` usages — only change the chart title `<p>` elements (lines ~206 and ~256), NOT the tooltip label spans (lines ~138 and ~166).
    Note: GaugeChartComponent.tsx title is `mb-2` not `mb-3` — keep `mb-2`, only change `font-semibold` to `font-bold`.

    **C) Custom legend dots (3 files that use Recharts `<Legend />`):**

    In `StackedBarChartComponent.tsx`:
    - Remove the `<Legend />` element from inside `<BarChart>`
    - Remove the `Legend` import from recharts (keep other imports)
    - Add a custom legend row between the title `<p>` and the `<ResponsiveContainer>`. Build legend items from the series keys used in the chart (typically 3 series). Use `var(--wf-chart-1)`, `var(--wf-chart-2)`, `var(--wf-chart-3)` as colors via `style={{ backgroundColor: color }}`. Each item: `<div className="flex items-center gap-1.5"><span className="h-2.5 w-2.5 flex-shrink-0 rounded-full" style={{ backgroundColor: color }} /><span className="text-xs text-wf-muted">{label}</span></div>`. Wrap in `<div className="mb-3 flex flex-wrap gap-3">`.

    In `StackedAreaChartComponent.tsx`:
    - Same pattern: remove `<Legend />` from `<AreaChart>`, remove Legend import, add custom dot legend between title and ResponsiveContainer. Use the series keys from the component's data shape.

    In `ComposedChartComponent.tsx`:
    - Same pattern: remove `<Legend />` from `<ComposedChart>`, remove Legend import, add custom dot legend.

    **D) DonutChart legend shape fix (1 file):**
    In `DonutChart.tsx`, change the legend dot span from `rounded-sm` to `rounded-full` (line ~60).

    **Anti-patterns to avoid:**
    - Do NOT use Tailwind classes like `bg-wf-chart-1` — they don't exist. Use `style={{ backgroundColor: 'var(--wf-chart-N)' }}`.
    - Do NOT use `dark:` Tailwind variants — wireframe theming uses `data-wf-theme`, not Tailwind dark mode.
    - WaterfallChart.tsx has a `<Legend wrapperStyle={{ display: 'none' }} />` — this hidden legend is used for Recharts internal tooltip pairing. Leave it as-is (do NOT remove or replace with custom dots).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    All 15 chart components use rounded-xl shadow-sm containers and font-bold titles.
    StackedBarChart, StackedAreaChart, and ComposedChart show custom rounded-full dot legends instead of Recharts Legend.
    DonutChart legend dots are circles (rounded-full) not squares (rounded-sm).
    Zero TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: ActiveBar opacity hover on bar chart variants</name>
  <files>
    tools/wireframe-builder/components/BarLineChart.tsx
    tools/wireframe-builder/components/StackedBarChartComponent.tsx
  </files>
  <action>
    Add muted-to-full opacity group-hover behavior to bar charts using Recharts `activeBar` prop.

    **In BarLineChart.tsx:**
    For the `<Bar>` element (rendered when type is 'bar' or 'bar-line'):
    - Add `opacity={0.7}` prop
    - Add `activeBar={{ opacity: 1, fill: chartColors?.[0] ?? 'var(--wf-chart-1)' }}` prop
    This makes non-hovered bars appear at 70% opacity while the hovered bar shows at full opacity with the same fill color.

    **In StackedBarChartComponent.tsx:**
    For each `<Bar>` element in the stacked series (typically 3 bars with different dataKeys):
    - Add `opacity={0.7}` prop to each `<Bar>`
    - Add `activeBar={{ opacity: 1 }}` prop to each `<Bar>` (no need to specify fill — Recharts preserves the existing fill)
    This makes the hovered stacked column stand out. Since all bars at the same index are highlighted together in stacked mode, the full column brightens on hover.

    **Do NOT apply to:** HorizontalBarChartComponent (horizontal bars have different UX), AreaChart, LineChart, DonutChart, or any non-bar chart type.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    BarLineChart bars (type 'bar' and 'bar-line') render at 0.7 opacity with activeBar at full opacity on hover.
    StackedBarChart bars render at 0.7 opacity with activeBar at full opacity on hover.
    Zero TypeScript errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 15 chart components have `rounded-xl` and `shadow-sm` in their container className
- All 15 chart components have `font-bold` (not `font-semibold`) on their title element
- No Recharts `<Legend />` remains in StackedBarChart, StackedAreaChart, or ComposedChart (WaterfallChart hidden Legend is OK)
- DonutChart legend uses `rounded-full`
- BarLineChart and StackedBarChart `<Bar>` elements have `opacity` and `activeBar` props
</verification>

<success_criteria>
Chart palette tokens (CHRT-01) are already correct from Phase 22 — this plan confirms they render via the existing var(--wf-chart-*) references.
Chart containers (CHRT-02) show rounded-xl + shadow-sm matching the KPI card aesthetic.
Chart headers (CHRT-03) use font-bold titles with custom rounded-full legend dots.
Bar charts (CHRT-04) transition from muted to full opacity on hover via activeBar.
</success_criteria>

<output>
After completion, create `.planning/phases/27-chart-palette-composition/27-01-SUMMARY.md`
</output>
