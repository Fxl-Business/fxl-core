---
phase: 26-filter-bar-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/components/WireframeFilterBar.tsx
autonomous: true
requirements:
  - FILT-01
  - FILT-02
  - FILT-03
  - FILT-04
  - FILT-05

must_haves:
  truths:
    - "Filter bar shows a blurred background when content scrolls behind it"
    - "Filter select controls display bold primary-colored text on transparent background with no border"
    - "Filter labels appear as 10px uppercase bold slate-500 text above their controls in a stacked layout"
    - "Action buttons (date picker, share, export) render with outline/filled hierarchy and rounded-lg shape"
    - "Compare toggle label is 11px bold"
  artifacts:
    - path: "tools/wireframe-builder/components/WireframeFilterBar.tsx"
      provides: "Restyled filter bar with all FILT requirements"
      contains: "backdropFilter"
  key_links:
    - from: "tools/wireframe-builder/components/WireframeFilterBar.tsx"
      to: "wireframe-tokens.css"
      via: "CSS custom properties"
      pattern: "var\\(--wf-(canvas|accent|neutral-500|card-border)"
---

<objective>
Restyle WireframeFilterBar.tsx to match the premium financial dashboard aesthetic: semi-transparent sticky bar with backdrop-blur, vertical stacked label+control layout with 10px uppercase labels, transparent select controls with bold primary text, action button hierarchy (outline secondary + filled primary), and bold compare toggle label.

Purpose: The filter bar is the primary control surface users interact with on every wireframe screen. This restyle brings it in line with the Phase 22-25 visual quality (KPI cards, tables, sidebar, header).
Output: Updated WireframeFilterBar.tsx with all five FILT requirements satisfied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-filter-bar-enhancement/26-RESEARCH.md

@tools/wireframe-builder/components/WireframeFilterBar.tsx
@tools/wireframe-builder/styles/wireframe-tokens.css

<interfaces>
<!-- WireframeFilterBar.tsx is self-contained. Key CSS tokens from wireframe-tokens.css: -->

CSS custom properties (both light and dark themes):
- --wf-canvas: page background (#f6f6f8 light, #101622 dark)
- --wf-accent: primary color (var(--wf-primary) = #1152d4)
- --wf-accent-fg: text on accent (#fff both themes)
- --wf-card: card background (#ffffff light, #1a2236 dark)
- --wf-card-border: border color (#e2e8f0 light, #2a3a52 dark)
- --wf-muted: muted text (var(--wf-neutral-400) = #94a3b8)
- --wf-neutral-500: slate-500 (#64748b both themes)
- --wf-body: body text color

FilterOption type (no changes needed):
```typescript
export type FilterOption = {
  key: string
  label: string
  options?: string[]
  filterType?: 'select' | 'date-range' | 'multi-select' | 'search' | 'toggle'
}
```

lucide-react icons already imported: Search, Calendar, ChevronDown
New icons needed: Share2, Download
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sticky blur container + vertical stacked label+control layout</name>
  <files>tools/wireframe-builder/components/WireframeFilterBar.tsx</files>
  <action>
Update the WireframeFilterBar component with these changes:

1. **Container (FILT-01):** Change the root div background from `'var(--wf-card)'` to `'color-mix(in srgb, var(--wf-canvas) 85%, transparent)'`. Add `backdropFilter: 'blur(8px)'` and `WebkitBackdropFilter: 'blur(8px)'`. Keep existing `position: 'sticky'`, `top: 0`, `zIndex: 9` unchanged.

2. **SelectFilter (FILT-02 + FILT-03):** Change from horizontal `alignItems: 'center'` to vertical `flexDirection: 'column', gap: 2`. Label style becomes: `fontSize: 10, fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.05em', color: 'var(--wf-neutral-500)'`. Remove the colon from label text. Select style: `color: 'var(--wf-accent)'`, `fontWeight: 700` (was 500), keep `border: 'none', background: 'transparent'`.

3. **DateRangeFilter (FILT-03):** Same vertical stacked layout for label. Label gets the 10px uppercase bold slate-500 style. Remove colon from label text. The trigger button keeps its existing styles (will be updated in Task 2 for FILT-04).

4. **MultiSelectFilter (FILT-03):** Same vertical stacked layout. Label gets 10px uppercase bold slate-500 style. Remove colon from label text.

5. **SearchFilter (FILT-03):** Same vertical stacked layout. Label gets 10px uppercase bold slate-500 style. Remove colon from label text.

6. **ToggleFilter (FILT-03):** Label gets `fontSize: 10, fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.05em', color: 'var(--wf-neutral-500)'`. Keep horizontal layout (toggle is naturally side-by-side).

7. **Compare toggle label (FILT-05):** Change the "Comparar" span from `fontWeight: 500` to `fontWeight: 700`. Keep `fontSize: 11`.

IMPORTANT: Use inline styles only (no Tailwind classes) — the component uses 100% inline styles. Use `'uppercase' as const` for textTransform to satisfy TypeScript.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Filter bar container is semi-transparent with backdrop-blur. All filter sub-components use vertical stacked label (10px uppercase bold slate-500) above control. Select controls show bold primary text on transparent background. Compare toggle label is bold. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Action button hierarchy — date picker, share, export buttons</name>
  <files>tools/wireframe-builder/components/WireframeFilterBar.tsx</files>
  <action>
Update the action area of WireframeFilterBar:

1. **Import new icons:** Add `Share2` and `Download` to the lucide-react import (alongside existing `Search`, `Calendar`, `ChevronDown`).

2. **DateRangeFilter trigger button (FILT-04):** Update to outline secondary style: `border: '1px solid var(--wf-card-border)'`, `background: 'transparent'` (was `'var(--wf-card)'`), `borderRadius: 8` (was 6).

3. **Add share + export buttons in main component (FILT-04):** In the `showCompareSwitch` block, BEFORE the `<div style={{ flex: 1 }} />` spacer, add a right-action area with three action buttons in this order:
   - Move the existing filter controls area to render first (they already do)
   - After the filters, add the spacer `<div style={{ flex: 1 }} />`
   - After the spacer, render action buttons BEFORE the compare divider:

   Action buttons group (wrap in a div with `display: 'flex', alignItems: 'center', gap: 6`):

   a) **Date picker button** (outline, secondary): Static button with Calendar icon (size 12, color `var(--wf-muted)`), text "Jan — Mar 2026", outline style: `border: '1px solid var(--wf-card-border)'`, `background: 'transparent'`, `borderRadius: 8`, `fontSize: 12`, `fontWeight: 500`, `color: 'var(--wf-body)'`, `padding: '4px 10px'`, `cursor: 'pointer'`, `fontFamily: 'Inter, sans-serif'`, `display: 'flex', alignItems: 'center', gap: 4`.

   b) **Share button** (outline, secondary): Static button with Share2 icon (size 12, color `var(--wf-muted)`), text "Compartilhar", same outline style as date picker button.

   c) **Export button** (filled, primary): Static button with Download icon (size 12), text "Exportar", filled style: `background: 'var(--wf-accent)'`, `color: 'var(--wf-accent-fg)'`, `border: 'none'`, `borderRadius: 8`, `fontSize: 12`, `fontWeight: 600`, `padding: '4px 10px'`, `cursor: 'pointer'`, `fontFamily: 'Inter, sans-serif'`, `display: 'flex', alignItems: 'center', gap: 4`.

4. **Restructure the compare area:** The existing compare area (divider + period selector + "Comparar" label + toggle) follows after the action buttons. Keep the vertical divider between action buttons and compare toggle.

5. **Remove the standalone DateRangeFilter from the filters array rendering** — the date range is now a static action button in the right area. Actually, keep DateRangeFilter as a filter type option since it's driven by blueprint config. The action buttons are ADDITIONAL static buttons that always appear when `showCompareSwitch` is true. The DateRangeFilter in the filters array still renders if a filter has `filterType: 'date-range'`.

Note: The share and export buttons are decorative wireframe mock buttons — no onClick handler needed. Use `type="button"` to prevent form submission.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three action buttons render in the right area: date picker (outline) + share (outline) + export (filled primary), all with rounded-lg (8px). Button hierarchy is visually distinct. Share2 and Download icons imported from lucide-react. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. Visual check: filter bar shows blur effect when content scrolls behind it
3. Visual check: filter labels are 10px uppercase bold above controls
4. Visual check: select controls show bold primary text, transparent background, no border
5. Visual check: three action buttons with outline/filled hierarchy
6. Visual check: compare toggle label is bold
</verification>

<success_criteria>
- WireframeFilterBar.tsx compiles with zero TypeScript errors
- All five FILT requirements (01-05) are implemented in the single file
- No new npm dependencies added
- Inline style pattern maintained (no Tailwind classes introduced)
- Both light and dark themes work correctly via color-mix() and CSS token references
</success_criteria>

<output>
After completion, create `.planning/phases/26-filter-bar-enhancement/26-01-SUMMARY.md`
</output>
