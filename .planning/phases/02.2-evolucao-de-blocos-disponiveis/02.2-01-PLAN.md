---
phase: 02.2-evolucao-de-blocos-disponiveis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ferramentas/blocos/index.md
  - docs/ferramentas/blocos/kpi-card.md
  - docs/ferramentas/blocos/kpi-card-full.md
  - docs/ferramentas/blocos/calculo-card.md
  - docs/ferramentas/blocos/bar-line-chart.md
  - docs/ferramentas/blocos/waterfall-chart.md
  - docs/ferramentas/blocos/donut-chart.md
  - docs/ferramentas/blocos/pareto-chart.md
  - docs/ferramentas/blocos/data-table.md
  - docs/ferramentas/blocos/drill-down-table.md
  - docs/ferramentas/blocos/clickable-table.md
  - docs/ferramentas/blocos/config-table.md
autonomous: true
requirements:
  - BLKSPEC-01

must_haves:
  truths:
    - "Landing page lists all 22 blocks with links to individual spec pages"
    - "Each spec page documents full props table with types, required, defaults"
    - "Each spec page describes visual appearance and conditional states"
    - "Screen recipes include layout/grid hints for Blueprint assembly"
  artifacts:
    - path: "docs/ferramentas/blocos/index.md"
      provides: "Landing page with summary table + screen recipes with layout hints"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/kpi-card.md"
      provides: "KpiCard spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/kpi-card-full.md"
      provides: "KpiCardFull spec with compareMode documentation"
      contains: "compareMode"
    - path: "docs/ferramentas/blocos/calculo-card.md"
      provides: "CalculoCard spec with compareMode and inverted color logic"
      contains: "compareMode"
    - path: "docs/ferramentas/blocos/bar-line-chart.md"
      provides: "BarLineChart spec with type variants"
      contains: "bar | line | bar-line"
    - path: "docs/ferramentas/blocos/waterfall-chart.md"
      provides: "WaterfallChart spec with compareMode"
      contains: "compareMode"
    - path: "docs/ferramentas/blocos/donut-chart.md"
      provides: "DonutChart spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/pareto-chart.md"
      provides: "ParetoChart spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/data-table.md"
      provides: "DataTable spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/drill-down-table.md"
      provides: "DrillDownTable spec with expandable rows"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/clickable-table.md"
      provides: "ClickableTable spec with onRowClick"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/config-table.md"
      provides: "ConfigTable spec"
      contains: "badge: Ferramentas"
  key_links:
    - from: "docs/ferramentas/blocos/index.md"
      to: "individual spec pages"
      via: "markdown links"
      pattern: "/ferramentas/blocos/"
    - from: "each spec page"
      to: "ComponentGallery"
      via: "Ver na galeria link"
      pattern: "/ferramentas/wireframe-builder/galeria"
---

<objective>
Create the landing page and first 11 block spec files (Cards + Charts + Tables categories) for the Blocos Disponiveis specification system.

Purpose: Transform Blocos Disponiveis from a flat catalog into a structured spec system where each component has a full behavior spec usable as a prompt for component creation. This plan covers the landing page with screen recipes and the 11 most complex/feature-rich components.

Output: 12 new .md files in docs/ferramentas/blocos/ (1 index + 11 specs)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-RESEARCH.md

@docs/referencias/blocos-disponiveis.md
@tools/wireframe-builder/types/blueprint.ts

<interfaces>
<!-- Spec file template structure (from RESEARCH.md) -->
Each spec .md file follows this template:

```markdown
---
title: [ComponentName]
badge: Ferramentas
description: [one-line purpose]
---

# [ComponentName]

[2-3 sentence description]

[Ver na galeria](/ferramentas/wireframe-builder/galeria)

## Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|

## Visual Description

[Default visual appearance]

## Conditional States

### [State Name]
[What changes visually]

## Sizing Rules

[Default size, responsive behavior]

## Blueprint Section Type

[The discriminated union type name and mapping]

## Usage Example

[Minimal blueprint.config.ts snippet]
```

Component Inventory (from RESEARCH.md):
| # | Component | Category | Key Props | compareMode? | Blueprint Section Type |
|---|-----------|----------|-----------|--------------|----------------------|
| 1 | KpiCard | Cards | label, value, variation?, description?, variationPositive? | No | (via kpi-grid) |
| 2 | KpiCardFull | Cards | label, value, sub?, variation?, variationPositive?, semaforo?, semaforoLabel?, sparkline?, wide?, compareMode? | Yes | (via kpi-grid) |
| 3 | CalculoCard | Cards | title?, rows: CalculoRow[], compareMode?, comparePeriodLabel? | Yes | calculo-card |
| 4 | BarLineChart | Charts | title, type: bar|line|bar-line, height? | No | bar-line-chart |
| 5 | WaterfallChart | Charts | title, bars: WaterfallBar[], height?, compareMode?, compareBars?, comparePeriodLabel? | Yes | waterfall-chart |
| 6 | DonutChart | Charts | title, data: Slice[], centerValue?, centerLabel?, height? | No | donut-chart |
| 7 | ParetoChart | Charts | title, data: Entry[], height?, valueLabel? | No | pareto-chart |
| 8 | DataTable | Tables | title?, columns: Column[], rowCount? | No | data-table |
| 9 | DrillDownTable | Tables | title?, subtitle?, columns: DrilColumn[], rows: DrilRow[] | No (via viewSwitcher) | drill-down-table |
| 10 | ClickableTable | Tables | title?, subtitle?, columns: ClickColumn[], rows: ClickRow[], onRowClick? | No | clickable-table |
| 11 | ConfigTable | Tables | title, addLabel?, columns: ConfigColumn[], rows: ConfigRow[] | No | config-table |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page and Cards category specs (4 files)</name>
  <files>
    docs/ferramentas/blocos/index.md
    docs/ferramentas/blocos/kpi-card.md
    docs/ferramentas/blocos/kpi-card-full.md
    docs/ferramentas/blocos/calculo-card.md
  </files>
  <action>
    Create directory `docs/ferramentas/blocos/` if it does not exist.

    **index.md** — Landing page with:
    - Frontmatter: `title: Blocos Disponiveis`, `badge: Ferramentas`, `description: Especificacoes completas dos componentes do Wireframe Builder`
    - Summary table listing ALL 22 components with columns: Componente (as link to spec page), Categoria, Descricao
    - Links use format: `[ComponentName](/ferramentas/blocos/component-slug)`
    - Screen Recipes section ("Receitas de Tela") with 8 recipe types evolved from the old `docs/referencias/blocos-disponiveis.md` but with layout/grid hints added. Use this format per recipe:
      ```
      ### DRE / Resultado Mensal
      - WireframeFilterBar (showCompareSwitch=true, filters: segmento, unidade)
      - 4x KpiCardFull in grid 4 col (value=R$, sub=%, compareMode, sparkline, semaforo)
      - CalculoCard + WaterfallChart in grid 2 col 50/50 (both receive compareMode)
      - DetailViewSwitcher above DrillDownTable (options: ["Por Grupo", "Por Categoria"])
      - DrillDownTable (colunas condicionais: compareMode adiciona colunas periodo anterior + var%)
      ```
    - Include all 8 recipe types from the old doc: DRE/Resultado Mensal, Visao Analitica, Centro de Custo/Margens, Fluxo de Caixa Mensal, Fluxo de Caixa Anual, Indicadores de Desempenho, Upload/Inputs, Configuracoes
    - Add layout hints (grid columns, 50/50 splits, "above", stacking) to each recipe

    **Cards specs (3 files)** — For each of KpiCard, KpiCardFull, CalculoCard:
    1. Read the corresponding .tsx file from `tools/wireframe-builder/components/` to extract exact props, visual behavior, and conditional states
    2. Read `tools/wireframe-builder/types/blueprint.ts` for the Blueprint section type mapping
    3. Write the spec .md following the template structure exactly
    4. Every prop MUST be documented (check .tsx carefully for implicit props)
    5. The visual description should be detailed enough to recreate the component from scratch (colors, layout direction, spacing patterns, font sizes)
    6. For components with compareMode (KpiCardFull, CalculoCard): document both ON and OFF states in the Conditional States section
    7. For CalculoCard: document the inverted color logic for operator `(-)` rows
    8. Include `[Ver na galeria](/ferramentas/wireframe-builder/galeria)` link in each spec
    9. Include a Usage Example with a minimal blueprint.config.ts snippet

    IMPORTANT: Specs describe WHAT the component does visually, NOT how the code works internally. Do not copy JSX or implementation details. Describe the visual output a user would see.
  </action>
  <verify>
    <automated>ls docs/ferramentas/blocos/index.md docs/ferramentas/blocos/kpi-card.md docs/ferramentas/blocos/kpi-card-full.md docs/ferramentas/blocos/calculo-card.md && echo "All 4 files exist"</automated>
  </verify>
  <done>
    - index.md has summary table with 22 component links and 8 screen recipes with layout hints
    - KpiCard spec has 5 props documented, visual description, no conditional states (simple component)
    - KpiCardFull spec has 10 props documented, compareMode conditional state, wide conditional state, sparkline behavior
    - CalculoCard spec has 4 props documented, compareMode conditional state, inverted color logic for (-) operator rows
    - All files have `badge: Ferramentas` in frontmatter
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Charts and Tables category specs (8 files)</name>
  <files>
    docs/ferramentas/blocos/bar-line-chart.md
    docs/ferramentas/blocos/waterfall-chart.md
    docs/ferramentas/blocos/donut-chart.md
    docs/ferramentas/blocos/pareto-chart.md
    docs/ferramentas/blocos/data-table.md
    docs/ferramentas/blocos/drill-down-table.md
    docs/ferramentas/blocos/clickable-table.md
    docs/ferramentas/blocos/config-table.md
  </files>
  <action>
    For each of the 8 components (BarLineChart, WaterfallChart, DonutChart, ParetoChart, DataTable, DrillDownTable, ClickableTable, ConfigTable):

    1. Read the corresponding .tsx file from `tools/wireframe-builder/components/`
    2. Read `tools/wireframe-builder/types/blueprint.ts` for Blueprint section type mapping
    3. Write the spec .md following the template structure

    Key details per component:

    **BarLineChart**: Document 3 type variants (bar, line, bar-line) as conditional states. Include how recharts renders each. Document height prop default.

    **WaterfallChart**: Document compareMode conditional state (compareBars shown alongside, comparePeriodLabel header). Document color coding: green=positive, red=negative, blue=subtotal. Complex component -- be thorough on visual description of floating bars.

    **DonutChart**: Document the centerValue/centerLabel center text overlay. Document data slices rendering with legends.

    **ParetoChart**: Document dual-axis rendering (bars on left Y, cumulative line on right Y). Document valueLabel prop.

    **DataTable**: Document column configuration (key, label, align). Document rowCount generating placeholder rows. Document alternating row striping.

    **DrillDownTable**: Document expandable row behavior (parent rows with toggle, child rows hidden by default). Document DrilColumn and DrilRow types. Note: compareMode is handled via viewSwitcher in Blueprint, not as a direct prop.

    **ClickableTable**: Document hover states, cursor pointer, onRowClick behavior. Document ClickColumn and ClickRow types.

    **ConfigTable**: Document inline selects, add row button (addLabel), edit/delete actions per row. Document ConfigColumn and ConfigRow types.

    Same rules as Task 1: describe visuals not code, every prop documented, `badge: Ferramentas`, gallery link, usage example.
  </action>
  <verify>
    <automated>ls docs/ferramentas/blocos/bar-line-chart.md docs/ferramentas/blocos/waterfall-chart.md docs/ferramentas/blocos/donut-chart.md docs/ferramentas/blocos/pareto-chart.md docs/ferramentas/blocos/data-table.md docs/ferramentas/blocos/drill-down-table.md docs/ferramentas/blocos/clickable-table.md docs/ferramentas/blocos/config-table.md && echo "All 8 files exist"</automated>
  </verify>
  <done>
    - All 8 spec files exist with correct frontmatter (`badge: Ferramentas`)
    - BarLineChart has 3 type variants documented
    - WaterfallChart has compareMode conditional state documented
    - DonutChart has center text overlay documented
    - ParetoChart has dual-axis behavior documented
    - All 4 table specs have their specific column/row types documented
    - DrillDownTable has expandable row behavior documented
    - ConfigTable has inline editing behavior documented
    - Every file has Props table, Visual Description, Blueprint Section Type, Usage Example
  </done>
</task>

</tasks>

<verification>
- `ls docs/ferramentas/blocos/*.md | wc -l` returns 12 (1 index + 11 specs)
- `npx tsc --noEmit` passes (no TypeScript changes in this plan, but ensure no broken imports)
- Each spec file has `badge: Ferramentas` in frontmatter
- index.md links point to correct paths matching file names (e.g., `/ferramentas/blocos/kpi-card` matches `kpi-card.md`)
</verification>

<success_criteria>
- 12 new .md files created in docs/ferramentas/blocos/
- Landing page has complete summary table (22 components) and 8 screen recipes with layout hints
- Each spec has: Props table, Visual Description, Conditional States (where applicable), Sizing Rules, Blueprint Section Type, Usage Example
- Spec content is detailed enough to recreate the component from scratch without seeing code
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-01-SUMMARY.md`
</output>
