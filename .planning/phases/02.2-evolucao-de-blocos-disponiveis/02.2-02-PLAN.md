---
phase: 02.2-evolucao-de-blocos-disponiveis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ferramentas/blocos/wireframe-filter-bar.md
  - docs/ferramentas/blocos/wireframe-header.md
  - docs/ferramentas/blocos/wireframe-sidebar.md
  - docs/ferramentas/blocos/wireframe-modal.md
  - docs/ferramentas/blocos/global-filters.md
  - docs/ferramentas/blocos/detail-view-switcher.md
  - docs/ferramentas/blocos/comment-overlay.md
  - docs/ferramentas/blocos/inputs-screen.md
  - docs/ferramentas/blocos/upload-section.md
  - docs/ferramentas/blocos/manual-input-section.md
  - docs/ferramentas/blocos/saldo-banco-input.md
autonomous: true
requirements:
  - BLKSPEC-01

must_haves:
  truths:
    - "All 22 block specs exist as individual .md files"
    - "Layout components document infrastructure role (not data components)"
    - "Input components document their form/upload behavior"
  artifacts:
    - path: "docs/ferramentas/blocos/wireframe-filter-bar.md"
      provides: "WireframeFilterBar spec with compareMode controls"
      contains: "compareMode"
    - path: "docs/ferramentas/blocos/wireframe-header.md"
      provides: "WireframeHeader spec with periodType variants"
      contains: "periodType"
    - path: "docs/ferramentas/blocos/wireframe-sidebar.md"
      provides: "WireframeSidebar spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/wireframe-modal.md"
      provides: "WireframeModal spec with size variants"
      contains: "size"
    - path: "docs/ferramentas/blocos/global-filters.md"
      provides: "GlobalFilters spec (legacy)"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/detail-view-switcher.md"
      provides: "DetailViewSwitcher spec"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/comment-overlay.md"
      provides: "CommentOverlay spec with Supabase/Clerk dependency"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/inputs-screen.md"
      provides: "InputsScreen spec (legacy upload)"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/upload-section.md"
      provides: "UploadSection spec with drag-and-drop"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/manual-input-section.md"
      provides: "ManualInputSection spec with CRUD table"
      contains: "badge: Ferramentas"
    - path: "docs/ferramentas/blocos/saldo-banco-input.md"
      provides: "SaldoBancoInput spec"
      contains: "badge: Ferramentas"
  key_links:
    - from: "each spec page"
      to: "ComponentGallery"
      via: "Ver na galeria link"
      pattern: "/ferramentas/wireframe-builder/galeria"
---

<objective>
Create the remaining 11 block spec files (Layout + Inputs categories) to complete the full 22-component specification system.

Purpose: Complete the spec library so every wireframe builder component has a full behavior spec. Layout components (7) are infrastructure blocks (sidebar, header, filters, modal, etc.) while Input components (4) handle data entry and upload flows.

Output: 11 new .md files in docs/ferramentas/blocos/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-RESEARCH.md

@docs/referencias/blocos-disponiveis.md
@tools/wireframe-builder/types/blueprint.ts

<interfaces>
<!-- Spec file template (same as Plan 01) -->
Each spec .md file follows this template:

```markdown
---
title: [ComponentName]
badge: Ferramentas
description: [one-line purpose]
---

# [ComponentName]

[2-3 sentence description]

[Ver na galeria](/ferramentas/wireframe-builder/galeria)

## Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|

## Visual Description

[Default visual appearance]

## Conditional States (if applicable)

## Sizing Rules

## Blueprint Section Type

## Usage Example
```

Component Inventory for this plan:
| # | Component | Category | Key Props | compareMode? | Blueprint Section Type |
|---|-----------|----------|-----------|--------------|----------------------|
| 12 | WireframeSidebar | Layout | screens: Screen[], onSelect? | No | N/A (infra) |
| 13 | WireframeHeader | Layout | title, periodType?: mensal|anual|none | No | N/A (infra) |
| 14 | WireframeFilterBar | Layout | filters: FilterOption[], showSearch?, showCompareSwitch?, compareMode?, comparePeriodType?, comparePeriod?, onComparePeriodChange? | Yes (controls it) | N/A (infra) |
| 15 | GlobalFilters | Layout | filters: FilterType[] | No | N/A (legacy) |
| 16 | WireframeModal | Layout | title, open, onClose, children, footer?, size?: md|lg|xl | No | N/A (infra) |
| 17 | DetailViewSwitcher | Layout | options: string[], activeOption, onChange | No | (via viewSwitcher) |
| 18 | CommentOverlay | Layout | clientSlug, screenId, targetId, authorId, authorName, authorRole, open, onClose | No | N/A (infra) |
| 19 | InputsScreen | Inputs | acceptedFormats: string[], instructions?, fieldName? | No | N/A (legacy) |
| 20 | UploadSection | Inputs | label, acceptedFormats?, successMessage?, errorMessages?, history? | No | upload-section |
| 21 | ManualInputSection | Inputs | title?, initialBalance?, entries?: AdjustEntry[] | No | manual-input |
| 22 | SaldoBancoInput | Inputs | title?, note?, banks: BankEntry[], total | No | saldo-banco |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout category specs (7 files)</name>
  <files>
    docs/ferramentas/blocos/wireframe-filter-bar.md
    docs/ferramentas/blocos/wireframe-header.md
    docs/ferramentas/blocos/wireframe-sidebar.md
    docs/ferramentas/blocos/wireframe-modal.md
    docs/ferramentas/blocos/global-filters.md
    docs/ferramentas/blocos/detail-view-switcher.md
    docs/ferramentas/blocos/comment-overlay.md
  </files>
  <action>
    For each of the 7 Layout components, read the .tsx file from `tools/wireframe-builder/components/` and write the spec .md.

    Key details per component:

    **WireframeFilterBar** (most complex layout component, 196 lines):
    - Document ALL props carefully -- this component has many (filters, showSearch, showCompareSwitch, compareMode, comparePeriodType, comparePeriod, onComparePeriodChange, onCompareModeChange)
    - Document the compareMode flow: switch toggle reveals period selector dropdown
    - Document sticky positioning behavior
    - Blueprint Section Type: N/A -- this is infrastructure, not a declarable section. Used via `hasCompareSwitch` and `filters` on BlueprintScreen.
    - Note: this component CONTROLS compareMode for the screen, it does not receive it as display-only

    **WireframeHeader** (90 lines):
    - Document 3 periodType variants (mensal, anual, none) as conditional states
    - Document the left title + center period navigator + right empty space layout
    - Blueprint Section Type: N/A -- rendered by WireframeViewer, not declared in sections

    **WireframeSidebar** (35 lines):
    - Document dark background (#212121), screen list with icons
    - Document onSelect callback
    - Blueprint Section Type: N/A -- rendered by WireframeViewer

    **WireframeModal** (49 lines):
    - Document 3 size variants (md, lg, xl) as conditional states
    - Document overlay, title bar, children area, footer slot
    - Blueprint Section Type: N/A -- triggered by ClickableTable row click

    **GlobalFilters** (35 lines):
    - Mark as LEGACY in the description -- superseded by WireframeFilterBar for new wireframes
    - Document filters array with FilterType
    - Blueprint Section Type: N/A (legacy)

    **DetailViewSwitcher** (26 lines):
    - Document pill/tab selector behavior
    - Document interaction with DrillDownTable (switches dimension, does not affect totals)
    - Blueprint Section Type: used via `viewSwitcher` field on drill-down-table section

    **CommentOverlay** (171 lines):
    - Document as infrastructure component requiring Supabase and Clerk auth
    - Document drawer behavior (slide-in from right, thread display, reply form)
    - Document all 8 props (clientSlug, screenId, targetId, authorId, authorName, authorRole, open, onClose)
    - Blueprint Section Type: N/A -- rendered by SectionWrapper, not declared in config

    For Layout components, the Blueprint Section Type section should explicitly state "N/A -- infrastructure component" and explain HOW the component is used (by WireframeViewer, by SectionWrapper, by other components) rather than as a declarable section.

    All files: `badge: Ferramentas`, gallery link, usage example showing typical usage pattern (even if not blueprint.config.ts, show the React usage).
  </action>
  <verify>
    <automated>ls docs/ferramentas/blocos/wireframe-filter-bar.md docs/ferramentas/blocos/wireframe-header.md docs/ferramentas/blocos/wireframe-sidebar.md docs/ferramentas/blocos/wireframe-modal.md docs/ferramentas/blocos/global-filters.md docs/ferramentas/blocos/detail-view-switcher.md docs/ferramentas/blocos/comment-overlay.md && echo "All 7 Layout files exist"</automated>
  </verify>
  <done>
    - WireframeFilterBar spec documents all props including compareMode flow
    - WireframeHeader spec documents 3 periodType variants
    - WireframeModal spec documents 3 size variants
    - GlobalFilters marked as legacy with note about WireframeFilterBar replacement
    - CommentOverlay documents Supabase/Clerk dependency
    - All files have `badge: Ferramentas` and gallery link
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Inputs category specs (4 files)</name>
  <files>
    docs/ferramentas/blocos/inputs-screen.md
    docs/ferramentas/blocos/upload-section.md
    docs/ferramentas/blocos/manual-input-section.md
    docs/ferramentas/blocos/saldo-banco-input.md
  </files>
  <action>
    For each of the 4 Input components, read the .tsx file from `tools/wireframe-builder/components/` and write the spec .md.

    Key details per component:

    **InputsScreen** (37 lines):
    - Mark as LEGACY in description -- superseded by UploadSection for new wireframes
    - Document basic drag-and-drop upload area
    - Document acceptedFormats display, instructions text, fieldName
    - Blueprint Section Type: N/A (legacy)

    **UploadSection** (126 lines):
    - Document drag-and-drop zone with file format badges
    - Document success/error feedback messages with visual indicators
    - Document history section showing past uploads (file name, date, status)
    - Document the simulated upload flow in wireframe context
    - Blueprint Section Type: `upload-section`

    **ManualInputSection** (117 lines):
    - Document CRUD table: add entry, edit entry, remove entry
    - Document initialBalance display
    - Document AdjustEntry type (description, value, date)
    - Document inline editing with Editar/Remover buttons per row
    - Blueprint Section Type: `manual-input`

    **SaldoBancoInput** (38 lines):
    - Document bank grid layout with one input field per bank
    - Document total consolidation at bottom (automatic sum)
    - Document BankEntry type (name, value)
    - Blueprint Section Type: `saldo-banco`

    All files: `badge: Ferramentas`, gallery link, usage example with blueprint.config.ts snippet where applicable (UploadSection, ManualInputSection, SaldoBancoInput), React usage for legacy InputsScreen.

    After creating all 4 files, verify that together with Plan 01's files, the total spec count is 22 (11 from Plan 01 + 11 from this plan).
  </action>
  <verify>
    <automated>ls docs/ferramentas/blocos/inputs-screen.md docs/ferramentas/blocos/upload-section.md docs/ferramentas/blocos/manual-input-section.md docs/ferramentas/blocos/saldo-banco-input.md && echo "All 4 Input files exist"</automated>
  </verify>
  <done>
    - InputsScreen marked as legacy, documents basic upload area
    - UploadSection documents drag-and-drop, feedback, and history
    - ManualInputSection documents CRUD table with inline editing
    - SaldoBancoInput documents bank grid with auto-total
    - All files have `badge: Ferramentas`, Props table, Visual Description, Usage Example
    - Combined with Plan 01: 23 total files (1 index + 22 specs)
  </done>
</task>

</tasks>

<verification>
- `ls docs/ferramentas/blocos/*.md | wc -l` returns at least 11 from this plan (22 or 23 total if Plan 01 also completed)
- Each spec file has `badge: Ferramentas` in frontmatter
- `npx tsc --noEmit` passes (no TypeScript changes)
- Every spec has Props, Visual Description, and Usage Example sections
</verification>

<success_criteria>
- 11 new .md files created in docs/ferramentas/blocos/ (7 Layout + 4 Inputs)
- Layout specs clearly document infrastructure role and how they integrate with WireframeViewer
- Input specs document form/upload behavior with CRUD patterns
- Legacy components (GlobalFilters, InputsScreen) marked as legacy with replacement notes
- Combined with Plan 01, all 22 components have individual spec files
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-02-SUMMARY.md`
</output>
