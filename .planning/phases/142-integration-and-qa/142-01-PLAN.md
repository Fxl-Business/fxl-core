---
phase: 142-integration-and-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/pages/admin/ModulesPanel.tsx
autonomous: true
requirements: [DIAG-03]

must_haves:
  truths:
    - "Clicking any module node in the diagram scrolls the page to the corresponding ModuleOverviewCard"
    - "The target card receives a visible ring highlight (ring-2 ring-indigo-500) that auto-removes after 2 seconds"
    - "Diagram nodes show cursor-pointer to indicate clickability"
    - "Ring highlight transitions smoothly via transition-shadow duration-300"
  artifacts:
    - path: "src/platform/pages/admin/ModulesPanel.tsx"
      provides: "Updated page with highlightedModuleId state wiring diagram onNodeClick to card highlight"
      contains: "highlightedModuleId"
  key_links:
    - from: "src/platform/pages/admin/ModulesPanel.tsx"
      to: "ModuleDependencyDiagram"
      via: "onNodeClick prop"
      pattern: "onNodeClick"
    - from: "src/platform/pages/admin/ModulesPanel.tsx"
      to: "ModuleOverviewCard"
      via: "highlighted prop"
      pattern: "highlighted"
---

<objective>
Wire click-to-scroll navigation between the ModuleDependencyDiagram and ModuleOverviewCard grid on /admin/modules.

Purpose: This is the primary integration deliverable of Phase 142 — connecting the diagram (Phase 140) to the cards (Phase 141) so clicking a node scrolls to and highlights the corresponding card.
Output: Clicking any node in the diagram smoothly scrolls to the matching card and applies a temporary ring highlight.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/platform/module-loader/registry.ts:
```typescript
export interface ModuleDefinition extends ModuleManifest {
  id: ModuleId
  description: string
  status: ModuleStatus // 'active' | 'beta' | 'coming-soon'
  extensions?: ModuleExtension[]
  // ... other fields
}

export const MODULE_REGISTRY: ModuleDefinition[]
```

ModuleDependencyDiagram component (created in Phase 140):
- Renders SVG nodes for each module
- Currently accepts hover-related props
- Needs: `onNodeClick?: (moduleId: string) => void` prop added

ModuleOverviewCard component (created in Phase 141):
- Renders read-only card with module info
- Needs: `highlighted?: boolean` prop and `id` attribute on root element

ModulesPanel.tsx (after Phase 139+140+141):
- No longer has tenant selector or toggle switches
- Contains ModuleDependencyDiagram and ModuleOverviewCard grid
- Derives all data from MODULE_REGISTRY via useMemo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read current state of ModulesPanel, diagram, and card components</name>
  <files>src/platform/pages/admin/ModulesPanel.tsx</files>
  <read_first>
    - src/platform/pages/admin/ModulesPanel.tsx
  </read_first>
  <action>
    Read the current ModulesPanel.tsx to understand the exact layout after Phases 139-141.
    Identify:
    1. Where ModuleDependencyDiagram is rendered and its current props
    2. Where ModuleOverviewCard grid is rendered and its current props
    3. What imports exist

    Also read the ModuleDependencyDiagram and ModuleOverviewCard component files to see their current prop interfaces. Use Glob to find them if needed (they were created in Phase 140 and 141).
  </action>
  <verify>
    <automated>echo "Read complete"</automated>
  </verify>
  <acceptance_criteria>
    - Understand exact current props for both components
    - Know the file paths of diagram and card components
  </acceptance_criteria>
  <done>Current component structure understood, ready to wire integration</done>
</task>

<task type="auto">
  <name>Task 2: Add onNodeClick prop to ModuleDependencyDiagram</name>
  <files>ModuleDependencyDiagram component file (find via Glob in src/platform/)</files>
  <read_first>
    - The diagram component file (found in Task 1)
  </read_first>
  <action>
    1. Add `onNodeClick?: (moduleId: string) => void` to the component's props interface
    2. In each SVG node's group element, add:
       - `onClick={() => onNodeClick?.(moduleId)}` handler
       - `className="cursor-pointer"` or `style={{ cursor: 'pointer' }}` on the node group
       - A brief opacity feedback: wrap click handler to add a CSS class or inline style pulse
    3. Ensure the onClick does not conflict with existing hover handlers
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Component accepts optional onNodeClick prop
    - Each node group has onClick handler and cursor-pointer
    - Existing hover behavior is unaffected
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Diagram nodes are clickable and call onNodeClick with the module ID</done>
</task>

<task type="auto">
  <name>Task 3: Add highlighted prop and id attribute to ModuleOverviewCard</name>
  <files>ModuleOverviewCard component file (find via Glob in src/platform/)</files>
  <read_first>
    - The card component file (found in Task 1)
  </read_first>
  <action>
    1. Add `highlighted?: boolean` and `id?: string` to the component's props interface
    2. On the root div element:
       - Add `id={id}` attribute
       - Add conditional ring classes: `highlighted && 'ring-2 ring-indigo-500 dark:ring-indigo-400'`
       - Add transition classes: `transition-shadow duration-300`
       - Use `cn()` utility to merge classes
    3. Ensure the ring does not clash with existing border or shadow styles
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Component accepts optional highlighted and id props
    - When highlighted=true, card shows ring-2 ring-indigo-500
    - Transition-shadow duration-300 is on the root element
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Cards can receive highlight state and have stable DOM id for scrollIntoView targeting</done>
</task>

<task type="auto">
  <name>Task 4: Wire highlightedModuleId state in ModulesPanel</name>
  <files>src/platform/pages/admin/ModulesPanel.tsx</files>
  <read_first>
    - src/platform/pages/admin/ModulesPanel.tsx (re-read after Tasks 2-3)
  </read_first>
  <action>
    1. Add state: `const [highlightedModuleId, setHighlightedModuleId] = useState<string | null>(null)`
    2. Create handler:
       ```typescript
       const handleNodeClick = useCallback((moduleId: string) => {
         const el = document.getElementById(`module-card-${moduleId}`)
         if (el) {
           el.scrollIntoView({ behavior: 'smooth', block: 'center' })
           setHighlightedModuleId(moduleId)
           setTimeout(() => setHighlightedModuleId(null), 2000)
         }
       }, [])
       ```
    3. Pass `onNodeClick={handleNodeClick}` to ModuleDependencyDiagram
    4. Pass `id={`module-card-${mod.id}`}` and `highlighted={highlightedModuleId === mod.id}` to each ModuleOverviewCard
    5. Add `useCallback` to imports if not already present
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ModulesPanel has highlightedModuleId state
    - handleNodeClick scrolls to card and sets highlight with 2s auto-clear
    - Diagram receives onNodeClick prop
    - Each card receives id and highlighted props
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Click any diagram node and the page scrolls to the corresponding card with a temporary ring highlight</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Diagram nodes show cursor-pointer
3. Clicking a node scrolls to the matching card
4. Card displays ring highlight for ~2 seconds then fades
5. Existing hover behavior on diagram still works
</verification>

<success_criteria>
- Admin can click any module node in the diagram and the page scrolls to the corresponding module card
- The scrolled-to card receives a visible ring highlight that auto-removes after 2 seconds
- All diagram hover behavior from Phase 140 continues to work
- npx tsc --noEmit passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/142-integration-and-qa/142-01-SUMMARY.md`
</output>
