---
phase: 142-integration-and-qa
plan: 02
type: execute
wave: 2
depends_on: [142-01]
files_modified: []
autonomous: true
requirements: [DIAG-03]

must_haves:
  truths:
    - "grep -r 'admin/modules' src/ returns zero results inside TenantDetailPage.tsx"
    - "grep -r '<Switch' src/platform/pages/admin/ModulesPanel returns zero results"
    - "npx tsc --noEmit passes with zero errors"
    - "No stale imports or unused variables in ModulesPanel.tsx"
  artifacts: []
  key_links: []
---

<objective>
Complete the full system QA pass for the v12.0 milestone, verifying all success criteria from Phases 139-142 are met.

Purpose: This is the quality gate for the entire v12.0 Admin Modules Overview milestone. No code ships without passing every check.
Output: All QA checks pass. Any issues found are fixed in-place.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify stale reference removal from TenantDetailPage</name>
  <files>src/platform/pages/admin/TenantDetailPage.tsx</files>
  <read_first>
    - src/platform/pages/admin/TenantDetailPage.tsx (search for "admin/modules")
  </read_first>
  <action>
    Run: `grep -rn "admin/modules" src/platform/pages/admin/TenantDetailPage.tsx`

    **Expected result:** Zero matches.

    If any matches are found:
    1. Read the surrounding context (10 lines before/after)
    2. Remove the stale deep-link to /admin/modules (the "Gerenciar modulos" link that was supposed to be removed in Phase 139)
    3. Clean up any orphaned imports (e.g., Link from react-router-dom if no longer used elsewhere in the file)

    Also verify there are no other references to the old modules management pattern:
    - `grep -rn "Gerenciar modulos" src/` should return zero results
    - `grep -rn "modules?org=" src/` should return zero results
  </action>
  <verify>
    <automated>grep -rn "admin/modules" src/platform/pages/admin/TenantDetailPage.tsx 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Zero grep matches for "admin/modules" in TenantDetailPage.tsx
    - Zero grep matches for "Gerenciar modulos" anywhere in src/
    - Zero grep matches for "modules?org=" anywhere in src/
  </acceptance_criteria>
  <done>TenantDetailPage has no stale references to /admin/modules</done>
</task>

<task type="auto">
  <name>Task 2: Verify toggle removal from ModulesPanel</name>
  <files>src/platform/pages/admin/ModulesPanel.tsx</files>
  <read_first>
    - src/platform/pages/admin/ModulesPanel.tsx
  </read_first>
  <action>
    Run: `grep -rn "<Switch" src/platform/pages/admin/ModulesPanel.tsx`

    **Expected result:** Zero matches.

    Also verify:
    - `grep -rn "Switch" src/platform/pages/admin/ModulesPanel.tsx` — zero matches (no Switch import either)
    - `grep -rn "onToggle" src/platform/pages/admin/ModulesPanel.tsx` — zero matches
    - `grep -rn "handleToggle" src/platform/pages/admin/ModulesPanel.tsx` — zero matches
    - `grep -rn "tenant_modules" src/platform/pages/admin/ModulesPanel.tsx` — zero matches (no Supabase toggle calls)
    - `grep -rn "selectedOrgId" src/platform/pages/admin/ModulesPanel.tsx` — zero matches (no tenant selector)
    - `grep -rn "useActiveOrg" src/platform/pages/admin/ModulesPanel.tsx` — zero matches

    If any matches are found, remove the offending code and clean up related imports/state.
  </action>
  <verify>
    <automated>grep -c "<Switch\|onToggle\|handleToggle\|selectedOrgId\|useActiveOrg" src/platform/pages/admin/ModulesPanel.tsx 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Zero Switch components in ModulesPanel
    - Zero toggle-related handlers or state
    - Zero tenant selector state or hooks
    - ModulesPanel is purely a read-only overview page
  </acceptance_criteria>
  <done>ModulesPanel contains zero toggle or tenant-selector code</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript compilation check</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run: `npx tsc --noEmit`

    **Expected result:** Zero errors, zero warnings.

    If errors are found:
    1. Read each error message carefully
    2. Fix the root cause (never use `any` as a workaround)
    3. Re-run `npx tsc --noEmit` until clean
    4. Common issues to watch for:
       - Missing prop types on updated components
       - Unused imports from removed code
       - Type mismatches from new prop additions
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0
    - Zero errors in output
  </acceptance_criteria>
  <done>Full TypeScript compilation passes with zero errors</done>
</task>

<task type="auto">
  <name>Task 4: Cross-reference audit of AdminDashboard module links</name>
  <files>src/platform/pages/admin/AdminDashboard.tsx</files>
  <read_first>
    - src/platform/pages/admin/AdminDashboard.tsx (search for "modules")
  </read_first>
  <action>
    Verify that AdminDashboard links to /admin/modules still make sense (they should — that page still exists, it's just now an overview page instead of a management page).

    Check:
    1. `grep -n "admin/modules" src/platform/pages/admin/AdminDashboard.tsx` — should still have links
    2. Verify the link text/description matches the new page purpose (overview, not "gerenciar modulos")
    3. If link text says "Gerenciar" or implies tenant management, update to reflect the overview nature (e.g., "Visao geral dos modulos")

    Also check AdminSidebar:
    - `grep -n "modules" src/platform/layout/AdminSidebar.tsx` — verify nav item label is appropriate
  </action>
  <verify>
    <automated>grep -n "admin/modules" src/platform/pages/admin/AdminDashboard.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - AdminDashboard links to /admin/modules exist and have appropriate labels
    - No link text implies per-tenant module management on the /admin/modules page
    - AdminSidebar nav item for modules is appropriately labeled
  </acceptance_criteria>
  <done>All cross-references to /admin/modules are consistent with the new overview page purpose</done>
</task>

<task type="auto">
  <name>Task 5: Visual QA checklist</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run `make dev` or `npm run dev` and verify in browser at localhost:

    1. Navigate to /admin/modules
    2. Verify diagram renders with all module nodes and edges
    3. Hover over a node — edges highlight correctly (Phase 140 requirement)
    4. Click a node — page scrolls to the corresponding card with ring highlight
    5. Ring highlight disappears after ~2 seconds
    6. Toggle dark mode — verify diagram and cards render correctly
    7. Toggle back to light mode — verify no visual artifacts
    8. Resize viewport: cards reflow from 1 to 3 columns correctly
    9. Navigate to /admin/tenants/{id} — verify TenantModulesSection has toggle controls
    10. Verify /admin/tenants/{id} has NO "Gerenciar modulos" link to /admin/modules

    **Note:** This task is a manual verification checklist. The executor should run the dev server and visually confirm each item. If any item fails, fix the issue before proceeding.
  </action>
  <verify>
    <automated>echo "Visual QA requires manual verification in browser"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 visual checks pass
    - Both dark and light mode render correctly
    - Responsive grid reflows properly
  </acceptance_criteria>
  <done>Full visual QA pass completed successfully</done>
</task>

</tasks>

<verification>
1. `grep -r "admin/modules" src/platform/pages/admin/TenantDetailPage.tsx` returns zero results
2. `grep -r "<Switch" src/platform/pages/admin/ModulesPanel.tsx` returns zero results
3. `npx tsc --noEmit` passes with zero errors
4. All cross-references to /admin/modules have appropriate labels
5. Visual QA confirms diagram-to-card click navigation works
</verification>

<success_criteria>
- Zero stale "admin/modules" references in TenantDetailPage.tsx
- Zero Switch/toggle code in ModulesPanel.tsx
- npx tsc --noEmit zero errors
- AdminDashboard links to /admin/modules have overview-appropriate labels
- Visual QA confirms full integration works in both themes
</success_criteria>

<output>
After completion, create `.planning/phases/142-integration-and-qa/142-02-SUMMARY.md`
</output>
