---
phase: 02.3-reformulacao-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - tailwind.config.ts
  - src/components/layout/ThemeToggle.tsx
  - src/components/layout/Layout.tsx
  - src/components/layout/TopNav.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - VISUAL-01

must_haves:
  truths:
    - "CSS variables define a complete light and dark palette with gold accent and cool gray neutrals"
    - "Tailwind config uses only semantic tokens -- no fxl.* color namespace"
    - "Dark mode toggle switches between light and dark themes and persists across page reload"
    - "Layout shell (TopNav, Sidebar, main area) renders correctly in both light and dark modes"
    - "Active sidebar items use the new design language instead of fxl-navy"
    - "Prose styles for markdown rendering use semantic tokens, not hardcoded fxl-navy/slate"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Complete CSS variable system with :root (light) and .dark blocks, plus migrated prose styles"
      contains: ".dark"
    - path: "tailwind.config.ts"
      provides: "Semantic-only color config -- no fxl.* namespace"
    - path: "src/components/layout/ThemeToggle.tsx"
      provides: "Dark mode toggle component with localStorage persistence"
      exports: ["ThemeToggle"]
    - path: "src/components/layout/Layout.tsx"
      provides: "Theme-aware layout shell"
    - path: "src/components/layout/TopNav.tsx"
      provides: "Theme-aware top nav with ThemeToggle integration"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Theme-aware sidebar with new active states"
  key_links:
    - from: "src/styles/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variables consumed by hsl(var(--token)) in Tailwind"
      pattern: "hsl\\(var\\(--"
    - from: "src/components/layout/ThemeToggle.tsx"
      to: "document.documentElement"
      via: "classList.toggle('dark') triggers CSS variable switch"
      pattern: "classList.toggle.*dark"
    - from: "src/components/layout/TopNav.tsx"
      to: "src/components/layout/ThemeToggle.tsx"
      via: "ThemeToggle rendered in TopNav header"
      pattern: "ThemeToggle"
---

<objective>
Design system foundation and layout shell migration. Define the complete CSS variable palette (light + dark with gold accent), remove the fxl.* color namespace from Tailwind, create the ThemeToggle component, and migrate the entire layout shell (Layout, TopNav, Sidebar) plus prose styles to semantic tokens.

Purpose: This is the foundation all other plans depend on. Every downstream component references these CSS variables and layout patterns.
Output: Working design system with light/dark mode, theme-aware layout shell, no fxl.* colors remaining in config or layout files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-reformulacao-visual/02.3-CONTEXT.md
@.planning/phases/02.3-reformulacao-visual/02.3-RESEARCH.md

Before making ANY visual changes, read and follow the rules from these skills:
- frontend-design (production-grade design quality)
- shadcn (correct shadcn/ui component usage)
- ui-ux-pro-max (palette, typography, design decisions)
- web-design-guidelines (accessibility, UI best practices)

<interfaces>
<!-- Current CSS variables in globals.css that must be replaced -->
From src/styles/globals.css:
```css
:root {
  --background: 0 0% 100%;
  --foreground: 215 28% 17%;
  --primary: 215 72% 24%;       /* This is fxl-navy -- MUST change */
  --accent: 210 40% 96%;        /* Currently same as secondary -- MUST become gold */
  /* ... other tokens */
}
```

<!-- Current fxl.* namespace in tailwind.config.ts that must be removed -->
From tailwind.config.ts:
```typescript
colors: {
  fxl: {
    navy: '#1B3A5C',
    'navy-light': '#244d7a',
    'navy-dark': '#122840',
    blue: '#2563EB',
    'blue-muted': '#90B4D4'
  },
  // ... semantic tokens (keep these)
}
```

<!-- Layout gradient that must become theme-aware -->
From src/components/layout/Layout.tsx:
```typescript
className="bg-[linear-gradient(180deg,#f7fafc_0%,#ffffff_28%,#f4f7fb_100%)]"
```

<!-- Sidebar active states that reference fxl-navy -->
From src/components/layout/Sidebar.tsx:
```typescript
isActive ? 'bg-fxl-navy text-white font-medium shadow-sm' : '...'
isActive && depth > 0 ? 'font-medium text-fxl-navy' : ''
```

<!-- TopNav logo badge that references fxl-navy -->
From src/components/layout/TopNav.tsx:
```typescript
className="bg-fxl-navy shadow-sm"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design token system and Tailwind config</name>
  <files>src/styles/globals.css, tailwind.config.ts, src/components/layout/ThemeToggle.tsx</files>
  <action>
1. **globals.css** -- Replace the entire `:root` block with a new light-mode palette inspired by Supabase docs:
   - Primary: cool dark gray-blue (NOT fxl-navy #1B3A5C, choose a new value per CONTEXT.md decision)
   - Accent: gold tone (HSL around hue 43, high saturation -- Claude picks best tone per discretion)
   - Neutrals: cool grays (slate/zinc scale mapped to semantic tokens)
   - Add sidebar-specific tokens: --sidebar, --sidebar-foreground, --sidebar-accent, --sidebar-border, --sidebar-muted, --sidebar-muted-foreground
   - Add code block tokens: --code-bg, --code-fg for MarkdownRenderer use
   - Keep --destructive and chart tokens appropriate

2. **globals.css** -- Add a complete `.dark` block with inverted palette:
   - Dark background: very dark cool gray (HSL ~222 47% 6-8%)
   - Dark foreground: near-white cool gray
   - Gold accent: slightly desaturated for dark mode comfort
   - All sidebar tokens inverted
   - Code block tokens appropriate for dark mode

3. **globals.css** -- Migrate ALL prose styles from hardcoded colors to semantic tokens:
   - `.prose h1`, `.prose h2`: `text-fxl-navy` -> `text-foreground`
   - `.prose code`: `text-fxl-navy` -> `text-primary`
   - `.prose pre`: `bg-slate-900 text-slate-100` -> use --code-bg/--code-fg tokens
   - `.prose th`: `bg-fxl-navy text-white` -> `bg-muted text-foreground font-semibold`
   - `.prose a`: `text-fxl-blue` -> `text-primary` with hover state
   - `.prose blockquote`: `border-fxl-navy` -> `border-accent`

4. **tailwind.config.ts** -- Remove the entire `fxl: { ... }` color block. Keep all semantic token mappings (border, background, foreground, primary, secondary, accent, muted, card, popover, destructive, chart). Add sidebar tokens mapped to CSS variables: `sidebar: { DEFAULT, foreground, accent, border, muted, 'muted-foreground' }`.

5. **ThemeToggle.tsx** -- Create new component at `src/components/layout/ThemeToggle.tsx`:
   - Use the pattern from RESEARCH.md: localStorage persistence + classList.toggle('dark')
   - On mount: check localStorage, then system preference (prefers-color-scheme)
   - Render shadcn Button variant="ghost" size="icon" with Moon/Sun icons from lucide-react
   - Export named `ThemeToggle`

  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>globals.css has complete :root and .dark blocks with gold accent and cool gray palette. Prose styles use semantic tokens. tailwind.config.ts has zero fxl.* references. ThemeToggle component created and type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Layout shell migration (Layout, TopNav, Sidebar)</name>
  <files>src/components/layout/Layout.tsx, src/components/layout/TopNav.tsx, src/components/layout/Sidebar.tsx</files>
  <action>
1. **Layout.tsx** -- Replace the hardcoded gradient `bg-[linear-gradient(180deg,#f7fafc_0%,#ffffff_28%,#f4f7fb_100%)]` with `bg-background`. The gradient was light-mode only and breaks in dark mode. A clean `bg-background` is the correct semantic approach. Also ensure the header `bg-white/90` in TopNav will be handled there.

2. **TopNav.tsx** -- Migrate all colors:
   - Logo badge: `bg-fxl-navy` -> use accent or primary (Claude decides which looks better for the FXL brand mark)
   - Header background: `bg-white/90` -> `bg-background/90` for dark mode support
   - Add `ThemeToggle` component next to `SearchCommand` (import from `./ThemeToggle`)
   - Ensure backdrop-blur works in both modes

3. **Sidebar.tsx** -- Migrate all active states and colors:
   - Leaf active state: `bg-fxl-navy text-white font-medium shadow-sm` -> new design (Claude's discretion using design skills -- options: `bg-primary/10 text-primary`, `bg-accent/10 text-accent-foreground border-l-2 border-accent`, or another approach that feels production-grade)
   - Parent-with-href active state: `text-fxl-navy` -> `text-primary` or `text-accent-foreground`
   - Sidebar container: `bg-white/80` -> `bg-sidebar` (uses sidebar CSS variable)
   - Verify all 3 fxl-navy occurrences are replaced
   - Ensure hover states, transition classes, and spacing remain functional
   - Keep the existing navigation data structure unchanged -- only change visual classes

  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit && grep -r "fxl-navy\|fxl-blue\|fxl\.navy\|fxl\.blue" src/components/layout/ src/styles/globals.css tailwind.config.ts; echo "Exit: $?"</automated>
  </verify>
  <done>Layout shell renders with new design system in both light and dark modes. Zero fxl-navy/fxl-blue references in layout files, globals.css, and tailwind.config.ts. ThemeToggle visible in TopNav. Sidebar active states use new design language. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` -- zero errors
2. `grep -r "fxl-navy\|fxl-blue" src/styles/ src/components/layout/ tailwind.config.ts` -- zero results
3. globals.css has both `:root` and `.dark` blocks
4. ThemeToggle component exists and is rendered in TopNav
</verification>

<success_criteria>
- Complete CSS variable system with light and dark palettes (gold accent, cool gray neutrals)
- Tailwind config has zero fxl.* colors
- Dark mode toggle component works with localStorage persistence
- Layout, TopNav, and Sidebar render correctly in both modes
- All prose styles use semantic tokens
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-reformulacao-visual/02.3-01-SUMMARY.md`
</output>
