---
phase: 09-component-library-expansion
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tools/wireframe-builder/components/sections/ChartRenderer.tsx
  - tools/wireframe-builder/components/RadarChartComponent.tsx
  - tools/wireframe-builder/components/TreemapComponent.tsx
  - tools/wireframe-builder/components/FunnelChartComponent.tsx
  - tools/wireframe-builder/components/ScatterChartComponent.tsx
  - tools/wireframe-builder/components/AreaChartComponent.tsx
  - tools/wireframe-builder/components/editor/property-forms/BarLineChartForm.tsx
  - tools/wireframe-builder/lib/section-registry.ts
autonomous: true
requirements: [COMP-08]

must_haves:
  truths:
    - "Operator can select RadarChart as a chart variant and see it render with sample data"
    - "Operator can select Treemap as a chart variant and see it render with sample data"
    - "Operator can select FunnelChart as a chart variant and see it render with sample data"
    - "Operator can select ScatterChart as a chart variant and see it render with sample data"
    - "Operator can select AreaChart as a chart variant and see it render with sample data"
    - "All 5 new chart types respect chartColors from branding"
  artifacts:
    - path: "tools/wireframe-builder/components/RadarChartComponent.tsx"
      provides: "Radar chart with PolarGrid, PolarAngleAxis, and wireframe tokens"
      min_lines: 20
    - path: "tools/wireframe-builder/components/TreemapComponent.tsx"
      provides: "Treemap with proportional rectangles and wireframe tokens"
      min_lines: 15
    - path: "tools/wireframe-builder/components/FunnelChartComponent.tsx"
      provides: "Funnel chart with stage-colored cells and wireframe tokens"
      min_lines: 20
    - path: "tools/wireframe-builder/components/ScatterChartComponent.tsx"
      provides: "Scatter plot with CartesianGrid and wireframe tokens"
      min_lines: 20
    - path: "tools/wireframe-builder/components/AreaChartComponent.tsx"
      provides: "Area chart with gradient fill and wireframe tokens"
      min_lines: 20
  key_links:
    - from: "tools/wireframe-builder/components/sections/ChartRenderer.tsx"
      to: "tools/wireframe-builder/components/*ChartComponent.tsx"
      via: "switch on section.chartType dispatches to chart components"
      pattern: "case 'radar'|case 'treemap'|case 'funnel'|case 'scatter'|case 'area'"
    - from: "tools/wireframe-builder/components/editor/property-forms/BarLineChartForm.tsx"
      to: "tools/wireframe-builder/types/blueprint.ts"
      via: "ChartType options in type selector dropdown"
      pattern: "radar|treemap|funnel|scatter|area"
---

<objective>
Implement 5 new chart variants (Radar, Treemap, Funnel, Scatter, Area) as sub-variants of the existing bar-line-chart section type, extend ChartRenderer to dispatch to them, and add them to the chart type selector in the property form.

Purpose: Expands chart visualization options from 4 to 9 types. These are sub-variants within the existing chart section type (not new top-level section types), keeping the ComponentPicker clean while giving operators significantly richer visualization choices.

Output: 5 new chart components using Recharts, extended ChartRenderer with 5 new cases, updated BarLineChartForm with chart type selector showing all 9 options.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-component-library-expansion/09-CONTEXT.md
@.planning/phases/09-component-library-expansion/09-RESEARCH.md
@.planning/phases/09-component-library-expansion/09-01-SUMMARY.md

@tools/wireframe-builder/components/sections/ChartRenderer.tsx
@tools/wireframe-builder/components/BarLineChart.tsx
@tools/wireframe-builder/components/editor/property-forms/BarLineChartForm.tsx
@tools/wireframe-builder/types/blueprint.ts

<interfaces>
<!-- ChartType extended in Plan 01 -->
From tools/wireframe-builder/types/blueprint.ts:
```typescript
export type ChartType = 'bar' | 'line' | 'bar-line' | 'radar' | 'treemap' | 'funnel' | 'scatter' | 'area'

export type BarLineChartSection = {
  type: 'bar-line-chart'
  title: string
  chartType: ChartType
  height?: number
  compareOnly?: boolean
  categories?: string[]
  xLabel?: string
  yLabel?: string
}
```

From tools/wireframe-builder/components/sections/ChartRenderer.tsx:
```typescript
type Props = {
  section: ChartSection
  compareMode: boolean
  comparePeriod: string
  chartColors?: string[]
}
```

Recharts verified imports (from 09-RESEARCH.md code examples):
```typescript
import { RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer } from 'recharts'
import { Treemap, ResponsiveContainer } from 'recharts'
import { FunnelChart, Funnel, Cell, Tooltip, ResponsiveContainer } from 'recharts'
import { ScatterChart, Scatter, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
```

Wireframe token pattern for charts:
- Grid lines: `stroke="var(--wf-grid)"`
- Axis text: `tick={{ fill: 'var(--wf-muted)', fontSize: 12 }}`
- Data fill: `chartColors?.[0] ?? 'var(--wf-accent)'`
- Card background: `stroke="var(--wf-card)"` (for treemap cell borders)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 chart components with Recharts + wireframe tokens</name>
  <files>
    tools/wireframe-builder/components/RadarChartComponent.tsx,
    tools/wireframe-builder/components/TreemapComponent.tsx,
    tools/wireframe-builder/components/FunnelChartComponent.tsx,
    tools/wireframe-builder/components/ScatterChartComponent.tsx,
    tools/wireframe-builder/components/AreaChartComponent.tsx
  </files>
  <action>
    Create 5 chart components following the same pattern as existing chart components (BarLineChart.tsx, DonutChart.tsx, etc.). Each receives `title`, `height?`, `chartColors?`, and renders with Recharts + wireframe tokens. Each includes built-in sample data that renders by default.

    **RadarChartComponent.tsx:**
    - Props: `{ title: string, height?: number, categories?: string[], chartColors?: string[] }`
    - Sample data: 5-6 business dimensions (Vendas, Marketing, Suporte, Financeiro, RH, Operacoes) with numeric values
    - Uses: RadarChart, PolarGrid (stroke=var(--wf-grid)), PolarAngleAxis, PolarRadiusAxis, Radar
    - Fill: chartColors[0] ?? var(--wf-accent), fillOpacity 0.3
    - Title rendered with --wf-body color

    **TreemapComponent.tsx:**
    - Props: `{ title: string, height?: number, chartColors?: string[] }`
    - Sample data: 5-6 business categories with proportional sizes (Vendas 4500, Marketing 2300, Operacoes 1800, etc.)
    - Uses: Treemap with dataKey="size", nameKey="name"
    - Fill cycles through chartColors array or uses accent
    - Cell borders: stroke=var(--wf-card)

    **FunnelChartComponent.tsx:**
    - Props: `{ title: string, height?: number, chartColors?: string[] }`
    - Sample data: 5 funnel stages (Visitantes 5000, Leads 3200, Qualificados 1800, Propostas 900, Fechados 400)
    - Uses: FunnelChart, Funnel, Cell (one per stage), Tooltip
    - Each Cell colored from chartColors[i] cycling through available colors

    **ScatterChartComponent.tsx:**
    - Props: `{ title: string, height?: number, xLabel?: string, yLabel?: string, chartColors?: string[] }`
    - Sample data: 8-10 random {x, y} points in reasonable ranges
    - Uses: ScatterChart, Scatter, XAxis, YAxis, CartesianGrid (stroke=var(--wf-grid)), Tooltip
    - Dot fill: chartColors[0] ?? var(--wf-accent)

    **AreaChartComponent.tsx:**
    - Props: `{ title: string, height?: number, categories?: string[], chartColors?: string[] }`
    - Sample data: 12 months (Jan-Dez) with sample revenue values
    - Uses: AreaChart, Area (type="monotone"), XAxis, YAxis, CartesianGrid, Tooltip
    - Stroke: chartColors[0] ?? var(--wf-accent), fill same with fillOpacity 0.2
    - Axis text: fill=var(--wf-muted)

    All components:
    - Wrap in ResponsiveContainer width="100%" height={height ?? 300}
    - Title in a header div with --wf-body color, matching existing chart title pattern
    - Card container with --wf-card background and --wf-border border
    - Export default function
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>
    - 5 chart components created in tools/wireframe-builder/components/
    - Each renders with Recharts + wireframe tokens (--wf-*)
    - Each has built-in sample data
    - Each respects chartColors prop from branding
    - Zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ChartRenderer dispatch + chart type selector in property form</name>
  <files>
    tools/wireframe-builder/components/sections/ChartRenderer.tsx,
    tools/wireframe-builder/components/editor/property-forms/BarLineChartForm.tsx,
    tools/wireframe-builder/lib/section-registry.ts
  </files>
  <action>
    1. Update `tools/wireframe-builder/components/sections/ChartRenderer.tsx`:
       a. Import all 5 new chart components.
       b. Add 5 new cases to the chartType switch inside the existing `case 'bar-line-chart'` handling:
          - When section.chartType is 'radar': render RadarChartComponent
          - When section.chartType is 'treemap': render TreemapComponent
          - When section.chartType is 'funnel': render FunnelChartComponent
          - When section.chartType is 'scatter': render ScatterChartComponent
          - When section.chartType is 'area': render AreaChartComponent
       c. Pass through chartColors, title, height, categories (where applicable), xLabel/yLabel (for scatter).
       d. The existing bar/line/bar-line cases remain unchanged.

    2. Update `tools/wireframe-builder/components/editor/property-forms/BarLineChartForm.tsx`:
       a. Find the chartType selector (likely a select/dropdown with options 'bar', 'line', 'bar-line').
       b. Add 5 new options: 'radar' (Radar), 'treemap' (Treemap), 'funnel' (Funil), 'scatter' (Dispersao), 'area' (Area).
       c. The form should continue to work for all chart types -- existing fields (title, height, categories) apply to most variants.
       d. Labels in Portuguese consistent with existing options.

    3. Optionally update the 'bar-line-chart' catalog entry label in section-registry.ts to something broader like 'Grafico' since it now covers 9 chart types. Or keep 'Barras / Linhas' as the picker label with the description mentioning all variants. Use judgment -- the user deferred this to Claude's discretion.

    4. Run registry tests to ensure bar-line-chart default still passes Zod validation.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx vitest run tools/wireframe-builder/lib/section-registry.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - ChartRenderer dispatches to all 9 chart types (4 existing + 5 new)
    - BarLineChartForm shows all 9 chart type options in selector
    - All 5 new chart types render with sample data when selected
    - chartColors from branding flow through to all new chart components
    - Registry tests pass, zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/cauetpinciara/Documents/fxl/fxl-core
npx vitest run tools/wireframe-builder/lib/section-registry.test.ts --reporter=verbose
npx tsc --noEmit

# Verify all 5 chart components exist
ls tools/wireframe-builder/components/{Radar,Treemap,Funnel,Scatter,Area}Chart*.tsx

# Verify ChartRenderer handles all 9 types
grep -c "case 'radar'\|case 'treemap'\|case 'funnel'\|case 'scatter'\|case 'area'" tools/wireframe-builder/components/sections/ChartRenderer.tsx
# Expected: 5
```
</verification>

<success_criteria>
- 5 new Recharts chart components render with sample data and wireframe tokens
- ChartRenderer dispatches all 9 chart types correctly
- Chart type selector in property form shows all 9 options
- chartColors from branding respected by all new chart types
- All tests pass, zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-component-library-expansion/09-03-SUMMARY.md`
</output>
