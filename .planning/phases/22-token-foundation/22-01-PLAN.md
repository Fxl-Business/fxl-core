---
phase: 22-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/styles/wireframe-tokens.css
  - tools/wireframe-builder/components/GaugeChartComponent.tsx
  - tailwind.config.ts
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03, TOK-04, TOK-05, TOK-06]

must_haves:
  truths:
    - "Wireframe components render with slate grays and primary blue #1152d4 instead of warm stone grays and gold #d4a017"
    - "Dark mode shows lighter blue #4d7ce8 with proper contrast on #101622 background"
    - "Light canvas is #f6f6f8, dark canvas is #101622"
    - "Three new tokens (--wf-header-search-bg, --wf-table-footer-bg, --wf-table-footer-fg) resolve in both themes"
    - "GaugeChart amber zone uses var(--wf-chart-warn) instead of hardcoded #f59e0b"
  artifacts:
    - path: "tools/wireframe-builder/styles/wireframe-tokens.css"
      provides: "Complete slate + blue token system for both light and dark themes"
      contains: "--wf-primary: #1152d4"
    - path: "tools/wireframe-builder/components/GaugeChartComponent.tsx"
      provides: "GaugeChart with token references only (no hardcoded hex colors)"
    - path: "tailwind.config.ts"
      provides: "Tailwind wf-* color registrations including new tokens"
  key_links:
    - from: "tools/wireframe-builder/styles/wireframe-tokens.css"
      to: "tailwind.config.ts"
      via: "var(--wf-*) references in Tailwind wf color config"
      pattern: "var\\(--wf-"
    - from: "tools/wireframe-builder/components/GaugeChartComponent.tsx"
      to: "tools/wireframe-builder/styles/wireframe-tokens.css"
      via: "var(--wf-chart-warn) token reference"
      pattern: "var\\(--wf-chart-warn\\)"
---

<objective>
Replace the wireframe token system from warm stone-gray + gold palette to slate + primary-blue palette, update both light and dark theme blocks simultaneously, add three new tokens for downstream phases, fix the only hardcoded color in GaugeChart, and register new tokens in Tailwind config.

Purpose: This is the foundation for all v1.4 visual changes. ~55 wireframe components auto-update via CSS cascade when these token values change.
Output: Updated wireframe-tokens.css, GaugeChartComponent.tsx, tailwind.config.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-token-foundation/22-RESEARCH.md

@tools/wireframe-builder/styles/wireframe-tokens.css
@tools/wireframe-builder/components/GaugeChartComponent.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace wireframe token values in both theme blocks</name>
  <files>tools/wireframe-builder/styles/wireframe-tokens.css, tailwind.config.ts</files>
  <action>
Rewrite wireframe-tokens.css with the new slate + primary-blue palette. Both [data-wf-theme="light"] and [data-wf-theme="dark"] blocks MUST be updated in the same edit (TOK-02).

**Light block changes:**
- Replace neutral scale from stone to Tailwind slate: --wf-neutral-50: #f8fafc through --wf-neutral-900: #0f172a (exact Tailwind slate values from RESEARCH.md)
- Add --wf-primary: #1152d4 (NEW token, canonical primary)
- Change --wf-accent to: var(--wf-primary) (backward compat alias, NOT a rename)
- Change --wf-accent-muted to: color-mix(in srgb, var(--wf-accent) 12%, transparent) (TOK-03)
- Change --wf-accent-fg to: #ffffff (white on blue, Pitfall 5 prevention)
- Set --wf-canvas: #f6f6f8 (hardcoded, NOT alias -- TOK-05 exact value)
- Set --wf-card: #ffffff
- Add three new tokens (TOK-04): --wf-header-search-bg: #f1f5f9 (slate-100), --wf-table-footer-bg: #0f172a (slate-900), --wf-table-footer-fg: #ffffff
- Add --wf-chart-warn: #f59e0b (amber-400, semantic warn for gauge)
- Update chart palette: --wf-chart-1: #1152d4, --wf-chart-2: #4f46e5 (indigo-600), --wf-chart-3: #60a5fa (blue-400), --wf-chart-4: #94a3b8 (slate-400), --wf-chart-5: #475569 (slate-600)

**Dark block changes:**
- Same slate neutral scale as light block (neutral scale is identical in both blocks)
- --wf-primary: #4d7ce8 (lighter for dark bg contrast -- Pitfall 1)
- --wf-accent: var(--wf-primary)
- --wf-accent-muted: color-mix(in srgb, var(--wf-accent) 15%, transparent) (slightly higher opacity for dark)
- --wf-accent-fg: #e0eaff
- --wf-canvas: #101622 (TOK-05 exact value)
- --wf-card: #1a2235
- --wf-header-search-bg: #1e293b (slate-800)
- --wf-table-footer-bg: #0f172a (slate-900)
- --wf-table-footer-fg: #f8fafc (slate-50)
- --wf-chart-warn: #fbbf24 (amber-400 brighter for dark)
- Chart palette (brighter): --wf-chart-1: #4d7ce8, --wf-chart-2: #818cf8 (indigo-400), --wf-chart-3: #93c5fd (blue-300), --wf-chart-4: #94a3b8, --wf-chart-5: #64748b
- Update --wf-sidebar-bg from #0f0e0d to #0f172a (slate-900, consistent with new palette)

**Update header comment** at top of file to say: "Neutral scale: slate grays", "Accent: primary blue (#1152d4 light, #4d7ce8 dark)".

**Tailwind config** -- add new token registrations to the wf color object:
- primary: 'var(--wf-primary)'
- 'header-search-bg': 'var(--wf-header-search-bg)'
- 'table-footer': { DEFAULT: 'var(--wf-table-footer-bg)', fg: 'var(--wf-table-footer-fg)' }

Do NOT rename any existing --wf-* token names. Only change values and add new names.
  </action>
  <verify>
    <automated>[ "$(grep -c -E '#d4a017|#e5b028|#fafaf9|#f5f5f4|#e7e5e4|#d6d3d1|#a8a29e|#78716c|#57534e|#44403c|#292524|#1c1917' tools/wireframe-builder/styles/wireframe-tokens.css)" -eq 0 ] && [ "$(grep -c 'wf-primary' tools/wireframe-builder/styles/wireframe-tokens.css)" -gt 0 ] && [ "$(grep -c 'wf-header-search-bg' tools/wireframe-builder/styles/wireframe-tokens.css)" -gt 0 ] && [ "$(grep -c 'color-mix' tools/wireframe-builder/styles/wireframe-tokens.css)" -gt 0 ] && echo "PASS: tokens updated"</automated>
  </verify>
  <done>
- Zero old stone/gold hex values remain in wireframe-tokens.css
- --wf-primary exists in both theme blocks with correct values (#1152d4 light, #4d7ce8 dark)
- --wf-accent aliases to var(--wf-primary) in both blocks
- --wf-accent-muted uses color-mix() in both blocks (no hardcoded rgba)
- --wf-canvas is #f6f6f8 (light) and #101622 (dark)
- Three new tokens exist in both blocks
- --wf-chart-warn exists in both blocks
- Tailwind config registers primary, header-search-bg, and table-footer tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace GaugeChart hardcoded color with token reference</name>
  <files>tools/wireframe-builder/components/GaugeChartComponent.tsx</files>
  <action>
In GaugeChartComponent.tsx, replace both instances of the hardcoded '#f59e0b' with 'var(--wf-chart-warn)'.

Line ~45 (zone data from props):
```
z.color ?? (i === 0 ? 'var(--wf-negative)' : i === 1 ? 'var(--wf-chart-warn)' : 'var(--wf-positive)')
```

Line ~51 (default zones):
```
{ value: total * 0.3, fill: 'var(--wf-chart-warn)' },
```

No other changes to this file.
  </action>
  <verify>
    <automated>[ "$(grep -c '#f59e0b' tools/wireframe-builder/components/GaugeChartComponent.tsx)" -eq 0 ] && [ "$(grep -c 'wf-chart-warn' tools/wireframe-builder/components/GaugeChartComponent.tsx)" -gt 0 ] && npx tsc --noEmit && echo "PASS: GaugeChart uses token"</automated>
  </verify>
  <done>
- Zero instances of #f59e0b in GaugeChartComponent.tsx
- Both zone color references use var(--wf-chart-warn)
- npx tsc --noEmit passes with zero errors
  </done>
</task>

</tasks>

<verification>
```bash
# No old palette hex values in token file
grep -E "#d4a017|#e5b028|#fafaf9|#f5f5f4|#e7e5e4|#d6d3d1|#a8a29e|#78716c|#57534e|#44403c|#292524|#1c1917" tools/wireframe-builder/styles/wireframe-tokens.css && echo "FAIL: old values remain" || echo "PASS"

# No hardcoded colors in GaugeChart
grep "#f59e0b" tools/wireframe-builder/components/GaugeChartComponent.tsx && echo "FAIL" || echo "PASS"

# New tokens exist
grep "wf-primary" tools/wireframe-builder/styles/wireframe-tokens.css && echo "PASS: primary exists"
grep "wf-header-search-bg" tools/wireframe-builder/styles/wireframe-tokens.css && echo "PASS: new tokens exist"
grep "color-mix" tools/wireframe-builder/styles/wireframe-tokens.css && echo "PASS: color-mix used"

# TypeScript gate
npx tsc --noEmit
```
</verification>

<success_criteria>
- wireframe-tokens.css contains complete slate + blue palette in both theme blocks
- All old stone/gold values replaced
- Three new tokens added to both blocks
- color-mix() used for accent-muted (no hardcoded rgba)
- GaugeChart uses var(--wf-chart-warn) not #f59e0b
- Tailwind config registers new tokens
- npx tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-token-foundation/22-01-SUMMARY.md`
</output>
