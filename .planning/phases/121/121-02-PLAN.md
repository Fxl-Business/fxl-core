---
phase: 121-auth-token-exchange
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/auth/ProtectedRoute.tsx
  - src/platform/tenants/useActiveOrg.ts
  - src/platform/module-loader/hooks/useModuleEnabled.tsx
  - src/platform/pages/SolicitarAcesso.tsx
  - src/platform/pages/admin/UsersPage.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]

must_haves:
  truths:
    - "Usuário autenticado com org ativa chega ao conteúdo sem flash de 'Autenticando...'"
    - "Quando token exchange falha, usuário vê mensagem de erro clara — nunca sidebar vazia silenciosa"
    - "Super admin autenticado consegue acessar dados de qualquer org"
    - "Uncommitted changes são commitados — nenhum código válido fica em staging"
  artifacts:
    - path: "src/platform/auth/ProtectedRoute.tsx"
      provides: "Auth gate com hydration guard correto usando useOrganization() em vez de useActiveOrg()"
    - path: "src/platform/tenants/useActiveOrg.ts"
      provides: "Auto-select de org e validação de membership com useEffect"
    - path: "src/platform/module-loader/hooks/useModuleEnabled.tsx"
      provides: "No-org = modules vazios (não todos habilitados como fallback)"
  key_links:
    - from: "ProtectedRoute step 4"
      to: "useOrganization().organization"
      via: "clerkCachedOrg hydration guard"
      pattern: "clerkCachedOrg.*useOrganization"
    - from: "useActiveOrg auto-select"
      to: "setActive({ organization: memberships[0].organization.id })"
      via: "useEffect on isOrgLoaded + isListLoaded"
      pattern: "setActive.*memberships\\[0\\]"
---

<objective>
Commit and verify the uncommitted auth changes that fix known bugs in ProtectedRoute and
useActiveOrg. These changes were authored as fixes for:
- Pitfall #17 (Clerk useOrganizationList hydration race — false redirect to /solicitar-acesso)
- Better auto-select behavior when no org is active

Also verify AUTH-04: super admin JWT claim passes through correctly.

Purpose: The uncommitted changes contain real bug fixes. Leaving them uncommitted means they
won't be tested and could be accidentally overwritten.

Output: All 5 uncommitted files committed with verified TypeScript compilation.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PITFALLS.md

<interfaces>
<!-- Uncommitted changes summary (from git diff) -->

ProtectedRoute.tsx changes:
- Removed: `import { useActiveOrg }` and `const { activeOrg } = useActiveOrg()`
- Added: `import { useOrganization }` and `const { organization: clerkCachedOrg } = useOrganization()`
- Step 4 guard: `userMemberships?.data?.length === 0 && !clerkCachedOrg` (was `&& !activeOrg`)

useActiveOrg.ts changes:
- Added `useEffect` import
- Added useEffect that auto-selects first org when none is active
- Added useEffect cleanup for stale orgs (no longer a member)
- activeOrg memo now validates membership before returning

useModuleEnabled.tsx changes:
- No-org branch: `setEnabledModules(new Set())` instead of `new Set(ALL_MODULE_IDS)`

SolicitarAcesso.tsx and UsersPage.tsx: minor uncommitted changes (not blocking)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and finalize uncommitted auth changes</name>
  <files>
    src/platform/auth/ProtectedRoute.tsx,
    src/platform/tenants/useActiveOrg.ts,
    src/platform/module-loader/hooks/useModuleEnabled.tsx,
    src/platform/pages/SolicitarAcesso.tsx,
    src/platform/pages/admin/UsersPage.tsx
  </files>
  <action>
Review the 5 uncommitted files and verify they are correct before committing.

**ProtectedRoute.tsx — verify:**
- Uses `useOrganization()` (not `useActiveOrg()`) for the hydration guard
- Step 4 condition: `userMemberships?.data?.length === 0 && !clerkCachedOrg`
- Steps 5 and 6 check `tokenError` then `!tokenReady` in that order
- Renders `<>{children}</>` only when both `tokenReady === true` AND `tokenError === null`

**useActiveOrg.ts — verify:**
- useEffect auto-selects first membership when `!organization`
- useEffect clears stale org via `setActive({ organization: null })` when user is no longer a member
- `activeOrg` memo returns null for orgs not in memberships (prevents token exchange with stale org)
- Import list includes `useEffect` (was missing before the change)

**useModuleEnabled.tsx — verify:**
- When `!activeOrg`, sets `enabledModules = new Set()` (empty) — correct, user is redirected anyway
- Opt-out model intact: when org has no rows in tenant_modules, all modules are enabled

**SolicitarAcesso.tsx and UsersPage.tsx:**
- Read the diff. If changes are sound (no regressions), include them in the commit.
- If any change looks risky or incomplete, revert it and note in the summary.

After reviewing all files, run `npx tsc --noEmit`. Fix any TypeScript errors found.
Do NOT add new functionality — only verify and potentially fix errors in the existing uncommitted changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
All 5 files verified. `npx tsc --noEmit` exits 0. Files are ready to commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify AUTH-04 — super_admin JWT claim flows to Supabase</name>
  <files>supabase/functions/auth-token-exchange/index.ts</files>
  <action>
Verify that the `auth-token-exchange` edge function correctly handles the `super_admin`
claim from the Clerk JWT and passes it through to the minted Supabase JWT.

Read the current edge function code. Check:
1. Does it read `super_admin` from the Clerk JWT payload?
   - Clerk stores `super_admin: true` in `publicMetadata` which appears in the session token.
   - The JWT payload may have `public_metadata.super_admin` or `metadata.public.super_admin`
     depending on Clerk version.
2. Does it include `super_admin` in the minted Supabase JWT claims?

If `super_admin` is NOT being forwarded:
- Add extraction: `const superAdmin = (payload as Record<string, unknown>).super_admin as boolean | undefined`
  OR for publicMetadata: check `(payload?.metadata as any)?.public?.super_admin`
- Add to the SignJWT payload: `...(superAdmin ? { super_admin: true } : {})`

If `super_admin` is already being forwarded correctly, document this as verified and move on.

**Important**: Do NOT redeploy the edge function in this task. Deployment is handled separately
(see AUTH-04 note below). Just ensure the source code is correct.

Note: After this phase, the executor should manually verify via Supabase Dashboard that:
- `auth-token-exchange` is deployed and ACTIVE
- The function has `verify_jwt: false`
- Secrets `CLERK_ISSUER` and `JWT_SIGNING_SECRET` are set

Run `npx tsc --noEmit` (even though edge function has `@ts-nocheck`, verify no regressions in other files).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
Edge function source correctly extracts and forwards `super_admin` claim.
`npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit
```
Zero TypeScript errors. All 5 uncommitted files are verified and ready to commit.
</verification>

<success_criteria>
1. ProtectedRoute.tsx uses useOrganization() hydration guard (not useActiveOrg())
2. useActiveOrg.ts auto-selects first org and validates membership in a useEffect
3. useModuleEnabled.tsx sets empty Set when no active org
4. auth-token-exchange/index.ts forwards super_admin claim to minted JWT
5. npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/121/121-02-SUMMARY.md` following the summary template.
</output>
