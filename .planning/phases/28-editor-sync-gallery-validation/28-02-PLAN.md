---
phase: 28-editor-sync-gallery-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified: []
autonomous: false
requirements:
  - GAL-02

must_haves:
  truths:
    - "All 6 gallery sections render without console errors"
    - "Dark mode toggle shows correct dark wireframe aesthetic on all previews"
    - "Branding toggle applies #1B6B93 primary color across all previews"
    - "npx tsc --noEmit reports zero errors after all v1.4 phases"
  artifacts: []
  key_links: []
---

<objective>
Validate the complete v1.4 visual redesign via gallery smoke test and final TypeScript audit.

Purpose: GAL-02 requires a validation checklist confirming all gallery sections work in dark mode, branding mode, and that TypeScript compilation passes. This is the milestone gate for v1.4.
Output: Validated gallery (all 6 sections, dark mode, branding), zero TypeScript errors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-editor-sync-gallery-validation/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript full audit</name>
  <files></files>
  <action>
Run `npx tsc --noEmit` and fix any errors found. This covers all files modified across phases 22-28.

Rules for fixing errors (per project CLAUDE.md):
- Never use `any` as a workaround for type errors
- Use proper type imports and type narrowing
- If errors are in files not related to v1.4 changes, still fix them (project-wide zero-error policy)

If zero errors on first run, this task is complete immediately.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>npx tsc --noEmit exits with code 0 and produces no output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gallery visual validation checklist</name>
  <what-built>
Complete v1.4 visual redesign: 22 (tokens), 23 (sidebar/header), 24 (KPI cards), 25 (tables), 26 (filter bar), 27 (charts/CompositionBar), 28 (editor sync + gallery toggles).
  </what-built>
  <how-to-verify>
1. Run `make dev` and navigate to the Component Gallery page
2. **All 6 sections visible:** Confirm Layout/Shell, Graficos, Cards e Metricas, Tabelas, Inputs, Modais e Overlays all render
3. **Light mode pass:** With wireframe theme set to Light, scroll through all 27 component previews — all should show the new blue primary (#1152d4) + slate palette
4. **Dark mode pass:** Click the "Dark Mode" toggle in the gallery toolbar — all previews should switch to dark wireframe aesthetic (dark canvas, light text)
5. **Branding pass:** Click the "Branding" toggle — all previews should switch to financeiro-conta-azul teal primary (#1B6B93) instead of blue
6. **ScreenManager check:** Navigate to any client wireframe (e.g., financeiro-conta-azul) — verify ScreenManager sidebar nav items match WireframeSidebar typography (compact text-xs size)
  </how-to-verify>
  <resume-signal>Type "approved" to complete v1.4 milestone, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits cleanly with zero errors
- All 6 gallery sections render in light mode, dark mode, and branding mode
- ScreenManager visually matches WireframeSidebar
</verification>

<success_criteria>
- GAL-02 checklist complete: dark mode toggle pass, branding-applied check, all 6 sections verified
- Zero TypeScript errors across entire codebase
- v1.4 milestone gate satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/28-editor-sync-gallery-validation/28-02-SUMMARY.md`
</output>
