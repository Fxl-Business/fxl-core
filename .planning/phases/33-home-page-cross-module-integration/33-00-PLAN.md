---
phase: 33-home-page-cross-module-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/pages/Home.test.tsx
  - src/components/layout/SearchCommand.test.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - KB-07

must_haves:
  truths:
    - "Test stubs exist so that Wave 2 tasks can run vitest as part of verification"
    - "Home.test.tsx covers MODULE_REGISTRY grid rendering and activity feed merge/sort logic"
    - "SearchCommand.test.tsx covers KB entries empty-query guard"
  artifacts:
    - path: "src/pages/Home.test.tsx"
      provides: "Test stubs for HOME-01 (registry grid) and HOME-02 (feed merge/sort)"
      min_lines: 30
    - path: "src/components/layout/SearchCommand.test.tsx"
      provides: "Test stub for KB-07 empty-query guard"
      min_lines: 15
  key_links: []
---

<objective>
Create test stub files for Phase 33 so that execution plans can run vitest as automated verification.

Purpose: Nyquist compliance -- every code-producing task must have an automated test command that references an existing file. Wave 0 creates the test scaffolds before implementation begins.
Output: Two test files with pending/skeleton tests that will be filled during Wave 2 execution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-home-page-cross-module-integration/33-RESEARCH.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for Home.tsx and SearchCommand.tsx</name>
  <files>src/pages/Home.test.tsx, src/components/layout/SearchCommand.test.tsx</files>
  <action>
Create two test files with skeleton tests that will be filled during plan execution.

**src/pages/Home.test.tsx:**
- Import `describe`, `it`, `expect` from 'vitest'.
- Create a `describe('Home page')` block with:
  - `describe('MODULE_REGISTRY grid (HOME-01)')` containing:
    - `it.todo('renders a card for each module in MODULE_REGISTRY')`
  - `describe('Activity feed merge/sort (HOME-02)')` containing:
    - `it('merges and sorts kb_entry and task items by updated_at descending', ...)` -- implement this test NOW as a pure function test. Define the `ActivityItem` type inline (same shape as in RESEARCH.md: `{ id, title, type, subtype, client_slug, updated_at, href }`). Write a `mergeAndSortActivityItems(kbItems: ActivityItem[], taskItems: ActivityItem[]): ActivityItem[]` pure function that merges two arrays, sorts by `updated_at` descending, and slices to 10. Test with 3 kb items and 3 task items with interleaved dates, asserting the output order is correct and length is <= 10.
    - Export the `mergeAndSortActivityItems` function from the test file for now -- Plan 33-01 will extract it to Home.tsx and the test will import from there.
    - IMPORTANT: Since the merge/sort logic is non-trivial, this test must be GREEN (passing) before Plan 33-01 runs. Write both the function and the test here. Plan 33-01 will move the function into Home.tsx and update the import.

**src/components/layout/SearchCommand.test.tsx:**
- Import `describe`, `it` from 'vitest'.
- Create a `describe('SearchCommand KB integration (KB-07)')` block with:
  - `it.todo('does not render KB entries when query is empty')` -- placeholder, will be filled if needed.

Both files must pass `npx vitest run` (todo tests are skipped, the merge/sort test must pass).
  </action>
  <verify>
    <automated>npx vitest run src/pages/Home.test.tsx src/components/layout/SearchCommand.test.tsx</automated>
  </verify>
  <done>Both test files exist, vitest runs without errors, merge/sort test passes with correct ordering assertion.</done>
</task>

</tasks>

<verification>
- `npx vitest run` completes without errors
- `src/pages/Home.test.tsx` exists with HOME-01 todo and HOME-02 passing test
- `src/components/layout/SearchCommand.test.tsx` exists with KB-07 todo
</verification>

<success_criteria>
- Both test files exist and are recognized by vitest
- The merge/sort unit test passes (validates activity feed ordering logic)
- Subsequent plans can reference these test files in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/33-home-page-cross-module-integration/33-00-SUMMARY.md`
</output>
