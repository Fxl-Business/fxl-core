# Plan 01-01: Structure + Navigation

**Phase:** 01-documentation
**Requirements:** DOCS-01
**Depends on:** nothing

## Goal

Restructure the sidebar, move files to their new locations, update routes, and fix Home.tsx links so the navigation reflects the new 4-section structure (Home, Processo, Ferramentas, Clientes) and every sidebar link resolves to an existing doc.

## Context

Read these files before starting:
- `.planning/phases/01-documentation/01-CONTEXT.md` (locked decisions)
- `.planning/phases/01-documentation/01-RESEARCH.md` (full doc inventory, new sidebar structure, route analysis)
- `src/components/layout/Sidebar.tsx` (current nav array to rewrite)
- `src/App.tsx` (routes to update)
- `src/pages/Home.tsx` (links to update)

## Tasks

### Task 1: Move build/ files to ferramentas/ and update badges

**File(s):**
- Move `docs/build/tech-radar.md` -> `docs/ferramentas/tech-radar.md`
- Move `docs/build/premissas-gerais.md` -> `docs/ferramentas/premissas-gerais.md`
- Move `docs/build/seguranca.md` -> `docs/ferramentas/seguranca.md`
- Move `docs/build/testes.md` -> `docs/ferramentas/testes.md`
- Move `docs/build/master-prompt.md` -> `docs/ferramentas/master-prompt.md`
- Move `docs/build/claude-md-template.md` -> `docs/ferramentas/claude-md-template.md`
- Move entire `docs/build/techs/` directory -> `docs/ferramentas/techs/`
- Delete `docs/build/index.md` (obsolete -- ferramentas/index.md replaces it)

**Action:**
1. Create `docs/ferramentas/techs/` directory
2. Use `git mv` for each file move (preserves history):
   - `git mv docs/build/tech-radar.md docs/ferramentas/tech-radar.md`
   - `git mv docs/build/premissas-gerais.md docs/ferramentas/premissas-gerais.md`
   - `git mv docs/build/seguranca.md docs/ferramentas/seguranca.md`
   - `git mv docs/build/testes.md docs/ferramentas/testes.md`
   - `git mv docs/build/master-prompt.md docs/ferramentas/master-prompt.md`
   - `git mv docs/build/claude-md-template.md docs/ferramentas/claude-md-template.md`
   - `git mv docs/build/techs/* docs/ferramentas/techs/`
3. Delete `docs/build/index.md` and the now-empty `docs/build/` directory
4. In every moved file, change the frontmatter `badge: Build` to `badge: Ferramentas`
5. Do NOT change any content yet -- this task is structure only

**After the moves, these 22 files should exist under `docs/ferramentas/`:**
- `index.md`, `wireframe-builder.md`, `tech-radar.md`, `premissas-gerais.md`, `seguranca.md`, `testes.md`, `master-prompt.md`, `claude-md-template.md`
- `techs/api-gateway.md`, `techs/backend-node.md`, `techs/backend-python.md`, `techs/docker.md`, `techs/github-actions.md`, `techs/github.md`, `techs/kafka.md`, `techs/keycloak.md`, `techs/kubernetes.md`, `techs/nextjs.md`, `techs/supabase.md`, `techs/tailwind-shadcn.md`, `techs/terraform.md`, `techs/vercel.md`, `techs/vite-react-ts.md`

**Verify:**
```bash
# No files remain in docs/build/
test ! -d docs/build && echo "PASS: docs/build/ removed" || echo "FAIL: docs/build/ still exists"

# All 22 files exist under ferramentas
find docs/ferramentas -name "*.md" | wc -l  # Should be 24 (22 moved + 2 existing)

# No badge: Build remaining
grep -r 'badge: Build' docs/ && echo "FAIL: badge Build still exists" || echo "PASS: no badge Build"

# TypeScript still compiles
npx tsc --noEmit
```

**Commit:** `docs: move build/ files to ferramentas/ and update badges`

---

### Task 2: Delete obsolete files and create placeholder files for new pages

**File(s):**
- Delete `docs/operacao/index.md`
- Delete `docs/operacao/fluxo-trabalho.md`
- Delete `docs/operacao/padrao-conversa-project.md`
- Delete `docs/operacao/padrao-prompt-code.md`
- Delete `docs/operacao/prompt-abertura.md`
- Delete `docs/processo/pacote-cliente.md`
- Delete `docs/processo/prompt-master.md`
- Delete `docs/processo/master.md` (replaced by visao-geral.md)
- Delete `docs/processo/bi-personalizado.md` (replaced by cliente-vs-produto.md)
- Delete `docs/processo/produto.md` (replaced by cliente-vs-produto.md)
- Create `docs/processo/visao-geral.md` (placeholder)
- Create `docs/processo/prompts.md` (placeholder)
- Create `docs/processo/cliente-vs-produto.md` (placeholder)
- Create `docs/processo/onboarding.md` (placeholder)

**Action:**
1. Delete the 10 files listed above using `git rm`
2. Create 4 new placeholder markdown files with valid frontmatter so they render and sidebar links resolve. Each placeholder should have:
   - Proper frontmatter (`title`, `badge: Processo`, `description`)
   - A single heading and one line: "Conteudo em construcao." (Plan 01-02 will write the real content)

Placeholder frontmatter for each:

`docs/processo/visao-geral.md`:
```yaml
---
title: Visao Geral do Processo FXL
badge: Processo
description: Visao geral do processo FXL — tipos de projeto, roteamento e ciclo de vida
---
```

`docs/processo/prompts.md`:
```yaml
---
title: Prompts
badge: Processo
description: Prompts reutilizaveis organizados por contexto — Claude Code, Claude Project, melhorias de processo
---
```

`docs/processo/cliente-vs-produto.md`:
```yaml
---
title: Cliente vs Produto
badge: Processo
description: Os dois tipos de projeto FXL — BI Personalizado (cliente) e Produto FXL (SaaS/sistema)
---
```

`docs/processo/onboarding.md`:
```yaml
---
title: Onboarding
badge: Processo
description: Guia passo a passo para operar o processo FXL
---
```

3. Delete the now-empty `docs/operacao/` directory

**Verify:**
```bash
# operacao/ directory gone
test ! -d docs/operacao && echo "PASS" || echo "FAIL: docs/operacao/ still exists"

# Obsolete files gone
for f in docs/processo/pacote-cliente.md docs/processo/prompt-master.md docs/processo/master.md docs/processo/bi-personalizado.md docs/processo/produto.md; do
  test ! -f "$f" && echo "PASS: $f removed" || echo "FAIL: $f still exists"
done

# New placeholders exist
for f in docs/processo/visao-geral.md docs/processo/prompts.md docs/processo/cliente-vs-produto.md docs/processo/onboarding.md; do
  test -f "$f" && echo "PASS: $f exists" || echo "FAIL: $f missing"
done

# No badge: Operacao remaining
grep -r 'badge: Operacao' docs/ && echo "FAIL" || echo "PASS: no badge Operacao"

npx tsc --noEmit
```

**Commit:** `docs: remove obsolete files, create placeholders for new pages`

---

### Task 3: Rewrite Sidebar.tsx, update App.tsx routes, and update Home.tsx links

**File(s):**
- `src/components/layout/Sidebar.tsx` (rewrite navigation array)
- `src/App.tsx` (remove obsolete routes)
- `src/pages/Home.tsx` (update links)

**Action:**

**Sidebar.tsx** -- Replace the `navigation` array with this exact structure (keep hardcoded per research recommendation). The `NavItem` type, `NavSection` component, `hasActiveChild` function, and `Sidebar` component remain unchanged -- only the `navigation` const changes:

```typescript
const navigation: NavItem[] = [
  { label: 'Home', href: '/' },
  {
    label: 'Processo',
    href: '/processo/index',
    children: [
      { label: 'Visao Geral', href: '/processo/visao-geral' },
      { label: 'Prompts', href: '/processo/prompts' },
      { label: 'Cliente vs Produto', href: '/processo/cliente-vs-produto' },
      { label: 'Identidade FXL', href: '/processo/identidade' },
      {
        label: 'Fases',
        children: [
          { label: 'Fase 1 — Diagnostico', href: '/processo/fases/fase1' },
          { label: 'Fase 2 — Wireframe', href: '/processo/fases/fase2' },
          { label: 'Fase 3 — Desenvolvimento', href: '/processo/fases/fase3' },
          { label: 'Fase 4 — Auditoria', href: '/processo/fases/fase4' },
          { label: 'Fase 5 — Entrega', href: '/processo/fases/fase5' },
          { label: 'Fase 6 — Tutorial', href: '/processo/fases/fase6' },
        ],
      },
      { label: 'Onboarding', href: '/processo/onboarding' },
    ],
  },
  {
    label: 'Ferramentas',
    href: '/ferramentas/index',
    children: [
      {
        label: 'Wireframe Builder',
        href: '/ferramentas/wireframe-builder',
        children: [
          { label: 'Biblioteca de KPIs', href: '/referencias/biblioteca-kpis' },
          { label: 'Blocos Disponiveis', href: '/referencias/blocos-disponiveis' },
          { label: 'Galeria de Componentes', href: '/ferramentas/wireframe-builder/galeria' },
        ],
      },
      { label: 'Tech Radar', href: '/ferramentas/tech-radar' },
      {
        label: 'Techs',
        children: [
          { label: 'Vite + React + TS', href: '/ferramentas/techs/vite-react-ts' },
          { label: 'Tailwind + shadcn/ui', href: '/ferramentas/techs/tailwind-shadcn' },
          { label: 'Next.js', href: '/ferramentas/techs/nextjs' },
          { label: 'Supabase', href: '/ferramentas/techs/supabase' },
          { label: 'Vercel', href: '/ferramentas/techs/vercel' },
          { label: 'GitHub', href: '/ferramentas/techs/github' },
          { label: 'GitHub Actions', href: '/ferramentas/techs/github-actions' },
          { label: 'Backend — Node.js', href: '/ferramentas/techs/backend-node' },
          { label: 'Backend — Python', href: '/ferramentas/techs/backend-python' },
          { label: 'Docker', href: '/ferramentas/techs/docker' },
          { label: 'Kubernetes', href: '/ferramentas/techs/kubernetes' },
          { label: 'Terraform', href: '/ferramentas/techs/terraform' },
          { label: 'API Gateway', href: '/ferramentas/techs/api-gateway' },
          { label: 'Kafka', href: '/ferramentas/techs/kafka' },
          { label: 'Keycloak', href: '/ferramentas/techs/keycloak' },
        ],
      },
      { label: 'Premissas Gerais', href: '/ferramentas/premissas-gerais' },
      { label: 'Seguranca', href: '/ferramentas/seguranca' },
      { label: 'Testes', href: '/ferramentas/testes' },
      { label: 'Template de Sprint', href: '/ferramentas/master-prompt' },
      { label: 'CLAUDE.md — Template', href: '/ferramentas/claude-md-template' },
    ],
  },
  {
    label: 'Clientes',
    children: [
      {
        label: 'Financeiro Conta Azul',
        children: [
          { label: 'Visao Geral', href: '/clients/financeiro-conta-azul' },
          { label: 'Briefing', href: '/clients/financeiro-conta-azul/briefing' },
          { label: 'Blueprint', href: '/clients/financeiro-conta-azul/blueprint' },
          { label: 'Branding', href: '/clients/financeiro-conta-azul/branding' },
          { label: 'Changelog', href: '/clients/financeiro-conta-azul/changelog' },
          { label: 'Wireframe', href: '/clients/financeiro-conta-azul/wireframe' },
        ],
      },
    ],
  },
]
```

**App.tsx** -- Remove the two obsolete catch-all routes:
- Remove: `<Route path="/build/*" element={<DocRenderer />} />`
- Remove: `<Route path="/operacao/*" element={<DocRenderer />} />`
- Keep all other routes unchanged (processo/*, referencias/*, ferramentas/*, clients/*, etc.)

**Home.tsx** -- Update the `quickActions` and `sections` arrays:

In `quickActions`:
- Change `href: '/operacao/prompt-abertura'` to `href: '/processo/prompts'`
- Change `href: '/build/tech-radar'` to `href: '/ferramentas/tech-radar'`

In `sections`:
- Change the "Processo" entry: `href: '/processo/master'` to `href: '/processo/visao-geral'`
- Change the "Build" entry entirely to: `{ badge: 'Ferramentas', title: 'Ferramentas e base tecnica', description: 'Stack, premissas, seguranca, deploy e todas as ferramentas do processo.', href: '/ferramentas/index' }`

**Verify:**
```bash
# TypeScript compiles
npx tsc --noEmit

# No references to old paths in Sidebar
grep -n "operacao\|/build/" src/components/layout/Sidebar.tsx && echo "FAIL: old paths in Sidebar" || echo "PASS: Sidebar clean"

# No /build/ or /operacao/ routes in App.tsx
grep -n 'path="/build' src/App.tsx && echo "FAIL: /build route remains" || echo "PASS"
grep -n 'path="/operacao' src/App.tsx && echo "FAIL: /operacao route remains" || echo "PASS"

# Home.tsx has no old paths
grep -n "operacao\|/build/" src/pages/Home.tsx && echo "FAIL: old paths in Home" || echo "PASS: Home clean"

# Every sidebar href resolves to an existing doc or route
# (manual: open the app, click every sidebar item -- no 404s)

npm run build
```

**Commit:** `app: rewrite sidebar navigation, update routes and Home links`

---

## Plan Verification

After all 3 tasks are complete, run this full verification:

```bash
# 1. TypeScript compiles clean
npx tsc --noEmit

# 2. Build succeeds
npm run build

# 3. No docs/build/ directory remains
test ! -d docs/build && echo "PASS" || echo "FAIL"

# 4. No docs/operacao/ directory remains
test ! -d docs/operacao && echo "PASS" || echo "FAIL"

# 5. No badge: Build or badge: Operacao in any doc
grep -r 'badge: Build' docs/ && echo "FAIL" || echo "PASS"
grep -r 'badge: Operacao' docs/ && echo "FAIL" || echo "PASS"

# 6. No old paths in source files
grep -rn "'/operacao/\|'/build/" src/ && echo "FAIL: old paths in src/" || echo "PASS"

# 7. All 4 new placeholder docs exist
for f in docs/processo/visao-geral.md docs/processo/prompts.md docs/processo/cliente-vs-produto.md docs/processo/onboarding.md; do
  test -f "$f" && echo "PASS: $f" || echo "FAIL: $f missing"
done

# 8. Sidebar has exactly 4 top-level sections (Home, Processo, Ferramentas, Clientes)
grep -c "label:" src/components/layout/Sidebar.tsx  # Sanity check -- should be ~50 entries
```

## UAT Criteria

1. Open the app in browser. The sidebar shows 4 top-level groups: Home, Processo, Ferramentas, Clientes
2. The old "Operacao" and "Build" top-level sections are gone
3. Processo section expands to show: Visao Geral, Prompts, Cliente vs Produto, Identidade FXL, Fases (1-6), Onboarding
4. Ferramentas section expands to show: Wireframe Builder (with KPIs, Blocos, Galeria sub-items), Tech Radar, Techs (15 tech pages), Premissas Gerais, Seguranca, Testes, Template de Sprint, CLAUDE.md Template
5. Click every sidebar link -- none show 404 (placeholder pages show "Conteudo em construcao" which is expected)
6. Home page quick actions and documentation cards link to correct new paths
7. Cmd+K search groups docs under "Processo", "Ferramentas", "Referencias" (no "Build" or "Operacao" groups)
