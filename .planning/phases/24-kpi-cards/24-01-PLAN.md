---
phase: 24-kpi-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/types/blueprint.ts
  - tools/wireframe-builder/components/KpiCardFull.tsx
  - tools/wireframe-builder/components/sections/KpiGridRenderer.tsx
  - tools/wireframe-builder/components/KpiCard.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05

must_haves:
  truths:
    - "KPI cards render with rounded-xl corners and subtle shadow on white/dark background"
    - "Hovering a KPI card transitions the icon container from muted accent to solid primary"
    - "Trend badges display as rounded-full pills with emerald (positive) or rose (negative) tint"
    - "Card values use extrabold weight and labels use text-sm medium weight without uppercase"
    - "Comparison/sub text appears in 10px muted style below the value"
  artifacts:
    - path: "tools/wireframe-builder/components/KpiCardFull.tsx"
      provides: "Primary KPI card with icon slot, group-hover, rounded-xl, extrabold, rounded-full badge"
      contains: "group rounded-xl"
    - path: "tools/wireframe-builder/components/KpiCard.tsx"
      provides: "Secondary KPI card with same visual treatment (no icon slot)"
      contains: "rounded-xl"
    - path: "tools/wireframe-builder/types/blueprint.ts"
      provides: "KpiConfig type with optional icon field"
      contains: "icon?: LucideIcon"
    - path: "tools/wireframe-builder/components/sections/KpiGridRenderer.tsx"
      provides: "Icon prop passthrough from KpiConfig to KpiCardFull"
      contains: "icon={item.icon}"
  key_links:
    - from: "tools/wireframe-builder/types/blueprint.ts"
      to: "tools/wireframe-builder/components/KpiCardFull.tsx"
      via: "KpiConfig.icon typed as LucideIcon"
      pattern: "icon.*LucideIcon"
    - from: "tools/wireframe-builder/components/sections/KpiGridRenderer.tsx"
      to: "tools/wireframe-builder/components/KpiCardFull.tsx"
      via: "icon={item.icon} prop passthrough"
      pattern: "icon=\\{item\\.icon\\}"
---

<objective>
Restyle KPI card components to match the v1.4 premium financial dashboard aesthetic: rounded-xl with shadow-sm, group-hover icon container transition, rounded-full trend badges, Inter extrabold values, and micro comparison text.

Purpose: KPI cards are the first thing users see in a dashboard — their visual quality defines the professional impression of the entire wireframe.
Output: Updated KpiCardFull.tsx, KpiCard.tsx, blueprint.ts (KpiConfig type), and KpiGridRenderer.tsx (icon passthrough).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-kpi-cards/24-RESEARCH.md

<interfaces>
<!-- Current KpiConfig type from blueprint.ts (lines 24-34) -->
```typescript
export type KpiConfig = {
  label: string
  value: string
  sub?: string
  variation?: string
  variationPositive?: boolean
  sparkline?: number[]
  semaforo?: 'verde' | 'amarelo' | 'vermelho'
  semaforoLabel?: string
  wide?: boolean
}
```

<!-- Current KpiCardFull Props (lines 5-16) -->
```typescript
type Props = {
  label: string
  value: string
  sub?: string
  variation?: string
  variationPositive?: boolean
  semaforo?: SemaforoStatus
  semaforoLabel?: string
  sparkline?: number[]
  wide?: boolean
  compareMode?: boolean
}
```

<!-- Current KpiCard Props (lines 1-7) -->
```typescript
type Props = {
  label: string
  value: string
  variation?: string
  description?: string
  variationPositive?: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KpiConfig type + KpiCardFull restyle + KpiGridRenderer passthrough</name>
  <files>tools/wireframe-builder/types/blueprint.ts, tools/wireframe-builder/components/KpiCardFull.tsx, tools/wireframe-builder/components/sections/KpiGridRenderer.tsx</files>
  <action>
**blueprint.ts — Add icon field to KpiConfig:**
- Add `import type { LucideIcon } from 'lucide-react'` at top of file
- Add `icon?: LucideIcon` to the `KpiConfig` type (after `wide?: boolean`)
- If `LucideIcon` import fails TypeScript check, fallback to `React.ComponentType<{ className?: string }>`

**KpiCardFull.tsx — Full visual restyle (CARD-01 through CARD-05):**
- Add `import type { LucideIcon } from 'lucide-react'` at top
- Add `icon?: LucideIcon` to Props type
- Destructure as `icon: Icon` in component params
- CARD-01: Change card root from `rounded-lg border border-wf-card-border bg-wf-card p-4` to `group rounded-xl border border-wf-card-border bg-wf-card p-4 shadow-sm` (add `group`, change `rounded-lg` to `rounded-xl`, add `shadow-sm`)
- CARD-02: Add icon container block BEFORE the label paragraph, rendered only when `Icon` is truthy:
  ```
  {Icon && (
    <div className="mb-3 inline-flex h-10 w-10 items-center justify-center rounded-lg bg-wf-accent-muted text-wf-accent transition-colors duration-200 group-hover:bg-wf-accent group-hover:text-wf-accent-fg">
      <Icon className="h-5 w-5" />
    </div>
  )}
  ```
  IMPORTANT: `transition-colors` goes on the icon container div, NOT the card root. This prevents unwanted animation of card borders/text.
- CARD-04 label: Change from `text-[10px] font-medium uppercase tracking-wider text-wf-muted` to `text-sm font-medium text-wf-muted` (drop `uppercase`, drop `tracking-wider`, change `text-[10px]` to `text-sm`)
- CARD-04 value: Change from `font-bold` to `font-extrabold` (keep `text-2xl text-wf-heading`)
- CARD-05 sub: Change from `text-xs text-wf-muted` to `text-[10px] text-wf-muted` (keep `text-wf-muted` for theme-awareness instead of hardcoded `text-slate-400`)
- CARD-03 badge: Remove `compareMode &&` gate — show badge whenever `variation` is provided. Change `rounded` to `rounded-full`. Change `inline-block` to `inline-flex items-center`. Change `px-1.5` to `px-2`. Keep the `color-mix()` inline styles for theme-aware dark mode support (do NOT switch to hardcoded emerald/rose Tailwind classes).
- Keep `compareMode` gate ONLY on the semaforo block (unchanged).
- Keep Sparkline component and its usage unchanged.

**KpiGridRenderer.tsx — Add icon passthrough:**
- Add `icon={item.icon}` to the KpiCardFull props spread (one new line in the JSX).
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>KpiCardFull renders with rounded-xl shadow-sm, group class, icon container with group-hover transition, extrabold values, text-sm labels (no uppercase), text-[10px] sub, rounded-full badge without compareMode gate. KpiConfig has icon field. KpiGridRenderer passes icon prop. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: KpiCard secondary restyle</name>
  <files>tools/wireframe-builder/components/KpiCard.tsx</files>
  <action>
Restyle the simpler `KpiCard.tsx` variant for gallery consistency (no icon slot needed — this is the simple variant):

- CARD-01: Change card root from `rounded-lg border border-wf-card-border bg-wf-card p-4` to `rounded-xl border border-wf-card-border bg-wf-card p-4 shadow-sm` (change `rounded-lg` to `rounded-xl`, add `shadow-sm`). No `group` class needed since this variant has no icon.
- CARD-04 label: Change from `text-xs font-medium uppercase tracking-wide text-wf-muted` to `text-sm font-medium text-wf-muted` (drop `uppercase`, drop `tracking-wide`, change `text-xs` to `text-sm`)
- CARD-04 value: Change from `font-bold` to `font-extrabold` (keep `text-2xl text-wf-heading`)
- CARD-03 badge: Change `rounded` to `rounded-full`. Change `inline-block` to `inline-flex items-center`. Change `px-1.5` to `px-2`. Change `text-xs` to `text-[11px]`. Keep `color-mix()` inline styles.
- Description text: Change from `text-xs text-wf-muted` to `text-[10px] text-wf-muted` to match CARD-05 comparison text style.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit</automated>
  </verify>
  <done>KpiCard renders with rounded-xl shadow-sm, extrabold values, text-sm labels (no uppercase), rounded-full badge, text-[10px] description. Consistent visual treatment with KpiCardFull. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `make dev` — open wireframe viewer, navigate to a screen with KPI grid section
3. Verify cards show rounded-xl corners with subtle shadow
4. Verify value text is visibly bolder (extrabold vs bold)
5. Verify labels are larger (text-sm) and no longer uppercase
6. Verify trend badges are pill-shaped (rounded-full) and show whenever variation data is present
7. Verify sub/comparison text is smaller (10px) below value
8. If any KpiConfig items include an icon field, verify hover effect on the icon container
</verification>

<success_criteria>
- All 5 CARD requirements (CARD-01 through CARD-05) implemented in both KpiCardFull and KpiCard (where applicable)
- KpiConfig type extended with optional icon field
- KpiGridRenderer passes icon prop through
- Zero TypeScript errors
- Visual consistency between KpiCardFull and KpiCard variants
</success_criteria>

<output>
After completion, create `.planning/phases/24-kpi-cards/24-01-SUMMARY.md`
</output>
