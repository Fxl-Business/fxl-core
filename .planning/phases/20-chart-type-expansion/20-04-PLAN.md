---
phase: 20-chart-type-expansion
plan: 04
type: execute
wave: 3
depends_on: [20-02, 20-03]
files_modified: []
autonomous: false
requirements: [CHART-01, CHART-02, CHART-03, CHART-04, CHART-05, CHART-06]

must_haves:
  truths:
    - "stacked-bar renders bars stacked vertically with multiple colored segments per category"
    - "stacked-area renders areas stacked vertically with gradient fills per series"
    - "horizontal-bar renders bars going left to right with category labels on the Y axis"
    - "bubble renders circles of varying sizes with z-dimension controlling bubble radius"
    - "gauge renders a semicircle with colored zone arcs and a needle pointing to current value"
    - "composed renders mixed bar + area + line in one chart with correct visual layering"
    - "All new chart types use wireframe palette colors matching the rest of the wireframe"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint: confirm all 6 new chart types render correctly in the browser.

Purpose: Automated tests verify schema contracts and registry wiring. This plan verifies the visual rendering — that Recharts outputs look correct, needle positions are right, colors match the wireframe palette, and no layout clipping occurs.
Output: Human approval that all 6 new chart types render correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-chart-type-expansion/20-02-SUMMARY.md
@.planning/phases/20-chart-type-expansion/20-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and TypeScript check</name>
  <files></files>
  <action>
    Run the full verification suite to confirm all automated checks pass before handing off to human verification:

    ```bash
    cd /Users/cauetpinciara/Documents/fxl/fxl-core
    npx vitest run --reporter=verbose
    npx tsc --noEmit
    ```

    Expected output:
    - All tests pass (257+ tests green, including Phase 20 describe block in blueprint-schema.test.ts and 22-entry count in section-registry.test.ts)
    - Zero TypeScript errors

    If any failures, fix them before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx vitest run --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Full test suite green. Zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verify all 6 new chart types in browser</name>
  <action>Human verifies that each new chart type and the gauge section render correctly in the wireframe viewer browser.</action>
  <what-built>
    Phase 20 implements 6 new chart types:
    - CHART-01: stacked-bar — BarChart with multiple Bar components sharing stackId="stack"
    - CHART-02: stacked-area — AreaChart with multiple Area components sharing stackId="area" and gradient fills
    - CHART-03: horizontal-bar — BarChart layout="vertical" with swapped axis types
    - CHART-04: bubble — ScatterChart with ZAxis for size scaling
    - CHART-05: gauge-chart — new section type with PieChart zone arcs + SVG needle overlay
    - CHART-06: composed — ComposedChart with Bar + Area + Line in correct visual layering

    All components registered, dispatched, and available in the property editor.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `make dev`
    2. Open a wireframe that uses the wireframe builder (or ComponentGallery)
    3. Add a `bar-line-chart` section and change chartType to each new value in the property editor:
       - "Barra Empilhada" → confirm stacked colored bar segments appear (3 series, top bar has rounded corners)
       - "Area Empilhada" → confirm stacked area fills with distinct gradient colors per series
       - "Barra Horizontal" → confirm bars are horizontal with category labels on left Y-axis
       - "Bolhas" → confirm scatter circles of different sizes appear (larger z = larger bubble)
       - "Composto" → confirm mixed bar columns + area fill + line all visible in one chart
    4. Add a `gauge-chart` section (new section type in the catalog):
       - Confirm semicircle arc with 3 color zones (red/amber/green) appears
       - Confirm needle points to approximately 65% of the arc (default value=65, max=100)
       - Change value in property form to 20 — needle should point to left/danger zone
       - Change value to 85 — needle should point to right/ok zone
    5. Verify colors: all charts use the wireframe palette (--wf-chart-* tokens), not harsh blacks
    6. Verify no layout issues: gauge arc should not clip at the bottom, charts fit in their cards
  </how-to-verify>
  <verify>Human visual confirmation in browser</verify>
  <done>All 6 chart types render correctly and the gauge needle responds to value changes.</done>
  <resume-signal>Type "aprovado" if all charts render correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human visual confirmation of all 6 new chart types rendering correctly.
Automated: `npx vitest run --reporter=verbose` green + `npx tsc --noEmit` zero errors.
</verification>

<success_criteria>
- Full test suite passes (257+ tests)
- Zero TypeScript errors
- Human confirms all 6 chart types render correctly in browser
- Gauge needle positions are visually correct at low (danger), mid (warning), and high (ok) values
- All charts use wireframe palette colors (--wf-chart-*)
- No clipping or layout issues in any chart
</success_criteria>

<output>
After completion, create `.planning/phases/20-chart-type-expansion/20-04-SUMMARY.md`
</output>
