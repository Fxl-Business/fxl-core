---
phase: 11-ai-assisted-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/lib/screen-recipes.ts
  - tools/wireframe-builder/lib/screen-recipes.test.ts
  - tools/wireframe-builder/lib/vertical-templates.ts
  - tools/wireframe-builder/lib/vertical-templates.test.ts
autonomous: true
requirements: [AIGE-02, AIGE-03]

must_haves:
  truths:
    - "Screen recipes exist as typed objects mapping business contexts to section arrangements"
    - "Each recipe produces sections that pass Zod validation"
    - "Vertical templates are valid BlueprintConfig objects that render in the wireframe viewer"
    - "financeiro template has 10 screens matching the reference blueprint quality bar"
    - "varejo and servicos templates have basic screen structures"
  artifacts:
    - path: "tools/wireframe-builder/lib/screen-recipes.ts"
      provides: "Typed ScreenRecipe objects for 8-10 business contexts"
      exports: ["SCREEN_RECIPES", "ScreenRecipe", "RecipeSection", "findBestRecipe"]
    - path: "tools/wireframe-builder/lib/screen-recipes.test.ts"
      provides: "Unit tests for recipe validity and Zod compliance"
    - path: "tools/wireframe-builder/lib/vertical-templates.ts"
      provides: "Pre-built BlueprintConfig skeletons for 3 verticals"
      exports: ["VERTICAL_TEMPLATES", "VerticalId"]
    - path: "tools/wireframe-builder/lib/vertical-templates.test.ts"
      provides: "Unit tests for template validity"
  key_links:
    - from: "tools/wireframe-builder/lib/screen-recipes.ts"
      to: "tools/wireframe-builder/types/blueprint.ts"
      via: "BlueprintSection type import"
      pattern: "import.*BlueprintSection.*from"
    - from: "tools/wireframe-builder/lib/vertical-templates.ts"
      to: "tools/wireframe-builder/lib/blueprint-schema.ts"
      via: "BlueprintConfigSchema for validation in tests"
      pattern: "BlueprintConfigSchema"
---

<objective>
Create screen recipes and vertical templates as the structured knowledge base for AI-assisted blueprint generation.

Purpose: Screen recipes map business contexts (faturamento, estoque, DRE) to recommended section structures with types and layouts. Vertical templates provide complete BlueprintConfig skeletons for FXL verticals (financeiro, varejo, servicos) as starting points for generation. These are the foundation that Plan 02's generation engine composes from.

Output: 4 files -- screen-recipes.ts, vertical-templates.ts, and their test files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tools/wireframe-builder/types/blueprint.ts
@tools/wireframe-builder/lib/blueprint-schema.ts
@tools/wireframe-builder/lib/section-registry.tsx
@tools/wireframe-builder/lib/briefing-schema.ts
@tools/wireframe-builder/types/briefing.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tools/wireframe-builder/types/blueprint.ts:
```typescript
export type BlueprintSection =
  | KpiGridSection | BarLineChartSection | DonutChartSection
  | WaterfallChartSection | ParetoChartSection | CalculoCardSection
  | DataTableSection | DrillDownTableSection | ClickableTableSection
  | SaldoBancoSection | ManualInputSectionConfig | UploadSectionConfig
  | ConfigTableSection | InfoBlockSection | ChartGridSection
  | SettingsPageSection | FormSectionSection | FilterConfigSection
  | StatCardSection | ProgressBarSection | DividerSection

export type BlueprintScreen = {
  id: string
  title: string
  icon?: string
  periodType: PeriodType  // 'mensal' | 'anual' | 'none'
  filters: FilterOption[]
  hasCompareSwitch: boolean
  sections: BlueprintSection[]
}

export type BlueprintConfig = {
  slug: string
  label: string
  schemaVersion?: number
  screens: BlueprintScreen[]
}

export type ChartType = 'bar' | 'line' | 'bar-line' | 'radar' | 'treemap' | 'funnel' | 'scatter' | 'area'
export type FilterOption = { key: string; label: string; options?: string[] }
```

From tools/wireframe-builder/lib/section-registry.tsx:
```typescript
export function getDefaultSection(type: BlueprintSection['type']): BlueprintSection
// Returns valid section with all required fields filled for any of 21 types
```

Reference: financeiro-conta-azul blueprint structure (10 screens):
1. Resultado Mensal (DRE) - kpi-grid + calculo-card + waterfall-chart + drill-down-table
2. Visao por Receita - kpi-grid + chart-grid(donut+bar) + clickable-table + data-table
3. Visao por Despesas - kpi-grid + chart-grid(donut+bar) + data-table
4. Visao por Centro Custo - kpi-grid + chart-grid(bar+donut) + data-table
5. Margens - kpi-grid + chart-grid(bar+bar) + data-table
6. Fluxo Mensal - saldo-banco + kpi-grid + bar-line-chart + clickable-table
7. Fluxo Anual - manual-input + kpi-grid + chart-grid(bar+line) + clickable-table
8. Indicadores - kpi-grid + pareto-chart + data-table
9. Upload - upload-section + data-table
10. Configuracoes - config-table (x4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed screen recipes</name>
  <files>tools/wireframe-builder/lib/screen-recipes.ts, tools/wireframe-builder/lib/screen-recipes.test.ts</files>
  <behavior>
    - Test: Each recipe in SCREEN_RECIPES has a valid id, name, category, and non-empty sections array
    - Test: Every RecipeSection.type is a valid BlueprintSection['type'] (one of the 21 types)
    - Test: findBestRecipe('DRE Gerencial', 'Financeiro') returns the dre-resultado recipe
    - Test: findBestRecipe('Receita', 'Financeiro') returns the revenue-analysis recipe
    - Test: findBestRecipe('unknown-module', 'unknown') returns the generic-dashboard fallback recipe
    - Test: Each recipe's sections, when expanded via getDefaultSection(type), produce Zod-valid sections
  </behavior>
  <action>
Create `tools/wireframe-builder/lib/screen-recipes.ts` with:

1. **Types:**
   - `RecipeSection` type: `{ type: BlueprintSection['type'], purpose: string, defaults: Partial<BlueprintSection> }` -- the `defaults` object overlays on top of getDefaultSection(type) output
   - `ScreenRecipe` type: `{ id: string, name: string, category: 'financeiro' | 'operacional' | 'comercial' | 'geral', description: string, matchKeywords: string[], periodType: PeriodType, hasCompareSwitch: boolean, suggestedFilters: FilterOption[], sections: RecipeSection[] }`

2. **SCREEN_RECIPES array** with 10 recipes derived from the financeiro-conta-azul reference:
   - `dre-resultado`: kpi-grid + calculo-card + waterfall-chart + drill-down-table (category: financeiro, keywords: ['DRE', 'Resultado', 'Demonstracao'])
   - `revenue-analysis`: kpi-grid + chart-grid(donut+bar) + clickable-table + data-table (category: financeiro, keywords: ['Receita', 'Faturamento', 'Vendas'])
   - `expense-analysis`: kpi-grid + chart-grid(donut+bar) + data-table (category: financeiro, keywords: ['Despesa', 'Custo', 'Gasto'])
   - `cost-center`: kpi-grid + chart-grid(bar+donut) + data-table (category: financeiro, keywords: ['Centro de Custo', 'Departamento'])
   - `margins`: kpi-grid + chart-grid(bar+bar) + data-table (category: financeiro, keywords: ['Margem', 'Lucratividade', 'Markup'])
   - `cash-flow-daily`: saldo-banco + kpi-grid + bar-line-chart + clickable-table (category: financeiro, keywords: ['Fluxo Mensal', 'Fluxo de Caixa', 'Caixa Diario'])
   - `cash-flow-annual`: manual-input + kpi-grid + chart-grid(bar+line) + clickable-table (category: financeiro, keywords: ['Fluxo Anual', 'Caixa Anual'])
   - `kpi-dashboard`: kpi-grid + pareto-chart + data-table (category: geral, keywords: ['Indicadores', 'KPI', 'Performance'])
   - `data-upload`: upload-section + data-table (category: geral, keywords: ['Upload', 'Importacao', 'Dados'])
   - `settings`: config-table x4 (category: geral, keywords: ['Configuracao', 'Config', 'Setup'])

3. **`findBestRecipe(moduleName: string, segment: string): ScreenRecipe`** function:
   - Score each recipe by keyword match against moduleName (case-insensitive, partial match)
   - Prefer recipes in category matching segment (financeiro -> financeiro category)
   - Return highest-scoring recipe, fallback to `kpi-dashboard` if no match

Export: `SCREEN_RECIPES`, `ScreenRecipe`, `RecipeSection`, `findBestRecipe`

The `defaults` in each RecipeSection should include meaningful placeholder data that makes the wireframe look realistic (e.g., KPI labels like 'Receita Total', 'R$ 0', chart titles like 'Evolucao Mensal'). Use the financeiro-conta-azul reference as the quality bar for what good placeholder data looks like.

Do NOT import from `@/lib/supabase` or any Vite-dependent module. Use only relative imports within `tools/wireframe-builder/`.
  </action>
  <verify>
    <automated>npx vitest run tools/wireframe-builder/lib/screen-recipes.test.ts --reporter=verbose</automated>
  </verify>
  <done>10 screen recipes exist as typed objects, findBestRecipe maps module names to appropriate recipes, all recipe sections reference valid BlueprintSection types, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vertical templates</name>
  <files>tools/wireframe-builder/lib/vertical-templates.ts, tools/wireframe-builder/lib/vertical-templates.test.ts</files>
  <behavior>
    - Test: VERTICAL_TEMPLATES has keys 'financeiro', 'varejo', 'servicos'
    - Test: financeiro template has 10 screens with valid structure
    - Test: Each vertical template passes BlueprintConfigSchema.parse() without error
    - Test: Every screen in every template has a non-empty id, title, and sections array
    - Test: varejo template has at least 4 screens (reduced scope vs financeiro)
    - Test: servicos template has at least 4 screens (reduced scope vs financeiro)
  </behavior>
  <action>
Create `tools/wireframe-builder/lib/vertical-templates.ts` with:

1. **Type:** `VerticalId = 'financeiro' | 'varejo' | 'servicos'`

2. **VERTICAL_TEMPLATES** as `Record<VerticalId, BlueprintConfig>`:

   **financeiro** (HIGH quality -- 10 screens based on financeiro-conta-azul reference):
   Build complete BlueprintConfig with 10 screens. Each screen uses getDefaultSection() as base, overlaid with recipe-specific placeholder data. Screens:
   - resultado-mensal: DRE screen with kpi-grid (Receita, Despesa, Resultado), calculo-card, waterfall-chart, drill-down-table
   - receita: Revenue screen with kpi-grid, chart-grid (donut+bar), clickable-table, data-table
   - despesas: Expense screen with kpi-grid, chart-grid (donut+bar), data-table
   - centro-custo: Cost center with kpi-grid, chart-grid (bar+donut), data-table
   - margens: Margins with kpi-grid, chart-grid (bar+bar), data-table
   - fluxo-mensal: Monthly cash flow with saldo-banco, kpi-grid, bar-line-chart, clickable-table
   - fluxo-anual: Annual cash flow with manual-input, kpi-grid, chart-grid (bar+line), clickable-table
   - indicadores: KPIs with kpi-grid, pareto-chart, data-table
   - upload: Data import with upload-section, data-table
   - configuracoes: Settings with 4 config-tables

   Set `schemaVersion: 1`, slug: 'template-financeiro', label: 'Template Financeiro'.

   **varejo** (MEDIUM quality -- 5-6 screens for retail):
   - visao-geral: Overview with kpi-grid (Faturamento, Ticket Medio, Volume), stat-card, bar-line-chart
   - vendas: Sales with kpi-grid, chart-grid (donut+bar), data-table
   - estoque: Inventory with kpi-grid, progress-bar, data-table
   - indicadores: KPIs with kpi-grid, pareto-chart, data-table
   - configuracoes: Settings with config-tables

   **servicos** (MEDIUM quality -- 5-6 screens for services):
   - visao-geral: Overview with kpi-grid (Receita, Projetos Ativos, Satisfacao), stat-card, bar-line-chart
   - projetos: Projects with kpi-grid, progress-bar, data-table
   - financeiro: Financial summary with kpi-grid, chart-grid (donut+bar), data-table
   - indicadores: KPIs with kpi-grid, pareto-chart, data-table
   - configuracoes: Settings with config-tables

Each screen must have: id (kebab-case), title, periodType, filters (at minimum `[{ key: 'period', label: 'Periodo' }]` for mensal screens), hasCompareSwitch, and sections array with VALID section objects (every section must pass its Zod schema).

IMPORTANT: Every section must have ALL required fields per its discriminated union type. Do NOT leave out `items` for kpi-grid, `title` for bar-line-chart, `columns` for data-table, etc. Use getDefaultSection() pattern as reference for what fields are required.

Do NOT import from `@/lib/supabase` or any Vite-dependent module. Use only relative imports.
  </action>
  <verify>
    <automated>npx vitest run tools/wireframe-builder/lib/vertical-templates.test.ts --reporter=verbose</automated>
  </verify>
  <done>3 vertical templates exist as valid BlueprintConfig objects, financeiro has 10 screens, varejo and servicos have 4+ screens each, all pass Zod validation, tests pass</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tools/wireframe-builder/lib/screen-recipes.test.ts tools/wireframe-builder/lib/vertical-templates.test.ts --reporter=verbose
npx tsc --noEmit
```
Both test files green, zero TypeScript errors.
</verification>

<success_criteria>
- 10 screen recipes cover the most common FXL business contexts
- findBestRecipe() correctly maps briefing module names to appropriate recipes
- 3 vertical templates (financeiro/varejo/servicos) are valid BlueprintConfigs
- financeiro template matches the quality bar of the reference blueprint (10 screens)
- All sections in all templates pass Zod validation
- No Vite-dependent imports (no @/lib/supabase)
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-assisted-generation/11-01-SUMMARY.md`
</output>
