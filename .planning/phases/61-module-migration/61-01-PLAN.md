---
phase: 61-module-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/layout/Layout.tsx
  - src/platform/layout/Sidebar.tsx
  - src/platform/layout/TopNav.tsx
  - src/platform/layout/ScrollToTop.tsx
  - src/platform/layout/SearchCommand.tsx
  - src/platform/layout/SearchCommand.test.tsx
  - src/platform/layout/ThemeToggle.tsx
  - src/platform/auth/ProtectedRoute.tsx
  - src/platform/auth/Login.tsx
  - src/platform/auth/Profile.tsx
  - src/platform/module-loader/registry.ts
  - src/platform/module-loader/module-ids.ts
  - src/platform/module-loader/extension-registry.ts
  - src/platform/module-loader/slots.tsx
  - src/platform/module-loader/hooks/useModuleEnabled.tsx
  - src/platform/services/activity-feed.ts
  - src/platform/services/module-stats.ts
  - src/platform/pages/Home.tsx
  - src/platform/pages/Home.test.tsx
  - src/platform/pages/admin/ModulesPanel.tsx
  - src/platform/supabase.ts
  - src/App.tsx
autonomous: true
requirements: [ESTR-02, ESTR-03, ESTR-04]

must_haves:
  truths:
    - "Layout, Sidebar, TopNav render from src/platform/layout/"
    - "ProtectedRoute, Login, Profile render from src/platform/auth/"
    - "Module system (registry, module-ids, extension-registry, slots, useModuleEnabled) loads from src/platform/module-loader/"
    - "Home page and admin pages render from src/platform/pages/"
    - "Platform services (activity-feed, module-stats, supabase) importable from src/platform/"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/platform/layout/Layout.tsx"
      provides: "Main shell layout component"
    - path: "src/platform/module-loader/registry.ts"
      provides: "MODULE_REGISTRY and type exports"
      exports: ["MODULE_REGISTRY", "ModuleDefinition", "ModuleManifest", "NavItem", "SlotComponentProps", "ModuleExtension"]
    - path: "src/platform/module-loader/module-ids.ts"
      provides: "MODULE_IDS constants"
      exports: ["MODULE_IDS", "ModuleId", "SLOT_IDS", "SlotId"]
    - path: "src/platform/auth/ProtectedRoute.tsx"
      provides: "Auth guard component"
  key_links:
    - from: "src/App.tsx"
      to: "src/platform/layout/Layout.tsx"
      via: "import"
      pattern: "from '@platform/layout/Layout'"
    - from: "src/App.tsx"
      to: "src/platform/module-loader/registry.ts"
      via: "import"
      pattern: "from '@platform/module-loader/registry'"
    - from: "src/platform/layout/Sidebar.tsx"
      to: "src/platform/module-loader/registry.ts"
      via: "import MODULE_REGISTRY"
      pattern: "from '@platform/module-loader/registry'"
---

<objective>
Move all platform-layer files to their new locations under src/platform/: layout components, auth components, module-loader system, platform services, and platform pages. Update all import paths across the entire codebase.

Purpose: Establishes the platform layer as the central shell infrastructure, separated from module-specific code. This is the foundation that all module migrations (Wave 2) depend on.
Output: All platform files in src/platform/, all imports updated, tsc passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-platform-scaffold-shared-layer/60-01-SUMMARY.md
@.planning/phases/61-module-migration/61-CONTEXT.md
@docs/superpowers/specs/2026-03-16-fxl-platform-evolution-design.md

<interfaces>
<!-- Current import patterns that will change. Executor must update ALL consumers. -->

From src/components/layout/Layout.tsx:
  Imported by: src/App.tsx as `import Layout from '@/components/layout/Layout'`

From src/components/layout/Sidebar.tsx:
  Imports: '@/components/ui/scroll-area', '@/lib/utils', '@/modules/registry', '@/modules/hooks/useModuleEnabled', '@/hooks/useDocsNav', '@/modules/module-ids'

From src/components/layout/SearchCommand.tsx:
  Imports: '@/components/ui/command', '@/lib/search-index', '@/lib/kb-service'
  NOTE: kb-service import stays as-is for now (Phase 62 removes KB module)

From src/components/ProtectedRoute.tsx:
  Imported by: src/App.tsx as `import ProtectedRoute from '@/components/ProtectedRoute'`

From src/modules/registry.ts:
  Exports: MODULE_REGISTRY, MODULE_IDS, ModuleId, SLOT_IDS, SlotId, ModuleStatus, NavItem, ModuleManifest, SlotComponentProps, ModuleExtension, ModuleDefinition
  Imported by: App.tsx, Home.tsx, ModulesPanel.tsx, Sidebar.tsx, all manifest files, extension components, extension-registry.ts, slots.tsx, useModuleEnabled.tsx

From src/modules/module-ids.ts:
  Exports: MODULE_IDS, ModuleId, SLOT_IDS, SlotId
  Imported by: Sidebar.tsx, all manifest files, useModuleEnabled.tsx
  NOTE: Also re-exported by registry.ts

From src/modules/extension-registry.ts:
  Exports: resolveExtensions, ExtensionMap
  Imported by: slots.tsx

From src/modules/slots.tsx:
  Exports: ExtensionProvider, useExtensions, ExtensionSlot
  Imported by: App.tsx, Home.tsx

From src/modules/hooks/useModuleEnabled.tsx:
  Exports: ModuleEnabledProvider, useModuleEnabled, useIsModuleEnabled
  Imported by: App.tsx, Sidebar.tsx

From src/lib/supabase.ts:
  Imported by: docs-service.ts, kb-service.ts, tasks-service.ts, activity-feed.ts, module-stats.ts, RecentTasksWidget.tsx, RecentKBWidget.tsx

From src/lib/activity-feed.ts:
  Imported by: Home.tsx, Home.test.tsx

From src/lib/module-stats.ts:
  Imported by: Home.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move platform layout, auth, module-loader, services, and pages files</name>
  <files>
    src/platform/layout/Layout.tsx
    src/platform/layout/Sidebar.tsx
    src/platform/layout/TopNav.tsx
    src/platform/layout/ScrollToTop.tsx
    src/platform/layout/SearchCommand.tsx
    src/platform/layout/SearchCommand.test.tsx
    src/platform/layout/ThemeToggle.tsx
    src/platform/auth/ProtectedRoute.tsx
    src/platform/auth/Login.tsx
    src/platform/auth/Profile.tsx
    src/platform/module-loader/registry.ts
    src/platform/module-loader/module-ids.ts
    src/platform/module-loader/extension-registry.ts
    src/platform/module-loader/slots.tsx
    src/platform/module-loader/hooks/useModuleEnabled.tsx
    src/platform/services/activity-feed.ts
    src/platform/services/module-stats.ts
    src/platform/pages/Home.tsx
    src/platform/pages/Home.test.tsx
    src/platform/pages/admin/ModulesPanel.tsx
    src/platform/supabase.ts
  </files>
  <read_first>
    src/components/layout/Layout.tsx
    src/components/layout/Sidebar.tsx
    src/components/layout/TopNav.tsx
    src/components/layout/ScrollToTop.tsx
    src/components/layout/SearchCommand.tsx
    src/components/layout/SearchCommand.test.tsx
    src/components/layout/ThemeToggle.tsx
    src/components/ProtectedRoute.tsx
    src/pages/Login.tsx
    src/pages/Profile.tsx
    src/modules/registry.ts
    src/modules/module-ids.ts
    src/modules/extension-registry.ts
    src/modules/slots.tsx
    src/modules/hooks/useModuleEnabled.tsx
    src/lib/activity-feed.ts
    src/lib/module-stats.ts
    src/lib/supabase.ts
    src/pages/Home.tsx
    src/pages/Home.test.tsx
    src/pages/admin/ModulesPanel.tsx
    src/App.tsx
  </read_first>
  <action>
For each file listed below, use `git mv` to move it to the new location, then update all internal imports within the moved file itself.

**Layout files — move from src/components/layout/ to src/platform/layout/:**
- `git mv src/components/layout/Layout.tsx src/platform/layout/Layout.tsx`
- `git mv src/components/layout/Sidebar.tsx src/platform/layout/Sidebar.tsx`
- `git mv src/components/layout/TopNav.tsx src/platform/layout/TopNav.tsx`
- `git mv src/components/layout/ScrollToTop.tsx src/platform/layout/ScrollToTop.tsx`
- `git mv src/components/layout/SearchCommand.tsx src/platform/layout/SearchCommand.tsx`
- `git mv src/components/layout/SearchCommand.test.tsx src/platform/layout/SearchCommand.test.tsx`
- `git mv src/components/layout/ThemeToggle.tsx src/platform/layout/ThemeToggle.tsx`

**Auth files — move to src/platform/auth/:**
- `git mv src/components/ProtectedRoute.tsx src/platform/auth/ProtectedRoute.tsx`
- `git mv src/pages/Login.tsx src/platform/auth/Login.tsx`
- `git mv src/pages/Profile.tsx src/platform/auth/Profile.tsx`

**Module-loader files — move from src/modules/ to src/platform/module-loader/:**
- `git mv src/modules/registry.ts src/platform/module-loader/registry.ts`
- `git mv src/modules/module-ids.ts src/platform/module-loader/module-ids.ts`
- `git mv src/modules/extension-registry.ts src/platform/module-loader/extension-registry.ts`
- `git mv src/modules/slots.tsx src/platform/module-loader/slots.tsx`
- `git mv src/modules/hooks/useModuleEnabled.tsx src/platform/module-loader/hooks/useModuleEnabled.tsx`

**Platform services — move from src/lib/ to src/platform/:**
- `git mv src/lib/supabase.ts src/platform/supabase.ts`
- `git mv src/lib/activity-feed.ts src/platform/services/activity-feed.ts`
- `git mv src/lib/module-stats.ts src/platform/services/module-stats.ts`

**Platform pages — move from src/pages/ to src/platform/pages/:**
- `git mv src/pages/Home.tsx src/platform/pages/Home.tsx`
- `git mv src/pages/Home.test.tsx src/platform/pages/Home.test.tsx`
- Create `src/platform/pages/admin/` directory, then: `git mv src/pages/admin/ModulesPanel.tsx src/platform/pages/admin/ModulesPanel.tsx`

After moving, update internal imports WITHIN each moved file:
- In Layout.tsx: change `@/components/layout/` refs to relative (./), change `@/components/ui/` to `@shared/ui/` (after Plan 02 — see note below)
- In Sidebar.tsx: change `@/modules/registry` to `@platform/module-loader/registry`, change `@/modules/hooks/useModuleEnabled` to `@platform/module-loader/hooks/useModuleEnabled`, change `@/modules/module-ids` to `@platform/module-loader/module-ids`, change `@/hooks/useDocsNav` to `@/hooks/useDocsNav` (stays — hooks move in Plan 03), change `@/lib/utils` to `@/lib/utils` (stays until Plan 02 moves utils), change `@/components/ui/` to `@/components/ui/` (stays until Plan 02 moves shared/ui/)
- In SearchCommand.tsx: change `@/components/ui/command` to `@/components/ui/command` (stays until Plan 02), change `@/lib/search-index` to `@/lib/search-index` (stays until Plan 03), change `@/lib/kb-service` to `@/lib/kb-service` (stays — Phase 62 handles KB removal)
- In ThemeToggle.tsx: change `@/components/ui/` to `@/components/ui/` (stays until Plan 02)
- In ProtectedRoute.tsx: no import changes needed (only uses react-router-dom and @clerk/react)
- In Login.tsx: no import changes expected (only uses @clerk/react)
- In Profile.tsx: no import changes expected (only uses @clerk/react)
- In registry.ts: change `'./docs/manifest'` to `@modules/docs/manifest`, change `'./ferramentas/manifest'` to `@modules/ferramentas/manifest`, change `'./clients/manifest'` to `@modules/clients/manifest`, change `'./knowledge-base/manifest'` to `@modules/knowledge-base/manifest`, change `'./tasks/manifest'` to `@modules/tasks/manifest`, change `'./module-ids'` to `./module-ids` (stays relative — both are in module-loader/)
- In extension-registry.ts: change `'./registry'` to `'./registry'` (stays relative)
- In slots.tsx: change `'./registry'` to `'./registry'` (stays relative), change `'./extension-registry'` to `'./extension-registry'` (stays relative), change `'./hooks/useModuleEnabled'` to `'./hooks/useModuleEnabled'` (stays relative)
- In useModuleEnabled.tsx: change `'@/modules/registry'` to `'@platform/module-loader/registry'`, change `'@/modules/module-ids'` to `'@platform/module-loader/module-ids'`
- In activity-feed.ts: change `'@/lib/supabase'` to `'@platform/supabase'`
- In module-stats.ts: change `'@/lib/supabase'` to `'@platform/supabase'`
- In supabase.ts: no import changes (only imports from @supabase/supabase-js)
- In Home.tsx: change `'@/modules/registry'` to `'@platform/module-loader/registry'`, change `'@/modules/slots'` to `'@platform/module-loader/slots'`, change `'@/lib/activity-feed'` to `'@platform/services/activity-feed'`, change `'@/lib/module-stats'` to `'@platform/services/module-stats'`, change `'@/components/ui/...'` to `'@/components/ui/...'` (stays until Plan 02)
- In Home.test.tsx: change `'@/lib/activity-feed'` to `'@platform/services/activity-feed'`
- In ModulesPanel.tsx: change `'@/modules/registry'` to `'@platform/module-loader/registry'`, change `'@/components/ui/...'` to `'@/components/ui/...'` (stays until Plan 02)

**IMPORTANT NOTE:** Imports referencing `@/components/ui/`, `@/lib/utils`, `@/hooks/useDocsNav`, `@/hooks/useDoc`, `@/lib/search-index`, `@/lib/kb-service`, `@/lib/docs-service`, `@/lib/docs-parser`, and `@/lib/tasks-service` should be left AS-IS in this task. Those files are moved in Plans 02-06. The `@/` alias still resolves them correctly in their current locations. Only update imports for files that THIS plan moves.

Remove .gitkeep files from target directories that now have real files.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/platform/layout/Layout.tsx` succeeds (file exists)
    - `ls src/platform/layout/Sidebar.tsx` succeeds
    - `ls src/platform/layout/TopNav.tsx` succeeds
    - `ls src/platform/layout/SearchCommand.tsx` succeeds
    - `ls src/platform/layout/ThemeToggle.tsx` succeeds
    - `ls src/platform/auth/ProtectedRoute.tsx` succeeds
    - `ls src/platform/auth/Login.tsx` succeeds
    - `ls src/platform/auth/Profile.tsx` succeeds
    - `ls src/platform/module-loader/registry.ts` succeeds
    - `ls src/platform/module-loader/module-ids.ts` succeeds
    - `ls src/platform/module-loader/extension-registry.ts` succeeds
    - `ls src/platform/module-loader/slots.tsx` succeeds
    - `ls src/platform/module-loader/hooks/useModuleEnabled.tsx` succeeds
    - `ls src/platform/services/activity-feed.ts` succeeds
    - `ls src/platform/services/module-stats.ts` succeeds
    - `ls src/platform/supabase.ts` succeeds
    - `ls src/platform/pages/Home.tsx` succeeds
    - `ls src/platform/pages/admin/ModulesPanel.tsx` succeeds
    - `ls src/components/layout/` fails or is empty (all files moved out)
    - `ls src/components/ProtectedRoute.tsx` fails (file moved)
    - `ls src/modules/registry.ts` fails (file moved)
    - `ls src/modules/module-ids.ts` fails (file moved)
    - `ls src/modules/extension-registry.ts` fails (file moved)
    - `ls src/modules/slots.tsx` fails (file moved)
    - `ls src/lib/supabase.ts` fails (file moved)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All platform-layer files live in src/platform/. All their internal imports updated. All consumer imports in files NOT being moved by this plan still use @/ paths that resolve correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer imports across the codebase to point to new platform locations</name>
  <files>
    src/App.tsx
    src/modules/docs/manifest.tsx
    src/modules/ferramentas/manifest.tsx
    src/modules/clients/manifest.tsx
    src/modules/knowledge-base/manifest.ts
    src/modules/tasks/manifest.ts
    src/modules/tasks/extensions/RecentTasksWidget.tsx
    src/modules/knowledge-base/extensions/RecentKBWidget.tsx
    src/modules/wireframe-builder/manifest.tsx
    src/hooks/useDocsNav.ts
    src/lib/docs-service.ts
    src/lib/kb-service.ts
    src/lib/tasks-service.ts
  </files>
  <read_first>
    src/App.tsx
    src/modules/docs/manifest.tsx
    src/modules/ferramentas/manifest.tsx
    src/modules/clients/manifest.tsx
    src/modules/knowledge-base/manifest.ts
    src/modules/tasks/manifest.ts
    src/modules/tasks/extensions/RecentTasksWidget.tsx
    src/modules/knowledge-base/extensions/RecentKBWidget.tsx
    src/modules/wireframe-builder/manifest.tsx
    src/hooks/useDocsNav.ts
    src/lib/docs-service.ts
    src/lib/kb-service.ts
    src/lib/tasks-service.ts
  </read_first>
  <action>
Update every file in the codebase that imports from the OLD platform locations. These are files NOT moved by Task 1 that reference files that WERE moved.

**src/App.tsx — update these imports:**
- `import Layout from '@/components/layout/Layout'` -> `import Layout from '@platform/layout/Layout'`
- `import ProtectedRoute from '@/components/ProtectedRoute'` -> `import ProtectedRoute from '@platform/auth/ProtectedRoute'`
- `import Home from '@/pages/Home'` -> `import Home from '@platform/pages/Home'`
- `import Login from '@/pages/Login'` -> `import Login from '@platform/auth/Login'`
- `import Profile from '@/pages/Profile'` -> `import Profile from '@platform/auth/Profile'`
- `import { MODULE_REGISTRY } from '@/modules/registry'` -> `import { MODULE_REGISTRY } from '@platform/module-loader/registry'`
- `import { ModuleEnabledProvider } from '@/modules/hooks/useModuleEnabled'` -> `import { ModuleEnabledProvider } from '@platform/module-loader/hooks/useModuleEnabled'`
- `import { ExtensionProvider } from '@/modules/slots'` -> `import { ExtensionProvider } from '@platform/module-loader/slots'`

**Module manifests — update registry/module-ids imports:**
- In `src/modules/docs/manifest.tsx`: change `from '@/modules/registry'` to `from '@platform/module-loader/registry'`, change `from '@/modules/module-ids'` to `from '@platform/module-loader/module-ids'`
- In `src/modules/ferramentas/manifest.tsx`: same pattern — `'@/modules/registry'` -> `'@platform/module-loader/registry'`, `'@/modules/module-ids'` -> `'@platform/module-loader/module-ids'`
- In `src/modules/clients/manifest.tsx`: same pattern
- In `src/modules/knowledge-base/manifest.ts`: same pattern
- In `src/modules/tasks/manifest.ts`: same pattern
- In `src/modules/wireframe-builder/manifest.tsx`: change `from '@/modules/registry'` to `from '@platform/module-loader/registry'`

**Extension components — update registry imports:**
- In `src/modules/tasks/extensions/RecentTasksWidget.tsx`: change `from '@/modules/registry'` to `from '@platform/module-loader/registry'`, change `from '@/lib/supabase'` to `from '@platform/supabase'`
- In `src/modules/knowledge-base/extensions/RecentKBWidget.tsx`: change `from '@/modules/registry'` to `from '@platform/module-loader/registry'`, change `from '@/lib/supabase'` to `from '@platform/supabase'`

**Hooks — update imports:**
- In `src/hooks/useDocsNav.ts`: change `from '@/modules/registry'` to `from '@platform/module-loader/registry'`

**Services — update supabase imports:**
- In `src/lib/docs-service.ts`: change `from '@/lib/supabase'` to `from '@platform/supabase'`
- In `src/lib/kb-service.ts`: change `from '@/lib/supabase'` to `from '@platform/supabase'`
- In `src/lib/tasks-service.ts`: change `from '@/lib/supabase'` to `from '@platform/supabase'`

After all import updates, run `npx tsc --noEmit` to confirm zero errors.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from '@/components/layout/" src/` returns zero matches (no old layout imports remain)
    - `grep -r "from '@/components/ProtectedRoute'" src/` returns zero matches
    - `grep -r "from '@/modules/registry'" src/` returns zero matches (all updated to @platform/module-loader/registry)
    - `grep -r "from '@/modules/module-ids'" src/` returns zero matches
    - `grep -r "from '@/modules/extension-registry'" src/` returns zero matches
    - `grep -r "from '@/modules/slots'" src/` returns zero matches
    - `grep -r "from '@/modules/hooks/useModuleEnabled'" src/` returns zero matches
    - `grep -r "from '@/pages/Home'" src/` returns zero matches
    - `grep -r "from '@/pages/Login'" src/` returns zero matches
    - `grep -r "from '@/pages/Profile'" src/` returns zero matches
    - `grep -r "from '@/lib/supabase'" src/` returns zero matches (all updated to @platform/supabase)
    - `grep -r "from '@/lib/activity-feed'" src/` returns zero matches
    - `grep -r "from '@/lib/module-stats'" src/` returns zero matches
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Every file in the codebase that referenced old platform locations now points to the new @platform/ paths. tsc passes with zero errors. No stale imports to moved files remain.</done>
</task>

</tasks>

<verification>
```bash
# Verify all platform files exist in new locations
ls src/platform/layout/Layout.tsx src/platform/layout/Sidebar.tsx src/platform/layout/TopNav.tsx
ls src/platform/auth/ProtectedRoute.tsx src/platform/auth/Login.tsx src/platform/auth/Profile.tsx
ls src/platform/module-loader/registry.ts src/platform/module-loader/module-ids.ts
ls src/platform/module-loader/extension-registry.ts src/platform/module-loader/slots.tsx
ls src/platform/module-loader/hooks/useModuleEnabled.tsx
ls src/platform/services/activity-feed.ts src/platform/services/module-stats.ts
ls src/platform/supabase.ts
ls src/platform/pages/Home.tsx src/platform/pages/admin/ModulesPanel.tsx

# Verify old locations are empty/gone
test ! -f src/components/layout/Layout.tsx && echo "OLD GONE"
test ! -f src/modules/registry.ts && echo "OLD GONE"
test ! -f src/lib/supabase.ts && echo "OLD GONE"

# Verify no stale imports
grep -r "from '@/components/layout/" src/ && echo "FAIL: stale layout imports" || echo "OK"
grep -r "from '@/modules/registry'" src/ && echo "FAIL: stale registry imports" || echo "OK"
grep -r "from '@/lib/supabase'" src/ && echo "FAIL: stale supabase imports" || echo "OK"

# TypeScript check
npx tsc --noEmit
```
</verification>

<success_criteria>
- All 21 platform files moved to src/platform/ subdirectories
- All import paths updated across the entire codebase
- Zero stale imports to old locations
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/61-module-migration/61-01-SUMMARY.md`
</output>
