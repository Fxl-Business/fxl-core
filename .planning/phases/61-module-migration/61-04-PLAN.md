---
phase: 61-module-migration
plan: 04
type: execute
wave: 2
depends_on: [61-01, 61-02]
files_modified:
  - src/modules/tasks/services/tasks-service.ts
  - src/modules/tasks/services/tasks-service.test.ts
  - src/modules/tasks/types/index.ts
  - src/modules/tasks/hooks/useTasks.ts
  - src/modules/tasks/pages/TaskForm.tsx
  - src/modules/tasks/pages/TaskList.tsx
  - src/modules/tasks/pages/KanbanBoard.tsx
  - src/modules/tasks/components/TaskStatusBadge.tsx
  - src/modules/tasks/components/TaskCard.tsx
  - src/modules/tasks/components/TaskFilters.tsx
  - src/modules/tasks/components/DocumentarButton.tsx
  - src/modules/tasks/extensions/RecentTasksWidget.tsx
  - src/modules/tasks/manifest.ts
autonomous: true
requirements: [MOD-02]

must_haves:
  truths:
    - "tasks-service.ts and its test live in src/modules/tasks/services/"
    - "All task pages (TaskList, KanbanBoard, TaskForm) import from module-internal paths"
    - "All task components import from @shared/ui/ and @shared/utils/ (not @/components/ui/ or @/lib/utils)"
    - "Task types re-export from module-internal service (not @/lib/tasks-service)"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/modules/tasks/services/tasks-service.ts"
      provides: "Task CRUD operations"
      exports: ["createTask", "updateTask", "getTask", "listTasks", "updateTaskStatus"]
    - path: "src/modules/tasks/services/tasks-service.test.ts"
      provides: "Task service unit tests"
  key_links:
    - from: "src/modules/tasks/hooks/useTasks.ts"
      to: "src/modules/tasks/services/tasks-service.ts"
      via: "import"
      pattern: "from.*tasks-service"
    - from: "src/modules/tasks/types/index.ts"
      to: "src/modules/tasks/services/tasks-service.ts"
      via: "re-export types"
      pattern: "from.*tasks-service"
    - from: "src/modules/tasks/pages/TaskForm.tsx"
      to: "src/modules/tasks/services/tasks-service.ts"
      via: "import createTask, updateTask, getTask"
      pattern: "from.*tasks-service"
---

<objective>
Complete the tasks module migration by moving tasks-service.ts (and its test) from src/lib/ into src/modules/tasks/services/, then updating all internal imports within the module to use module-relative paths instead of @/lib/ paths.

Purpose: Makes the tasks module fully self-contained with its service layer inside the module boundary.
Output: tasks-service in module, all internal imports updated, tsc passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-module-migration/61-CONTEXT.md

<interfaces>
<!-- The tasks module already has components/, pages/, hooks/, types/, extensions/ populated -->
<!-- Only the service file needs to move from src/lib/ -->

From src/lib/tasks-service.ts:
  Exports: Task, TaskStatus, TaskPriority, createTask, updateTask, getTask, listTasks, updateTaskStatus
  Imported by (within tasks module):
    - src/modules/tasks/types/index.ts (re-exports Task, TaskStatus, TaskPriority)
    - src/modules/tasks/hooks/useTasks.ts (imports listTasks, Task, TaskStatus, TaskPriority)
    - src/modules/tasks/pages/TaskForm.tsx (imports createTask, updateTask, getTask, TaskStatus, TaskPriority)
    - src/modules/tasks/pages/TaskList.tsx (imports updateTaskStatus)
    - src/modules/tasks/pages/KanbanBoard.tsx (imports updateTaskStatus)

Current @/components/ui/ and @/lib/utils imports in tasks module files (need @shared/ update):
  - TaskForm.tsx: imports from @/components/ui/ (input, label, select, textarea)
  - TaskList.tsx: imports from @/components/ui/ (badge)
  - KanbanBoard.tsx: imports from @/components/ui/ (badge)
  - TaskFilters.tsx: imports from @/components/ui/ (select, input)
  - DocumentarButton.tsx: imports from @/components/ui/ (button)
  - TaskCard.tsx: imports from @/components/ui/ (card, badge), @/lib/utils (cn)
  - TaskStatusBadge.tsx: imports from @/lib/utils (cn)

From src/modules/tasks/extensions/RecentTasksWidget.tsx:
  Imports: @/lib/supabase, @/modules/registry (should be @platform/* after Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move tasks-service into module and update all tasks module imports</name>
  <files>
    src/modules/tasks/services/tasks-service.ts
    src/modules/tasks/services/tasks-service.test.ts
    src/modules/tasks/types/index.ts
    src/modules/tasks/hooks/useTasks.ts
    src/modules/tasks/pages/TaskForm.tsx
    src/modules/tasks/pages/TaskList.tsx
    src/modules/tasks/pages/KanbanBoard.tsx
    src/modules/tasks/components/TaskStatusBadge.tsx
    src/modules/tasks/components/TaskCard.tsx
    src/modules/tasks/components/TaskFilters.tsx
    src/modules/tasks/components/DocumentarButton.tsx
    src/modules/tasks/extensions/RecentTasksWidget.tsx
    src/modules/tasks/manifest.ts
  </files>
  <read_first>
    src/lib/tasks-service.ts
    src/lib/tasks-service.test.ts
    src/modules/tasks/types/index.ts
    src/modules/tasks/hooks/useTasks.ts
    src/modules/tasks/pages/TaskForm.tsx
    src/modules/tasks/pages/TaskList.tsx
    src/modules/tasks/pages/KanbanBoard.tsx
    src/modules/tasks/components/TaskStatusBadge.tsx
    src/modules/tasks/components/TaskCard.tsx
    src/modules/tasks/components/TaskFilters.tsx
    src/modules/tasks/components/DocumentarButton.tsx
    src/modules/tasks/extensions/RecentTasksWidget.tsx
    src/modules/tasks/manifest.ts
  </read_first>
  <action>
**Step 1: Move service files:**
```
git mv src/lib/tasks-service.ts src/modules/tasks/services/tasks-service.ts
git mv src/lib/tasks-service.test.ts src/modules/tasks/services/tasks-service.test.ts
```

**Step 2: Update the moved service file's internal import:**
In `src/modules/tasks/services/tasks-service.ts`:
- Change `from '@/lib/supabase'` to `from '@platform/supabase'` (if not already done by Plan 01)

**Step 3: Update all tasks module files that import from @/lib/tasks-service:**

In `src/modules/tasks/types/index.ts`:
- Change `from '@/lib/tasks-service'` to `from '../services/tasks-service'` (two occurrences — the type export and the type import)

In `src/modules/tasks/hooks/useTasks.ts`:
- Change `from '@/lib/tasks-service'` to `from '../services/tasks-service'` (two lines — listTasks import and types import)

In `src/modules/tasks/pages/TaskForm.tsx`:
- Change `from '@/lib/tasks-service'` to `from '../services/tasks-service'` (two lines — function imports and type imports)
- Update any `from '@/components/ui/...'` to `from '@shared/ui/...'` (if not already done by Plan 02)

In `src/modules/tasks/pages/TaskList.tsx`:
- Change `from '@/lib/tasks-service'` to `from '../services/tasks-service'`
- Update any `from '@/components/ui/...'` to `from '@shared/ui/...'`

In `src/modules/tasks/pages/KanbanBoard.tsx`:
- Change `from '@/lib/tasks-service'` to `from '../services/tasks-service'`
- Update any `from '@/components/ui/...'` to `from '@shared/ui/...'`

**Step 4: Update remaining @/ imports in tasks module to @shared/ or @platform/:**

In `src/modules/tasks/components/TaskStatusBadge.tsx`:
- Change `from '@/lib/utils'` to `from '@shared/utils'` (if not already done by Plan 02)

In `src/modules/tasks/components/TaskCard.tsx`:
- Change `from '@/lib/utils'` to `from '@shared/utils'`
- Change `from '@/components/ui/...'` to `from '@shared/ui/...'`

In `src/modules/tasks/components/TaskFilters.tsx`:
- Change `from '@/components/ui/...'` to `from '@shared/ui/...'`

In `src/modules/tasks/components/DocumentarButton.tsx`:
- Change `from '@/components/ui/...'` to `from '@shared/ui/...'`

In `src/modules/tasks/extensions/RecentTasksWidget.tsx`:
- Change `from '@/lib/supabase'` to `from '@platform/supabase'` (if not already done by Plan 01)
- Change `from '@/modules/registry'` to `from '@platform/module-loader/registry'` (if not already done by Plan 01)

In `src/modules/tasks/manifest.ts`:
- Change `from '@/modules/registry'` to `from '@platform/module-loader/registry'` (if not already done by Plan 01)
- Change `from '@/modules/module-ids'` to `from '@platform/module-loader/module-ids'` (if not already done by Plan 01)

**IMPORTANT:** Before updating each file, read its current content. Plans 01 and 02 may have already updated some of these imports. Only update imports that still point to old paths. Use grep to check.

**Step 5: Remove .gitkeep from src/modules/tasks/services/ if it exists.**
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/modules/tasks/services/tasks-service.ts` succeeds
    - `ls src/modules/tasks/services/tasks-service.test.ts` succeeds
    - `test ! -f src/lib/tasks-service.ts` succeeds (file moved)
    - `test ! -f src/lib/tasks-service.test.ts` succeeds (file moved)
    - `grep -r "from '@/lib/tasks-service'" src/` returns zero matches
    - `grep "from '@/lib/" src/modules/tasks/` returns zero matches (no old lib imports in tasks module)
    - `grep "from '@/components/ui/" src/modules/tasks/` returns zero matches (all updated to @shared/ui/)
    - `grep "from '@/modules/" src/modules/tasks/` returns zero matches (all updated to @platform/)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Tasks module is fully self-contained: services/tasks-service.ts and its test live in module, all internal imports use relative paths or @platform/@shared aliases. No @/lib/ or @/components/ui/ imports remain in the tasks module.</done>
</task>

</tasks>

<verification>
```bash
# Verify service moved
ls src/modules/tasks/services/tasks-service.ts src/modules/tasks/services/tasks-service.test.ts

# Verify no stale imports in tasks module
grep -r "from '@/lib/" src/modules/tasks/ && echo "FAIL" || echo "OK"
grep -r "from '@/components/" src/modules/tasks/ && echo "FAIL" || echo "OK"
grep -r "from '@/modules/" src/modules/tasks/ && echo "FAIL" || echo "OK"

# TypeScript check
npx tsc --noEmit
```
</verification>

<success_criteria>
- tasks-service.ts and tasks-service.test.ts in src/modules/tasks/services/
- All imports within tasks module use module-relative or @platform/@shared paths
- No stale @/lib/ or @/components/ui/ imports remain in tasks module
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/61-module-migration/61-04-SUMMARY.md`
</output>
