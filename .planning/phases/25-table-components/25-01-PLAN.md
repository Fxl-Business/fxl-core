---
phase: 25-table-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/components/DataTable.tsx
  - tools/wireframe-builder/components/ClickableTable.tsx
  - tools/wireframe-builder/components/DrillDownTable.tsx
  - tools/wireframe-builder/components/ConfigTable.tsx
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "All four table headers show text-[10px] font-black uppercase tracking-widest styling"
    - "Interactive table rows highlight on hover with slate background transition"
    - "Total and highlight rows display primary-colored text with font-extrabold uppercase"
  artifacts:
    - path: "tools/wireframe-builder/components/DataTable.tsx"
      provides: "Header typography upgrade + row hover"
      contains: "font-black uppercase tracking-widest"
    - path: "tools/wireframe-builder/components/ClickableTable.tsx"
      provides: "Header + hover + total/highlight row restyle"
      contains: "text-wf-accent font-extrabold uppercase"
    - path: "tools/wireframe-builder/components/DrillDownTable.tsx"
      provides: "Header + hover + isTotal row restyle"
      contains: "text-wf-accent font-extrabold uppercase"
    - path: "tools/wireframe-builder/components/ConfigTable.tsx"
      provides: "Header typography + background upgrade"
      contains: "bg-wf-table-header"
  key_links:
    - from: "DataTable.tsx"
      to: "wireframe-tokens.css"
      via: "Tailwind wf-table-header token class"
      pattern: "bg-wf-table-header"
    - from: "ClickableTable.tsx"
      to: "wireframe-tokens.css"
      via: "wf-accent token for total/highlight rows"
      pattern: "text-wf-accent"
---

<objective>
Restyle table headers, row hover states, and total/highlight rows across all four wireframe table components.

Purpose: Establish the professional financial dashboard table aesthetic — tight bold headers, consistent hover feedback, and primary-colored total rows.
Output: Four updated table components with new header typography, hover states, and row variant styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-table-components/25-RESEARCH.md

@tools/wireframe-builder/components/DataTable.tsx
@tools/wireframe-builder/components/ClickableTable.tsx
@tools/wireframe-builder/components/DrillDownTable.tsx
@tools/wireframe-builder/components/ConfigTable.tsx

<interfaces>
<!-- Token classes available from Phase 22 (wireframe-tokens.css + tailwind.config.ts) -->
bg-wf-table-header     <!-- resolves to --wf-neutral-100 light / --wf-neutral-800 dark -->
text-wf-table-header-fg <!-- resolves to --wf-neutral-500 light / --wf-neutral-400 dark -->
text-wf-accent          <!-- resolves to --wf-accent = #1152d4 light / #4d7ce8 dark -->
bg-wf-accent-muted      <!-- resolves to color-mix(--wf-accent, 12% light / 15% dark) -->
hover:bg-wf-table-header <!-- token-aware hover — avoids dark: variant issue in wireframe context -->

<!-- Existing row types from ClickableTable.tsx -->
ClickRow.variant: 'default' | 'total' | 'highlight'

<!-- Existing row types from DrillDownTable.tsx -->
DrilRow.isTotal: boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header typography and row hover across all 4 tables</name>
  <files>
    tools/wireframe-builder/components/DataTable.tsx,
    tools/wireframe-builder/components/ClickableTable.tsx,
    tools/wireframe-builder/components/DrillDownTable.tsx,
    tools/wireframe-builder/components/ConfigTable.tsx
  </files>
  <action>
Update header `<th>` classes in all four table components and add/update row hover states.

**Header typography (TBL-01) — all four tables:**
Change `text-[11px] font-medium uppercase tracking-wide` to `text-[10px] font-black uppercase tracking-widest` in every `<th>` className.

For ConfigTable specifically:
- Change `<tr className="bg-wf-canvas">` to `<tr className="bg-wf-table-header">`
- Change `<th>` from `"px-4 py-2.5 text-left font-medium text-wf-muted whitespace-nowrap"` to `"px-4 py-2.5 text-left text-[10px] font-black uppercase tracking-widest text-wf-table-header-fg whitespace-nowrap"` — preserve the `style={col.width ? ...}` prop.

**Row hover states (TBL-02):**

DataTable: Add `hover:bg-wf-table-header transition-colors` to each `<tr>` in tbody. No cursor-pointer (DataTable is static/preview).
- Before: `className="border-t border-wf-table-border"`
- After: `className="border-t border-wf-table-border hover:bg-wf-table-header transition-colors"`

ClickableTable: Replace `onRowClick && 'cursor-pointer hover:bg-wf-accent-muted'` with `'cursor-pointer hover:bg-wf-table-header'` — ClickableTable is always interactive, cursor-pointer should always be present regardless of onRowClick.

DrillDownTable Row: Replace `hasKids && 'cursor-pointer hover:bg-wf-canvas'` with `hasKids && 'cursor-pointer hover:bg-wf-table-header'`.

ConfigTable: Keep existing `hover:bg-wf-canvas/50` as-is — ConfigTable is a settings table, not an analytical table. TBL-02 specifies cursor-pointer for interactive tables only.

IMPORTANT: Use `hover:bg-wf-table-header` (token-aware) instead of `hover:bg-slate-100 dark:hover:bg-slate-800` to avoid the Tailwind `dark:` variant issue inside the wireframe `data-wf-theme` context. The token resolves to the exact same colors (slate-100 light / slate-800 dark).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All four tables render with text-[10px] font-black uppercase tracking-widest headers. DataTable, ClickableTable, and DrillDownTable have hover:bg-wf-table-header on rows. ConfigTable header uses bg-wf-table-header background.</done>
</task>

<task type="auto">
  <name>Task 2: Total and highlight row variants</name>
  <files>
    tools/wireframe-builder/components/ClickableTable.tsx,
    tools/wireframe-builder/components/DrillDownTable.tsx
  </files>
  <action>
Update total/highlight row variant styling to use primary-colored text with font-extrabold uppercase (TBL-03).

**ClickableTable `<tr>` className changes:**
- Remove `row.variant === 'total' && 'bg-wf-canvas font-semibold'` — replace with `row.variant === 'total' && 'bg-wf-canvas'` (keep background, remove font-semibold from tr since it moves to td)
- Replace `row.variant === 'highlight' && 'bg-red-50/60'` with `row.variant === 'highlight' && 'bg-wf-accent-muted'`

**ClickableTable `<td>` className changes:**
- Replace `row.variant === 'total' && 'font-semibold text-wf-heading'` with `(row.variant === 'total' || row.variant === 'highlight') && 'text-wf-accent font-extrabold uppercase'`

**DrillDownTable Row `<tr>` className:**
No change needed — `row.isTotal && 'bg-wf-canvas'` is already correct (no font-semibold on tr).

**DrillDownTable Row `<td>` className:**
- Replace `row.isTotal ? 'font-semibold text-wf-heading' : 'text-wf-body'` with `row.isTotal ? 'text-wf-accent font-extrabold uppercase' : 'text-wf-body'`

NOTE: `uppercase` on numeric cells is harmless (numbers have no case). Do not add conditional logic to skip uppercase on right-aligned columns.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Total rows in ClickableTable and DrillDownTable show primary blue text (text-wf-accent) with font-extrabold uppercase. Highlight rows in ClickableTable use bg-wf-accent-muted background with same primary text treatment.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit — zero TypeScript errors
Visual: Tables in wireframe viewer show bold tight headers, hover states on interactive rows, primary-colored total/highlight rows
</verification>

<success_criteria>
- All four table headers use text-[10px] font-black uppercase tracking-widest
- ConfigTable header background changed from bg-wf-canvas to bg-wf-table-header
- Interactive rows (ClickableTable, DrillDownTable) have hover:bg-wf-table-header with cursor-pointer
- DataTable rows have subtle hover:bg-wf-table-header without cursor-pointer
- Total/highlight rows display text-wf-accent font-extrabold uppercase
- npx tsc --noEmit passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-table-components/25-01-SUMMARY.md`
</output>
