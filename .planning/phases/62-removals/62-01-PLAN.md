---
phase: 62-removals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/knowledge-base/ (DELETE entire directory)
  - src/lib/kb-service.ts (DELETE)
  - src/lib/kb-service.test.ts (DELETE)
  - src/platform/module-loader/module-ids.ts
  - src/platform/module-loader/registry.ts
  - src/platform/router/AppRouter.tsx
  - src/platform/layout/SearchCommand.tsx
  - src/platform/services/module-stats.ts
  - src/platform/services/activity-feed.ts
  - src/platform/pages/Home.test.tsx
  - src/modules/tasks/components/DocumentarButton.tsx
  - src/modules/tasks/manifest.ts
  - src/modules/clients/pages/FinanceiroContaAzul/Index.tsx
  - src/pages/docs/ProcessDocsViewer.tsx (DELETE)
  - src/pages/docs/ (DELETE empty dir)
  - src/pages/tools/ (DELETE empty dir)
  - src/modules/docs/components/PageHeader.tsx (DELETE)
  - src/modules/docs/components/PromptBlock.tsx (DELETE)
  - src/modules/docs/pages/DocRenderer.tsx
  - src/modules/docs/CLAUDE.md
autonomous: true
requirements: [REM-01, REM-02, REM-03]

must_haves:
  truths:
    - "Knowledge Base module does not exist anywhere in the codebase"
    - "No import references knowledge-base, kb-service, or KNOWLEDGE_BASE"
    - "ProcessDocsViewer.tsx does not exist"
    - "No duplicate PageHeader.tsx or PromptBlock.tsx copies remain"
    - "tsc --noEmit passes with zero errors"
    - "All remaining routes and features work (zero functional regression)"
  artifacts:
    - path: "src/platform/module-loader/module-ids.ts"
      provides: "Module IDs without KNOWLEDGE_BASE"
      contains: "DOCS.*FERRAMENTAS.*CLIENTS.*TASKS"
    - path: "src/platform/module-loader/registry.ts"
      provides: "MODULE_REGISTRY without knowledgeBaseManifest"
    - path: "src/platform/router/AppRouter.tsx"
      provides: "Routes without knowledge-base paths"
    - path: "src/platform/layout/SearchCommand.tsx"
      provides: "Search without KB entries section"
  key_links:
    - from: "src/platform/module-loader/registry.ts"
      to: "src/platform/module-loader/module-ids.ts"
      via: "MODULE_IDS import"
      pattern: "MODULE_IDS\\."
    - from: "src/platform/router/AppRouter.tsx"
      to: "src/platform/module-loader/registry.ts"
      via: "MODULE_REGISTRY import"
      pattern: "MODULE_REGISTRY"
---

<objective>
Remove all dead and redundant code from the codebase: the entire Knowledge Base module (directory, service files, IDs, routes, and all references), ProcessDocsViewer.tsx (dead code), and duplicate component copies (PageHeader.tsx, PromptBlock.tsx). This reduces codebase size and eliminates dead references without any functional change.

Purpose: Clean up after Phase 61 module migration by removing code the design spec explicitly marks for deletion (Section 4.5). This is the final cleanup before Phase 63 integration verification.

Output: A codebase with zero references to Knowledge Base, no dead files, no duplicate components, and `tsc --noEmit` still passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key interfaces the executor needs:

From src/platform/module-loader/module-ids.ts:
```typescript
export const MODULE_IDS = {
  DOCS: 'docs',
  FERRAMENTAS: 'ferramentas',
  CLIENTS: 'clients',
  KNOWLEDGE_BASE: 'knowledge-base',  // <-- REMOVE this line
  TASKS: 'tasks',
} as const
export type ModuleId = typeof MODULE_IDS[keyof typeof MODULE_IDS]
```

From src/platform/module-loader/registry.ts:
```typescript
import { knowledgeBaseManifest } from '@modules/knowledge-base/manifest'  // <-- REMOVE
export const MODULE_REGISTRY: ModuleDefinition[] = [
  docsManifest,
  ferramentasManifest,
  clientsManifest,
  knowledgeBaseManifest,  // <-- REMOVE
  tasksManifest,
]
```

From src/platform/services/activity-feed.ts:
```typescript
export type ActivityItem = {
  type: 'kb_entry' | 'task'  // <-- 'kb_entry' must remain as a type for backward compat with Supabase data
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Knowledge Base module, service files, and all references</name>
  <files>
    src/modules/knowledge-base/ (DELETE entire directory)
    src/lib/kb-service.ts (DELETE)
    src/lib/kb-service.test.ts (DELETE)
    src/platform/module-loader/module-ids.ts
    src/platform/module-loader/registry.ts
    src/platform/router/AppRouter.tsx
    src/platform/layout/SearchCommand.tsx
    src/platform/services/module-stats.ts
    src/platform/services/activity-feed.ts
    src/platform/pages/Home.test.tsx
    src/modules/tasks/components/DocumentarButton.tsx
    src/modules/tasks/manifest.ts
    src/modules/clients/pages/FinanceiroContaAzul/Index.tsx
  </files>
  <read_first>
    src/platform/module-loader/module-ids.ts
    src/platform/module-loader/registry.ts
    src/platform/router/AppRouter.tsx
    src/platform/layout/SearchCommand.tsx
    src/platform/services/module-stats.ts
    src/platform/services/activity-feed.ts
    src/platform/pages/Home.test.tsx
    src/modules/tasks/components/DocumentarButton.tsx
    src/modules/tasks/manifest.ts
    src/modules/clients/pages/FinanceiroContaAzul/Index.tsx
    src/lib/kb-service.ts
  </read_first>
  <action>
    **Step 1 — Delete entire Knowledge Base module directory and service files:**
    ```bash
    rm -rf src/modules/knowledge-base/
    rm -f src/lib/kb-service.ts src/lib/kb-service.test.ts
    ```
    After deletion, if `src/lib/` is now empty, remove it too:
    ```bash
    rmdir src/lib/ 2>/dev/null || true
    ```

    **Step 2 — Edit src/platform/module-loader/module-ids.ts:**
    Remove line `KNOWLEDGE_BASE: 'knowledge-base',` from the MODULE_IDS object (line 10).
    The resulting MODULE_IDS should be:
    ```typescript
    export const MODULE_IDS = {
      DOCS: 'docs',
      FERRAMENTAS: 'ferramentas',
      CLIENTS: 'clients',
      TASKS: 'tasks',
    } as const
    ```

    **Step 3 — Edit src/platform/module-loader/registry.ts:**
    - Remove line 7: `import { knowledgeBaseManifest } from '@modules/knowledge-base/manifest'`
    - Remove `knowledgeBaseManifest,` from the MODULE_REGISTRY array (line 70).
    The resulting array should be:
    ```typescript
    export const MODULE_REGISTRY: ModuleDefinition[] = [
      docsManifest,
      ferramentasManifest,
      clientsManifest,
      tasksManifest,
    ]
    ```

    **Step 4 — Edit src/platform/router/AppRouter.tsx:**
    - Remove lines 18-22 (all 4 KB lazy imports: KBListPage, KBDetailPage, KBFormPage, KBSearchPage and the comment).
    - Remove lines 46-51 (all 6 KB route definitions and the comment).
    - Keep all other routes intact. Remove the `Suspense` import ONLY if no other routes use it (they do — SharedWireframeView, ModulesPanel, KanbanBoard, TaskForm all use Suspense, so keep it).

    **Step 5 — Edit src/platform/layout/SearchCommand.tsx:**
    - Remove line 13: `import { listKnowledgeEntries, type KnowledgeEntry } from '@/lib/kb-service'`
    - Remove line 3 import of `BookOpen` from lucide-react (only used by KB section).
    - Remove state: `const [kbEntries, setKbEntries] = useState<KnowledgeEntry[]>([])` (line 19).
    - Remove the KB loading logic inside handleOpenChange: lines 35-37 (`if (kbEntries.length === 0) { listKnowledgeEntries({}).then(setKbEntries).catch(() => {}) }`).
    - Remove the entire KB CommandGroup block: lines 110-126 (the `{query.length > 0 && kbEntries.length > 0 && (` block).
    - Update placeholder text from "Pesquisar documentacao e conhecimento..." to "Pesquisar documentacao..." (line 86).

    **Step 6 — Edit src/platform/services/module-stats.ts:**
    - Remove the entire `kbResult` from Promise.all — remove the supabase query for `knowledge_entries` table (lines 25-26).
    - Remove the `if (kbResult.count !== null)` block (lines 37-39).
    - Update the destructuring from `const [taskResult, kbResult]` to `const [taskResult]` (or just use `const taskResult = await supabase...` without Promise.all since only one query remains).

    **Step 7 — Edit src/platform/services/activity-feed.ts:**
    - Remove the KB query from Promise.all (lines 40-45, the `knowledge_entries` select).
    - Remove the kbItems mapping (lines 55-63).
    - Update the destructuring: `const [kbResult, taskResult]` becomes `const [taskResult]` or just direct assignment.
    - Update the merge call: `mergeAndSortActivityItems(kbItems, taskItems)` becomes just sorting taskItems directly.
    - Keep the `ActivityItem` type BUT remove `'kb_entry'` from the type union: change `type: 'kb_entry' | 'task'` to `type: 'task'`. This is safe since we're deleting all KB data sources.
    - Update `mergeAndSortActivityItems` to only accept taskItems (or simplify to inline sort since there's only one source now).

    **Step 8 — Edit src/platform/pages/Home.test.tsx:**
    - Update all test fixtures: change `href: '/knowledge-base/kb-1'` etc to use task hrefs or remove kb items entirely.
    - The test tests `mergeAndSortActivityItems`. Since we're removing kb_entry type, update test to only use task items, or simplify accordingly.
    - If tests become trivial (only one list, no merge needed), simplify test assertions to match the new simpler logic.

    **Step 9 — Edit src/modules/tasks/components/DocumentarButton.tsx:**
    The DocumentarButton navigates to `/knowledge-base/new` which no longer exists. This entire component is dead code since KB is removed. Remove the ENTIRE file:
    ```bash
    rm -f src/modules/tasks/components/DocumentarButton.tsx
    ```
    Then grep for any imports of DocumentarButton in the tasks module and remove them. Check `src/modules/tasks/` for any file importing `DocumentarButton`.

    **Step 10 — Edit src/modules/tasks/manifest.ts:**
    Check if it references KNOWLEDGE_BASE anywhere. It does not directly (only MODULE_IDS.TASKS), so no change needed unless DocumentarButton was exported from manifest.

    **Step 11 — Edit src/modules/clients/pages/FinanceiroContaAzul/Index.tsx:**
    - Remove line 5: `import { listKnowledgeEntries, type KnowledgeEntry } from '@/lib/kb-service'`
    - Remove the KB state and effect: `const [kbEntries, setKbEntries] = useState<KnowledgeEntry[]>([])`, `const [kbLoading, setKbLoading] = useState(true)`, and the `useEffect` that calls `listKnowledgeEntries` (lines 52-62).
    - Remove the entire "Conhecimento" section from JSX (lines 127-164, the `<div className="mt-8">` with heading "Conhecimento" and the kbEntries grid).
    - Remove the `ENTRY_TYPE_COLORS` constant (lines 30-35) since it's only used by the KB entries display.
    - Keep all other sections (Documentos table, Prompt Master) intact.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && ! test -d src/modules/knowledge-base && ! test -f src/lib/kb-service.ts && ! test -f src/lib/kb-service.test.ts && ! grep -r "knowledge.base\|kb-service\|KNOWLEDGE_BASE\|knowledgeBase\|KBListPage\|KBDetailPage\|KBFormPage\|KBSearchPage\|DocumentarButton" src/ --include="*.ts" --include="*.tsx" -l && echo "KB REMOVAL VERIFIED" || echo "KB REFERENCES STILL EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/knowledge-base/` directory does not exist
    - `src/lib/kb-service.ts` and `src/lib/kb-service.test.ts` do not exist
    - `grep -r "knowledge.base\|kb-service\|KNOWLEDGE_BASE" src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `grep -r "DocumentarButton" src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - MODULE_IDS in module-ids.ts has exactly 4 entries: DOCS, FERRAMENTAS, CLIENTS, TASKS
    - MODULE_REGISTRY in registry.ts has exactly 4 entries: docsManifest, ferramentasManifest, clientsManifest, tasksManifest
    - AppRouter.tsx has zero /knowledge-base routes
    - SearchCommand.tsx has no kb-service import and no KB CommandGroup
    - module-stats.ts queries only tasks table (no knowledge_entries)
    - activity-feed.ts queries only tasks table (no knowledge_entries)
    - FinanceiroContaAzul/Index.tsx has no KB imports or "Conhecimento" section
  </acceptance_criteria>
  <done>All Knowledge Base code, service files, IDs, routes, search integration, stats, activity feed references, DocumentarButton, and client page KB sections are completely removed. Zero grep matches for KB-related terms across all .ts/.tsx files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ProcessDocsViewer, duplicate components, empty directories, and verify tsc</name>
  <files>
    src/pages/docs/ProcessDocsViewer.tsx (DELETE)
    src/pages/docs/ (DELETE empty dir)
    src/pages/tools/ (DELETE empty dir)
    src/modules/docs/components/PageHeader.tsx (DELETE)
    src/modules/docs/components/PromptBlock.tsx (DELETE)
    src/modules/docs/pages/DocRenderer.tsx
    src/modules/docs/CLAUDE.md
  </files>
  <read_first>
    src/pages/docs/ProcessDocsViewer.tsx
    src/modules/docs/components/PageHeader.tsx
    src/modules/docs/components/PromptBlock.tsx
    src/shared/ui/PromptBlock.tsx
    src/modules/docs/components/DocPageHeader.tsx
    src/modules/docs/pages/DocRenderer.tsx
    src/modules/docs/CLAUDE.md
  </read_first>
  <action>
    **Step 1 — Delete ProcessDocsViewer and empty old directories:**
    ```bash
    rm -f src/pages/docs/ProcessDocsViewer.tsx
    rmdir src/pages/docs/ 2>/dev/null || true
    rmdir src/pages/tools/ 2>/dev/null || true
    rmdir src/pages/ 2>/dev/null || true
    ```
    Note: `rmdir` only removes empty directories, so it's safe.

    **Step 2 — Delete duplicate PageHeader.tsx:**
    ```bash
    rm -f src/modules/docs/components/PageHeader.tsx
    ```
    This file is NOT imported anywhere (DocRenderer uses DocPageHeader). It is dead code.
    Verify: `grep -r "PageHeader" src/ --include="*.tsx" --include="*.ts"` should only show DocPageHeader references.

    **Step 3 — Remove duplicate PromptBlock.tsx from docs module, keep shared/ui version:**
    The canonical PromptBlock is at `src/shared/ui/PromptBlock.tsx` (more complete version with `cn()` and `className` prop). The copy at `src/modules/docs/components/PromptBlock.tsx` uses `Button` component but is otherwise similar.

    Delete the docs module copy:
    ```bash
    rm -f src/modules/docs/components/PromptBlock.tsx
    ```

    Update `src/modules/docs/pages/DocRenderer.tsx`:
    - Change line 5 from: `import PromptBlock from '../components/PromptBlock'`
    - To: `import PromptBlock from '@shared/ui/PromptBlock'`

    This makes PromptBlock a shared UI component (which it is — it's used by both docs and clients modules).

    **Step 4 — Update src/modules/docs/CLAUDE.md:**
    Update the components list to remove PageHeader and PromptBlock entries:
    - Remove `PageHeader` and `PromptBlock` from the components list line
    - The line should read: `- components/ — MarkdownRenderer, Callout, Operational, PhaseCard, DocBreadcrumb, DocPageHeader, DocTableOfContents, InfoBlock`

    **Step 5 — Remove .gitkeep files from directories that now have real content:**
    The following .gitkeep files are in directories that contain real files and are no longer needed:
    ```bash
    rm -f src/modules/docs/types/.gitkeep
    rm -f src/modules/tasks/pages/.gitkeep
    rm -f src/modules/tasks/components/.gitkeep
    rm -f src/modules/tasks/hooks/.gitkeep
    rm -f src/modules/tasks/types/.gitkeep
    rm -f src/platform/router/.gitkeep
    ```
    Keep .gitkeep in directories that are still empty placeholders:
    - src/modules/clients/components/.gitkeep (empty dir)
    - src/modules/clients/hooks/.gitkeep (empty dir)
    - src/modules/clients/types/.gitkeep (empty dir)
    - src/modules/clients/services/.gitkeep (empty dir)
    - src/modules/wireframe/components/.gitkeep (empty dir)
    - src/modules/wireframe/hooks/.gitkeep (empty dir)
    - src/modules/wireframe/types/.gitkeep (empty dir)
    - src/modules/wireframe/services/.gitkeep (empty dir)
    - src/shared/hooks/.gitkeep (empty dir)
    - src/shared/types/.gitkeep (empty dir)
    - src/platform/tenants/.gitkeep (empty dir)

    **Step 6 — Run tsc --noEmit to verify zero TypeScript errors:**
    ```bash
    npx tsc --noEmit
    ```
    Fix any errors that arise from the removals. Common issues:
    - Missing imports (all should be caught by the explicit changes above)
    - Type errors from removing kb_entry from ActivityItem type

    **Step 7 — Delete src/lib/ directory if now empty:**
    After removing kb-service files in Task 1, check if src/lib/ is empty:
    ```bash
    rmdir src/lib/ 2>/dev/null || true
    ```
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx tsc --noEmit && ! test -f src/pages/docs/ProcessDocsViewer.tsx && ! test -f src/modules/docs/components/PageHeader.tsx && ! test -f src/modules/docs/components/PromptBlock.tsx && ! test -d src/pages/docs && ! test -d src/pages/tools && echo "ALL REMOVALS AND TSC VERIFIED" || echo "ISSUES FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - `src/pages/docs/ProcessDocsViewer.tsx` does not exist
    - `src/pages/docs/` directory does not exist
    - `src/pages/tools/` directory does not exist
    - `src/modules/docs/components/PageHeader.tsx` does not exist
    - `src/modules/docs/components/PromptBlock.tsx` does not exist
    - `src/modules/docs/pages/DocRenderer.tsx` imports PromptBlock from `@shared/ui/PromptBlock`
    - `grep -r "from.*\.\./components/PromptBlock\|from.*\.\./components/PageHeader" src/ --include="*.tsx"` returns zero matches
    - `src/modules/docs/CLAUDE.md` no longer lists PageHeader or PromptBlock in components
  </acceptance_criteria>
  <done>ProcessDocsViewer deleted, empty old directories removed, duplicate PageHeader.tsx deleted, duplicate PromptBlock.tsx consolidated to shared/ui version, DocRenderer updated to use shared PromptBlock, unnecessary .gitkeep files cleaned up, and tsc --noEmit passes with zero errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` — zero errors
2. `grep -r "knowledge.base\|kb-service\|KNOWLEDGE_BASE\|knowledgeBase" src/ --include="*.ts" --include="*.tsx"` — zero matches
3. `grep -r "ProcessDocsViewer" src/ --include="*.ts" --include="*.tsx"` — zero matches
4. `test -d src/modules/knowledge-base && echo FAIL || echo PASS` — PASS
5. `test -f src/pages/docs/ProcessDocsViewer.tsx && echo FAIL || echo PASS` — PASS
6. `test -f src/modules/docs/components/PageHeader.tsx && echo FAIL || echo PASS` — PASS
7. `test -f src/modules/docs/components/PromptBlock.tsx && echo FAIL || echo PASS` — PASS
</verification>

<success_criteria>
1. Knowledge Base module completely removed (directory, services, IDs, routes, all references)
2. ProcessDocsViewer.tsx deleted
3. No duplicate PageHeader.tsx or PromptBlock.tsx copies (only canonical versions remain)
4. Empty old directories (src/pages/docs/, src/pages/tools/) cleaned up
5. `tsc --noEmit` passes with zero errors
6. Zero grep matches for KB-related terms, ProcessDocsViewer, or duplicate component imports
</success_criteria>

<output>
After completion, create `.planning/phases/62-removals/62-01-SUMMARY.md`
</output>
