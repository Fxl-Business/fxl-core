---
phase: 122-document-scoping-rls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/020_documents_rls_fix.sql
  - src/modules/docs/services/docs-service.ts
  - src/modules/docs/hooks/useDocsNav.ts
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "Membro da org FXL ve tenant docs da FXL e nao ve tenant docs de outra org"
    - "Super admin ve product docs de qualquer org em que estiver autenticado"
    - "Sidebar de docs popula corretamente apos login e apos troca de org"
    - "Apos trocar de org, nenhum doc da org anterior aparece — cache e invalidado na troca"
  artifacts:
    - path: "supabase/migrations/020_documents_rls_fix.sql"
      provides: "Politica RLS corrigida: product docs para super_admin apenas, tenant docs por org"
      contains: "documents_select"
    - path: "src/modules/docs/services/docs-service.ts"
      provides: "Cache invalidation ao trocar de org"
      exports: ["invalidateDocsCache"]
    - path: "src/modules/docs/hooks/useDocsNav.ts"
      provides: "Re-fetch de docs quando org muda (deps array atualizado)"
      min_lines: 80
  key_links:
    - from: "src/platform/auth/ProtectedRoute.tsx"
      to: "src/modules/docs/services/docs-service.ts"
      via: "invalidateDocsCache callback em useOrgTokenExchange"
      pattern: "invalidateDocsCache"
    - from: "src/modules/docs/hooks/useDocsNav.ts"
      to: "src/modules/docs/services/docs-service.ts"
      via: "getAllDocuments() chamado no useEffect com deps corretos"
      pattern: "getAllDocuments"
---

<objective>
Corrigir dois bugs independentes que quebram o scoping de documentos:

1. **RLS bug no banco**: A policy `documents_select` permite que `scope='product'` seja lido por
   qualquer usuario autenticado — deveria ser apenas super_admin. Adicionar migration 020 que
   recria a policy corretamente.

2. **Client-side bug no cache**: `useDocsNav` tem `useEffect` com deps array vazio `[]`. Quando
   o usuario troca de org, `invalidateDocsCache()` limpa o cache mas o effect nao re-executa —
   o Supabase nunca e consultado novamente com o novo JWT. Resultado: sidebar fica vazia ou mostra
   docs stale da org anterior.

Purpose: Garantir que cada org ve exatamente os documentos a que tem direito.
Output: Migration SQL + dois arquivos TypeScript corrigidos.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PITFALLS.md

<!-- RLS atual (migration 011) — executor precisa entender o bug antes de corrigir -->
@supabase/migrations/011_documents_scope.sql
@supabase/migrations/008_multi_tenant_schema.sql

<!-- Pipeline de docs -->
@src/modules/docs/services/docs-service.ts
@src/modules/docs/hooks/useDocsNav.ts
@src/modules/docs/hooks/useDocsNavItems.ts
@src/platform/auth/ProtectedRoute.tsx
@src/platform/tenants/useOrgTokenExchange.ts

<interfaces>
<!-- Interfaces chave que o executor precisa. Nao explorar o codebase — usar estas. -->

De src/modules/docs/services/docs-service.ts:
```typescript
// Cache em modulo — duas variaveis que controlam o estado
let docsCache: DocumentRow[] | null = null
let docsCachePromise: Promise<DocumentRow[]> | null = null

export function invalidateDocsCache(): void
export async function getAllDocuments(): Promise<DocumentRow[]>
export async function getDocBySlug(slug: string): Promise<DocumentRow | null>
```

De src/modules/docs/hooks/useDocsNav.ts:
```typescript
// BUG: deps array vazio — effect nao re-executa apos org switch
useEffect(() => {
  // ... fetch docs ...
}, []) // <- este array precisa ser atualizado
```

De src/platform/auth/ProtectedRoute.tsx:
```typescript
// onOrgChange e chamado APOS setIsReady(true) em useOrgTokenExchange
// Ou seja: Supabase client ja tem o novo token quando invalidateDocsCache() roda
const { isReady: tokenReady, error: tokenError } = useOrgTokenExchange({ onOrgChange: invalidateDocsCache })
```

RLS policy atual (migration 011 — com bug):
```sql
-- BUG: scope = 'product' visivel para qualquer usuario autenticado
CREATE POLICY "documents_select" ON public.documents
  FOR SELECT TO anon, authenticated
  USING (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR scope = 'product'                          -- <- BUG: sem checar super_admin
    OR org_id = COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id',
      org_id                                      -- <- anon fallback: org_id = org_id = true
    )
  );
```

RLS correta (o que deve ficar apos migration 020):
```sql
-- product docs: apenas super_admin
-- tenant docs: apenas org correta (sem anon fallback perigoso)
CREATE POLICY "documents_select" ON public.documents
  FOR SELECT TO authenticated
  USING (
    (
      COALESCE(
        nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
        'false'
      ) = 'true'
    )
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  );
```

Nota sobre anon: A policy original em migration 008 usava anon como backward compat.
Migration 020 deve REMOVER o anon fallback para docs — o Nexo exige JWT para ver documentos.
O Supabase client no Nexo sempre passa o org JWT (definido em useOrgTokenExchange), nao anon key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 020 — corrigir documents_select RLS policy</name>
  <files>supabase/migrations/020_documents_rls_fix.sql</files>
  <action>
Criar migration SQL que substitui a policy `documents_select` por uma versao correta.

O bug atual: `OR scope = 'product'` na policy permite que qualquer usuario autenticado leia
product docs sem ser super_admin. Alem disso, o anon fallback (`org_id = org_id`) permite
que requests sem JWT leiam todos os documentos.

A migration deve:

1. Dropar a policy atual: `DROP POLICY IF EXISTS "documents_select" ON public.documents;`

2. Recriar com logica correta:
   - Super admin (super_admin = 'true' no JWT) ve TUDO (bypass total)
   - Tenant docs (scope = 'tenant'): visivel apenas se `org_id` no JWT bate com `org_id` da linha
   - Product docs (scope = 'product'): visivel APENAS para super_admin (coberto pela primeira clausula)
   - Remover o anon fallback — documentos exigem JWT valido com org_id
   - Alterar TO clause de `anon, authenticated` para `authenticated` apenas

3. Tambem corrigir policies de INSERT/UPDATE/DELETE para usar `authenticated` em vez de
   `anon, authenticated` (documentos nunca sao criados via anon key no Nexo atual).

Estrutura do arquivo:
```sql
-- Migration 020: Fix documents RLS — product docs super_admin only, remove anon fallback
--
-- Fixes two security issues in migration 011:
-- 1. scope='product' was readable by any authenticated user (should be super_admin only)
-- 2. anon fallback (org_id = org_id) allowed unauthenticated reads of all documents

-- Step 1: Drop all four document policies from migration 011
DROP POLICY IF EXISTS "documents_select" ON public.documents;
DROP POLICY IF EXISTS "documents_insert" ON public.documents;
DROP POLICY IF EXISTS "documents_update" ON public.documents;
DROP POLICY IF EXISTS "documents_delete" ON public.documents;

-- Step 2: Recreate SELECT with correct scoping
-- Super admin sees everything (bypass).
-- Tenant members see their org's tenant docs only.
-- Product docs: only super_admin (covered by the first clause).
-- No anon fallback — documents require an authenticated JWT with org_id.
CREATE POLICY "documents_select" ON public.documents
  FOR SELECT TO authenticated
  USING (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  );

-- Step 3: Recreate INSERT (super_admin or own org, authenticated only)
CREATE POLICY "documents_insert" ON public.documents
  FOR INSERT TO authenticated
  WITH CHECK (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  );

-- Step 4: Recreate UPDATE (super_admin or own org tenant docs, authenticated only)
CREATE POLICY "documents_update" ON public.documents
  FOR UPDATE TO authenticated
  USING (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  )
  WITH CHECK (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  );

-- Step 5: Recreate DELETE (super_admin or own org tenant docs, authenticated only)
CREATE POLICY "documents_delete" ON public.documents
  FOR DELETE TO authenticated
  USING (
    COALESCE(
      nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'super_admin',
      'false'
    ) = 'true'
    OR (
      scope = 'tenant'
      AND org_id = nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'org_id'
    )
  );
```

Apos criar o arquivo, executar a migration via MCP Supabase:
`mcp__supabase__execute_sql` com o conteudo do arquivo (ou via `supabase db push`).

Verificar que as 4 policies foram recriadas corretamente:
`SELECT policyname, cmd, roles FROM pg_policies WHERE tablename = 'documents';`
  </action>
  <verify>
    <automated>
      Execute via MCP ou CLI:
      ```sql
      SELECT policyname, cmd, roles::text
      FROM pg_policies
      WHERE tablename = 'documents'
      ORDER BY policyname;
      ```
      Deve retornar 4 linhas: documents_delete, documents_insert, documents_select, documents_update.
      Cada uma com `roles = {authenticated}` (sem `anon`).

      Verificar que `scope='product'` NAO aparece na policy documents_select sem checar super_admin:
      ```sql
      SELECT pg_get_expr(qual, 'documents'::regclass)
      FROM pg_policies
      WHERE tablename = 'documents' AND policyname = 'documents_select';
      ```
      O resultado NAO deve conter `scope = 'product'` como clausula OR independente.
    </automated>
  </verify>
  <done>
    Migration 020 existe em supabase/migrations/020_documents_rls_fix.sql.
    Aplicada no Supabase (policies recriadas com roles={authenticated}).
    documents_select nao mais permite scope='product' para usuarios sem super_admin=true.
    Anon fallback removido de todas as 4 policies.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir useDocsNav para re-fetch apos org switch</name>
  <files>
    src/modules/docs/hooks/useDocsNav.ts
    src/modules/docs/services/docs-service.ts
  </files>
  <behavior>
    - Quando org muda (invalidateDocsCache chamado + org switch), useDocsNav deve buscar docs novamente
    - Antes do re-fetch: tenantItems e productItems voltam a [] (nao mostram docs stale)
    - Apos o re-fetch: items populados com docs da nova org
    - getOrgAccessToken() disponivel no momento do fetch (Supabase client ja tem novo JWT)
  </behavior>
  <action>
**Problema raiz:** `useDocsNav` tem `useEffect(() => { ... }, [])` com deps array vazio.
Quando `invalidateDocsCache()` e chamado (via `onOrgChange` em `ProtectedRoute`), o cache e
limpo mas o effect nao re-executa. O Supabase nunca e consultado com o novo token.

**Solucao:** Introducao de um estado `cacheVersion` em `docs-service.ts` que incrementa
quando `invalidateDocsCache()` e chamado. `useDocsNav` observa esse valor como dependencia
no useEffect, disparando re-fetch automatico.

**Em `src/modules/docs/services/docs-service.ts`:**

Adicionar um contador de versao exportado que `useDocsNav` pode assinar:

```typescript
// Incrementa toda vez que invalidateDocsCache() e chamado.
// Permite que hooks React re-reajam ao invalidate via useDocsNavVersion().
let cacheVersion = 0

export function getDocsCacheVersion(): number {
  return cacheVersion
}

export function invalidateDocsCache(): void {
  docsCache = null
  docsCachePromise = null
  cacheVersion += 1
}
```

**Em `src/modules/docs/hooks/useDocsNav.ts`:**

Importar `getDocsCacheVersion` e usar como dep no useEffect:

```typescript
import { getAllDocuments, getDocsCacheVersion, type DocumentRow } from '../services/docs-service'

export function useDocsNav(): UseDocsNavResult {
  const [tenantItems, setTenantItems] = useState<NavItem[]>([])
  const [productItems, setProductItems] = useState<NavItem[]>([])
  const [cacheVersion, setCacheVersion] = useState(() => getDocsCacheVersion())

  // Listen for cache invalidation (org switch) by polling getDocsCacheVersion().
  // When invalidateDocsCache() is called, cacheVersion increments, re-triggering the fetch effect.
  useEffect(() => {
    const id = setInterval(() => {
      const v = getDocsCacheVersion()
      setCacheVersion((prev) => (prev !== v ? v : prev))
    }, 100)
    return () => clearInterval(id)
  }, [])

  useEffect(() => {
    let cancelled = false

    // Reset to empty while re-fetching (avoid showing stale docs from previous org)
    setTenantItems([])
    setProductItems([])

    getAllDocuments()
      .then((docs) => {
        if (cancelled) return
        // ... existing tree-building logic unchanged ...
      })
      .catch(() => { /* on error, items remain [] */ })

    return () => { cancelled = true }
  }, [cacheVersion]) // re-runs when cache is invalidated
```

NOTA IMPORTANTE: O polling de 100ms e um padrao intencionalmente simples. A alternativa seria
um EventEmitter ou React Context, mas ambos exigiriam mudancas em mais arquivos e criariam
acoplamento plataforma->modulo. O polling e limitado ao intervalo de vida do componente e para
assim que o cacheVersion estabiliza.

Nao alterar a logica de `buildNavTree`, apenas adicionar o cacheVersion como dep.
Manter todos os comentarios existentes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    docs-service.ts exporta getDocsCacheVersion() que retorna numero crescente.
    invalidateDocsCache() incrementa cacheVersion alem de limpar cache/promise.
    useDocsNav tem useEffect com [cacheVersion] como dep.
    useDocsNav reseta tenantItems/productItems para [] antes de cada re-fetch.
    Zero erros TypeScript (tsc --noEmit limpo).
  </done>
</task>

</tasks>

<verification>
Verificacao geral da fase:

1. RLS correta no banco:
   ```sql
   -- Simular query como usuario da org A (sem super_admin) — deve ver apenas tenant docs org A
   SET request.jwt.claims = '{"org_id":"org_A","super_admin":"false"}';
   SELECT scope, org_id FROM documents WHERE scope = 'product'; -- deve retornar 0 rows
   SELECT scope, org_id FROM documents WHERE org_id = 'org_A'; -- deve retornar rows

   -- Simular super admin — deve ver tudo
   SET request.jwt.claims = '{"org_id":"org_A","super_admin":"true"}';
   SELECT COUNT(*) FROM documents; -- deve retornar 91
   ```

2. Cache invalidation funciona:
   - Abrir Nexo autenticado como membro da FXL → sidebar mostra docs FXL
   - Trocar para outra org sem docs → sidebar esvazia (nao mostra docs FXL)
   - Nenhum dado stale da org anterior aparece
</verification>

<success_criteria>
- Migration 020 aplicada: `pg_policies` mostra documents_select com roles={authenticated} e sem `scope='product'` como clausula independente
- getDocsCacheVersion() exportado de docs-service.ts
- useDocsNav re-executa useEffect quando cacheVersion muda
- tenantItems reseta para [] antes de re-fetch (sem flicker de dados stale)
- `npx tsc --noEmit` retorna zero erros
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/122/122-01-SUMMARY.md`
</output>
