---
phase: 73-rename-nexo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/auth/Login.tsx
  - src/platform/layout/TopNav.tsx
  - src/platform/pages/Home.tsx
  - src/platform/router/AppRouter.tsx
  - index.html
  - CLAUDE.md
  - .planning/PROJECT.md
  - docs/processo/fases/fase2.md
  - docs/processo/spoke-onboarding.md
  - docs/ferramentas/branding-collection.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05
  - BRAND-06
  - META-01
  - META-02
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "App title bar and browser tab show 'Nexo' not 'Nucleo FXL'"
    - "TopNav brand label shows 'Nexo'"
    - "Login page heading shows 'Nexo'"
    - "AppRouter loading heading shows 'Nexo'"
    - "Home page identity card shows 'Nexo'"
    - "index.html title tag contains 'Nexo'"
    - "CLAUDE.md header references 'Nexo' not 'FXL Core'"
    - "Zero occurrences of 'Nucleo FXL' or 'FXL Core' in src/ and key docs"
  artifacts:
    - path: "src/platform/auth/Login.tsx"
      provides: "Login page branding"
      contains: "Nexo"
    - path: "src/platform/layout/TopNav.tsx"
      provides: "TopNav brand label"
      contains: "Nexo"
    - path: "src/platform/pages/Home.tsx"
      provides: "Home page identity"
      contains: "Nexo"
    - path: "src/platform/router/AppRouter.tsx"
      provides: "Loading/auth heading"
      contains: "Nexo"
    - path: "index.html"
      provides: "Browser tab title"
      contains: "<title>Nexo</title>"
    - path: "CLAUDE.md"
      provides: "Project instructions header"
      contains: "# CLAUDE.md — Nexo"
  key_links:
    - from: "index.html"
      to: "browser tab"
      via: "<title> tag"
      pattern: "<title>Nexo</title>"
    - from: "src/platform/layout/TopNav.tsx"
      to: "app header brand"
      via: "brand span text"
      pattern: ">Nexo<"
---

<objective>
Replace all user-visible product name occurrences of "FXL Core" and "Nucleo FXL" with "Nexo" across UI components, meta files, and documentation. Zero functional changes — pure string replacement.

Purpose: Complete the v4.0 rebrand so the product is consistently named "Nexo" everywhere.
Output: 10 files updated, zero TypeScript errors, zero regressions.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-rename-nexo/73-CONTEXT.md

## Scope rules (from CONTEXT.md — NON-NEGOTIABLE)

- "FXL SDK" keeps "FXL" — company/SDK name, not product
- GitHub repo name stays `fxl-core` — do NOT rename `package.json "name"` field
- MODULE_IDS and internal registry names stay unchanged
- Filesystem folder names stay unchanged
- Replace: "FXL Core" → "Nexo", "Nucleo FXL" → "Nexo"
- Skip: `docs/superpowers/specs/` — historical design spec files, not product UI strings
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename UI components (src/)</name>
  <files>
    src/platform/auth/Login.tsx,
    src/platform/layout/TopNav.tsx,
    src/platform/pages/Home.tsx,
    src/platform/router/AppRouter.tsx
  </files>
  <read_first>
    - src/platform/auth/Login.tsx (line 7: `Nucleo FXL` in h1)
    - src/platform/layout/TopNav.tsx (line 15: `Nucleo FXL` in span)
    - src/platform/pages/Home.tsx (lines 120, 233: `Nucleo FXL` in h2 and text node)
    - src/platform/router/AppRouter.tsx (line 65: `Nucleo FXL` in h1)
  </read_first>
  <action>
    Replace every occurrence of "Nucleo FXL" with "Nexo" in each file.

    Exact replacements (5 occurrences across 4 files):

    src/platform/auth/Login.tsx line ~7:
      BEFORE: >Nucleo FXL</h1>
      AFTER:  >Nexo</h1>

    src/platform/layout/TopNav.tsx line ~15:
      BEFORE: >Nucleo FXL</span>
      AFTER:  >Nexo</span>

    src/platform/pages/Home.tsx line ~120:
      BEFORE: >Nucleo FXL</h2>
      AFTER:  >Nexo</h2>

    src/platform/pages/Home.tsx line ~233:
      BEFORE: Nucleo FXL  (JSX text node)
      AFTER:  Nexo

    src/platform/router/AppRouter.tsx line ~65:
      BEFORE: >Nucleo FXL</h1>
      AFTER:  >Nexo</h1>

    Do NOT change: import paths, component names, function names, prop names, CSS classes.
    Do NOT change: "FXL SDK" if it appears anywhere.
    Only visible string content changes.
  </action>
  <verify>
    <automated>grep -rn "Nucleo FXL\|FXL Core" src/ 2>/dev/null && echo "FAIL: old names found" || echo "PASS: src/ clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Nucleo FXL\|FXL Core" src/` returns zero matches
    - `grep "Nexo" src/platform/auth/Login.tsx` returns a match
    - `grep "Nexo" src/platform/layout/TopNav.tsx` returns a match
    - `grep -c "Nexo" src/platform/pages/Home.tsx` returns at least 2
    - `grep "Nexo" src/platform/router/AppRouter.tsx` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 5 "Nucleo FXL" strings in src/ replaced with "Nexo". TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update meta file (index.html)</name>
  <files>
    index.html
  </files>
  <read_first>
    - index.html (line 7: `<title>Nucleo FXL</title>`)
  </read_first>
  <action>
    index.html line ~7:
      BEFORE: <title>Nucleo FXL</title>
      AFTER:  <title>Nexo</title>

    If there are any og:title or og:site_name meta tags with "Nucleo FXL", change those too.
    Do NOT change any script src paths, link hrefs, or other attributes.

    NOTE: package.json `"name": "fxl-core"` stays unchanged.
    Per CONTEXT.md decision: "GitHub repo name stays fxl-core".
    package.json has no description field — no other changes needed there.
  </action>
  <verify>
    <automated>grep -n "title\|Nucleo\|FXL Core" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "<title>" index.html` returns `<title>Nexo</title>`
    - `grep "Nucleo FXL\|FXL Core" index.html` returns zero matches
  </acceptance_criteria>
  <done>Browser tab shows "Nexo". No other meta changes needed.</done>
</task>

<task type="auto">
  <name>Task 3: Update documentation (CLAUDE.md, PROJECT.md, docs/)</name>
  <files>
    CLAUDE.md,
    .planning/PROJECT.md,
    docs/processo/fases/fase2.md,
    docs/processo/spoke-onboarding.md,
    docs/ferramentas/branding-collection.md
  </files>
  <read_first>
    - CLAUDE.md (line 1: heading, line 5: "O FXL Core e", line 10: "O que o FXL Core NAO e")
    - .planning/PROJECT.md (lines 170, 251, 310: "FXL Core" in constraint/stack notes)
    - docs/processo/fases/fase2.md (line 78: `Nucleo FXL (Vercel)`)
    - docs/processo/spoke-onboarding.md (lines 4, 9, 15, 20, 101, 147: "FXL Core" as hub name)
    - docs/ferramentas/branding-collection.md (line 16: "app FXL Core")
  </read_first>
  <action>
    CLAUDE.md — 3 replacements:
      Line 1:  `# CLAUDE.md — FXL Core (Nucleo FXL)` → `# CLAUDE.md — Nexo`
      Line 5:  `O FXL Core e a plataforma` → `O Nexo e a plataforma`
      Line 10: `O que o FXL Core NAO e:` → `O que o Nexo NAO e:`

    .planning/PROJECT.md — replace "FXL Core" on lines 170, 251, 310:
      Line ~170: `no FXL Core` / `FXL Core e documentacao` → `no Nexo` / `Nexo e documentacao`
      Line ~251: `FXL Core app theme` → `Nexo app theme`
      Line ~310: `Stack FXL Core` → `Stack Nexo`
      KEEP line ~13 unchanged — it is the milestone goal statement: "Renomear o produto de 'FXL Core'..."
      KEEP any `fxl-core` repo path references unchanged.

    docs/processo/fases/fase2.md line ~78:
      BEFORE: | Nucleo FXL (Vercel) |
      AFTER:  | Nexo (Vercel) |

    docs/processo/spoke-onboarding.md — 6 replacements:
      All occurrences of "FXL Core" in prose → "Nexo"
      Examples: "ao FXL Core como spoke" → "ao Nexo como spoke"
                "Acesso ao FXL Core" → "Acesso ao Nexo"
                "Hub (FXL Core)" → "Hub (Nexo)"
                "No FXL Core, acessar" → "No Nexo, acessar"
                "no FXL Core" → "no Nexo"

    docs/ferramentas/branding-collection.md line ~16:
      BEFORE: app FXL Core (sidebar, header, docs)
      AFTER:  app Nexo (sidebar, header, docs)

    GLOBAL RULE: Do NOT change "FXL SDK", "fxl-sdk", or standalone "FXL" (company name).
    Do NOT touch docs/superpowers/specs/ — historical design spec files.
  </action>
  <verify>
    <automated>grep -n "Nucleo FXL\|FXL Core" CLAUDE.md .planning/PROJECT.md docs/processo/fases/fase2.md docs/processo/spoke-onboarding.md docs/ferramentas/branding-collection.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Nucleo FXL\|FXL Core" CLAUDE.md` returns zero matches
    - `grep "Nucleo FXL" .planning/PROJECT.md` returns zero matches
    - `grep "FXL Core" docs/processo/spoke-onboarding.md` returns zero matches
    - `grep "Nucleo FXL" docs/processo/fases/fase2.md` returns zero matches
    - `grep "FXL Core" docs/ferramentas/branding-collection.md` returns zero matches
    - `grep "FXL SDK" CLAUDE.md` still returns matches (SDK name preserved)
    - CLAUDE.md line 1 reads: `# CLAUDE.md — Nexo`
  </acceptance_criteria>
  <done>CLAUDE.md, PROJECT.md, and affected docs use "Nexo" as product name. FXL SDK and repo path references untouched.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the full sweep:

```bash
# src/ must be zero
grep -rn "Nucleo FXL\|FXL Core" src/ 2>/dev/null && echo "FAIL: src not clean" || echo "PASS: src clean"

# index.html must show Nexo
grep "<title>" index.html

# key docs must be zero
grep -n "Nucleo FXL\|FXL Core" CLAUDE.md .planning/PROJECT.md docs/processo/fases/fase2.md docs/processo/spoke-onboarding.md docs/ferramentas/branding-collection.md 2>/dev/null

# TypeScript must pass
npx tsc --noEmit && echo "PASS: TS clean" || echo "FAIL: TS errors"
```
</verification>

<success_criteria>
1. `grep -rn "Nucleo FXL\|FXL Core" src/` → zero matches
2. `grep "<title>" index.html` → `<title>Nexo</title>`
3. `grep "Nucleo FXL\|FXL Core" CLAUDE.md` → zero matches
4. `grep "FXL SDK" CLAUDE.md` → still returns matches (SDK name preserved)
5. `npx tsc --noEmit` → exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/73-rename-nexo/73-01-SUMMARY.md` following the summary template.
</output>
