---
phase: 74-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VERIF-01, VERIF-02]

must_haves:
  truths:
    - "npx tsc --noEmit exits with code 0 (zero TypeScript errors)"
    - "npm run build completes without errors"
    - "No occurrences of 'FXL Core' or 'Nucleo FXL' remain in src/ (excluding SDK references)"
  artifacts:
    - path: "dist/"
      provides: "Production build output"
  key_links:
    - from: "src/"
      to: "dist/"
      via: "npm run build (Vite)"
      pattern: "build.*success"
---

<objective>
Verify the production build is clean after the Phase 73 rename.

Purpose: Confirm that renaming "FXL Core"/"Nucleo FXL" to "Nexo" introduced no TypeScript
errors, broken imports, or residual stale strings in src/.
Output: Clean tsc + build output; any regressions fixed.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-build-verification/74-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript check, build, and residual string grep</name>
  <files></files>
  <action>
    Run the three verification commands in sequence:

    1. `npx tsc --noEmit`
       - Must exit 0. If errors, read each error, identify the file and line, fix the
         TypeScript issue (never use `any` as a fix). Re-run until clean.

    2. `npm run build`
       - Must complete without errors. If Vite reports errors, read them, fix imports
         or missing exports, re-run until clean.

    3. `grep -r "FXL Core\|Nucleo FXL" src/ --include="*.tsx" --include="*.ts"`
       - Must return no results. If results found, replace each occurrence with "Nexo"
         (or the appropriate contextual replacement). Re-run grep to confirm zero results.
         Exception: do NOT rename strings that clearly refer to the FXL SDK
         (e.g. package names like `@fxl/sdk`, import paths, or SDK-internal identifiers).

    Fix any issues found before marking complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && ! grep -r "FXL Core\|Nucleo FXL" src/ --include="*.tsx" --include="*.ts"</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - grep for "FXL Core" and "Nucleo FXL" in src/ returns no matches
  </done>
</task>

</tasks>

<verification>
All three commands must succeed:
- `npx tsc --noEmit` → exit 0
- `npm run build` → exit 0
- `grep -r "FXL Core\|Nucleo FXL" src/ --include="*.tsx" --include="*.ts"` → no output
</verification>

<success_criteria>
Production build is clean. No TypeScript errors. No residual old brand strings in src/.
Phase 73 rename is confirmed stable.
</success_criteria>

<output>
After completion, create `.planning/phases/74-build-verification/74-01-SUMMARY.md`
</output>
