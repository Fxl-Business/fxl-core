---
phase: 132
plan: 2
title: "Update ci-cd.md to reflect actual v9.0 pipeline"
wave: 1
depends_on: []
files_modified:
  - docs/sdk/ci-cd.md
autonomous: true
requirements: [SDKU-01]
---

# Plan 132-02: Update ci-cd.md to reflect actual v9.0 pipeline

## Objective

Update the existing ci-cd.md SDK page to accurately reflect the current CI pipeline as implemented in `.github/workflows/ci.yml` during Nexo v9.0. The page currently documents an outdated workflow with wrong Node version, wrong install command, missing vitest, and missing env var placeholders.

## must_haves

- CI workflow section reflects actual ci.yml: Node 20, npm install --ignore-scripts, vitest run, placeholder env vars, PR-only trigger, concurrency group
- The callout recommending npm ci is removed (project explicitly uses npm install)
- The "Install dependencies" step in the table says `npm install --ignore-scripts` not `npm ci`
- Vitest appears as a test step in both the YAML and the details table
- Placeholder env vars (VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY, VITE_CLERK_PUBLISHABLE_KEY) documented
- Updated page synced to Supabase documents table

## Tasks

### Task 1: Update CI workflow section in ci-cd.md

<read_first>
- docs/sdk/ci-cd.md (full current content — must understand all sections to update surgically)
- .github/workflows/ci.yml (source of truth for the actual pipeline)
- CLAUDE.md (doc format rules, commit conventions)
</read_first>

<action>
Update `docs/sdk/ci-cd.md` with the following specific changes:

**1. Replace the YAML workflow block** (lines ~22-58) with the actual ci.yml content:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  type-check-and-test:
    name: Type Check & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Type check
        run: npx tsc --noEmit

      - name: Run tests
        run: npx vitest run
        env:
          VITE_SUPABASE_URL: https://placeholder.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.placeholder
          VITE_CLERK_PUBLISHABLE_KEY: pk_test_placeholder
```

**2. Update the "Workflow CI" intro text:**
- Change "O workflow roda automaticamente em:" to remove push to main
- New text: "O workflow roda automaticamente em **Pull Requests** targeting `main`."
- Remove the bullet list (push para main, pull request targeting main)

**3. Update the "Detalhes dos Steps" table** to match the actual workflow:

| Step | Comando | Proposito |
|------|---------|----------|
| Checkout | `actions/checkout@v4` | Clona o repositorio |
| Setup Node | `actions/setup-node@v4` | Configura Node.js 20 com cache npm |
| Install | `npm install --ignore-scripts` | Instala dependencias sem rodar scripts de build |
| Type check | `npx tsc --noEmit` | Verifica tipos TypeScript |
| Tests | `npx vitest run` | Roda testes unitarios com vitest |

**4. Remove the npm ci callout** — Find and delete the entire callout block:
```
{% callout type="info" %}
Usar `npm ci` (nao `npm install`) no CI garante que as dependencias sao identicas ao lockfile. Isso previne "funciona na minha maquina" por versoes diferentes.
{% /callout %}
```

Replace with a new callout explaining the npm install choice:
```
{% callout type="info" %}
O pipeline usa `npm install --ignore-scripts` em vez de `npm ci` para evitar falhas causadas por lockfile desatualizado em PRs de dependencia. O flag `--ignore-scripts` pula scripts de postinstall para maior seguranca.
{% /callout %}
```

**5. Add new section "Variaveis de Ambiente no CI"** after the steps table:

```markdown
## Variaveis de Ambiente no CI

O step de testes usa placeholders para as variaveis de ambiente obrigatorias. Isso permite que testes importem modulos que referenciam `import.meta.env.VITE_*` sem falhar.

| Variavel | Valor Placeholder | Proposito |
|----------|------------------|-----------|
| `VITE_SUPABASE_URL` | `https://placeholder.supabase.co` | URL do Supabase (nao acessada em testes unitarios) |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | JWT placeholder | Chave anon (nao usada em testes unitarios) |
| `VITE_CLERK_PUBLISHABLE_KEY` | `pk_test_placeholder` | Chave Clerk (nao usada em testes unitarios) |

{% callout type="warning" %}
Esses valores sao placeholders — nao apontam para nenhum servico real. Testes unitarios nao devem fazer chamadas de rede. Se um teste precisa de Supabase real, use um projeto de teste dedicado.
{% /callout %}
```

**6. Add "Concurrency" subsection** after the workflow YAML block:

```markdown
### Concurrency

O workflow usa `concurrency` para cancelar execucoes anteriores quando um novo push chega na mesma PR:

\`\`\`yaml
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
\`\`\`

Isso economiza minutos de CI e evita resultados desatualizados no check do PR.
```

**7. Update Troubleshooting section:**
- Change "Verificar que a versao do Node.js e 18" to "Verificar que a versao do Node.js e 20"
- Change "Rodar `npm ci` localmente (nao `npm install`)" to "Rodar `npm install --ignore-scripts` localmente para replicar o CI"

**Keep unchanged:** fxl-doctor.sh section, Branch Protection section, Vercel Deploy section, Preview Deploys section, SPA Routing section, API Routes section, Build Optimization section.
</action>

<acceptance_criteria>
- docs/sdk/ci-cd.md contains "node-version: 20" (not 18)
- docs/sdk/ci-cd.md contains "npm install --ignore-scripts" in the YAML block
- docs/sdk/ci-cd.md contains "npx vitest run" in the YAML block
- docs/sdk/ci-cd.md contains "VITE_SUPABASE_URL: https://placeholder.supabase.co" in the YAML block
- docs/sdk/ci-cd.md contains "VITE_SUPABASE_PUBLISHABLE_KEY" in the YAML block
- docs/sdk/ci-cd.md contains "## Variaveis de Ambiente no CI" section
- docs/sdk/ci-cd.md does NOT contain the old callout text "Usar `npm ci` (nao `npm install`) no CI garante"
- docs/sdk/ci-cd.md contains "cancel-in-progress: true"
- docs/sdk/ci-cd.md Troubleshooting section contains "Node.js e 20" (not 18)
- docs/sdk/ci-cd.md Troubleshooting contains "npm install --ignore-scripts" (not "npm ci")
</acceptance_criteria>

### Task 2: Sync updated ci-cd.md to Supabase

<read_first>
- docs/sdk/ci-cd.md (updated content to sync)
- CLAUDE.md (sync rules — body without frontmatter)
</read_first>

<action>
Sync the updated ci-cd.md to the Supabase `documents` table. Extract the body content (everything after the frontmatter `---` block) and update:

```sql
UPDATE documents
SET body = '<markdown body without frontmatter>',
    description = 'GitHub Actions, Vercel, vitest e configuracao de deploy'
WHERE slug = 'sdk/ci-cd';
```

Note: update the description to mention vitest (currently says "fxl-doctor"). Keep title and badge unchanged.
</action>

<acceptance_criteria>
- Supabase `documents` table row with slug 'sdk/ci-cd' has body containing "vitest run"
- Supabase `documents` table row with slug 'sdk/ci-cd' has body containing "npm install --ignore-scripts"
- Supabase `documents` table row with slug 'sdk/ci-cd' has body containing "Variaveis de Ambiente no CI"
- The page renders correctly in the Nexo SDK sidebar
</acceptance_criteria>

## Verification

After all tasks complete:
1. `npx tsc --noEmit` — zero errors
2. ci-cd.md YAML block matches actual .github/workflows/ci.yml
3. ci-cd.md does not recommend npm ci anywhere
4. ci-cd.md documents vitest as test runner
5. ci-cd.md documents placeholder env vars
6. Supabase `documents` table reflects updated content
