---
phase: "0133-sdk-content-polish-supabase-sync"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sdk/code-standards.md
  - docs/sdk/error-boundaries.md
  - docs/sdk/analytics.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SDKP-01
  - SDKN-01
  - SDKN-03
  - SDKP-04

must_haves:
  truths:
    - "code-standards.md links to /sdk/retry-backoff for withRetry pattern"
    - "error-boundaries.md links to /sdk/observabilidade for Sentry setup"
    - "analytics.md links to /sdk/security for auth and data protection"
    - "REQUIREMENTS.md shows all 11 requirements as Complete in traceability table"
  artifacts:
    - path: "docs/sdk/code-standards.md"
      provides: "Cross-link to retry-backoff"
      contains: "/sdk/retry-backoff"
    - path: "docs/sdk/error-boundaries.md"
      provides: "Cross-link to observabilidade"
      contains: "/sdk/observabilidade"
    - path: "docs/sdk/analytics.md"
      provides: "Cross-link to security"
      contains: "/sdk/security"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability status"
      contains: "Complete"
  key_links:
    - from: "docs/sdk/code-standards.md"
      to: "/sdk/retry-backoff"
      via: "markdown link"
      pattern: "\\[.*\\]\\(/sdk/retry-backoff\\)"
    - from: "docs/sdk/error-boundaries.md"
      to: "/sdk/observabilidade"
      via: "markdown link"
      pattern: "\\[.*\\]\\(/sdk/observabilidade\\)"
    - from: "docs/sdk/analytics.md"
      to: "/sdk/security"
      via: "markdown link"
      pattern: "\\[.*\\]\\(/sdk/security\\)"
---

<objective>
Add cross-links between related SDK pages and verify REQUIREMENTS.md traceability status.

Purpose: Improve SDK page discoverability with navigational links and ensure requirement tracking is accurate.
Output: Updated .md files with cross-links synced to Supabase, confirmed REQUIREMENTS.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v10.0-MILESTONE-AUDIT.md
@.planning/phases/0133-sdk-content-polish-supabase-sync/0133-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-links between SDK pages + sync to Supabase</name>
  <read_first>
    - docs/sdk/code-standards.md
    - docs/sdk/error-boundaries.md
    - docs/sdk/analytics.md
  </read_first>
  <files>
    docs/sdk/code-standards.md,
    docs/sdk/error-boundaries.md,
    docs/sdk/analytics.md
  </files>
  <action>
**CROSS-LINK 1: code-standards.md to retry-backoff.md**

In `docs/sdk/code-standards.md`, there is already a section about `withRetry` (around line 585 "### withRetry para chamadas criticas"). At the END of that section (after the table at line ~618), add:

```markdown

Para detalhes completos sobre configuracao de tentativas, backoff exponencial e call sites criticos, ver [Retry com Backoff](/sdk/retry-backoff).
```

**CROSS-LINK 2: error-boundaries.md to observabilidade.md**

In `docs/sdk/error-boundaries.md`, the class component sends errors to Sentry via `componentDidCatch`. Find the section where Sentry integration is mentioned (around the `Sentry.captureException` calls). At the END of the file (before the closing content), add a new section:

```markdown

## Leitura Complementar

- [Observabilidade](/sdk/observabilidade) — Setup completo do Sentry para frontend: DSN, source maps, alertas e captura de erros com contexto de modulo
- [Retry com Backoff](/sdk/retry-backoff) — Para erros em chamadas async, combinar error boundaries com retry automatico
```

**CROSS-LINK 3: analytics.md to security.md**

In `docs/sdk/analytics.md`, the Connector module extracts data which requires authenticated access. At the END of the file, add:

```markdown

## Leitura Complementar

- [Seguranca](/sdk/security) — Autenticacao Clerk, JWT bridge para Supabase e protecao de dados por tenant (RLS)
- [Infraestrutura](/sdk/infrastructure) — Ambientes, monitoring Sentry e deploy Vercel
```

**SUPABASE SYNC:**

After editing each .md file, sync to Supabase by running `mcp__supabase__execute_sql` with UPDATE on the `documents` table:
- `sdk/code-standards` — update body
- `sdk/error-boundaries` — update body
- `sdk/analytics` — update body

CRITICAL: The `body` column must contain ONLY the markdown content WITHOUT the `---frontmatter---` block.
  </action>
  <verify>
    <automated>grep -n "/sdk/retry-backoff" docs/sdk/code-standards.md && grep -n "/sdk/observabilidade" docs/sdk/error-boundaries.md && grep -n "/sdk/security" docs/sdk/analytics.md && echo "PASS: All cross-links present" || echo "FAIL: Missing cross-links"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/sdk/retry-backoff" docs/sdk/code-standards.md` returns at least 1 match with a markdown link
    - `grep "/sdk/observabilidade" docs/sdk/error-boundaries.md` returns at least 1 match with a markdown link
    - `grep "/sdk/security" docs/sdk/analytics.md` returns at least 1 match with a markdown link
    - All 3 Supabase UPDATE queries executed successfully
  </acceptance_criteria>
  <done>Three cross-links added: code-standards links to retry-backoff, error-boundaries links to observabilidade, analytics links to security. All synced to Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update REQUIREMENTS.md traceability</name>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
The audit noted that REQUIREMENTS.md checkboxes were stale. However, re-reading the file shows all 11 checkboxes are already marked `[x]` and the traceability table already shows all as "Complete".

**Verify the following are TRUE in .planning/REQUIREMENTS.md:**
1. All 11 requirement checkboxes are `[x]` (SDKP-01 through SDKP-07, SDKN-01 through SDKN-03, SDKU-01)
2. The Traceability table shows all 11 as "Complete"
3. Coverage line says "11 total" and "Unmapped: 0"

**If any are not checked or not Complete**, update them.

**If all are already correct** (which appears to be the case based on the current file), no changes needed — just confirm in verification output.

**Additionally**, add Phase 133 to the Traceability table for the requirements it addresses. Add these rows:

| SDKU-01 | Phase 133 | Complete |
| SDKP-03 | Phase 133 | Complete |
| SDKP-05 | Phase 133 | Complete |
| SDKP-06 | Phase 133 | Complete |
| SDKN-01 | Phase 133 | Complete |
| SDKP-01 | Phase 133 | Complete |
| SDKP-02 | Phase 133 | Complete |
| SDKP-04 | Phase 133 | Complete |
| SDKN-03 | Phase 133 | Complete |

These should be added as additional rows (Phase 133 gap closure entries) below the existing Phase 129-132 entries, showing these requirements were also addressed in Phase 133 for polish/fixes.

Update `Last updated` date to current date.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md | xargs test 11 -le && echo "PASS: All 11+ checkboxes marked" || echo "FAIL: Missing checkboxes"; grep "Phase 133" .planning/REQUIREMENTS.md && echo "PASS: Phase 133 in traceability" || echo "FAIL: Phase 133 missing"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns 11 or more
    - `grep "Phase 133" .planning/REQUIREMENTS.md` returns at least 1 match
    - All 11 requirements show "Complete" status in the traceability table
  </acceptance_criteria>
  <done>REQUIREMENTS.md confirmed with all 11 checkboxes marked, traceability table updated with Phase 133 entries, all showing Complete status.</done>
</task>

</tasks>

<verification>
1. `grep "/sdk/retry-backoff" docs/sdk/code-standards.md` returns link
2. `grep "/sdk/observabilidade" docs/sdk/error-boundaries.md` returns link
3. `grep "/sdk/security" docs/sdk/analytics.md` returns link
4. All 3 Supabase UPDATE queries confirmed successful
5. REQUIREMENTS.md has all 11 checkboxes marked and Phase 133 in traceability
</verification>

<success_criteria>
- Three cross-links added between related SDK pages and synced to Supabase
- REQUIREMENTS.md traceability table includes Phase 133 entries, all 11 requirements confirmed Complete
</success_criteria>

<output>
After completion, create `.planning/phases/0133-sdk-content-polish-supabase-sync/0133-02-SUMMARY.md`
</output>
