---
phase: "0133-sdk-content-polish-supabase-sync"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sdk/ci-cd.md
  - docs/sdk/documentation.md
  - docs/sdk/security.md
  - docs/sdk/stack.md
  - docs/sdk/infrastructure.md
  - docs/sdk/index.md
autonomous: true
requirements:
  - SDKU-01
  - SDKP-03
  - SDKP-05
  - SDKP-06
  - SDKN-01
  - SDKP-01
  - SDKP-02
  - SDKP-04

must_haves:
  truths:
    - "Nenhuma pagina SDK menciona VITE_SUPABASE_ANON_KEY — todas usam VITE_SUPABASE_PUBLISHABLE_KEY"
    - "infrastructure.md nao tem exemplo inline de ErrorBoundary contradizendo error-boundaries.md"
    - "index.md mostra paginas completas como Completo, nao Em Construcao"
    - "Cada pagina SDK tem sort_order unico na tabela documents do Supabase"
  artifacts:
    - path: "docs/sdk/ci-cd.md"
      provides: "Corrected env var name"
      contains: "VITE_SUPABASE_PUBLISHABLE_KEY"
    - path: "docs/sdk/security.md"
      provides: "Corrected env var name"
      contains: "VITE_SUPABASE_PUBLISHABLE_KEY"
    - path: "docs/sdk/documentation.md"
      provides: "Corrected env var name"
      contains: "VITE_SUPABASE_PUBLISHABLE_KEY"
    - path: "docs/sdk/stack.md"
      provides: "Corrected env var name"
      contains: "VITE_SUPABASE_PUBLISHABLE_KEY"
    - path: "docs/sdk/infrastructure.md"
      provides: "Reference to error-boundaries instead of inline example"
    - path: "docs/sdk/index.md"
      provides: "Updated status for completed pages"
  key_links:
    - from: "docs/sdk/infrastructure.md"
      to: "/sdk/error-boundaries"
      via: "markdown link reference"
      pattern: "/sdk/error-boundaries"
---

<objective>
Fix HIGH and MEDIUM severity gaps from v10.0 milestone audit: env var inconsistency, error boundary contradiction, index stale status, and sort_order collisions.

Purpose: Eliminate content contradictions and data integrity issues across SDK pages before milestone completion.
Output: Corrected .md files AND corresponding Supabase documents table rows.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v10.0-MILESTONE-AUDIT.md
@.planning/phases/0133-sdk-content-polish-supabase-sync/0133-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix env var names and error boundary contradiction in .md files + Supabase</name>
  <read_first>
    - docs/sdk/ci-cd.md
    - docs/sdk/documentation.md
    - docs/sdk/security.md
    - docs/sdk/stack.md
    - docs/sdk/infrastructure.md
  </read_first>
  <files>
    docs/sdk/ci-cd.md,
    docs/sdk/documentation.md,
    docs/sdk/security.md,
    docs/sdk/stack.md,
    docs/sdk/infrastructure.md
  </files>
  <action>
**ENV VAR FIX — Replace VITE_SUPABASE_ANON_KEY with VITE_SUPABASE_PUBLISHABLE_KEY in 4 files:**

1. `docs/sdk/ci-cd.md` line ~237: Change `VITE_SUPABASE_ANON_KEY` to `VITE_SUPABASE_PUBLISHABLE_KEY` in the Vercel env vars table. Also update the description from "Chave anon publica" to "Chave publica do Supabase".

2. `docs/sdk/documentation.md` line ~70: Change `VITE_SUPABASE_ANON_KEY` to `VITE_SUPABASE_PUBLISHABLE_KEY` in the README env vars table. Also update the description from "Chave anon publica" to "Chave publica do Supabase".

3. `docs/sdk/security.md` line ~241: Change `VITE_SUPABASE_ANON_KEY=eyJ...` to `VITE_SUPABASE_PUBLISHABLE_KEY=eyJ...`

4. `docs/sdk/stack.md` line ~127: Change `VITE_SUPABASE_ANON_KEY=eyJ...` to `VITE_SUPABASE_PUBLISHABLE_KEY=eyJ...`

**ERROR BOUNDARY CONTRADICTION — Fix infrastructure.md:**

In `docs/sdk/infrastructure.md`, lines 95-111, replace the entire "### Error Boundary" subsection (which shows `Sentry.ErrorBoundary` functional wrapper) with:

```markdown
### Error Boundary

Para capturar erros de renderizacao por modulo, usar o padrao de class component com `getDerivedStateFromError`. O padrao completo — com envio ao Sentry e fallback UI — esta documentado na pagina dedicada:

Ver: [Error Boundaries](/sdk/error-boundaries)

{% callout type="warning" %}
Nao usar `Sentry.ErrorBoundary` diretamente. O padrao aprovado e o class component `ModuleErrorBoundary` que integra Sentry via `componentDidCatch`. Detalhes em [Error Boundaries](/sdk/error-boundaries).
{% /callout %}
```

**SUPABASE SYNC — Update all 5 pages in the documents table:**

After editing each .md file, read the updated file content and run `mcp__supabase__execute_sql` to UPDATE the `body` column in the `documents` table for each slug:
- `sdk/ci-cd` — update body with new content (no frontmatter)
- `sdk/documentation` — update body with new content (no frontmatter)
- `sdk/security` — update body with new content (no frontmatter)
- `sdk/stack` — update body with new content (no frontmatter)
- `sdk/infrastructure` — update body with new content (no frontmatter)

CRITICAL: The `body` column must contain ONLY the markdown content WITHOUT the `---frontmatter---` block. Strip everything between and including the `---` delimiters at the top of the file.
  </action>
  <verify>
    <automated>grep -rn "VITE_SUPABASE_ANON_KEY" docs/sdk/ && echo "FAIL: ANON_KEY still found" || echo "PASS: No ANON_KEY references"; grep -n "Sentry.ErrorBoundary" docs/sdk/infrastructure.md && echo "FAIL: Sentry.ErrorBoundary still in infrastructure.md" || echo "PASS: No Sentry.ErrorBoundary in infrastructure.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "VITE_SUPABASE_ANON_KEY" docs/sdk/` returns zero matches
    - `grep -n "VITE_SUPABASE_PUBLISHABLE_KEY" docs/sdk/ci-cd.md` returns at least 1 match
    - `grep -n "VITE_SUPABASE_PUBLISHABLE_KEY" docs/sdk/documentation.md` returns at least 1 match
    - `grep -n "VITE_SUPABASE_PUBLISHABLE_KEY" docs/sdk/security.md` returns at least 1 match
    - `grep -n "VITE_SUPABASE_PUBLISHABLE_KEY" docs/sdk/stack.md` returns at least 1 match
    - `grep -n "/sdk/error-boundaries" docs/sdk/infrastructure.md` returns at least 1 match
    - `grep -n "Sentry.ErrorBoundary" docs/sdk/infrastructure.md` returns zero matches
    - All 5 Supabase UPDATE queries executed successfully
  </acceptance_criteria>
  <done>All 4 env var occurrences replaced with VITE_SUPABASE_PUBLISHABLE_KEY. Error boundary section in infrastructure.md replaced with reference to /sdk/error-boundaries. All changes synced to Supabase documents table.</done>
</task>

<task type="auto">
  <name>Task 2: Fix index.md status and sort_order collisions</name>
  <read_first>
    - docs/sdk/index.md
  </read_first>
  <files>docs/sdk/index.md</files>
  <action>
**INDEX STATUS UPDATE — Move completed pages out of "Em Construcao":**

In `docs/sdk/index.md`, restructure the sections table. Currently lines 62-74 list 9 pages under "### Em Construcao". After the fix:

The "### Em Construcao" section should be REMOVED entirely and replaced with two new sections:

**Replace the current "### Em Construcao" section (lines 62-74) with:**

```markdown
### Padroes e Seguranca

| Secao | Descricao | Status |
|-------|-----------|--------|
| [Padroes de Codigo](/sdk/code-standards) | Convencoes, naming, lint, TypeScript strict | Completo |
| [Banco de Dados](/sdk/database) | Migrations, RLS, modelagem Supabase | Completo |
| [Seguranca](/sdk/security) | Auth Clerk, JWT bridge, RLS, env vars | Completo |
| [Documentacao](/sdk/documentation) | README obrigatorio, CLAUDE.md, changelog | Completo |

### Operacional

| Secao | Descricao | Status |
|-------|-----------|--------|
| [Analytics](/sdk/analytics) | Metricas, extracao de dados, dashboards | Completo |
| [Infraestrutura](/sdk/infrastructure) | Ambientes, Sentry, Vercel deploy | Completo |
| [Mobile](/sdk/mobile) | React Native/Expo, push notifications | Completo |

### Ferramentas

| Secao | Descricao | Status |
|-------|-----------|--------|
| [MCP Server](/sdk/mcp-server) | Cerebro central de conhecimento | Completo |
| [Nexo Skill](/sdk/nexo-skill) | Skill unificada do Claude Code | Completo |
```

After editing the .md file, sync to Supabase:
- UPDATE `documents` SET `body` = (new content without frontmatter) WHERE `slug` = 'sdk/index'

**SORT ORDER FIX — Assign unique sort_order values via SQL:**

Run the following SQL UPDATEs on the `documents` table to resolve collisions:

Current collisions:
- sort_order 80: code-standards, error-boundaries, mcp-server
- sort_order 90: database, nexo-skill

New unique values (maintaining logical grouping order):

```sql
-- Fundamentais group (10-60 already fine)
-- Padroes e Seguranca group
UPDATE documents SET sort_order = 70 WHERE slug = 'sdk/code-standards';
UPDATE documents SET sort_order = 75 WHERE slug = 'sdk/database';
UPDATE documents SET sort_order = 80 WHERE slug = 'sdk/security';
UPDATE documents SET sort_order = 85 WHERE slug = 'sdk/documentation';

-- Operacional group
UPDATE documents SET sort_order = 90 WHERE slug = 'sdk/analytics';
UPDATE documents SET sort_order = 95 WHERE slug = 'sdk/infrastructure';
UPDATE documents SET sort_order = 100 WHERE slug = 'sdk/mobile';

-- Ferramentas group
UPDATE documents SET sort_order = 110 WHERE slug = 'sdk/mcp-server';
UPDATE documents SET sort_order = 115 WHERE slug = 'sdk/nexo-skill';

-- Resiliencia group
UPDATE documents SET sort_order = 120 WHERE slug = 'sdk/error-boundaries';
UPDATE documents SET sort_order = 125 WHERE slug = 'sdk/observabilidade';
UPDATE documents SET sort_order = 130 WHERE slug = 'sdk/retry-backoff';
```
  </action>
  <verify>
    <automated>grep -c "Em Construcao" docs/sdk/index.md | xargs test 0 -eq && echo "PASS: No Em Construcao section" || echo "FAIL: Em Construcao still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Em Construcao" docs/sdk/index.md` returns zero matches
    - `grep "Completo" docs/sdk/index.md` returns matches for all completed pages (code-standards, database, security, documentation, analytics, infrastructure, mobile, mcp-server, nexo-skill)
    - All sort_order UPDATE queries executed successfully with no two pages sharing the same sort_order
    - Supabase documents table for slug 'sdk/index' has updated body content
  </acceptance_criteria>
  <done>All 9 completed pages moved from "Em Construcao" to proper sections with "Completo" status. Sort_order collisions resolved with unique values per page. Index synced to Supabase.</done>
</task>

</tasks>

<verification>
1. `grep -rn "VITE_SUPABASE_ANON_KEY" docs/sdk/` returns zero results
2. `grep -n "Sentry.ErrorBoundary" docs/sdk/infrastructure.md` returns zero results
3. `grep -n "/sdk/error-boundaries" docs/sdk/infrastructure.md` returns at least 1 result
4. `grep "Em Construcao" docs/sdk/index.md` returns zero results
5. All Supabase UPDATE queries confirmed successful
6. `npx tsc --noEmit` passes (no code changes, but confirm no regressions)
</verification>

<success_criteria>
- All VITE_SUPABASE_ANON_KEY references replaced with VITE_SUPABASE_PUBLISHABLE_KEY in 4 SDK .md files and their Supabase rows
- infrastructure.md error boundary section references /sdk/error-boundaries instead of showing contradictory inline example
- index.md shows all completed pages with "Completo" status, no "Em Construcao" section for finished pages
- Every SDK page has a unique sort_order in the Supabase documents table
</success_criteria>

<output>
After completion, create `.planning/phases/0133-sdk-content-polish-supabase-sync/0133-01-SUMMARY.md`
</output>
