---
phase: 131
plan: 1
title: "Create error-boundaries.md SDK page"
wave: 1
depends_on: []
requirements: [SDKN-01]
files_modified:
  - docs/sdk/error-boundaries.md
autonomous: true
---

# Plan 131-01: Create error-boundaries.md SDK page

## Objective

Write the complete `docs/sdk/error-boundaries.md` page documenting the error boundary pattern for module isolation with fallback UI. The page explains the pattern, shows the Nexo reference implementation, and provides setup instructions for spoke projects.

## Tasks

<task id="1">
<title>Write docs/sdk/error-boundaries.md</title>

<read_first>
- src/platform/layout/ModuleErrorBoundary.tsx
- src/platform/router/AppRouter.tsx (lines 55-108 — how ModuleErrorBoundary wraps routes)
- docs/sdk/stack.md (style reference — frontmatter format, callout usage, code blocks)
- docs/sdk/ci-cd.md (style reference — another complete SDK page)
- .planning/phases/0131-resilience-patterns/0131-CONTEXT.md
</read_first>

<action>
Create `docs/sdk/error-boundaries.md` with the following structure:

**Frontmatter:**
```yaml
---
title: Error Boundaries
badge: SDK
description: Padrao v9.0 — isolamento de erros por modulo com fallback UI
scope: product
sort_order: 80
---
```

**Content sections (all in Portuguese):**

1. **Title + intro callout** — `# Error Boundaries`
   - Add callout: `{% callout type="info" %}Este padrao foi introduzido no Nexo v9.0 (Resiliencia de Plataforma).{% /callout %}`
   - Explain the problem: a crash in one module (docs, tasks, wireframe) should NOT bring down the entire app shell (header, sidebar, other modules)

2. **## O Padrao** — Explain the error boundary pattern:
   - React error boundaries require class components with `componentDidCatch` / `getDerivedStateFromError`
   - Each module route is wrapped in its own boundary
   - When a module crashes: (1) error is caught, (2) Sentry receives the error with module tag, (3) fallback UI renders with retry button
   - Rest of the app stays navigable

3. **## Referencia: Nexo** — Show the exact `ModuleErrorBoundary` component from `src/platform/layout/ModuleErrorBoundary.tsx`:
   - Include the full component code (all 72 lines)
   - Annotate key parts: `Sentry.captureException` with module tag, fallback card with retry button, the `handleRetry` method that resets error state
   - Show how AppRouter wraps routes: `<ModuleErrorBoundary moduleName="Tarefas"><TaskList /></ModuleErrorBoundary>`

4. **## Setup para Spoke** — Step-by-step setup:
   - Step 1: Install `@sentry/react` (already in stack)
   - Step 2: Create `src/components/ModuleErrorBoundary.tsx` — provide the full component code adapted for a spoke (same pattern, spoke-specific module names)
   - Step 3: Wrap each top-level route with the boundary, providing the `moduleName` prop
   - Step 4: Example router setup showing 3 routes wrapped

5. **## Fallback UI** — Guidelines for the fallback:
   - Must be visually clear (error icon + message + retry button)
   - Must NOT use components that might also be broken (keep it simple: plain HTML/Tailwind)
   - Must include the module name so user knows what broke
   - Show the fallback JSX from the Nexo implementation

6. **## Erros Comuns** — Common pitfalls:
   - Error boundaries only catch render errors, NOT event handler errors or async errors
   - For async errors, use try/catch + Sentry.captureException directly
   - Don't wrap the entire app in one boundary — wrap per-module for isolation
   - Functional components cannot be error boundaries (must be class component)
</action>

<acceptance_criteria>
- File `docs/sdk/error-boundaries.md` exists
- File contains frontmatter with `title: Error Boundaries`, `badge: SDK`, `sort_order: 80`, `scope: product`
- File contains `{% callout type="info" %}` mentioning v9.0
- File contains `class ModuleErrorBoundary extends Component`
- File contains `Sentry.captureException`
- File contains `componentDidCatch`
- File contains `getDerivedStateFromError`
- File contains `moduleName` prop reference
- File contains `## Setup para Spoke` section
- File contains `## Fallback UI` section
- File contains `## Erros Comuns` section
- `npx tsc --noEmit` passes (no TypeScript errors introduced)
</acceptance_criteria>
</task>

## Verification

- [ ] docs/sdk/error-boundaries.md exists with correct frontmatter
- [ ] Page documents the module isolation pattern with Nexo reference code
- [ ] Page includes spoke setup guide with copy-paste component code
- [ ] Page includes fallback UI guidelines and common pitfalls

## Must-Haves (Goal-Backward)

- Error boundary pattern described with module isolation concept
- Nexo's ModuleErrorBoundary shown as reference
- Spoke setup instructions with complete component code
- Sentry integration (captureException with module tag) documented
