---
phase: 117-access-control-lockdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/pages/SolicitarAcesso.tsx
autonomous: true
requirements:
  - ACC-03
  - ACC-04

must_haves:
  truths:
    - "A user with no org membership lands on a screen with Nexo branding and a clear Portuguese message explaining they need admin approval"
    - "The screen has a functional sign out button that ends the Clerk session"
    - "The screen does NOT contain any form, input field, or submission mechanism"
  artifacts:
    - path: "src/platform/pages/SolicitarAcesso.tsx"
      provides: "Request-access holding screen"
      exports: ["default SolicitarAcesso"]
      contains: "useClerk"
  key_links:
    - from: "SolicitarAcesso.tsx"
      to: "useClerk().signOut"
      via: "onClick handler on sign out button"
      pattern: "signOut"
---

<objective>
Create the `SolicitarAcesso.tsx` page — a minimal branded holding screen shown to signed-in users who have no org membership.

Purpose: Replaces the now-removed self-service org creation flow with a clear, welcoming screen that explains the user must wait for admin approval. No form, no submission — purely informational.

Output: `src/platform/pages/SolicitarAcesso.tsx` — standalone page component ready to be imported by AppRouter.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key patterns from Login.tsx (branding reference) -->

Auth page wrapper pattern (from Login.tsx):
```tsx
<div className="min-h-screen bg-slate-50 flex flex-col items-center justify-center px-4 dark:bg-background">
  <div className="mb-8 text-center">
    <h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-foreground">Nexo</h1>
    <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">...</p>
  </div>
  <Card className="w-full max-w-sm">...</Card>
</div>
```

Sign out hook (from @clerk/react):
```tsx
import { useClerk } from '@clerk/react'
const { signOut } = useClerk()
// call: await signOut()
// After sign out, redirect to /login or let Clerk handle it
```

Shadcn imports available:
- @shared/ui/button → Button
- @shared/ui/card → Card, CardContent, CardFooter, CardHeader, CardTitle, CardDescription
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SolicitarAcesso page</name>
  <files>src/platform/pages/SolicitarAcesso.tsx</files>
  <read_first>
    - src/platform/auth/Login.tsx (branding pattern to replicate)
    - src/platform/pages/CriarEmpresa.tsx (loading state pattern reference)
  </read_first>
  <action>
Create `src/platform/pages/SolicitarAcesso.tsx` as a new file. This is NOT a modification — it is a new file from scratch.

The component must:

1. Import `useClerk` from `@clerk/react` for sign out
2. Import `useNavigate` from `react-router-dom` for post-signout redirect
3. Import shadcn components: `Button` from `@shared/ui/button`, `Card`, `CardContent`, `CardFooter` from `@shared/ui/card`

Layout structure (replicate the centered auth page pattern from Login.tsx):
```tsx
export default function SolicitarAcesso() {
  const { signOut } = useClerk()
  const navigate = useNavigate()

  async function handleSignOut() {
    await signOut()
    navigate('/login')
  }

  return (
    <div className="min-h-screen bg-slate-50 flex flex-col items-center justify-center px-4 dark:bg-background">
      {/* Nexo logo block — identical structure to Login.tsx */}
      <div className="mb-8 text-center">
        <h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-foreground">Nexo</h1>
        <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Plataforma operacional interna</p>
      </div>

      <Card className="w-full max-w-sm">
        <CardContent className="pt-6 pb-2 text-center">
          {/* icon/illustration at Claude's discretion — a simple lucide icon works */}
          {/* Message — welcoming, not blocking */}
          <h2 className="text-lg font-semibold text-slate-900 dark:text-foreground mb-2">
            Conta criada com sucesso
          </h2>
          <p className="text-sm text-slate-500 dark:text-slate-400 leading-relaxed">
            Sua conta foi criada. Para acessar a plataforma, solicite acesso a uma organização ao administrador do Nexo.
          </p>
        </CardContent>

        <CardFooter className="pt-4">
          <Button
            variant="outline"
            className="w-full"
            onClick={handleSignOut}
          >
            Sair da conta
          </Button>
        </CardFooter>
      </Card>
    </div>
  )
}
```

Discretion notes:
- Adding a `Building2` or `Lock` icon from `lucide-react` above the heading is encouraged for visual polish
- The icon should be `h-10 w-10 text-slate-400 mx-auto mb-4` or similar muted style — not alarming
- Do NOT add any form inputs, text areas, or submit buttons
- Do NOT import `createOrganization` or anything from `useOrganizationList`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "SolicitarAcesso|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/platform/pages/SolicitarAcesso.tsx`
    - `grep -n "createOrganization\|useOrganizationList\|form\|input\|submit" src/platform/pages/SolicitarAcesso.tsx` returns NO matches (no form elements)
    - `grep -n "signOut" src/platform/pages/SolicitarAcesso.tsx` returns at least 1 match
    - `grep -n "useClerk" src/platform/pages/SolicitarAcesso.tsx` returns at least 1 match
    - `grep -n "Nexo" src/platform/pages/SolicitarAcesso.tsx` returns at least 1 match (branding present)
    - `grep -n "administrador\|organização\|solicite" src/platform/pages/SolicitarAcesso.tsx` returns at least 1 match (Portuguese message)
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>SolicitarAcesso.tsx exists, compiles cleanly, has no form elements, has sign out functionality, and displays Nexo branding with a Portuguese explanation message</done>
</task>

</tasks>

<verification>
After plan completion:
1. `npx tsc --noEmit` — zero TypeScript errors
2. `grep -c "signOut" src/platform/pages/SolicitarAcesso.tsx` — returns >= 1
3. `grep -c "form\|input\|createOrganization" src/platform/pages/SolicitarAcesso.tsx` — returns 0
</verification>

<success_criteria>
- `src/platform/pages/SolicitarAcesso.tsx` exists and exports a default component
- Component uses `useClerk().signOut()` for the sign out button
- Component has Nexo branding (h1 "Nexo"), a Portuguese explanation message, and no form elements
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/117-access-control-lockdown/117-01-SUMMARY.md`
</output>
