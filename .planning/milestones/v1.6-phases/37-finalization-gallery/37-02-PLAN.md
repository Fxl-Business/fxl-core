---
phase: 37-finalization-gallery
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified: []
autonomous: false
requirements:
  - XCUT-04

must_haves:
  truths:
    - "All 12 gallery previews render without errors in light mode"
    - "All 12 gallery previews render without errors in dark mode"
    - "Branding toggle applies override color across all 12 new previews"
    - "npx tsc --noEmit reports zero errors — v1.6 milestone gate satisfied"
  artifacts: []
  key_links: []
---

<objective>
Validate that all 12 new gallery entries render correctly in both light and dark wireframe mode, completing the v1.6 milestone visual gate.

Purpose: Success Criterion #2 for Phase 37 requires all 12 gallery previews to render without errors in both light and dark mode. This plan performs the visual validation that cannot be automated.
Output: Validated gallery with all 12 new types confirmed working in both themes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-finalization-gallery/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript final gate</name>
  <files></files>
  <action>
Run `npx tsc --noEmit` as the final v1.6 milestone gate. This should already pass from Plan 01, but serves as the definitive check.

If errors are found, fix them following project rules (no `any`, proper type narrowing).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>npx tsc --noEmit exits cleanly with zero errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gallery visual validation — all 12 new types</name>
  <what-built>
Complete v1.6 milestone: 12 new chart/section types (Phases 34-36) with ComponentGallery fully synchronized (Phase 37 Plan 01). All types have mock data previews.
  </what-built>
  <how-to-verify>
1. Run `make dev` and navigate to the Component Gallery page (/ferramentas/galeria or equivalent)
2. **Component count check:** Verify the gallery header shows ~41 components (29 existing + 12 new)
3. **Scroll to new entries:** In the Graficos section, scroll to find all 12 new entries:
   - Wave 1: Grouped Bar, Bullet Chart, Step Line, Lollipop, Range Bar, Bump Chart, Polar
   - Wave 2: Pie Chart, Heatmap, Sparkline Grid, Progress Grid
   - Wave 3: Sankey
4. **Light mode pass:** With wireframe theme set to Light, expand each of the 12 new entries and verify:
   - Each renders a visible chart/visualization (not blank, not errored)
   - Mock data shows realistic Brazilian Portuguese financial labels (R$ values, Portuguese text)
   - No console errors in browser DevTools
5. **Dark mode pass:** Click the "Dark Mode" toggle in the gallery toolbar and re-check all 12:
   - Charts remain readable (axes, labels, legends visible against dark background)
   - No visual breakage (white text on white, invisible elements, etc.)
   - Chart colors remain distinguishable
6. **Branding pass:** Click the "Branding: OFF" toggle to activate branding and verify:
   - Primary color changes from blue to teal (#1B6B93) across all previews
   - Charts using chartColors reflect the branding override
7. **Specific checks per type:**
   - Grouped Bar: multiple bars side-by-side per category (not stacked)
   - Bullet: horizontal bar with visible target marker line
   - Step Line: step-shaped line (not smooth curve)
   - Lollipop: thin sticks with circle heads
   - Range Bar: bars starting from non-zero positions
   - Bump: lines with reversed Y axis (rank 1 at top)
   - Polar: radial/circular bar layout
   - Pie: full circle (no hole in center, distinct from Donut)
   - Heatmap: colored grid matrix with varying intensity
   - Sparkline Grid: multiple mini-charts in a grid layout
   - Progress Grid: progress bars with target markers
   - Sankey: flow diagram with nodes and proportional links
  </how-to-verify>
  <resume-signal>Type "approved" to complete v1.6 milestone, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits cleanly with zero errors
- All 12 gallery previews render without errors in light mode
- All 12 gallery previews render without errors in dark mode
- Branding toggle applies color override across all 12 new previews
- Each of the 12 types displays its characteristic visual form (not generic placeholder)
</verification>

<success_criteria>
- XCUT-04 fully satisfied: ComponentGallery shows all 12 new types with representative mock data
- Phase 37 Success Criterion #1: All 12 types present with representative mock data
- Phase 37 Success Criterion #2: All 12 render without errors in both light and dark mode
- Phase 37 Success Criterion #3: tsc --noEmit passes with zero errors
- v1.6 milestone visually complete
</success_criteria>

<output>
After completion, create `.planning/phases/37-finalization-gallery/37-02-SUMMARY.md`
</output>
