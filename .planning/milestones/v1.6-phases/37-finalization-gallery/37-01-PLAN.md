---
phase: 37-finalization-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/tools/galleryMockData.ts
  - src/pages/tools/ComponentGallery.tsx
autonomous: true
requirements:
  - XCUT-04

must_haves:
  truths:
    - "galleryMockData.ts exports 12 new mock data objects for all v1.6 chart types"
    - "ComponentGallery.tsx imports and renders all 12 new chart components with mock data"
    - "All 12 entries appear in the 'Graficos' category (or appropriate category) with status 'available'"
    - "Each mock data object uses realistic Brazilian Portuguese financial labels and values"
    - "npx tsc --noEmit reports zero errors after all additions"
  artifacts:
    - path: "src/pages/tools/galleryMockData.ts"
      provides: "12 new mock data exports for v1.6 chart types"
      contains: "groupedBarChartMock"
    - path: "src/pages/tools/ComponentGallery.tsx"
      provides: "12 new ComponentEntry objects in the categories array"
      contains: "GroupedBarChartComponent"
  key_links:
    - from: "src/pages/tools/ComponentGallery.tsx"
      to: "tools/wireframe-builder/components/"
      via: "Component imports for gallery preview rendering"
      pattern: "import.*from '@tools/wireframe-builder/components/"
---

<objective>
Add all 12 new v1.6 chart types to the ComponentGallery with representative mock data, completing the XCUT-04 requirement.

Purpose: The ComponentGallery must show preview cards for all 12 new types (Grouped Bar, Bullet, Step Line, Lollipop, Range Bar, Bump, Polar, Pie, Heatmap, Sparkline Grid, Progress Grid, Sankey) with realistic financial BI mock data.
Output: Updated galleryMockData.ts with 12 new exports, updated ComponentGallery.tsx with 12 new entries and imports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-finalization-gallery/RESEARCH.md

@src/pages/tools/ComponentGallery.tsx
@src/pages/tools/galleryMockData.ts

<interfaces>
<!-- The executor MUST read the actual component files at execution time to get exact prop interfaces.
     The names and approximate props below are based on research and ROADMAP requirements.
     Phase 34 components (chartType sub-variants — leaf components imported directly): -->

Wave 1 chartType sub-variants (Phase 34):
- GroupedBarChartComponent: title, height?, categories?, chartColors?
- BulletChartComponent: title, height?, chartColors? (check for items/value/target props)
- StepLineChartComponent: title, height?, categories?, chartColors?
- LollipopChartComponent: title, height?, categories?, chartColors?
- RangeBarComponent: title, height?, chartColors? (CSS-flex, check for items/rows props)
- BumpChartComponent: title, height?, categories?, chartColors?
- PolarChartComponent: title, height?, chartColors? (check for items prop)

Wave 2 standalone sections (Phase 35) — use the LEAF component, not the renderer:
- PieChartComponent (or PieChart): title, slices/data?, height?, chartColors?
- HeatmapComponent (from HeatmapRenderer or separate): title, xLabels, yLabels, cells
- SparklineGridComponent (from SparklineGridRenderer or separate): title, columns?, items
- ProgressGridComponent (from ProgressGridRenderer or separate): title, items

Wave 3 (Phase 36):
- SankeyComponent: title, nodes, links, height?, chartColors?

NOTE: Exact file names and prop interfaces may differ. The executor must `ls` the components
directory and read the actual files to determine the correct imports and props.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover actual component files and prop interfaces</name>
  <files></files>
  <action>
Before writing any code, discover the exact component files created by Phases 34-36:

1. List all files in `tools/wireframe-builder/components/` to find the 12 new component files
2. For each of the 12 new components, read the file to identify:
   - The exact export name (default vs named)
   - The exact props interface (required + optional props)
   - Any types that need to be imported for mock data
3. For standalone section types (Pie, Heatmap, Sparkline Grid, Progress Grid, Sankey), determine whether the gallery should import the leaf component directly or use the renderer. The gallery pattern is to import LEAF components (e.g., `DonutChart`, not `ChartRenderer`). If the standalone type has a separate presentational component, use that. If it only has a renderer, import the renderer.

Document the findings before proceeding to Task 2.
  </action>
  <verify>
    <automated>ls tools/wireframe-builder/components/ | grep -iE "grouped|bullet|step|lollipop|range|bump|polar|pie|heatmap|sparkline|progress-grid|sankey"</automated>
  </verify>
  <done>All 12 component files identified with exact export names, prop interfaces, and type imports</done>
</task>

<task type="auto">
  <name>Task 2: Add 12 mock data exports to galleryMockData.ts</name>
  <files>src/pages/tools/galleryMockData.ts</files>
  <action>
Add 12 new mock data exports to `galleryMockData.ts`. Each mock must use realistic Brazilian Portuguese financial/business labels and values (R$ currency format, Portuguese names, BR date format).

Required mock data (adjust prop names to match actual component interfaces from Task 1):

**Wave 1 — chartType sub-variants:**

1. `groupedBarChartMock` — title + any required props
   Context: "Receita: Realizado vs Meta" — 3 series across 6 months

2. `bulletChartMock` — title + value/target data
   Context: "Meta de Vendas" — current vs target

3. `stepLineChartMock` — title + any required props
   Context: "Evolucao de Preco do Produto" — 8 discrete price changes

4. `lollipopChartMock` — title + any required props
   Context: "Top Produtos por Receita" — 6-8 categories

5. `rangeBarMock` — title + items/rows with start/end
   Context: "Cronograma de Projetos" — 5 project phases with start/end

6. `bumpChartMock` — title + any required props
   Context: "Ranking de Vendedores" — 4 entities ranked across 6 months

7. `polarChartMock` — title + items
   Context: "Performance por Regiao" — 5-6 regions with values

**Wave 2 — standalone sections:**

8. `pieChartMock` — title + slices/data array
   Context: "Distribuicao de Receita por Produto" — 5 slices with label, value, pct

9. `heatmapMock` — title + xLabels + yLabels + cells matrix
   Context: "Vendas por Produto x Mes" — 5 products x 6 months

10. `sparklineGridMock` — title + items array (label, value, data[])
    Context: "Tendencias Executivas" — 6 KPIs with 12-point sparkline data

11. `progressGridMock` — title + items array (label, current, target, max)
    Context: "Metas do Trimestre" — 5 metrics with current/target/max

**Wave 3 — Sankey:**

12. `sankeyMock` — title + nodes[] + links[] with INTEGER indices
    Context: "Fluxo de Receita" — 6 nodes (Vendas Diretas, Parceiros, Online -> Produto A, Produto B, Servicos) + 8 links
    CRITICAL: links.source and links.target MUST be integer indices into nodes[], not string names

Import any required types from the component files (same pattern as existing imports like `WaterfallBar`, `CalculoRow`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>galleryMockData.ts has 12 new named exports with realistic BR financial mock data, and TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 3: Add 12 gallery entries to ComponentGallery.tsx</name>
  <files>src/pages/tools/ComponentGallery.tsx</files>
  <action>
Add imports and entries for all 12 new components to ComponentGallery.tsx.

**Step 1 — Add component imports:**
Add import statements for each of the 12 new leaf components from `@tools/wireframe-builder/components/`.

**Step 2 — Add mock data imports:**
Add the 12 new mock data imports to the existing import block from `./galleryMockData`.

**Step 3 — Add ComponentEntry objects to the categories array:**
Add all 12 entries to the `charts` category (id: 'charts', label: 'Graficos') in the `categories` array. Each entry follows this pattern:

```typescript
{
  name: 'ComponentDisplayName',
  status: 'available',
  props: ['prop1', 'prop2: Type', 'prop3?'],
  render: () => (
    <div className="rounded-lg border border-dashed border-border bg-wf-canvas p-4">
      <ComponentName {...mockData} />
    </div>
  ),
},
```

For components that benefit from a prop toggle (e.g., Bullet Chart might toggle between showing/hiding target), create a dedicated `[Name]Preview` function component with `PropsToolbar` and set `hasToolbar: true`. Most of the 12 types should use simple inline rendering since they are primarily display-only.

**Ordering:** Add the 12 new entries AFTER the existing chart entries (after CompositionBar) in the `charts` category. Group them by wave:
- Wave 1: GroupedBar, Bullet, StepLine, Lollipop, RangeBar, Bump, Polar
- Wave 2: PieChart, Heatmap, SparklineGrid, ProgressGrid
- Wave 3: Sankey

**Props list:** Include the key props for each entry in the `props` array, showing types where helpful (e.g., `'nodes: Node[]'`, `'links: Link[]'`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ComponentGallery has 12 new entries in the charts category, all rendering correctly with mock data, and TypeScript passes with zero errors</done>
</task>

<task type="auto">
  <name>Task 4: TypeScript full audit</name>
  <files></files>
  <action>
Run `npx tsc --noEmit` and fix any errors found. This is the final TypeScript gate for the entire v1.6 milestone.

Rules:
- Never use `any` as a workaround
- Use proper type imports and narrowing
- Fix ALL errors, not just gallery-related ones (project-wide zero-error policy)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>npx tsc --noEmit exits with code 0 and produces no output</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports zero errors across entire codebase
- ComponentGallery page shows all 12 new chart types in the Graficos category
- Each preview renders with realistic Brazilian Portuguese financial mock data
- Gallery component count increases by 12 (from ~29 to ~41 entries)
</verification>

<success_criteria>
- XCUT-04: ComponentGallery updated with preview and mock data for all 12 new types
- All 12 entries have status 'available' and a render function producing a visible preview
- Mock data uses realistic BR financial context (R$ values, Portuguese labels, BR dates)
- Zero TypeScript errors across entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/37-finalization-gallery/37-01-SUMMARY.md`
</output>
