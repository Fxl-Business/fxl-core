---
phase: 61-module-migration
plan: 06
type: execute
wave: 2
depends_on: [61-01, 61-02]
files_modified:
  - src/modules/wireframe/manifest.ts
  - src/modules/wireframe/pages/SharedWireframeView.tsx
  - src/modules/wireframe/pages/ComponentGallery.tsx
  - src/modules/wireframe/pages/galleryMockData.ts
  - src/modules/ferramentas/manifest.tsx
autonomous: true
requirements: [MOD-04]

must_haves:
  truths:
    - "SharedWireframeView page lives in src/modules/wireframe/pages/"
    - "ComponentGallery page lives in src/modules/wireframe/pages/"
    - "Wireframe module has its own manifest.ts (created new, replacing ferramentas manifest)"
    - "The ferramentas manifest is updated to become the wireframe manifest OR a new wireframe manifest is created"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/modules/wireframe/manifest.ts"
      provides: "Wireframe module manifest (replaces ferramentas)"
    - path: "src/modules/wireframe/pages/SharedWireframeView.tsx"
      provides: "Public wireframe viewer"
    - path: "src/modules/wireframe/pages/ComponentGallery.tsx"
      provides: "Component gallery viewer"
  key_links:
    - from: "src/modules/wireframe/manifest.ts"
      to: "src/modules/wireframe/pages/ComponentGallery.tsx"
      via: "import for routeConfig"
      pattern: "from.*ComponentGallery"
    - from: "src/App.tsx"
      to: "src/modules/wireframe/pages/SharedWireframeView.tsx"
      via: "lazy import"
      pattern: "import.*SharedWireframeView"
    - from: "src/platform/module-loader/registry.ts"
      to: "src/modules/wireframe/manifest.ts"
      via: "import wireframeManifest"
      pattern: "from.*wireframe/manifest"
---

<objective>
Create the wireframe module by migrating SharedWireframeView and ComponentGallery pages into src/modules/wireframe/pages/, creating a wireframe manifest that replaces the ferramentas manifest, and updating the module registry. The wireframe module is a hybrid: pages and manifest live in src/modules/wireframe/ but visual components stay in tools/wireframe-builder/ (imported via @tools/ alias).

Purpose: Establishes the wireframe module as a proper autocontido module with its own manifest and pages, while respecting the documented hybrid exception for builder components.
Output: Wireframe module with manifest + pages, ferramentas renamed, registry updated, tsc passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-module-migration/61-CONTEXT.md
@docs/superpowers/specs/2026-03-16-fxl-platform-evolution-design.md

<interfaces>
<!-- Design spec Section 4.2: wireframe module replaces ferramentas -->
<!-- Design spec Section 4.4: pages/tools/ComponentGallery.tsx -> modules/wireframe/pages/ -->
<!-- Design spec Section 4.4: pages/SharedWireframeView.tsx -> modules/wireframe/pages/ -->

From src/modules/ferramentas/manifest.tsx:
  Exports: ferramentasManifest (ModuleDefinition)
  Imported by: src/modules/registry.ts (now at platform/module-loader/)
  Uses MODULE_IDS.FERRAMENTAS — this ID is "ferramentas" in module-ids.ts

From src/pages/SharedWireframeView.tsx:
  Lazy-imported by: src/App.tsx
  `const SharedWireframeView = lazy(() => import('@/pages/SharedWireframeView'))`

From src/pages/tools/ComponentGallery.tsx:
  Imported by: src/modules/ferramentas/manifest.tsx for routeConfig
  `import ComponentGallery from '@/pages/tools/ComponentGallery'`
  Also imports: @/lib/utils (cn)

From src/pages/tools/galleryMockData.ts:
  Imported by: ComponentGallery.tsx (likely)

From src/modules/wireframe-builder/manifest.tsx:
  Separate manifest for wireframe-builder module
  Imports: ModuleManifest from @/modules/registry

Current MODULE_IDS includes FERRAMENTAS: 'ferramentas' — this stays as-is for now
(renaming FERRAMENTAS to WIREFRAME would be a behavioral change; v3.0 is zero functional change)

IMPORTANT: The design spec says "renomear ferramentas -> wireframe" but v3.0 is
zero functional change. The MODULE_IDS value stays 'ferramentas' to preserve URLs
and localStorage state. Only the directory/manifest name changes. The old ferramentas
manifest becomes the wireframe manifest. The wireframe-builder module under src/modules/
can be cleaned up (it was a scaffold from v2.0 with empty dirs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move wireframe pages and create wireframe manifest</name>
  <files>
    src/modules/wireframe/pages/SharedWireframeView.tsx
    src/modules/wireframe/pages/ComponentGallery.tsx
    src/modules/wireframe/pages/galleryMockData.ts
    src/modules/wireframe/manifest.ts
    src/modules/ferramentas/manifest.tsx
    src/platform/module-loader/registry.ts
    src/App.tsx
  </files>
  <read_first>
    src/pages/SharedWireframeView.tsx
    src/pages/tools/ComponentGallery.tsx
    src/pages/tools/galleryMockData.ts
    src/modules/ferramentas/manifest.tsx
    src/modules/wireframe-builder/manifest.tsx
    src/platform/module-loader/registry.ts
    src/App.tsx
  </read_first>
  <action>
**Step 1: Move pages into wireframe module:**
```
git mv src/pages/SharedWireframeView.tsx src/modules/wireframe/pages/SharedWireframeView.tsx
git mv src/pages/tools/ComponentGallery.tsx src/modules/wireframe/pages/ComponentGallery.tsx
git mv src/pages/tools/galleryMockData.ts src/modules/wireframe/pages/galleryMockData.ts
```

**Step 2: Update imports within moved files:**

In `src/modules/wireframe/pages/ComponentGallery.tsx`:
- `from '@/lib/utils'` -> `from '@shared/utils'` (if not already done by Plan 02)
- Any other @/components/ui/ or @/lib/ imports -> update to @shared/ or @platform/

In `src/modules/wireframe/pages/SharedWireframeView.tsx`:
- Check and update any @/components/ui/, @/lib/ imports to @shared/, @platform/

**Step 3: Move ferramentas manifest to wireframe module.**

The ferramentas manifest currently drives the "Ferramentas" sidebar entry with all the
wireframe-builder nav children. Move it to become the wireframe module's manifest:

```
git mv src/modules/ferramentas/manifest.tsx src/modules/wireframe/manifest.ts
```

Rename the file extension from .tsx to .ts only if it contains no JSX. If it contains JSX (like `<ComponentGallery />`), keep .tsx.

Actually, reading the current ferramentas/manifest.tsx — it imports ComponentGallery and uses it in routeConfig as `element: <ComponentGallery />`, so it IS JSX. Keep as .tsx:

```
git mv src/modules/ferramentas/manifest.tsx src/modules/wireframe/manifest.tsx
```

Then update the manifest's internal imports:
- `from '@/pages/tools/ComponentGallery'` -> `from './pages/ComponentGallery'`
- `from '@/modules/registry'` -> `from '@platform/module-loader/registry'` (if not already)
- `from '@/modules/module-ids'` -> `from '@platform/module-loader/module-ids'` (if not already)

Keep the export name as `ferramentasManifest` and the MODULE_IDS.FERRAMENTAS reference
to maintain zero functional change. (The registry still imports `ferramentasManifest` by name.)

**Step 4: Update the registry to import from new location.**

In `src/platform/module-loader/registry.ts`:
- Change `from '@modules/ferramentas/manifest'` (or `'./ferramentas/manifest'` or whatever it currently says) to `from '@modules/wireframe/manifest'`

The import line should become: `import { ferramentasManifest } from '@modules/wireframe/manifest'`

**Step 5: Update App.tsx lazy import for SharedWireframeView:**

In `src/App.tsx`:
- `const SharedWireframeView = lazy(() => import('@/pages/SharedWireframeView'))` ->
  `const SharedWireframeView = lazy(() => import('@modules/wireframe/pages/SharedWireframeView'))`

**Step 6: Clean up old directories.**

- Remove the now-empty `src/modules/ferramentas/` directory (all its content moved to wireframe)
- Remove the `src/modules/wireframe-builder/` directory (it was a v2.0 scaffold with empty subdirs and a manifest.tsx that is now superseded by wireframe/manifest.tsx). Check if wireframe-builder/manifest.tsx is imported anywhere — if yes, consolidate. If it's only imported by the registry (which now imports from wireframe/manifest), remove it.

Actually, check the registry.ts more carefully. It currently imports:
- `docsManifest` from `./docs/manifest`
- `ferramentasManifest` from `./ferramentas/manifest`
- `clientsManifest` from `./clients/manifest`
- `knowledgeBaseManifest` from `./knowledge-base/manifest`
- `tasksManifest` from `./tasks/manifest`

The wireframe-builder manifest is NOT in the registry. It exists separately. So we can safely remove the entire src/modules/wireframe-builder/ directory.

```
git rm -r src/modules/wireframe-builder/
```

Also remove src/pages/tools/ directory if empty after ComponentGallery and galleryMockData are moved.

**Step 7: Remove .gitkeep files from directories that now have real files.**

Remove from src/modules/wireframe/pages/ and src/modules/wireframe/components/ etc.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/modules/wireframe/manifest.tsx` succeeds (wireframe has its own manifest)
    - `ls src/modules/wireframe/pages/SharedWireframeView.tsx` succeeds
    - `ls src/modules/wireframe/pages/ComponentGallery.tsx` succeeds
    - `test ! -d src/modules/ferramentas` succeeds (old directory removed)
    - `test ! -d src/modules/wireframe-builder` succeeds (scaffold directory removed)
    - `test ! -f src/pages/SharedWireframeView.tsx` succeeds (file moved)
    - `test ! -f src/pages/tools/ComponentGallery.tsx` succeeds (file moved)
    - `grep -r "from '@/pages/SharedWireframeView'" src/` returns zero matches
    - `grep -r "from '@/pages/tools/" src/` returns zero matches
    - `grep "ferramentas/manifest" src/platform/module-loader/registry.ts` returns zero matches (updated to wireframe)
    - `grep "wireframe/manifest" src/platform/module-loader/registry.ts` returns one match
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Wireframe module has manifest, SharedWireframeView, and ComponentGallery. Old ferramentas/ and wireframe-builder/ directories removed. Registry imports from wireframe/manifest. App.tsx lazy-imports SharedWireframeView from wireframe module. tsc passes.</done>
</task>

</tasks>

<verification>
```bash
# Verify wireframe module structure
ls src/modules/wireframe/manifest.tsx
ls src/modules/wireframe/pages/SharedWireframeView.tsx
ls src/modules/wireframe/pages/ComponentGallery.tsx

# Verify old dirs gone
test ! -d src/modules/ferramentas && echo "ferramentas gone"
test ! -d src/modules/wireframe-builder && echo "wireframe-builder gone"

# Verify registry updated
grep "wireframe/manifest" src/platform/module-loader/registry.ts

# TypeScript check
npx tsc --noEmit
```
</verification>

<success_criteria>
- Wireframe module self-contained with manifest + pages
- Old ferramentas/ and wireframe-builder/ directories removed
- Registry and App.tsx updated to new paths
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/61-module-migration/61-06-SUMMARY.md`
</output>
