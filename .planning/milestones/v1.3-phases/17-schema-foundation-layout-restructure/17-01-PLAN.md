---
phase: 17-schema-foundation-layout-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/styles/wireframe-tokens.css
  - tailwind.config.ts
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - "Card, table, form, and section borders in the wireframe viewer display using the soft stone gray color (--wf-neutral-200 in light, --wf-neutral-700 in dark), not browser default black or transparent"
    - "The token --wf-border resolves to the same value as --wf-card-border in both light and dark modes"
  artifacts:
    - path: "tools/wireframe-builder/styles/wireframe-tokens.css"
      provides: "--wf-border alias in both [data-wf-theme='light'] and [data-wf-theme='dark'] blocks"
      contains: "--wf-border: var(--wf-card-border)"
    - path: "tailwind.config.ts"
      provides: "wf.border Tailwind color token"
      contains: "border: 'var(--wf-border)'"
  key_links:
    - from: "StatCardRenderer.tsx, SettingsPageRenderer.tsx, FormSectionRenderer.tsx, ProgressBarRenderer.tsx, FilterConfigRenderer.tsx, DividerRenderer.tsx"
      to: "wireframe-tokens.css [data-wf-theme] blocks"
      via: "var(--wf-border) CSS custom property"
      pattern: "var\\(--wf-border\\)"
---

<objective>
Fix the missing `--wf-border` CSS token that causes six wireframe section renderers to display harsh black or transparent borders instead of the designed soft stone gray.

Purpose: Six section renderers added in v1.1 reference `var(--wf-border)` which is never defined in `wireframe-tokens.css`. The correct token is `--wf-card-border`. Adding a one-line alias in both theme blocks resolves all six components without touching component files.

Output: `wireframe-tokens.css` with `--wf-border` alias in both light and dark blocks. `tailwind.config.ts` updated with `wf.border` Tailwind color for Tailwind consumers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-schema-foundation-layout-restructure/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --wf-border alias to CSS token file and Tailwind config</name>
  <files>tools/wireframe-builder/styles/wireframe-tokens.css, tailwind.config.ts</files>
  <action>
In `tools/wireframe-builder/styles/wireframe-tokens.css`:

1. In the `[data-wf-theme="light"]` block, add after the `--wf-card-border` line:
   ```css
   --wf-border: var(--wf-card-border);
   ```
   Exact placement: after `--wf-card-border: var(--wf-neutral-200);` in the "Semantic mapping" section.

2. In the `[data-wf-theme="dark"]` block, add after the `--wf-card-border` line:
   ```css
   --wf-border: var(--wf-card-border);
   ```
   Exact placement: after `--wf-card-border: var(--wf-neutral-700);` in the dark mode block.

In `tailwind.config.ts`:

3. In the `wf:` color map (around line 90 before the closing `}`), add:
   ```ts
   border: 'var(--wf-border)',
   ```
   Place it after the last direct string value (after `'table-border': 'var(--wf-table-border)',`) and before the closing `}` of the `wf` block.

Do NOT change any existing token definitions. Do NOT touch component files. The token alias pattern is the correct fix — do not add inline styles to the six affected components.

Six components that will now render correctly (no code changes needed in them):
- `StatCardRenderer.tsx` — `var(--wf-border)` on card border
- `SettingsPageRenderer.tsx` — `var(--wf-border)` on card border + row dividers
- `FormSectionRenderer.tsx` — `var(--wf-border)` on card border + input borders
- `ProgressBarRenderer.tsx` — `var(--wf-border)` on card border + track background
- `FilterConfigRenderer.tsx` — `var(--wf-border)` on row borders + input borders
- `DividerRenderer.tsx` — `var(--wf-border)` on divider line color
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    `wireframe-tokens.css` contains `--wf-border: var(--wf-card-border)` in both the light and dark theme blocks. `tailwind.config.ts` contains `border: 'var(--wf-border)'` in the `wf` color map. TypeScript check passes with zero errors.

    Visual confirmation (open wireframe viewer in browser): card borders on StatCard, Settings, Form, ProgressBar, FilterConfig, and Divider sections should display as soft stone gray, not black or invisible.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` returns zero errors
- `wireframe-tokens.css` contains `--wf-border: var(--wf-card-border)` twice (once in each theme block)
- `tailwind.config.ts` contains `border: 'var(--wf-border)'` in the `wf:` color map
</verification>

<success_criteria>
The `--wf-border` token resolves in both light and dark wireframe themes. All six section renderers that reference `var(--wf-border)` display soft stone gray borders consistent with the rest of the wireframe design system. No component files were modified.
</success_criteria>

<output>
After completion, create `.planning/phases/17-schema-foundation-layout-restructure/17-01-SUMMARY.md`
</output>
