---
phase: 19-filter-bar-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified: []
autonomous: false
requirements:
  - FILT-02
  - FILT-03
  - FILT-04
  - FILT-05
  - FILT-06

must_haves:
  truths:
    - "Date range picker opens a panel with calendar inputs and preset buttons when clicked"
    - "Multi-select dropdown opens a checkbox list when clicked"
    - "Search filter shows an inline text input with magnifying glass icon"
    - "Toggle filter shows an animated switch that changes state on click"
    - "Existing filters without filterType still render as a plain select"
  artifacts:
    - path: "tools/wireframe-builder/components/WireframeFilterBar.tsx"
      provides: "All filter type widgets visually verified"
  key_links:
    - from: "Browser"
      to: "WireframeFilterBar"
      via: "Blueprint config with each filterType value"
      pattern: "visual inspection in localhost:5173"
---

<objective>
Human visual verification of all five filter type renderers in the wireframe viewer.

Purpose: Unit tests confirm schema correctness; this checkpoint confirms the visual fidelity of each filter widget in the real browser — calendar panel opens/closes, chips display, search input is visible, toggle animates.

Output: Human-approved sign-off that all filter types render correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-filter-bar-expansion/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verify all filter type renderers</name>
  <action>Human verifies that each filterType renders the correct widget in the wireframe viewer browser.</action>
  <what-built>
Five new filter type renderers in WireframeFilterBar (date-range with calendar panel and preset buttons, multi-select with chip tokens and checkbox dropdown, search with inline text input, toggle switch, and unchanged select fallback). All implemented in Plan 01.
  </what-built>
  <how-to-verify>
1. Run `make dev` to start the development server at http://localhost:5173

2. Open a wireframe that uses filter bars, OR temporarily add test filters to any blueprint config. The easiest path is to open `clients/[any-client]/wireframe/` blueprint and temporarily add these to the filters array:

   ```json
   { "key": "periodo", "label": "Período", "filterType": "date-range" }
   { "key": "categoria", "label": "Categoria", "filterType": "multi-select", "options": ["Operacional", "Financeiro", "Comercial", "RH"] }
   { "key": "busca", "label": "Buscar", "filterType": "search" }
   { "key": "ativo", "label": "Ativo", "filterType": "toggle" }
   { "key": "status", "label": "Status", "options": ["Todos", "Aberto", "Fechado"] }
   ```

3. Verify each filter renders correctly:

   **date-range (FILT-02 + FILT-05):**
   - Shows a button with calendar icon, date range text, and chevron
   - Click the button → panel opens below with: 5 preset buttons (Últimos 7 dias, Últimos 30 dias, Mês anterior, YTD, Último ano) and two disabled date inputs
   - Click again → panel closes

   **multi-select (FILT-03):**
   - Shows first 2 options as colored chip tokens
   - If more than 2 options: shows "+N" counter
   - Click → dropdown opens showing all options with checkbox indicators

   **search (FILT-04):**
   - Shows the label then an inline input box with magnifying glass icon
   - Input is disabled (wireframe mock) but visually present

   **toggle (FILT-06):**
   - Shows the filter label followed by a switch
   - Click switch → it animates ON (accent color) / OFF (gray)

   **select fallback (backward compat):**
   - The filter with no filterType renders as a plain `<select>` as before

4. Confirm the filter bar layout still looks correct — filters wrap cleanly, Comparar toggle still works if present
  </how-to-verify>
  <verify>Human visual confirmation in browser</verify>
  <done>All five filter type widgets render correctly and the filter bar layout is intact.</done>
  <resume-signal>Type "approved" if all filter types render correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval of all five filter type visual behaviors.
</verification>

<success_criteria>
- date-range filter opens calendar panel with preset buttons on click
- multi-select filter shows chips and opens dropdown with checkbox list
- search filter shows inline text input with Search icon
- toggle filter shows animated switch
- plain select filter still renders for filters without filterType
- Filter bar layout remains clean and wrapped correctly
</success_criteria>

<output>
After completion, create `.planning/phases/19-filter-bar-expansion/19-02-SUMMARY.md`
</output>
