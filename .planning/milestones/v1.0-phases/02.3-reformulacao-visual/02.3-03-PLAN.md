---
phase: 02.3-reformulacao-visual
plan: 03
type: execute
wave: 2
depends_on:
  - 02.3-01
files_modified:
  - src/pages/Home.tsx
  - src/pages/DocRenderer.tsx
  - src/pages/Login.tsx
  - src/pages/Profile.tsx
  - src/App.tsx
  - src/pages/docs/ProcessDocsViewer.tsx
autonomous: true
requirements:
  - VISUAL-02

must_haves:
  truths:
    - "Home page renders with semantic tokens and no stale content (Referencias->Padroes, updated href and description)"
    - "All core pages render correctly in both light and dark modes"
    - "No hardcoded fxl-navy, fxl-blue, slate-*, or gray-* colors remain in core pages"
    - "Login and Profile pages use bg-background instead of bg-gray-50"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Theme-aware home page with corrected stale content"
    - path: "src/pages/DocRenderer.tsx"
      provides: "Theme-aware doc renderer"
    - path: "src/pages/Login.tsx"
      provides: "Theme-aware login page"
    - path: "src/pages/Profile.tsx"
      provides: "Theme-aware profile page"
    - path: "src/App.tsx"
      provides: "Theme-aware app shell (signup bg)"
    - path: "src/pages/docs/ProcessDocsViewer.tsx"
      provides: "Theme-aware process docs viewer"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/styles/globals.css"
      via: "Uses semantic tokens from CSS variables"
      pattern: "text-foreground|bg-card|border-border"
    - from: "src/pages/Home.tsx"
      to: "src/pages/Home.tsx sections array"
      via: "Stale content fix: Referencias badge and href updated"
      pattern: "Padroes|ferramentas/blocos"
---

<objective>
Migrate all core application pages (Home, DocRenderer, Login, Profile, App shell, ProcessDocsViewer) from hardcoded colors to semantic tokens. Fix stale content on Home page per CONTEXT.md decision.

Purpose: These pages are the main navigation surfaces users interact with. After Plan 01 establishes the design tokens, these pages must use them for consistent light/dark mode rendering.
Output: All 6 core page files migrated to semantic tokens, Home page stale content corrected.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.3-reformulacao-visual/02.3-CONTEXT.md
@.planning/phases/02.3-reformulacao-visual/02.3-RESEARCH.md
@.planning/phases/02.3-reformulacao-visual/02.3-01-SUMMARY.md

Before making ANY visual changes, read and follow the rules from these skills:
- frontend-design (production-grade design quality)
- shadcn (correct shadcn/ui component usage)
- ui-ux-pro-max (palette, typography, design decisions)
- web-design-guidelines (accessibility, UI best practices)

<interfaces>
<!-- Semantic token mapping from Plan 01 -->
Migration pattern for pages:
- text-slate-900 -> text-foreground
- text-slate-500 -> text-muted-foreground
- text-slate-400 -> text-muted-foreground (or text-muted-foreground/70 for lighter)
- bg-slate-100 -> bg-muted
- bg-slate-200 -> bg-muted
- border-slate-200 -> border-border
- border-slate-400 -> border-border
- bg-white -> bg-card (for cards) or bg-background (for page bg)
- bg-gray-50 -> bg-background
- hover:border-fxl-navy -> hover:border-primary or hover:border-accent
- text-blue-700 -> text-primary (for links/hover states)
- fxl-navy -> primary or accent (context-dependent)

Home.tsx stale content (from CONTEXT.md + RESEARCH.md):
- sections[2].badge: "Referencias" -> "Padroes"
- sections[2].href: "/referencias/biblioteca-kpis" -> "/padroes/index" (or current Padroes landing)
- sections[2].description: update to reflect current Padroes content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Home page with stale content fix</name>
  <files>src/pages/Home.tsx</files>
  <action>
Home.tsx has 18x slate references, 1x fxl-navy, and stale content. Two concerns in one file.

**Color migration** (systematic replacement):
- `text-slate-900` -> `text-foreground` (all heading text)
- `text-slate-500` -> `text-muted-foreground` (all body/description text)
- `text-slate-400` -> `text-muted-foreground` (section headers -- they currently use uppercase + tracking-wide, keep those)
- `bg-slate-100` -> `bg-muted` (icon backgrounds, badge backgrounds)
- `text-slate-600` -> `text-muted-foreground` (icon colors)
- `border-slate-200` -> `border-border` (card borders)
- `border-slate-400` -> `border-border` (hover states on doc cards)
- `bg-white` -> `bg-card` (card backgrounds)
- `hover:border-fxl-navy` -> `hover:border-primary` or `hover:border-accent` (quick action hover)
- `text-blue-700` -> `text-primary` (client name hover color)
- `bg-green-50`, `text-green-700` -> KEEP (semantic status color for "Em andamento")

**Stale content fix** (per CONTEXT.md: "Claude identifies stale content during visual pass and fixes while restyling"):
- `sections` array, third item:
  - `badge`: `'Referencias'` -> `'Padroes'`
  - `title`: keep or update to reflect Padroes content
  - `description`: `'KPIs, blocos de wireframe e repertorio para acelerar a execucao.'` -> update to describe the Padroes section accurately (tech radar, premissas, seguranca, testes)
  - `href`: `'/referencias/biblioteca-kpis'` -> `'/padroes/index'`

**Design polish** (Claude's discretion):
- Evaluate card shadows, hover transitions, and spacing per design skills
- Consider whether cards need bg-card with subtle border or a different treatment for dark mode contrast

  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit && grep -n "fxl-navy\|fxl-blue\|text-slate-\|bg-slate-\|border-slate-\|bg-white\|bg-gray-\|text-blue-700\|Referencias\|biblioteca-kpis" src/pages/Home.tsx; echo "Exit: $?"</automated>
  </verify>
  <done>Home.tsx uses only semantic tokens. Stale content corrected: "Referencias" -> "Padroes", href updated, description updated. Zero hardcoded color references (except semantic status colors). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining core pages (DocRenderer, Login, Profile, App, ProcessDocsViewer)</name>
  <files>src/pages/DocRenderer.tsx, src/pages/Login.tsx, src/pages/Profile.tsx, src/App.tsx, src/pages/docs/ProcessDocsViewer.tsx</files>
  <action>
Each of these files has a small number of hardcoded color references. Migrate systematically:

**DocRenderer.tsx** (2x slate):
- `text-slate-*` references -> `text-foreground` or `text-muted-foreground` as appropriate (likely in 404/not-found state text)

**Login.tsx** (1x gray):
- `bg-gray-50` -> `bg-background` (full-screen login page background)
- Verify Clerk's `<SignIn>` component still renders correctly with new CSS variables (per RESEARCH.md Pitfall 2: Clerk reads standard CSS variables via `@clerk/ui/themes/shadcn.css`)

**Profile.tsx** (1x gray):
- `bg-gray-50` -> `bg-background` (full-screen profile page background)
- Same Clerk verification as Login

**App.tsx** (1x gray):
- `bg-gray-50` -> `bg-background` (signup route background)

**ProcessDocsViewer.tsx** (fxl-navy references if any from audit):
- Check for any fxl-navy or slate references and migrate to semantic tokens
- This file may already use semantic tokens -- verify and only change if needed

For all files: ensure no `bg-white` remains where `bg-card` or `bg-background` is appropriate for dark mode support.

  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/fxl-core && npx tsc --noEmit && grep -rn "fxl-navy\|fxl-blue\|text-slate-\|bg-slate-\|bg-gray-\|text-gray-" src/pages/DocRenderer.tsx src/pages/Login.tsx src/pages/Profile.tsx src/App.tsx src/pages/docs/ProcessDocsViewer.tsx; echo "Exit: $?"</automated>
  </verify>
  <done>DocRenderer, Login, Profile, App.tsx, and ProcessDocsViewer use only semantic tokens. bg-gray-50 replaced with bg-background everywhere. Clerk components verified to render with new theme. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` -- zero errors
2. `grep -r "fxl-navy\|fxl-blue\|bg-gray-\|text-gray-" src/pages/Home.tsx src/pages/DocRenderer.tsx src/pages/Login.tsx src/pages/Profile.tsx src/App.tsx src/pages/docs/` -- zero results
3. Home.tsx contains "Padroes" badge and "/padroes/index" href (stale content fixed)
4. All core pages use bg-background or bg-card instead of bg-white/bg-gray-50
</verification>

<success_criteria>
- Home page has corrected stale content (Padroes instead of Referencias)
- All 6 core page files use semantic tokens exclusively
- Login and Profile pages work correctly with Clerk components under new theme
- TypeScript compiles with zero errors
- Zero hardcoded color references in core pages (except semantic status colors)
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-reformulacao-visual/02.3-03-SUMMARY.md`
</output>
