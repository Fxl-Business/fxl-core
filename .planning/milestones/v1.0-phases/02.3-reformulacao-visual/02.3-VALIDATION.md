---
phase: 02.3
slug: reformulacao-visual
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-08
---

# Phase 02.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None -- TypeScript compiler as validation gate |
| **Config file** | tsconfig.json |
| **Quick run command** | `npx tsc --noEmit` |
| **Full suite command** | `npx tsc --noEmit && grep -r "text-fxl-\|bg-fxl-" src/ --include="*.tsx" --include="*.ts"` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit`
- **After every plan wave:** Run full suite command (tsc + grep for stale colors)
- **Before `/gsd:verify-work`:** Full suite must be green + visual inspection
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.3-01-01 | 01 | 1 | VISUAL-01 | compile | `npx tsc --noEmit` | N/A | pending |
| 02.3-01-02 | 01 | 1 | VISUAL-01 | grep | `grep -r "fxl-navy\|fxl-blue" src/` returns empty | N/A | pending |
| 02.3-02-01 | 02 | 2 | VISUAL-02 | compile | `npx tsc --noEmit` | N/A | pending |
| 02.3-02-02 | 02 | 2 | VISUAL-02 | grep | `grep -r "text-fxl-\|bg-fxl-" src/` returns empty | N/A | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No test framework installation needed for a visual-only phase.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Design system CSS variables correct for light + dark | VISUAL-01 | Visual quality is subjective | Toggle dark mode in browser, verify palette consistency |
| All pages have production-grade visual quality | VISUAL-02 | Visual quality is subjective | Navigate all pages in both modes, check consistency |
| Dark mode toggle works | VISUAL-02 | Requires browser interaction | Click toggle, verify all pages switch cleanly |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
