---
phase: 02.2-evolucao-de-blocos-disponiveis
plan: 03
type: execute
wave: 2
depends_on:
  - 02.2-01
  - 02.2-02
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/pages/tools/ComponentGallery.tsx
  - tools/wireframe-builder/SKILL.md
  - docs/ferramentas/wireframe-builder.md
  - docs/ferramentas/index.md
  - docs/processo/onboarding.md
  - docs/processo/prompts.md
  - docs/processo/fases/fase1.md
  - docs/processo/fases/fase2.md
  - CLAUDE.md
  - docs/referencias/blocos-disponiveis.md
autonomous: true
requirements:
  - BLKSPEC-01
  - BLKSPEC-02

must_haves:
  truths:
    - "Sidebar shows collapsible Blocos Disponiveis group with 22 spec page links"
    - "Gallery ComponentCard links to corresponding spec page"
    - "SKILL.md enforces sync convention between specs and gallery"
    - "No references to old /referencias/blocos-disponiveis path remain in src/ or docs/"
    - "Old blocos-disponiveis.md file is deleted"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Navigation with collapsible Blocos Disponiveis group under Wireframe Builder"
      contains: "ferramentas/blocos/"
    - path: "src/pages/tools/ComponentGallery.tsx"
      provides: "ComponentCard with specHref linking to spec pages"
      contains: "specHref"
    - path: "tools/wireframe-builder/SKILL.md"
      provides: "Sync convention rule for spec-gallery maintenance"
      contains: "spec"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "docs/ferramentas/blocos/*.md"
      via: "href paths in navigation array"
      pattern: "/ferramentas/blocos/"
    - from: "src/pages/tools/ComponentGallery.tsx"
      to: "docs/ferramentas/blocos/*.md"
      via: "specHref on ComponentEntry"
      pattern: "specHref.*ferramentas/blocos"
    - from: "tools/wireframe-builder/SKILL.md"
      to: "docs/ferramentas/blocos/"
      via: "sync convention rule text"
      pattern: "docs/ferramentas/blocos"
---

<objective>
Wire spec pages into the application: update Sidebar navigation, add gallery cross-links, update SKILL.md, migrate all references from old path, and delete the old file.

Purpose: Make the new spec system navigable and discoverable. Without this plan, the 23 spec files created by Plans 01+02 would be invisible (no sidebar links) and disconnected from the gallery. Also ensures no broken links remain after removing the old file.

Output: Updated Sidebar with 22 spec children, Gallery with spec links, SKILL.md with sync rule, zero broken references
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-RESEARCH.md

@src/components/layout/Sidebar.tsx
@src/pages/tools/ComponentGallery.tsx
@tools/wireframe-builder/SKILL.md

<interfaces>
<!-- Current Sidebar navigation structure (Ferramentas section) -->
```typescript
{
  label: 'Ferramentas',
  href: '/ferramentas/index',
  children: [
    {
      label: 'Wireframe Builder',
      href: '/ferramentas/wireframe-builder',
      children: [
        { label: 'Blocos Disponiveis', href: '/referencias/blocos-disponiveis' },
        { label: 'Galeria de Componentes', href: '/ferramentas/wireframe-builder/galeria' },
      ],
    },
  ],
}
```

<!-- Current ComponentEntry type -->
```typescript
type ComponentEntry = {
  name: string
  status: ComponentStatus
  description?: string
  props?: string[]
  render?: () => React.ReactNode
  hasToolbar?: boolean
}
```

<!-- Files referencing old path (from codebase grep) -->
Non-planning files with `/referencias/blocos-disponiveis`:
1. CLAUDE.md (line 45)
2. docs/ferramentas/wireframe-builder.md (line 14)
3. docs/ferramentas/index.md (line 12)
4. docs/processo/onboarding.md (line 115)
5. src/components/layout/Sidebar.tsx (line 76)
6. docs/processo/prompts.md (line 111)
7. docs/processo/fases/fase2.md (lines 19, 33, 111, 119)
8. docs/processo/fases/fase1.md (lines 22, 29)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sidebar, Gallery, and SKILL.md</name>
  <files>
    src/components/layout/Sidebar.tsx
    src/pages/tools/ComponentGallery.tsx
    tools/wireframe-builder/SKILL.md
  </files>
  <action>
    **Sidebar.tsx** — Replace the single "Blocos Disponiveis" link under Wireframe Builder with a collapsible parent + 22 children. The new structure:

    ```typescript
    {
      label: 'Wireframe Builder',
      href: '/ferramentas/wireframe-builder',
      children: [
        {
          label: 'Blocos Disponiveis',
          href: '/ferramentas/blocos/index',
          children: [
            { label: 'KpiCard', href: '/ferramentas/blocos/kpi-card' },
            { label: 'KpiCardFull', href: '/ferramentas/blocos/kpi-card-full' },
            { label: 'CalculoCard', href: '/ferramentas/blocos/calculo-card' },
            { label: 'BarLineChart', href: '/ferramentas/blocos/bar-line-chart' },
            { label: 'WaterfallChart', href: '/ferramentas/blocos/waterfall-chart' },
            { label: 'DonutChart', href: '/ferramentas/blocos/donut-chart' },
            { label: 'ParetoChart', href: '/ferramentas/blocos/pareto-chart' },
            { label: 'DataTable', href: '/ferramentas/blocos/data-table' },
            { label: 'DrillDownTable', href: '/ferramentas/blocos/drill-down-table' },
            { label: 'ClickableTable', href: '/ferramentas/blocos/clickable-table' },
            { label: 'ConfigTable', href: '/ferramentas/blocos/config-table' },
            { label: 'WireframeSidebar', href: '/ferramentas/blocos/wireframe-sidebar' },
            { label: 'WireframeHeader', href: '/ferramentas/blocos/wireframe-header' },
            { label: 'WireframeFilterBar', href: '/ferramentas/blocos/wireframe-filter-bar' },
            { label: 'GlobalFilters', href: '/ferramentas/blocos/global-filters' },
            { label: 'WireframeModal', href: '/ferramentas/blocos/wireframe-modal' },
            { label: 'DetailViewSwitcher', href: '/ferramentas/blocos/detail-view-switcher' },
            { label: 'CommentOverlay', href: '/ferramentas/blocos/comment-overlay' },
            { label: 'InputsScreen', href: '/ferramentas/blocos/inputs-screen' },
            { label: 'UploadSection', href: '/ferramentas/blocos/upload-section' },
            { label: 'ManualInputSection', href: '/ferramentas/blocos/manual-input-section' },
            { label: 'SaldoBancoInput', href: '/ferramentas/blocos/saldo-banco-input' },
          ],
        },
        { label: 'Galeria de Componentes', href: '/ferramentas/wireframe-builder/galeria' },
      ],
    }
    ```

    Order: Cards first (KpiCard, KpiCardFull, CalculoCard), then Charts (BarLineChart, WaterfallChart, DonutChart, ParetoChart), then Tables (DataTable, DrillDownTable, ClickableTable, ConfigTable), then Layout (WireframeSidebar, WireframeHeader, WireframeFilterBar, GlobalFilters, WireframeModal, DetailViewSwitcher, CommentOverlay), then Inputs (InputsScreen, UploadSection, ManualInputSection, SaldoBancoInput). This mirrors the gallery category order.

    CRITICAL: verify each href matches the corresponding file name exactly. `/ferramentas/blocos/kpi-card` must match `docs/ferramentas/blocos/kpi-card.md`.

    **ComponentGallery.tsx** — Two changes:

    1. Add `specHref?: string` to the `ComponentEntry` type:
    ```typescript
    type ComponentEntry = {
      name: string
      status: ComponentStatus
      description?: string
      props?: string[]
      render?: () => React.ReactNode
      hasToolbar?: boolean
      specHref?: string  // link to spec page
    }
    ```

    2. Add `specHref` to every component entry in the `categories` array. Map component name to slug:
    - KpiCard -> `/ferramentas/blocos/kpi-card`
    - KpiCardFull -> `/ferramentas/blocos/kpi-card-full`
    - CalculoCard -> `/ferramentas/blocos/calculo-card`
    - etc. (all 22 components)

    3. In the `ComponentCard` function, add the cross-link after the props section (inside the `expanded` conditional block, after the props div):
    ```tsx
    {entry.specHref && (
      <a
        href={entry.specHref}
        className="inline-flex items-center gap-1 text-[11px] font-medium text-blue-600 hover:underline"
      >
        Ver spec completa
      </a>
    )}
    ```

    **SKILL.md** — Add a new rule at the end of the Regras section:

    ```markdown
    - Ao criar ou alterar um componente, atualizar AMBOS: a spec em `docs/ferramentas/blocos/[nome].md` E a entrada na galeria (`src/pages/tools/ComponentGallery.tsx`). Spec e galeria devem sempre refletir as mesmas capabilities.
    ```

    Also add a reference to the new spec location in the "Componentes disponiveis" section header:
    ```markdown
    ## Componentes disponiveis

    Todos os componentes usam dados ficticios por padrao, configuraveis via props.
    Nenhum componente contem dados reais.
    Specs completas: `docs/ferramentas/blocos/` ([ver no app](/ferramentas/blocos/index))
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - Sidebar shows collapsible "Blocos Disponiveis" parent with 22 children under Wireframe Builder
    - ComponentEntry type has specHref field
    - All 22 gallery entries have specHref pointing to correct spec page
    - ComponentCard renders "Ver spec completa" link when specHref is present
    - SKILL.md has sync convention rule
    - `npx tsc --noEmit` passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate references and delete old file</name>
  <files>
    docs/ferramentas/wireframe-builder.md
    docs/ferramentas/index.md
    docs/processo/onboarding.md
    docs/processo/prompts.md
    docs/processo/fases/fase1.md
    docs/processo/fases/fase2.md
    CLAUDE.md
    docs/referencias/blocos-disponiveis.md
  </files>
  <action>
    Update all references from the old path `/referencias/blocos-disponiveis` to the new path `/ferramentas/blocos/index` across the codebase. Then delete the old file.

    **docs/ferramentas/wireframe-builder.md** (line 14):
    - Change phase-card href from `/referencias/blocos-disponiveis` to `/ferramentas/blocos/index`

    **docs/ferramentas/index.md** (line 12):
    - Change phase-card href from `/referencias/blocos-disponiveis` to `/ferramentas/blocos/index`

    **docs/processo/onboarding.md** (line 115):
    - Change link from `[Blocos Disponiveis](/referencias/blocos-disponiveis)` to `[Blocos Disponiveis](/ferramentas/blocos/index)`

    **docs/processo/prompts.md** (line 111):
    - Change reference from `docs/referencias/blocos-disponiveis.md` to `docs/ferramentas/blocos/index.md`

    **docs/processo/fases/fase1.md** (lines 22, 29):
    - Change links from `/referencias/blocos-disponiveis` to `/ferramentas/blocos/index`

    **docs/processo/fases/fase2.md** (lines 19, 33, 111, 119):
    - Change all 4 references from `/referencias/blocos-disponiveis` to `/ferramentas/blocos/index`

    **CLAUDE.md** (line 45):
    - Change `docs/referencias/blocos-disponiveis.md` to `docs/ferramentas/blocos/index.md`

    **Delete old file:**
    - `rm docs/referencias/blocos-disponiveis.md`
    - After deletion, check if `docs/referencias/` directory is now empty. If empty, leave it (other phases may use it, or it may contain other files).

    **Verification after all changes:**
    - Run `grep -r "referencias/blocos-disponiveis" src/ docs/ CLAUDE.md tools/` to confirm zero results
    - The only remaining references should be in `.planning/` files (which are historical and not user-facing)
  </action>
  <verify>
    <automated>test ! -f docs/referencias/blocos-disponiveis.md && grep -rL "referencias/blocos-disponiveis" src/ docs/ CLAUDE.md tools/ > /dev/null 2>&1 && echo "Old file deleted and no broken references in source"</automated>
  </verify>
  <done>
    - Old `docs/referencias/blocos-disponiveis.md` is deleted
    - All 8 source files updated with new path `/ferramentas/blocos/index`
    - Zero results from `grep -r "referencias/blocos-disponiveis" src/ docs/ CLAUDE.md tools/`
    - `npx tsc --noEmit` still passes
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (TypeScript check after ComponentGallery changes)
- `test ! -f docs/referencias/blocos-disponiveis.md` (old file deleted)
- `grep -rc "referencias/blocos-disponiveis" src/ docs/ CLAUDE.md tools/ | grep -v ':0$'` returns nothing (no broken references)
- `ls docs/ferramentas/blocos/*.md | wc -l` returns 23 (1 index + 22 specs -- from Plans 01+02)
- `grep -c "specHref" src/pages/tools/ComponentGallery.tsx` returns at least 22 (one per component entry)
- `grep -c "ferramentas/blocos/" src/components/layout/Sidebar.tsx` returns at least 22 (one per sidebar entry)
</verification>

<success_criteria>
- Sidebar has collapsible Blocos Disponiveis group with 22 navigable children
- Gallery has "Ver spec completa" link on every component card
- SKILL.md enforces spec-gallery sync convention
- Zero broken references to old /referencias/blocos-disponiveis path
- Old file deleted
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-evolucao-de-blocos-disponiveis/02.2-03-SUMMARY.md`
</output>
