---
phase: 58-preview-rendering-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wireframe-builder/lib/section-registry.tsx
  - tools/wireframe-builder/lib/section-registry.test.ts
autonomous: true
requirements: [PREV-01]

must_haves:
  truths:
    - "Every one of the 28 section types in SECTION_REGISTRY has a defaultProps() that returns complete, renderable props (no missing required fields)"
    - "defaultProps() for chart types (bar-line-chart, donut-chart, etc.) includes sample data arrays so charts render visually, not blank"
    - "defaultProps() for table types (data-table, drill-down-table, clickable-table, config-table) includes at least 2-3 sample rows so tables render with visible content"
    - "defaultProps() for chart-grid includes at least 2 sample items with valid sub-chart props"
    - "Existing section-registry.test.ts passes with zero failures including the new Zod round-trip validation"
    - "No defaultProps() throws a runtime error when called"
  artifacts:
    - path: "tools/wireframe-builder/lib/section-registry.tsx"
      provides: "Hardened defaultProps() for all 28 section types with complete sample data"
      contains: "defaultProps"
    - path: "tools/wireframe-builder/lib/section-registry.test.ts"
      provides: "Enhanced test validating all 28 defaultProps produce renderable, Zod-valid props"
      exports: []
  key_links:
    - from: "tools/wireframe-builder/lib/section-registry.tsx"
      to: "tools/wireframe-builder/lib/blueprint-schema.ts"
      via: "imports section Zod schemas for validation"
      pattern: "import.*Schema.*blueprint-schema"
---

<objective>
Harden all 28 defaultProps() in SECTION_REGISTRY to produce complete, visually renderable sample props.

Purpose: Many section types currently return minimal defaultProps (e.g., tables with empty `rows: []`, chart-grid with empty `items: []`). For the preview feature, each defaultProps() must produce enough sample data that when the component renders, it displays meaningful visual content — not blank cards.

Output: Updated defaultProps() for section types that currently have insufficient sample data, and an enhanced test confirming every type produces renderable output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tools/wireframe-builder/lib/section-registry.tsx
@tools/wireframe-builder/lib/section-registry.test.ts
@tools/wireframe-builder/lib/blueprint-schema.ts
@tools/wireframe-builder/types/blueprint.ts

<interfaces>
<!-- Key types the executor needs -->

From tools/wireframe-builder/lib/section-registry.tsx:
```typescript
export type SectionRegistration = {
  renderer: ComponentType<SectionRendererProps>
  propertyForm: ComponentType<PropertyFormProps>
  catalogEntry: CatalogEntry
  defaultProps: () => BlueprintSection
  schema: z.ZodType
  label: string
}
```

The following section types currently have INCOMPLETE defaultProps (empty arrays or missing sample data):
- `drill-down-table`: `rows: []` — needs 2-3 sample rows
- `clickable-table`: `rows: []` — needs 2-3 sample rows
- `config-table`: `columns: [], rows: []` — needs sample columns and rows
- `saldo-banco`: `banks: []` — needs 2-3 sample banks
- `calculo-card`: `rows: []` — needs sample calculation rows
- `chart-grid`: `items: []` — needs 2 sample sub-chart items

The following types already have good defaultProps with sample data:
- `kpi-grid`, `bar-line-chart`, `donut-chart`, `waterfall-chart`, `pareto-chart`, `data-table`, `manual-input`, `upload-section`, `info-block`, `divider`, `settings-page`, `form-section`, `filter-config`, `stat-card`, `progress-bar`, `gauge-chart`, `branding-editor`, `pie-chart`, `progress-grid`, `heatmap`, `sparkline-grid`, `sankey`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich defaultProps for section types with empty/minimal sample data</name>
  <files>tools/wireframe-builder/lib/section-registry.tsx</files>
  <action>
Update the defaultProps() for the following section types in SECTION_REGISTRY to include complete, visually renderable sample data. Every value must conform to its Zod schema.

**`drill-down-table`** — add sample columns and rows:
```typescript
defaultProps: () => ({
  type: 'drill-down-table' as const,
  title: 'Nova Tabela Drill',
  columns: [
    { key: 'categoria', label: 'Categoria' },
    { key: 'valor', label: 'Valor' },
    { key: 'percentual', label: '%' },
  ],
  rows: [
    { categoria: 'Produto A', valor: 'R$ 12.500', percentual: '45%' },
    { categoria: 'Produto B', valor: 'R$ 8.300', percentual: '30%' },
    { categoria: 'Produto C', valor: 'R$ 6.900', percentual: '25%' },
  ],
}),
```

**`clickable-table`** — add sample columns and rows:
```typescript
defaultProps: () => ({
  type: 'clickable-table' as const,
  title: 'Nova Tabela Clicavel',
  columns: [
    { key: 'nome', label: 'Nome' },
    { key: 'status', label: 'Status' },
    { key: 'valor', label: 'Valor' },
  ],
  rows: [
    { nome: 'Item A', status: 'Ativo', valor: 'R$ 1.200' },
    { nome: 'Item B', status: 'Pendente', valor: 'R$ 850' },
    { nome: 'Item C', status: 'Ativo', valor: 'R$ 2.100' },
  ],
}),
```

**`config-table`** — add sample columns and rows:
```typescript
defaultProps: () => ({
  type: 'config-table' as const,
  title: 'Nova Config',
  columns: [
    { key: 'parametro', label: 'Parametro' },
    { key: 'valor', label: 'Valor' },
  ],
  rows: [
    { parametro: 'Taxa Base', valor: '12%' },
    { parametro: 'Limite', valor: 'R$ 50.000' },
  ],
}),
```

**`saldo-banco`** — add sample banks:
```typescript
defaultProps: () => ({
  type: 'saldo-banco' as const,
  banks: [
    { name: 'Banco do Brasil', balance: 'R$ 45.000' },
    { name: 'Itau', balance: 'R$ 32.000' },
    { name: 'Bradesco', balance: 'R$ 18.500' },
  ],
  total: 'R$ 95.500',
}),
```

**`calculo-card`** — add sample calculation rows:
```typescript
defaultProps: () => ({
  type: 'calculo-card' as const,
  title: 'Novo Calculo',
  rows: [
    { label: 'Receita Bruta', value: 'R$ 100.000', type: 'add' as const },
    { label: '(-) Impostos', value: 'R$ 15.000', type: 'subtract' as const },
    { label: '(-) Custos', value: 'R$ 35.000', type: 'subtract' as const },
    { label: 'Lucro Liquido', value: 'R$ 50.000', type: 'result' as const },
  ],
}),
```

**`chart-grid`** — add 2 sample sub-chart items:
```typescript
defaultProps: () => ({
  type: 'chart-grid' as const,
  columns: 2,
  items: [
    {
      type: 'bar-line-chart' as const,
      title: 'Vendas por Mes',
      chartType: 'bar' as const,
    },
    {
      type: 'donut-chart' as const,
      title: 'Distribuicao',
    },
  ],
}),
```

IMPORTANT: Before updating each defaultProps, read the corresponding Zod schema in blueprint-schema.ts to confirm the field names and types are correct. The sample values above are guidelines — adjust field names to match the actual schema if they differ (e.g., `banks` might have different property names).

Also verify that the existing defaultProps for ALL other types produce enough visual content. If any type (e.g., bar-line-chart) is missing sample data arrays needed for rendering, add them too.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx vitest run tools/wireframe-builder/lib/section-registry.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 28 defaultProps() produce complete sample data suitable for visual rendering. Zero Zod validation failures in the test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance section-registry tests to validate renderability</name>
  <files>tools/wireframe-builder/lib/section-registry.test.ts</files>
  <action>
Add a new test block to section-registry.test.ts that validates the "renderability" of each defaultProps output — specifically checking that section types which display data have non-empty data arrays:

```typescript
describe('defaultProps renderability', () => {
  const typesWithItems = [
    'kpi-grid', 'progress-bar', 'sparkline-grid', 'progress-grid',
  ] as const

  const typesWithRows = [
    'data-table', 'drill-down-table', 'clickable-table',
    'config-table', 'calculo-card', 'heatmap',
  ] as const

  const typesWithChartData = [
    'chart-grid',
  ] as const

  for (const type of typesWithItems) {
    it(`${type}: defaultProps has non-empty items array`, () => {
      const defaults = getDefaultSection(type) as Record<string, unknown>
      expect(Array.isArray(defaults.items)).toBe(true)
      expect((defaults.items as unknown[]).length).toBeGreaterThan(0)
    })
  }

  for (const type of typesWithRows) {
    it(`${type}: defaultProps has non-empty rows array`, () => {
      const defaults = getDefaultSection(type) as Record<string, unknown>
      expect(Array.isArray(defaults.rows)).toBe(true)
      expect((defaults.rows as unknown[]).length).toBeGreaterThan(0)
    })
  }

  for (const type of typesWithChartData) {
    it(`${type}: defaultProps has non-empty items array`, () => {
      const defaults = getDefaultSection(type) as Record<string, unknown>
      expect(Array.isArray(defaults.items)).toBe(true)
      expect((defaults.items as unknown[]).length).toBeGreaterThan(0)
    })
  }

  it('saldo-banco: defaultProps has non-empty banks array', () => {
    const defaults = getDefaultSection('saldo-banco') as Record<string, unknown>
    expect(Array.isArray(defaults.banks)).toBe(true)
    expect((defaults.banks as unknown[]).length).toBeGreaterThan(0)
  })

  it('pie-chart: defaultProps has non-empty slices array', () => {
    const defaults = getDefaultSection('pie-chart') as Record<string, unknown>
    expect(Array.isArray(defaults.slices)).toBe(true)
    expect((defaults.slices as unknown[]).length).toBeGreaterThan(0)
  })

  it('sankey: defaultProps has non-empty nodes and links arrays', () => {
    const defaults = getDefaultSection('sankey') as Record<string, unknown>
    expect(Array.isArray(defaults.nodes)).toBe(true)
    expect((defaults.nodes as unknown[]).length).toBeGreaterThan(0)
    expect(Array.isArray(defaults.links)).toBe(true)
    expect((defaults.links as unknown[]).length).toBeGreaterThan(0)
  })
})
```

Adjust the property names (items/rows/banks/slices/nodes/links) to match the actual Zod schemas for each type. The key invariant: any section that renders a list/grid/chart of items must have sample data in its defaultProps.
  </action>
  <verify>
    <automated>cd /Users/cauetpinciara/Documents/fxl/Projetos/fxl-core && npx vitest run tools/wireframe-builder/lib/section-registry.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>New "defaultProps renderability" test block passes, confirming all section types with data arrays have non-empty sample data in defaultProps.</done>
</task>

</tasks>

<verification>
- `npx vitest run tools/wireframe-builder/lib/section-registry.test.ts` passes all tests
- `npx tsc --noEmit` compiles with zero errors
- Every defaultProps() returns props with enough sample data to render visible content
</verification>

<success_criteria>
- All 28 defaultProps() produce Zod-valid props with non-empty data arrays
- Section types that were previously empty (drill-down-table, clickable-table, config-table, saldo-banco, calculo-card, chart-grid) now have meaningful sample data
- Test suite confirms renderability invariants
</success_criteria>

<output>
After completion, create `.planning/phases/58-preview-rendering-infrastructure/58-01-SUMMARY.md`
</output>
