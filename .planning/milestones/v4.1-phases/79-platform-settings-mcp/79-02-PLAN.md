---
phase: 79-platform-settings-mcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/settings.json
  - docs/ferramentas/super-admin-mcp.md
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03]

must_haves:
  truths:
    - "Claude Code can access Supabase via MCP server configured in settings"
    - "Claude Code can access Clerk via MCP server configured in settings"
    - "Documentation describes available super admin operations via MCP"
  artifacts:
    - path: ".claude/settings.json"
      provides: "MCP server configuration"
      contains: "mcpServers"
    - path: "docs/ferramentas/super-admin-mcp.md"
      provides: "Super admin MCP operations documentation"
      contains: "MCP"
  key_links:
    - from: ".claude/settings.json"
      to: "Supabase MCP server"
      via: "mcpServers.supabase"
      pattern: "supabase"
    - from: ".claude/settings.json"
      to: "Clerk MCP server"
      via: "mcpServers.clerk"
      pattern: "clerk"
---

<objective>
Configure Supabase and Clerk MCP servers in Claude Code settings and document available super admin operations.

Purpose: Claude Code needs direct access to Supabase (database queries, migrations) and Clerk (user/org management) via MCP to perform super admin operations without the user manually navigating dashboards.
Output: Updated .claude/settings.json with MCP configs, documentation page in docs/.
</objective>

<execution_context>
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cauetpinciara/Documents/fxl/Projetos/fxl-core/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/settings.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure MCP servers in .claude/settings.json</name>
  <files>.claude/settings.json</files>
  <read_first>
    .claude/settings.json
  </read_first>
  <action>
Add `mcpServers` key to the root of `.claude/settings.json` (same level as `env`, `permissions`, `hooks`, `statusLine`).

The updated JSON must preserve ALL existing keys exactly as they are and add:

```json
"mcpServers": {
  "supabase": {
    "command": "npx",
    "args": ["-y", "@supabase/mcp-server-supabase@latest", "--read-only"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
    }
  },
  "clerk": {
    "command": "npx",
    "args": ["-y", "@anthropic/mcp-server-clerk@latest"],
    "env": {
      "CLERK_SECRET_KEY": "${CLERK_SECRET_KEY}"
    }
  }
}
```

Key details:
- Supabase MCP server uses `@supabase/mcp-server-supabase@latest` with `--read-only` flag for safety (can be removed when write operations are needed)
- Clerk MCP server uses `@anthropic/mcp-server-clerk@latest`
- Both use env var references (`${VAR}`) so secrets come from the user's shell environment, NOT hardcoded
- The `mcpServers` key goes AFTER `statusLine` (last key before closing brace)
  </action>
  <verify>
    <automated>node -e "const s=require('./.claude/settings.json'); if(s.mcpServers?.supabase?.command && s.mcpServers?.clerk?.command && s.permissions && s.hooks) { console.log('PASS') } else { console.log('FAIL') }"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/settings.json is valid JSON (parseable by Node.js)
    - Contains `mcpServers` key at root level
    - `mcpServers.supabase.command` equals `"npx"`
    - `mcpServers.supabase.args` contains `"@supabase/mcp-server-supabase@latest"` and `"--read-only"`
    - `mcpServers.supabase.env.SUPABASE_ACCESS_TOKEN` equals `"${SUPABASE_ACCESS_TOKEN}"`
    - `mcpServers.clerk.command` equals `"npx"`
    - `mcpServers.clerk.args` contains `"@anthropic/mcp-server-clerk@latest"`
    - `mcpServers.clerk.env.CLERK_SECRET_KEY` equals `"${CLERK_SECRET_KEY}"`
    - All existing keys preserved: `env`, `permissions`, `hooks`, `statusLine`
  </acceptance_criteria>
  <done>Both Supabase and Clerk MCP servers configured in .claude/settings.json with env var references for secrets</done>
</task>

<task type="auto">
  <name>Task 2: Document super admin MCP operations</name>
  <files>docs/ferramentas/super-admin-mcp.md</files>
  <read_first>
    CLAUDE.md
    docs/ferramentas/techs/
  </read_first>
  <action>
Create `docs/ferramentas/super-admin-mcp.md` with frontmatter and content documenting all super admin operations available via MCP.

Frontmatter:
```yaml
---
title: Super Admin — Operacoes via MCP
badge: Ferramentas
description: Operacoes de administracao da plataforma disponíveis via Claude Code MCP
---
```

Content sections:

**1. Visao Geral**
Brief explanation: Super admin operations are available via Claude Code using MCP (Model Context Protocol) servers for Supabase and Clerk. These allow direct database queries and user/org management without navigating external dashboards.

**2. Pre-requisitos**
- `SUPABASE_ACCESS_TOKEN` — Obtido em Supabase Dashboard > Account > Access Tokens
- `CLERK_SECRET_KEY` — Obtido em Clerk Dashboard > API Keys > Secret keys
- Ambos devem estar exportados no shell antes de iniciar o Claude Code

**3. Operacoes Supabase (via MCP)**
Table with operations:

| Operacao | Descricao | Exemplo |
|----------|-----------|---------|
| Listar tenants | Query na tabela tenant_modules agrupada por org_id | `SELECT DISTINCT org_id FROM tenant_modules` |
| Gerenciar feature flags | Ler/atualizar platform_settings | `SELECT * FROM platform_settings` |
| Habilitar modulo | Insert/update em tenant_modules | `INSERT INTO tenant_modules (org_id, module_id, enabled) VALUES (...)` |
| Desabilitar modulo | Update enabled=false em tenant_modules | `UPDATE tenant_modules SET enabled=false WHERE ...` |
| Verificar RLS | Consultar policies ativas | `SELECT * FROM pg_policies` |
| Aplicar migrations | Executar SQL de migracao | Via Supabase MCP SQL execution |

**4. Operacoes Clerk (via MCP)**
Table with operations:

| Operacao | Descricao |
|----------|-----------|
| Criar tenant (Organization) | Cria nova Clerk Organization com slug e metadata |
| Listar Organizations | Lista todas as orgs com membros e metadata |
| Adicionar membro a tenant | Convida usuario para Organization |
| Definir super_admin | Atualiza publicMetadata.super_admin de um usuario |
| Listar usuarios | Lista todos os usuarios com claims e orgs |

**5. Configuracao**
Reference to `.claude/settings.json` showing the mcpServers block. Explain that `--read-only` flag on Supabase can be removed for write operations.

**6. Seguranca**
- Nunca commitar tokens no repositorio
- Usar `--read-only` no Supabase MCP para operacoes exploratórias
- Remover `--read-only` apenas quando write é necessario
- Clerk secret key tem acesso total — usar com cuidado

Add an `{% operational %}` section at the end with a quick-reference for Claude:
```
{% operational %}
## Referencia Rapida para Claude

Ao executar operacoes de super admin:
1. Usar Supabase MCP para queries e mutations no banco
2. Usar Clerk MCP para gestao de orgs e usuarios
3. Sempre confirmar com o usuario antes de operacoes destrutivas (DELETE, remover membro)
4. Para criar tenant: Clerk MCP (criar org) + Supabase MCP (seed tenant_modules)
5. Para habilitar modulo: Supabase MCP (INSERT/UPDATE tenant_modules)
{% /operational %}
```
  </action>
  <verify>
    <automated>test -f docs/ferramentas/super-admin-mcp.md && grep -q "badge: Ferramentas" docs/ferramentas/super-admin-mcp.md && grep -q "MCP" docs/ferramentas/super-admin-mcp.md && grep -q "Supabase" docs/ferramentas/super-admin-mcp.md && grep -q "Clerk" docs/ferramentas/super-admin-mcp.md && grep -q "operational" docs/ferramentas/super-admin-mcp.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File docs/ferramentas/super-admin-mcp.md exists
    - Frontmatter contains `title: Super Admin — Operacoes via MCP`
    - Frontmatter contains `badge: Ferramentas`
    - Contains section on Supabase MCP operations with at least 5 operations listed
    - Contains section on Clerk MCP operations with at least 4 operations listed
    - Contains section on pre-requisitos mentioning SUPABASE_ACCESS_TOKEN and CLERK_SECRET_KEY
    - Contains section on seguranca
    - Contains `{% operational %}` section with quick-reference for Claude
    - Contains reference to creating tenant (criar tenant) and enabling module (habilitar modulo)
  </acceptance_criteria>
  <done>Documentation page describes all super admin operations available via Supabase and Clerk MCP servers, including prerequisites, security notes, and operational quick-reference for Claude</done>
</task>

</tasks>

<verification>
1. `.claude/settings.json` is valid JSON with mcpServers containing supabase and clerk entries
2. `docs/ferramentas/super-admin-mcp.md` exists with proper frontmatter and comprehensive operations documentation
3. All existing settings.json keys preserved (env, permissions, hooks, statusLine)
</verification>

<success_criteria>
- Both MCP servers configured with proper commands, args, and env var references
- Documentation covers all key super admin operations for both Supabase and Clerk
- Security guidance included (no hardcoded tokens, read-only default)
- Operational section provides Claude-facing quick-reference
</success_criteria>

<output>
After completion, create `.planning/phases/79-platform-settings-mcp/79-02-SUMMARY.md`
</output>
