---
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/111-audit-closure/111-VERIFICATION.md
autonomous: true
requirements:
  - DATA-04
  - ARCH-01
  - ARCH-02
---

# Plan 111-01: Audit Closure — Verify DATA-04, ARCH-01, ARCH-02 and Close REQUIREMENTS.md

## Goal

Explicitly verify DATA-04 (docs sidebar org scoping), ARCH-01 (module registry tool/data separation), and ARCH-02 (wireframe manifest tenantScoped flag) using code evidence. Mark all 12 REQUIREMENTS.md checkboxes as `[x]`. Run `npx tsc --noEmit` and confirm zero errors. Produce 111-VERIFICATION.md.

## Context

This is a verification-only phase — no new code changes. All three requirements were implemented in prior phases:

- **DATA-04**: Implemented via `useOrgTokenExchange` (ProtectedRoute.tsx) + Supabase JWT org token + RLS on `documents` table. The docs cache is invalidated on org switch via `onOrgChange: invalidateDocsCache`.
- **ARCH-01**: Implemented via `ModuleDefinition.tenantScoped?: boolean` in `src/platform/module-loader/registry.ts`. This flag separates tool (global) from org-scoped data modules.
- **ARCH-02**: Implemented via `ferramentasManifest.tenantScoped = true` in `src/modules/wireframe/manifest.tsx`. The builder tool itself lives in `tools/wireframe-builder/` (global), while data (blueprint_configs) is org-scoped via RLS.

Phases 105–110 all have VERIFICATION.md files confirming their requirements. This plan audits the remaining 3 requirements and closes the REQUIREMENTS.md.

## Tasks

### Task 1: Verify DATA-04 — Docs Sidebar Org Scoping

<read_first>
- src/platform/auth/ProtectedRoute.tsx
- src/platform/tenants/useOrgTokenExchange.ts
- src/modules/docs/services/docs-service.ts
- .planning/REQUIREMENTS.md (DATA-04 definition)
</read_first>

<action>
Read the three source files listed above and confirm all of the following are present:

1. In `ProtectedRoute.tsx`:
   - Import: `import { useOrgTokenExchange } from '@platform/tenants/useOrgTokenExchange'`
   - Import: `import { invalidateDocsCache } from '@modules/docs/services/docs-service'`
   - Call: `useOrgTokenExchange({ onOrgChange: invalidateDocsCache })`

2. In `useOrgTokenExchange.ts`:
   - `setOrgAccessToken(result.access_token)` — sets the org-scoped JWT on the Supabase client
   - The hook re-runs when `activeOrg?.id` changes (effect dependency)
   - `onOrgChange` callback fired on org switch after successful token exchange

3. In `docs-service.ts`:
   - `supabase.from('documents').select('*')` — queries via the Supabase client which carries the org JWT
   - `invalidateDocsCache()` function exported (clears `docsCache` and `docsCachePromise`)

The mechanism: Clerk org → JWT with org_id claim → Supabase client carries JWT → RLS on `documents` table filters by `org_id` from JWT → docs sidebar shows only the current org's documents.

No code changes needed — this is a read-and-confirm audit.
</action>

<acceptance_criteria>
- ProtectedRoute.tsx contains `onOrgChange: invalidateDocsCache`
- ProtectedRoute.tsx contains `useOrgTokenExchange`
- useOrgTokenExchange.ts contains `setOrgAccessToken(result.access_token)`
- useOrgTokenExchange.ts effect depends on `activeOrg?.id`
- docs-service.ts contains `export function invalidateDocsCache`
- docs-service.ts queries `supabase.from('documents')`
</acceptance_criteria>

---

### Task 2: Verify ARCH-01 — Module Registry Tool/Data Separation

<read_first>
- src/platform/module-loader/registry.ts
- .planning/REQUIREMENTS.md (ARCH-01 definition)
</read_first>

<action>
Read `src/platform/module-loader/registry.ts` and confirm:

1. `ModuleDefinition` interface has `tenantScoped?: boolean` field with comment explaining it controls per-org visibility via `tenant_modules` table
2. `MODULE_REGISTRY` array exports the complete list of module definitions
3. The interface clearly separates the concept: modules without `tenantScoped` or with `tenantScoped: false` are global tools; modules with `tenantScoped: true` are org-scoped data modules

ARCH-01 required: "Separacao clara entre funcionalidade do modulo (ferramenta) e dados do cliente (org-scoped)"

The `tenantScoped` flag IS that separation — it is the architectural boundary between tool (global, available to all orgs) and data (org-scoped, controlled per tenant).

No code changes needed — this is a read-and-confirm audit.
</action>

<acceptance_criteria>
- registry.ts contains `tenantScoped?: boolean` in ModuleDefinition interface
- registry.ts contains `MODULE_REGISTRY: ModuleDefinition[]`
- registry.ts comment on tenantScoped mentions "per-org" or "tenant" or "org mode"
</acceptance_criteria>

---

### Task 3: Verify ARCH-02 — Wireframe Builder as Global Tool

<read_first>
- src/modules/wireframe/manifest.tsx
- src/modules/wireframe/CLAUDE.md
- .planning/REQUIREMENTS.md (ARCH-02 definition)
</read_first>

<action>
Read `src/modules/wireframe/manifest.tsx` and `src/modules/wireframe/CLAUDE.md` and confirm:

1. `ferramentasManifest` has `tenantScoped: true` — this is the MODULE UI visibility flag (controls whether the module appears in an org's sidebar)
2. `CLAUDE.md` documents the hybrid architecture:
   - `src/modules/wireframe/` — manifest, pages, hooks (module system integration)
   - `tools/wireframe-builder/` — visual components, editor, types (global/reusable across contexts)
3. The builder components in `tools/wireframe-builder/` are NOT inside any org-scoped module — they are global shared components

ARCH-02 required: "Wireframe Builder como ferramenta global, wireframes de clientes como dados da org"

The architecture satisfies this:
- Builder TOOL: `tools/wireframe-builder/` (global, no org scope)
- Client wireframe DATA: `blueprint_configs` table with org_id RLS (org-scoped)
- Module MODULE UI: `src/modules/wireframe/` with `tenantScoped: true` (controls org visibility)

No code changes needed — this is a read-and-confirm audit.
</action>

<acceptance_criteria>
- manifest.tsx contains `tenantScoped: true` on ferramentasManifest
- manifest.tsx has id `MODULE_IDS.FERRAMENTAS`
- CLAUDE.md documents "Hybrid Architecture Note" separating src/modules/wireframe from tools/wireframe-builder
- tools/wireframe-builder/ directory exists (global tool components)
</acceptance_criteria>

---

### Task 4: Run TypeScript Check

<read_first>
- CLAUDE.md (requirement: zero TypeScript errors before task complete)
</read_first>

<action>
Run from the project root:

```bash
npx tsc --noEmit
```

This phase has no code changes, so this should pass clean. Record the exit code (0 = pass).
</action>

<acceptance_criteria>
- `npx tsc --noEmit` exits with code 0
- Zero TypeScript errors in output
</acceptance_criteria>

---

### Task 5: Mark DATA-04, ARCH-01, ARCH-02 as Done in REQUIREMENTS.md

<read_first>
- .planning/REQUIREMENTS.md (current state — DATA-04, ARCH-01, ARCH-02 are `[ ]`)
</read_first>

<action>
Edit `.planning/REQUIREMENTS.md`:

1. Change `- [ ] **DATA-04**` to `- [x] **DATA-04**`
2. Change `- [ ] **ARCH-01**` to `- [x] **ARCH-01**`
3. Change `- [ ] **ARCH-02**` to `- [x] **ARCH-02**`
4. Update the Coverage section at the bottom:
   - Change `- Done: 9 (DATA-01, DATA-02, DATA-03, DATA-05, HEAD-01, HEAD-02, HEAD-03, ADMN-01, ADMN-02)` to `- Done: 12 (all)`
   - Change `- Pending gap closure: 3 (DATA-04, ARCH-01, ARCH-02)` to `- Pending gap closure: 0`
5. Update the Traceability table to set DATA-04, ARCH-01, ARCH-02 status = Done:
   - `| DATA-04 | Phase 111 (gap closure) | Done |`
   - `| ARCH-01 | Phase 111 (gap closure) | Done |`
   - `| ARCH-02 | Phase 111 (gap closure) | Done |`
6. Remove the gap closure comments (`<!-- gap closure: ... -->`) from the ARCH-01 and ARCH-02 lines since they are now closed
</action>

<acceptance_criteria>
- REQUIREMENTS.md contains `[x] **DATA-04**`
- REQUIREMENTS.md contains `[x] **ARCH-01**`
- REQUIREMENTS.md contains `[x] **ARCH-02**`
- REQUIREMENTS.md coverage section says "Done: 12" or "Pending gap closure: 0"
- Traceability table shows DATA-04 status = Done
- Traceability table shows ARCH-01 status = Done
- Traceability table shows ARCH-02 status = Done
</acceptance_criteria>

---

### Task 6: Write 111-VERIFICATION.md

<read_first>
- .planning/phases/111-audit-closure/111-CONTEXT.md
- .planning/REQUIREMENTS.md (updated — all 12 done)
- .planning/ROADMAP.md (Phase 111 success criteria)
- .planning/phases/109-blueprint-rls/109-VERIFICATION.md (reference format)
</read_first>

<action>
Create `.planning/phases/111-audit-closure/111-VERIFICATION.md` with the following structure:

```markdown
---
phase: 111
status: passed
verified: 2026-03-18
---

# Phase 111: Audit Closure — Verification

## Goal

Close all partial verification gaps for v5.3: explicitly verify DATA-04, ARCH-01, ARCH-02 via code evidence; confirm all phase VERIFICATION.md artifacts exist; mark all 12 REQUIREMENTS.md checkboxes as done.

## Success Criteria Checklist

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | DATA-04: docs sidebar filters by org_id | PASS | ProtectedRoute.tsx wires useOrgTokenExchange({ onOrgChange: invalidateDocsCache }); Supabase client carries JWT org token; RLS on documents table enforces org_id filter |
| 2 | ARCH-01: module registry separates tool from org-scoped data | PASS | ModuleDefinition.tenantScoped?: boolean in registry.ts is the architectural separator; global tools omit flag; org-scoped modules set tenantScoped: true |
| 3 | ARCH-02: Wireframe Builder is global tool, wireframes are org data | PASS | ferramentasManifest.tenantScoped = true (module UI visibility); tools/wireframe-builder/ is global (no org scope); blueprint_configs has RLS with org_id |
| 4 | VERIFICATION.md exists for phases 105–110 | PASS | 105, 106, 107, 108, 109, 110 — all have VERIFICATION.md |
| 5 | All 12 REQUIREMENTS.md checkboxes marked [x] | PASS | DATA-01–05, HEAD-01–03, ADMN-01–02, ARCH-01–02 all [x] |
| 6 | Traceability table all status = Done | PASS | All 12 rows updated in REQUIREMENTS.md |
| 7 | npx tsc --noEmit passes with zero errors | PASS | Exit code 0 |

## DATA-04 Evidence

**Requirement:** Docs da org (processo, padroes) scoped por org_id na sidebar

**Mechanism (code trace):**
1. `ProtectedRoute.tsx` calls `useOrgTokenExchange({ onOrgChange: invalidateDocsCache })`
2. `useOrgTokenExchange` calls `exchangeToken(clerkToken)` → gets Supabase-compatible JWT with org_id claim
3. `setOrgAccessToken(result.access_token)` sets the JWT on the Supabase client
4. When org changes, `onOrgChange: invalidateDocsCache` fires → clears docs cache
5. Next docs query hits Supabase with org JWT → RLS on `documents` table filters by `(jwt->>'org_id') = org_id`
6. Only documents belonging to the current org are returned → sidebar shows only org's docs

**Files:**
- `src/platform/auth/ProtectedRoute.tsx` — wiring point
- `src/platform/tenants/useOrgTokenExchange.ts` — token exchange + onOrgChange
- `src/modules/docs/services/docs-service.ts` — cache + fetchAllDocs via Supabase

**Status: CLOSED**

## ARCH-01 Evidence

**Requirement:** Separacao clara entre funcionalidade do modulo (ferramenta) e dados do cliente (org-scoped)

**Mechanism:**
- `ModuleDefinition` interface in `src/platform/module-loader/registry.ts` has `tenantScoped?: boolean`
- Comment: "When true, module visibility is controlled per-org via tenant_modules table in org mode"
- `tenantScoped: true` = org-scoped module (visibility controlled per tenant)
- `tenantScoped: false` or absent = global tool (visible to all orgs)

**Status: CLOSED**

## ARCH-02 Evidence

**Requirement:** Wireframe Builder como ferramenta global, wireframes de clientes como dados da org

**Mechanism:**
- Builder TOOL: `tools/wireframe-builder/` — global, shared components, no org scope
- Module UI: `src/modules/wireframe/manifest.tsx` — `tenantScoped: true` (org visibility control)
- Client DATA: `blueprint_configs` table with RLS `(jwt->>'org_id') = org_id`
- Documented in `src/modules/wireframe/CLAUDE.md` as "Hybrid Architecture Note"

**Status: CLOSED**

## Nyquist Coverage

All phases in v5.3 have VERIFICATION.md:

| Phase | VERIFICATION.md | Status |
|-------|----------------|--------|
| 105. Data Isolation | .planning/phases/105-data-isolation/105-VERIFICATION.md | ✓ |
| 106. Data Recovery | .planning/phases/106-data-recovery/106-VERIFICATION.md | ✓ |
| 107. Header UX | .planning/phases/107-header-ux/107-VERIFICATION.md | ✓ |
| 108. Admin Enhancements | .planning/phases/108-admin-enhancements/108-VERIFICATION.md | ✓ |
| 109. Blueprint RLS | .planning/phases/109-blueprint-rls/109-VERIFICATION.md | ✓ |
| 110. Phase 108 Verification | .planning/phases/110-phase108-verification/110-VERIFICATION.md | ✓ |
| 111. Audit Closure | .planning/phases/111-audit-closure/111-VERIFICATION.md | ✓ (this file) |

## TypeScript Check

```
$ npx tsc --noEmit
(no output — zero errors)
Exit code: 0
```

## Verification Summary

Score: 7/7 success criteria verified. Phase 111 complete.

**v5.3 milestone is now fully audited:**
- All 12 requirements closed
- All phases have VERIFICATION.md
- Zero TypeScript errors
```
</action>

<acceptance_criteria>
- File `.planning/phases/111-audit-closure/111-VERIFICATION.md` exists
- File contains "status: passed" in frontmatter
- File contains all 7 success criteria rows with PASS status
- File contains DATA-04, ARCH-01, ARCH-02 evidence sections
- File contains Nyquist coverage table showing phases 105-111
- File contains TypeScript check section with exit code 0
- File contains "Score: 7/7"
</acceptance_criteria>

---

### Task 7: Commit All Artifacts

<read_first>
- CLAUDE.md (commit convention: `docs:` for planning artifacts and verification)
</read_first>

<action>
Commit REQUIREMENTS.md update and 111-VERIFICATION.md together:

```bash
git add .planning/REQUIREMENTS.md .planning/phases/111-audit-closure/111-VERIFICATION.md .planning/phases/111-audit-closure/111-CONTEXT.md .planning/phases/111-audit-closure/111-01-PLAN.md
git commit -m "docs: close v5.3 audit — DATA-04, ARCH-01, ARCH-02 verified, all 12 requirements done"
```

Then update STATE.md:

```bash
node ".claude/get-shit-done/bin/gsd-tools.cjs" state record-session \
  --stopped-at "Phase 111 complete — v5.3 audit closure done, all 12 requirements verified" \
  --resume-file ".planning/phases/111-audit-closure/111-VERIFICATION.md"
```

Commit STATE.md:
```bash
node ".claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(state): record phase 111 completion — v5.3 fully audited" --files .planning/STATE.md
```
</action>

<acceptance_criteria>
- `git log --oneline -1` shows commit with message containing "DATA-04" or "audit"
- `.planning/phases/111-audit-closure/111-VERIFICATION.md` is committed
- `.planning/REQUIREMENTS.md` is committed with all 12 `[x]`
</acceptance_criteria>

## Must-Haves (Goal-Backward Verification)

The phase goal is: "Fechar todos os gaps de verificacao parcial e garantir compliance total — REQUIREMENTS.md com todos os checkboxes marcados, DATA-04/ARCH-01/ARCH-02 explicitamente verificados"

For this to be TRUE:

1. DATA-04 verified via code evidence (ProtectedRoute + useOrgTokenExchange + RLS)
2. ARCH-01 verified via ModuleDefinition.tenantScoped in registry.ts
3. ARCH-02 verified via ferramentasManifest.tenantScoped + tools/ vs modules/ split
4. All 12 REQUIREMENTS.md checkboxes = `[x]`
5. Traceability table all status = Done
6. VERIFICATION.md files exist for phases 105–111
7. `npx tsc --noEmit` = 0 errors

## Verification

```bash
npx tsc --noEmit && echo "TypeScript: PASS"
grep -c "\[x\]" .planning/REQUIREMENTS.md && echo "requirements done count (expect 12)"
ls .planning/phases/111-audit-closure/111-VERIFICATION.md && echo "VERIFICATION.md: EXISTS"
```

All commands must succeed and grep must return 12 for phase to be COMPLETE.
