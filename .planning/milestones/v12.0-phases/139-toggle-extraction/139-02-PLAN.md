---
phase: 139-toggle-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/pages/admin/ModulesPanel.tsx
autonomous: true
requirements: [TOGL-02]

must_haves:
  truths:
    - "ModulesPanel no longer contains a tenant selector dropdown"
    - "ModulesPanel no longer contains module toggle switches (no <Switch> elements)"
    - "ModulesPanel no longer contains the ModuleCard component"
    - "ModulesPanel shows a placeholder message indicating the overview is coming"
    - "STATUS_LABELS and STATUS_CLASSES constants are preserved for Phase 141 reuse"
  artifacts:
    - path: "src/platform/pages/admin/ModulesPanel.tsx"
      provides: "Empty scaffold for future module overview page"
      contains: "Visao geral"
  key_links: []
---

<objective>
Strip all toggle and tenant-selector UI from ModulesPanel, leaving it as a clean scaffold for the upcoming module overview features (diagram in Phase 140, cards in Phase 141).

Purpose: ModulesPanel becomes a read-only overview page — all per-tenant management is now on TenantDetailPage.
Output: Minimal ModulesPanel with page header and placeholder content, no toggle controls.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
Current ModulesPanel.tsx structure (310 lines):
- Lines 1-18: Imports (useCallback, useEffect, useState, useSearchParams, toast, MODULE_REGISTRY, ModuleDefinition, ModuleId, MODULE_IDS, supabase, useActiveOrg, Switch, Select components, Loader2)
- Lines 20: ALL_MODULE_IDS constant
- Lines 26-38: STATUS_LABELS, STATUS_CLASSES constants (KEEP THESE)
- Lines 44-131: ModuleCard component with toggle Switch (REMOVE)
- Lines 137-309: ModulesPanel component with tenant selector + grid (REPLACE)

After stripping, the file should be approximately 40-60 lines.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip ModulesPanel to scaffold</name>
  <files>src/platform/pages/admin/ModulesPanel.tsx</files>
  <read_first>
    - src/platform/pages/admin/ModulesPanel.tsx (current full file — understand what to keep vs remove)
  </read_first>
  <action>
    Rewrite `src/platform/pages/admin/ModulesPanel.tsx` to a minimal scaffold.

    **New file content (complete replacement):**

    ```typescript
    import { Blocks } from 'lucide-react'

    // ---------------------------------------------------------------------------
    // Status badge helpers — preserved for Phase 141 ModuleOverviewCard reuse
    // ---------------------------------------------------------------------------

    export const STATUS_LABELS: Record<string, string> = {
      active: 'Ativo',
      beta: 'Beta',
      'coming-soon': 'Em breve',
    }

    export const STATUS_CLASSES: Record<string, string> = {
      active:
        'bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-400',
      beta: 'bg-amber-50 text-amber-700 dark:bg-amber-950 dark:text-amber-400',
      'coming-soon':
        'bg-slate-100 text-slate-500 dark:bg-slate-800 dark:text-slate-400',
    }

    // ---------------------------------------------------------------------------
    // ModulesPanel — scaffold for upcoming module overview (Phase 140 + 141)
    // ---------------------------------------------------------------------------

    export default function ModulesPanel() {
      return (
        <div className="mx-auto max-w-4xl space-y-6 px-4 py-8">
          {/* Page header */}
          <div>
            <h1 className="text-2xl font-bold text-slate-900 dark:text-foreground">
              Modulos
            </h1>
            <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">
              Visao geral dos modulos da plataforma.
            </p>
          </div>

          {/* Placeholder — will be replaced by diagram (Phase 140) and card grid (Phase 141) */}
          <div className="rounded-xl border border-dashed border-slate-200 p-12 text-center dark:border-slate-700">
            <Blocks className="mx-auto mb-3 h-8 w-8 text-slate-300 dark:text-slate-600" />
            <p className="text-sm font-medium text-slate-500 dark:text-slate-400">
              Visao geral dos modulos sera adicionada em breve
            </p>
            <p className="mt-1 text-xs text-slate-400 dark:text-slate-500">
              Gerencie modulos por tenant na pagina de detalhes do tenant.
            </p>
          </div>
        </div>
      )
    }
    ```

    **Key changes:**
    1. REMOVED: all React hooks imports (useCallback, useEffect, useState)
    2. REMOVED: useSearchParams import
    3. REMOVED: toast import
    4. REMOVED: MODULE_REGISTRY, ModuleDefinition, ModuleId, MODULE_IDS imports
    5. REMOVED: supabase import
    6. REMOVED: useActiveOrg import
    7. REMOVED: Switch import
    8. REMOVED: Select, SelectContent, SelectItem, SelectTrigger, SelectValue imports
    9. REMOVED: Loader2 import
    10. REMOVED: ALL_MODULE_IDS constant
    11. REMOVED: ModuleCard component (entire function, lines 44-131)
    12. REMOVED: All state management, effects, and handlers from ModulesPanel
    13. REMOVED: Tenant selector dropdown
    14. REMOVED: Module card grid
    15. KEPT: STATUS_LABELS and STATUS_CLASSES (now exported for Phase 141 reuse)
    16. ADDED: `export` keyword to STATUS_LABELS and STATUS_CLASSES (were const-only before)
    17. ADDED: Blocks icon import for placeholder
    18. ADDED: Placeholder dashed-border box with "Visao geral dos modulos sera adicionada em breve"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>grep -c "Switch" src/platform/pages/admin/ModulesPanel.tsx</automated>
    <automated>grep -c "useActiveOrg" src/platform/pages/admin/ModulesPanel.tsx</automated>
    <automated>grep -c "SelectTrigger" src/platform/pages/admin/ModulesPanel.tsx</automated>
    <automated>grep -c "onToggle" src/platform/pages/admin/ModulesPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/platform/pages/admin/ModulesPanel.tsx exists with approximately 40-60 lines
    - File does NOT contain `<Switch` (grep returns 0)
    - File does NOT contain `useActiveOrg` (grep returns 0)
    - File does NOT contain `SelectTrigger` (grep returns 0)
    - File does NOT contain `onToggle` (grep returns 0)
    - File does NOT contain `ModuleCard` (grep returns 0)
    - File does NOT contain `Selecione um tenant` (grep returns 0)
    - File contains `export const STATUS_LABELS` (exported for reuse)
    - File contains `export const STATUS_CLASSES` (exported for reuse)
    - File contains `Visao geral dos modulos da plataforma`
    - File contains `Visao geral dos modulos sera adicionada em breve`
    - File contains `export default function ModulesPanel()`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ModulesPanel is stripped to a minimal scaffold with page header and placeholder, no toggle or tenant-selector UI, STATUS_LABELS/STATUS_CLASSES exported for Phase 141</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -c "<Switch" src/platform/pages/admin/ModulesPanel.tsx` returns 0
3. `grep -c "useActiveOrg" src/platform/pages/admin/ModulesPanel.tsx` returns 0
4. `grep -c "SelectTrigger" src/platform/pages/admin/ModulesPanel.tsx` returns 0
5. ModulesPanel renders a page header + placeholder content only
</verification>

<success_criteria>
- ModulesPanel no longer contains tenant selector dropdown or module toggle switches
- STATUS_LABELS and STATUS_CLASSES are preserved and exported
- Placeholder message indicates overview coming soon
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/139-toggle-extraction/139-02-SUMMARY.md`
</output>
