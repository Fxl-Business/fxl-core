---
phase: 141-module-overview-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/module-loader/registry.ts
  - src/platform/pages/admin/module-status-constants.ts
  - src/modules/docs/manifest.tsx
  - src/modules/wireframe/manifest.tsx
  - src/modules/projects/manifest.tsx
  - src/modules/clients/manifest.tsx
  - src/modules/tasks/manifest.ts
  - src/modules/connector/manifest.tsx
autonomous: true
requirements: [CARD-02]

must_haves:
  truths:
    - "ModuleDefinition interface includes an optional features field typed as string[]"
    - "Every module manifest provides 3-5 feature strings describing its capabilities"
    - "Status badge constants are importable from a shared file, not duplicated"
  artifacts:
    - path: "src/platform/module-loader/registry.ts"
      provides: "Updated ModuleDefinition with features field"
      contains: "features"
    - path: "src/platform/pages/admin/module-status-constants.ts"
      provides: "Shared STATUS_LABELS and STATUS_CLASSES"
      exports: ["STATUS_LABELS", "STATUS_CLASSES"]
    - path: "src/modules/docs/manifest.tsx"
      provides: "Docs manifest with features array"
      contains: "features"
    - path: "src/modules/wireframe/manifest.tsx"
      provides: "Wireframe manifest with features array"
      contains: "features"
    - path: "src/modules/projects/manifest.tsx"
      provides: "Projects manifest with features array"
      contains: "features"
    - path: "src/modules/clients/manifest.tsx"
      provides: "Clients manifest with features array"
      contains: "features"
    - path: "src/modules/tasks/manifest.ts"
      provides: "Tasks manifest with features array"
      contains: "features"
    - path: "src/modules/connector/manifest.tsx"
      provides: "Connector manifest with features array"
      contains: "features"
  key_links:
    - from: "src/modules/*/manifest.*"
      to: "src/platform/module-loader/registry.ts"
      via: "ModuleDefinition type satisfaction"
      pattern: "features:"
---

<objective>
Add the `features` field to ModuleDefinition, extract shared status constants, and populate features in all 6 module manifests.

Purpose: Provides the data layer that ModuleOverviewCard (Plan 02) needs to display feature lists (CARD-02) and shared badge styling.
Output: Updated type interface, shared constants file, and 6 updated manifests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-module-overview-cards/141-CONTEXT.md

<interfaces>
<!-- Key types the executor needs. -->

From src/platform/module-loader/registry.ts:
```typescript
export interface ModuleDefinition extends ModuleManifest {
  id: ModuleId
  description: string
  badge?: number
  enabled?: boolean
  tenantScoped?: boolean
  extensions?: ModuleExtension[]
  useNavItems?: () => UseNavItemsResult
}

export const MODULE_REGISTRY: ModuleDefinition[] = [
  docsManifest, ferramentasManifest, projectsManifest,
  clientsManifest, tasksManifest, connectorManifest,
]
```

Current status constants in ModulesPanel.tsx (to be extracted):
```typescript
const STATUS_LABELS: Record<string, string> = {
  active: 'Ativo', beta: 'Beta', 'coming-soon': 'Em breve',
}
const STATUS_CLASSES: Record<string, string> = {
  active: 'bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-400',
  beta: 'bg-amber-50 text-amber-700 dark:bg-amber-950 dark:text-amber-400',
  'coming-soon': 'bg-slate-100 text-slate-500 dark:bg-slate-800 dark:text-slate-400',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add features field to ModuleDefinition and create shared status constants</name>
  <files>src/platform/module-loader/registry.ts, src/platform/pages/admin/module-status-constants.ts</files>
  <read_first>
    - src/platform/module-loader/registry.ts
    - src/platform/pages/admin/ModulesPanel.tsx (lines 26-38 for status constants)
  </read_first>
  <action>
    1. In `src/platform/module-loader/registry.ts`:
       - Add `features?: string[]` to the ModuleDefinition interface, after the `description` field:
         ```typescript
         export interface ModuleDefinition extends ModuleManifest {
           id: ModuleId
           description: string
           features?: string[]
           // ... rest unchanged
         }
         ```

    2. Create `src/platform/pages/admin/module-status-constants.ts`:
       ```typescript
       export const STATUS_LABELS: Record<string, string> = {
         active: 'Ativo',
         beta: 'Beta',
         'coming-soon': 'Em breve',
       }

       export const STATUS_CLASSES: Record<string, string> = {
         active:
           'bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-400',
         beta: 'bg-amber-50 text-amber-700 dark:bg-amber-950 dark:text-amber-400',
         'coming-soon':
           'bg-slate-100 text-slate-500 dark:bg-slate-800 dark:text-slate-400',
       }
       ```

    3. In `src/platform/pages/admin/ModulesPanel.tsx`:
       - Remove the local STATUS_LABELS and STATUS_CLASSES constants (lines ~26-38)
       - Add import: `import { STATUS_LABELS, STATUS_CLASSES } from './module-status-constants'`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ModuleDefinition has features?: string[] field, status constants extracted to shared file, ModulesPanel imports from shared file</done>
</task>

<task type="auto">
  <name>Task 2: Populate features arrays in all 6 module manifests</name>
  <files>src/modules/docs/manifest.tsx, src/modules/wireframe/manifest.tsx, src/modules/projects/manifest.tsx, src/modules/clients/manifest.tsx, src/modules/tasks/manifest.ts, src/modules/connector/manifest.tsx</files>
  <read_first>
    - src/modules/docs/manifest.tsx
    - src/modules/wireframe/manifest.tsx
    - src/modules/projects/manifest.tsx
    - src/modules/clients/manifest.tsx
    - src/modules/tasks/manifest.ts
    - src/modules/connector/manifest.tsx
  </read_first>
  <action>
    Add a `features` array to each manifest, placed after the `description` field. Use 3-5 concise strings per module describing main capabilities. Keep language consistent (Portuguese, matching the existing label/description style).

    1. **docs/manifest.tsx** (Processo):
       ```typescript
       features: [
         'Documentacao de processo, padroes e ferramentas',
         'Parser de markdown com tags customizadas',
         'Busca integrada (Cmd+K) em todos os docs',
         'Navegacao hierarquica com sidebar dinamica',
       ],
       ```

    2. **wireframe/manifest.tsx** (Ferramentas):
       ```typescript
       features: [
         'Galeria de componentes com preview interativo',
         'Viewer publico de wireframes com compartilhamento',
         'Suporte a branding customizado por cliente',
         '20+ blocos de dashboard (KPI, graficos, tabelas)',
       ],
       ```

    3. **projects/manifest.tsx** (Projetos):
       ```typescript
       features: [
         'Gestao de projetos por cliente',
         'Acompanhamento de status e timeline',
         'Vinculacao com wireframes e entregas',
       ],
       ```

    4. **clients/manifest.tsx** (Clientes):
       ```typescript
       features: [
         'Cadastro e gestao de clientes',
         'Workspace dedicado por cliente',
         'Historico de interacoes e entregas',
         'Branding e configuracoes por cliente',
       ],
       ```

    5. **tasks/manifest.ts** (Tarefas):
       ```typescript
       features: [
         'Kanban de tarefas por projeto',
         'Widget de tarefas recentes no Home',
         'Atribuicao e acompanhamento de responsaveis',
       ],
       ```

    6. **connector/manifest.tsx** (Connector):
       ```typescript
       features: [
         'Integracao com APIs externas via conectores',
         'Configuracao visual de conexoes',
         'Widget de conectores ativos no Home',
       ],
       ```

    Place each `features` array immediately after the `description` field in the manifest object, before `tenantScoped` or `label` (whichever comes next).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 6 manifests have features arrays with 3-5 descriptive strings each, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "features:" src/modules/*/manifest.*` returns 6 matches (one per module)
3. `grep -r "STATUS_LABELS" src/platform/pages/admin/module-status-constants.ts` returns a match
4. ModulesPanel.tsx no longer defines STATUS_LABELS locally — imports from shared file
</verification>

<success_criteria>
- ModuleDefinition interface includes features?: string[]
- All 6 manifests have features arrays populated
- Status constants live in shared file, ModulesPanel imports them
- npx tsc --noEmit passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/141-module-overview-cards/141-01-SUMMARY.md`
</output>
