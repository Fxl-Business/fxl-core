---
phase: 31-knowledge-base-module
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/modules/knowledge-base/hooks/useKBEntries.test.ts
  - src/modules/knowledge-base/hooks/useKBSearch.test.ts
  - src/modules/knowledge-base/pages/KBFormPage.test.ts
autonomous: true
requirements:
  - KB-02
  - KB-05
  - KB-06

must_haves:
  truths:
    - "Test stubs exist before any implementation code is written"
    - "Each test file mocks @/lib/supabase with vi.mock() and fluent chain pattern"
    - "Tests define expected behaviors that will initially fail (red phase)"
  artifacts:
    - path: "src/modules/knowledge-base/hooks/useKBEntries.test.ts"
      provides: "Test stubs for KB-02 filter composition"
      contains: "vi.mock"
    - path: "src/modules/knowledge-base/hooks/useKBSearch.test.ts"
      provides: "Test stubs for KB-05 full-text search"
      contains: "vi.mock"
    - path: "src/modules/knowledge-base/pages/KBFormPage.test.ts"
      provides: "Test stubs for KB-06 ADR template injection"
      contains: "vi.mock"
  key_links: []
---

<objective>
Create Wave 0 test stubs for the Knowledge Base module so that implementation plans have automated verification targets from the start.

Purpose: Satisfy Nyquist compliance — every implementation task with a unit test requirement must have its test file created before the implementation code exists. This enables RED-GREEN workflow.
Output: 3 test stub files with mocked Supabase and failing test cases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-knowledge-base-module/31-VALIDATION.md

@tools/wireframe-builder/lib/blueprint-store.test.ts

<interfaces>
<!-- Expected interfaces from Plan 01 (not yet created). Tests mock these. -->

From src/lib/kb-service.ts (Phase 30):
```typescript
export type KBEntry = {
  id: string
  entry_type: string
  title: string
  body: string
  tags: string[]
  client_slug: string | null
  created_by: string | null
  created_at: string
  updated_at: string
}

export type KBFilters = {
  type?: string
  clientSlug?: string
  tags?: string[]
}

export async function listKBEntries(filters: KBFilters): Promise<KBEntry[]>
export async function searchKBEntries(query: string): Promise<KBEntry[]>
export async function createKBEntry(data: Omit<KBEntry, 'id' | 'created_at' | 'updated_at' | 'search_vec'>): Promise<KBEntry>
export async function updateKBEntry(id: string, data: Partial<KBEntry>): Promise<KBEntry>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 test stub files with mocked Supabase</name>
  <files>
    src/modules/knowledge-base/hooks/useKBEntries.test.ts,
    src/modules/knowledge-base/hooks/useKBSearch.test.ts,
    src/modules/knowledge-base/pages/KBFormPage.test.ts
  </files>
  <action>
Create 3 test files following the mock pattern from `tools/wireframe-builder/lib/blueprint-store.test.ts`. Each file uses `vi.mock('@/lib/supabase')` with fluent chain mocking via `vi.fn()`.

**src/modules/knowledge-base/hooks/useKBEntries.test.ts (KB-02):**
- Mock `@/lib/kb-service` module (mock `listKBEntries`)
- Describe block: "useKBEntries"
- Test stubs (describe expected behavior, can use `it.skip` or `it.todo` since implementation does not exist yet):
  1. "returns entries from listKBEntries"
  2. "passes type filter to listKBEntries"
  3. "passes tags filter to listKBEntries"
  4. "passes clientSlug filter to listKBEntries"
  5. "sets loading to true while fetching"
  6. "sets error on failure"
- Use `vi.mock('@/lib/kb-service', () => ({ listKBEntries: vi.fn() }))` pattern
- Import `renderHook` from `@testing-library/react` if available, otherwise note dependency in test file comment

**src/modules/knowledge-base/hooks/useKBSearch.test.ts (KB-05):**
- Mock `@/lib/kb-service` module (mock `searchKBEntries`)
- Describe block: "useKBSearch"
- Test stubs:
  1. "returns results for query >= 2 chars"
  2. "returns empty results for query < 2 chars without calling searchKBEntries"
  3. "sets loading to true while searching"
  4. "sets error on failure"

**src/modules/knowledge-base/pages/KBFormPage.test.ts (KB-06):**
- Mock `@/lib/kb-service` module (mock `createKBEntry`, `updateKBEntry`)
- Mock `react-router-dom` (mock `useParams`, `useNavigate`)
- Describe block: "KBFormPage"
- Test stubs:
  1. "renders form with title, type, body, tags, client fields"
  2. "injects ADR template when type changes to 'decision' and body is empty"
  3. "does NOT inject ADR template when type changes to 'decision' and body has content"
  4. "calls createKBEntry on submit in create mode"
  5. "calls updateKBEntry on submit in edit mode"
  6. "parses comma-separated tags into array on submit"

All test stubs should use `it.todo('...')` for tests that cannot run without the implementation, or `it.skip` with a comment indicating "Wave 0 stub — implementation pending". The goal is to have the test FILE exist with clearly defined expected behaviors, not to have passing tests.

IMPORTANT: Create the directory structure first (`src/modules/knowledge-base/hooks/` and `src/modules/knowledge-base/pages/`) if they do not exist.
  </action>
  <verify>
    <automated>ls -la src/modules/knowledge-base/hooks/useKBEntries.test.ts src/modules/knowledge-base/hooks/useKBSearch.test.ts src/modules/knowledge-base/pages/KBFormPage.test.ts 2>&1 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>3 test stub files exist with vi.mock patterns and todo/skip test cases defining expected behaviors for KB-02, KB-05, and KB-06. Files compile or are structurally valid (todo tests do not require compilation of non-existent modules).</done>
</task>

</tasks>

<verification>
- All 3 test files exist in correct locations
- Each file has `vi.mock` for `@/lib/kb-service`
- Each file has `describe` block with named test stubs
- Test stubs match the behaviors listed in VALIDATION.md
</verification>

<success_criteria>
- src/modules/knowledge-base/hooks/useKBEntries.test.ts exists with 6 test stubs for filter composition
- src/modules/knowledge-base/hooks/useKBSearch.test.ts exists with 4 test stubs for FTS search
- src/modules/knowledge-base/pages/KBFormPage.test.ts exists with 6 test stubs for form + ADR template
- VALIDATION.md Wave 0 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/31-knowledge-base-module/31-00-SUMMARY.md`
</output>
