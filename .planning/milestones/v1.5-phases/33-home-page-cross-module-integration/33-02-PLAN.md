---
phase: 33-home-page-cross-module-integration
plan: 02
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - src/pages/clients/FinanceiroContaAzul/Index.tsx
  - src/components/layout/SearchCommand.tsx
autonomous: false
requirements:
  - HOME-03
  - KB-07

must_haves:
  truths:
    - "Client page shows a Conhecimento section listing KB entries for that client_slug"
    - "Cmd+K search returns KB entries as a separate group alongside docs results"
    - "KB entries only appear in Cmd+K when there is a non-empty search query"
    - "KB entries are fetched once on dialog open and cached across open/close cycles"
  artifacts:
    - path: "src/pages/clients/FinanceiroContaAzul/Index.tsx"
      provides: "Conhecimento section with KB entries for financeiro-conta-azul"
      min_lines: 50
    - path: "src/components/layout/SearchCommand.tsx"
      provides: "KB entries in CommandGroup with fetch-on-open"
      min_lines: 60
  key_links:
    - from: "src/pages/clients/FinanceiroContaAzul/Index.tsx"
      to: "src/lib/kb-service.ts"
      via: "listKBEntries({ clientSlug })"
      pattern: "listKBEntries.*clientSlug"
    - from: "src/components/layout/SearchCommand.tsx"
      to: "src/lib/kb-service.ts"
      via: "listKBEntries({}) on dialog open"
      pattern: "listKBEntries"
---

<objective>
Integrate KB entries into the client workspace page and Cmd+K search dialog, making knowledge accessible where operators need it.

Purpose: Knowledge surfaces inline on client pages and is discoverable via universal search.
Output: Updated FinanceiroIndex.tsx with Conhecimento section, updated SearchCommand.tsx with KB group.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-home-page-cross-module-integration/33-RESEARCH.md
@src/pages/clients/FinanceiroContaAzul/Index.tsx
@src/components/layout/SearchCommand.tsx
</context>

<interfaces>
<!-- Key types the executor needs from Phase 30 output -->

From src/lib/kb-service.ts (Phase 30):
```typescript
export type KBEntry = {
  id: string
  title: string
  body: string
  entry_type: 'bug' | 'decision' | 'pattern' | 'lesson'
  tags: string[]
  client_slug: string | null
  created_at: string
  updated_at: string
}

export function listKBEntries(filters: { clientSlug?: string; type?: string; tags?: string[] }): Promise<KBEntry[]>
```

Note: Verify the actual exports from Phase 30 output before implementing. If the function signature differs, adapt accordingly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Conhecimento section to FinanceiroIndex</name>
  <files>src/pages/clients/FinanceiroContaAzul/Index.tsx</files>
  <action>
Add a "Conhecimento" section to FinanceiroIndex.tsx that displays KB entries for the `financeiro-conta-azul` client slug.

**Implementation:**
- Import `listKBEntries` and `KBEntry` type from `@/lib/kb-service`.
- Import `useState`, `useEffect` from React and `Link` (already imported).
- Add state hooks at the TOP of the component (before any JSX or conditional returns): `const [kbEntries, setKbEntries] = useState<KBEntry[]>([])` and `const [kbLoading, setKbLoading] = useState(true)`.
- Add useEffect that calls `listKBEntries({ clientSlug: 'financeiro-conta-azul' })`, sets entries on success, sets loading false in both success and catch paths. Use `.catch(() => setKbLoading(false))` to handle missing tables gracefully.
- CRITICAL: All hooks must be declared before any conditional return (hooks-before-early-returns rule).

**Render section between the Documentos table and the Prompt Master section:**
- Section header: "Conhecimento" in same style as "Documentos" (text-xs font-bold uppercase tracking-wider).
- Loading state: "Carregando..." in text-sm text-slate-400.
- Empty state: "Nenhum conhecimento registrado para este cliente." in text-sm text-slate-400.
- If entries exist: grid of cards (grid gap-3 sm:grid-cols-2).
- Each card: Link to `/knowledge-base/${entry.id}`, rounded-xl border, shows entry_type badge (10px uppercase), title (text-sm font-semibold), and tags as rounded-full pills (indigo-50 bg).
- Card styling should match the existing design language (slate borders, indigo hover, dark mode support).

Do NOT use `any`. All types explicit.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>FinanceiroIndex shows a Conhecimento section that fetches and displays KB entries for financeiro-conta-azul slug, with loading and empty states. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add KB entries to Cmd+K search dialog</name>
  <files>src/components/layout/SearchCommand.tsx</files>
  <action>
Extend SearchCommand.tsx to fetch KB entries on dialog open and display them as a separate CommandGroup.

**State additions (all hooks at top, before any returns):**
- Add `const [kbEntries, setKbEntries] = useState<KBEntry[]>([])` — import KBEntry from `@/lib/kb-service`.
- Add `const [query, setQuery] = useState('')` — to control CommandInput and gate KB group visibility.

**Fetch-on-open logic:**
- Create `handleOpenChange(next: boolean)` function that:
  - Calls `setOpen(next)`.
  - If `next === true && kbEntries.length === 0`, calls `listKBEntries({}).then(setKbEntries).catch(() => {})`.
  - If `next === false`, calls `setQuery('')` to reset search.
- Replace `onOpenChange={setOpen}` on CommandDialog with `onOpenChange={handleOpenChange}`.
- Do NOT clear `kbEntries` on close — keep cached for instant re-open.

**Keyboard handler update (stale closure fix):**
- Add `const openRef = useRef(open)` and sync it: `openRef.current = open`.
- In the existing keyboard useEffect, replace `setOpen((prev) => !prev)` with `handleOpenChange(!openRef.current)`.
- Import `useRef` from React.

**CommandInput update:**
- Make CommandInput controlled: `value={query} onValueChange={setQuery}`.
- Update placeholder to: "Pesquisar documentacao e conhecimento...".

**KB CommandGroup (after existing docs groups):**
- Only render when `query.length > 0 && kbEntries.length > 0`.
- `<CommandGroup heading="Base de Conhecimento">` containing CommandItem entries.
- Each CommandItem: `value` set to `${entry.title} ${entry.entry_type} ${entry.tags.join(' ')}` for cmdk filtering.
- Each item shows BookOpen icon (import from lucide-react), title, and entry_type as subtitle.
- `onSelect` calls `handleSelect(/knowledge-base/${entry.id})`.

Do NOT use `any`. Import `BookOpen` from lucide-react. Import `listKBEntries, type KBEntry` from `@/lib/kb-service`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>SearchCommand fetches KB entries on dialog open, shows them in a "Base de Conhecimento" group only when query is non-empty, uses controlled input, handles stale closure correctly. Zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify KB integration in client page and Cmd+K</name>
  <files>src/pages/clients/FinanceiroContaAzul/Index.tsx, src/components/layout/SearchCommand.tsx</files>
  <action>Visual and functional verification of KB integration in client page and Cmd+K search.</action>
  <what-built>KB integration in client page (Conhecimento section) and Cmd+K search (KB entries group)</what-built>
  <how-to-verify>
    1. Run `make dev` and open http://localhost:5173/clients/financeiro-conta-azul
    2. Verify a "Conhecimento" section appears below the Documentos table
    3. If KB entries exist for this client, verify they show as cards with type badge and tags
    4. If no KB entries, verify "Nenhum conhecimento registrado para este cliente." shows (no errors)
    5. Open Cmd+K (Ctrl+K / Cmd+K)
    6. With empty query, verify NO KB entries are shown (only docs groups)
    7. Type a query that matches a KB entry title — verify "Base de Conhecimento" group appears
    8. Click a KB entry — verify it navigates to /knowledge-base/:id
    9. Close and re-open Cmd+K — verify KB entries load instantly (cached)
    10. Test in dark mode — both features render correctly
  </how-to-verify>
  <verify>Visual browser check per steps above</verify>
  <done>Both KB integrations render correctly with proper loading, empty, and populated states in light and dark mode.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors after each task
- FinanceiroIndex shows Conhecimento section with KB entries for client slug
- Cmd+K shows KB group only when query is non-empty
- Both features handle empty/error states gracefully
</verification>

<success_criteria>
- Client page displays KB entries filtered by client_slug
- Cmd+K fetches KB entries on open and shows them in a separate group
- KB group only visible when user has typed a query (not on empty state)
- Cached KB entries persist across dialog open/close cycles
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-home-page-cross-module-integration/33-02-SUMMARY.md`
</output>
