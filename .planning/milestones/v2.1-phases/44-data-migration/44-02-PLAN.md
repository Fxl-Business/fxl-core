---
phase: 44-data-migration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: false
requirements: [MIG-01, MIG-02, MIG-03]
must_haves:
  truths:
    - "All 62 documents from docs/ exist in the Supabase documents table"
    - "Custom tags appear intact in the body field (not stripped or modified)"
    - "parent_path values match filesystem directory structure"
    - "sort_order values are correct (index.md = 0, others sequential per parent_path)"
    - "Frontmatter fields (title, badge, description) match original .md files"
  artifacts: []
  key_links:
    - from: "Supabase documents table"
      to: "docs/ filesystem"
      via: "seed script upsert"
      pattern: "62 rows matching 62 files"
---

<objective>
Execute the seed script and verify that all 62 documents are correctly migrated to Supabase with intact data.

Purpose: Confirm the migration is complete and data integrity is preserved before Phase 45 switches the app to read from the database.
Output: Verified database with 62 documents matching the filesystem source
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-data-migration/44-01-SUMMARY.md
@scripts/seed-documents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute seed script and verify row count</name>
  <files></files>
  <action>
1. Run the seed script with --force flag:
   ```bash
   npx tsx --env-file .env.local scripts/seed-documents.ts --force
   ```

2. Verify total document count matches 62:
   - Write a quick verification script or use the seed script's own verification output
   - If count != 62, investigate which files were missed or errored

3. Verify specific documents exist with correct data by querying a few representative docs:

   **Test slug derivation:**
   - Query slug = `processo/index` — should exist
   - Query slug = `processo/fases/fase1` — should exist
   - Query slug = `ferramentas/blocos/kpi-card` — should exist
   - Query slug = `padroes/index` — should exist

   **Test parent_path derivation:**
   - slug `processo/fases/fase1` should have parent_path = `processo/fases`
   - slug `ferramentas/blocos/kpi-card` should have parent_path = `ferramentas/blocos`
   - slug `ferramentas/index` should have parent_path = `ferramentas`

   **Test sort_order:**
   - All `index` slugs should have sort_order = 0
   - Query docs with parent_path = `processo/fases` and verify sort_order is sequential (0 for index, then 1, 2, 3... for fase1, fase2, etc.)

   **Test custom tag preservation (MIG-02):**
   - Query slug = `processo/fases/fase1` and verify body contains `{% callout` or `{% operational` literally
   - Query slug = `processo/fases/index` and verify body contains `{% phase-card` literally
   - Body should NOT have these tags stripped, escaped, or modified in any way

   **Test frontmatter extraction:**
   - Query slug = `processo/fases/fase1` and verify:
     - title = `Fase 1 — Diagnostico Estrategico`
     - badge = `Processo`
     - description = `Coleta, mapeamento e formacao do Briefing`
   - Query slug = `ferramentas/blocos/kpi-card` and verify:
     - title = `KpiCard`
     - badge = `Ferramentas`

4. Log all verification results. If any check fails, report the specific failure.
  </action>
  <verify>
    <automated>npx tsx --env-file .env.local scripts/seed-documents.ts --force 2>&1 | grep -E "Seeded|Error|documents"</automated>
  </verify>
  <done>All 62 documents exist in Supabase with correct slug, parent_path, sort_order, title, badge, description, and body (with custom tags preserved)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of migrated data in Supabase Dashboard</name>
  <what-built>All 62 documents from docs/ have been migrated to the Supabase documents table via the seed script. The script extracted frontmatter, preserved custom tags, derived parent_path from directory structure, and assigned sort_order (index.md = 0, others alphabetical).</what-built>
  <how-to-verify>
1. Open Supabase Dashboard -> Table Editor -> documents table
2. Verify row count is 62
3. Sort by parent_path and check a few entries:
   - processo/fases/fase1: title should be "Fase 1 — Diagnostico Estrategico", badge "Processo"
   - ferramentas/blocos/kpi-card: title should be "KpiCard", badge "Ferramentas"
4. Click on a row with custom tags (e.g., processo/fases/fase1) and verify body contains `{% callout` or `{% operational` text literally
5. Check sort_order: rows with slug ending in /index should have sort_order = 0
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Supabase documents table has exactly 62 rows
2. Every slug in the table corresponds to a real .md file in docs/
3. Custom tags ({% operational %}, {% callout %}, {% prompt %}, {% phase-card %}) appear literally in body field for the 14 files that use them
4. parent_path values are correct for all 4 directory levels (processo/, processo/fases/, ferramentas/, ferramentas/blocos/, ferramentas/techs/, padroes/)
5. sort_order = 0 for all index.md files, sequential for others within same parent_path
6. Frontmatter fields match the source .md files exactly
</verification>

<success_criteria>
- 62 rows in documents table (matching 62 .md files in docs/)
- Zero data integrity issues (no missing fields, no corrupted body content)
- Custom tags verified intact in body field
- parent_path matches filesystem directory structure
- sort_order correctly assigns 0 to index, sequential to rest
- Human verification approved via Supabase Dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/44-data-migration/44-02-SUMMARY.md`
</output>
